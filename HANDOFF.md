# 인수인계 — 일정공유 달력 앱 (calapp)

## 현재 기준선 — v641

- 이 저장소가 업무·조직·하자 데이터의 단일 운영 앱이다. 하자 데이터는 HCS 원본 업로드 → 로컬 저장 → 집계 → 게시 → 조회까지 이 앱에서 처리한다.
- 하자 원본 IndexedDB 저장소는 `calapp_defects_v1`로 독립 운영한다. 기존 로컬 설치에서 데이터가 발견되면 최초 1회만 현재 저장소로 자동 복사하고 이후에는 현재 저장소만 사용한다.
- 회귀 테스트는 이 저장소만으로 실행하며 외부 저장소 비교 하네스는 운영 게이트에서 제외했다.
- README와 보안 규칙은 현재 운영 구조만 설명하며 과거 운영 구조를 전제로 하지 않는다.

새 대화를 시작할 때 이 파일을 먼저 읽으면 됩니다. **현재 기준 v641**입니다.
(⚠ 이 줄의 차수는 배포마다 갱신할 것 — 한동안 81차로 방치돼 인수인계 문서 구실을 못 했다.)

## 1. 이 앱이 무엇인가

팀 업무 일정 달력과 담당자별 주요업무 현황을 함께 쓰는 사내 웹앱입니다.
GitHub Pages에 올리고 Firebase Realtime Database(무료 플랜)를 씁니다.
하자처리 현황은 이 앱의 독립 기능으로 제공됩니다.

화면은 다섯 개입니다. 업무 일정(달력 + 우측 일자 패널), 주요업무 현황,
내 업무, 조직 관리, 설정입니다. 주소 뒤에 `?local=1`을 붙이면 로그인 없이
브라우저 저장소만으로 동작하고, `?w=1`은 위젯 모드입니다.

## 1-1. 519차 개편 — 지금 화면의 골격

사내 CS 업무관리시스템과 같은 계열로 맞춘 개편입니다. **바꾸기 전에 이 절을 먼저 읽으세요.**

**셸 구조** — 어두운 셸(`--shell:#2C2F34`) 위에 사이드바 판(`--shell2:#5F636E`)과
본문 판(`--bg:#FBFBFB`)이 얹힌 형태입니다. 좌우는 화면 끝까지 가고 위(헤더 48px)와
아래(8px)만 셸이 보입니다. 두 판 사이는 8px, 본문 안쪽 패딩은 18px.
판의 **바깥 모서리 두 곳이 8px**입니다(사이드바 `0 8px 8px 0` · 본문 `8px 0 0 8px`).
534차에 사내 CS 업무관리시스템 화면을 픽셀로 재어 맞춘 값입니다 — 그 전에는 헤더 44px,
아래 14px, 모서리 14px 이었습니다.
⚠ 532차에 아래 여백을 0 으로, 아래 모서리를 각지게 바꿨다가 되돌렸습니다.
참조 화면의 어두운 아래 띠(8px)를 작업표시줄로 오독한 것이 원인입니다 — 실측할 때
창 테두리(973~976행)와 작업표시줄(977행~)을 셸과 갈라 보세요.
⚠ `#sidebar` 는 `overflow:visible` 이라 모서리가 자식을 자르지 않습니다 — 맨 위 `.sb-title` 이
같은 값을 따로 가져야 합니다(533차에 판만 8px 로 바꿨다가 제목줄만 14px 로 남았습니다).
532차에 사내 CS 업무관리시스템 화면을 실측해 맞춘 값입니다 — 그 전에는 헤더 44px,
아래 여백 14px, 모서리 14px 두 곳이었습니다.

**헤더** — 좌측에 로고 + 앱 이름, 우측에 기준월 · 인쇄 · 검색.
모두 **무몰딩**(배경·테두리 없음)이고, 성격이 다른 묶음 사이에만 얇은 세로선을 둡니다.
글자는 전부 `--shell-dim` 한 값으로 맞춰져 있습니다 — 하나만 바꾸면 균형이 깨집니다.
기준월·인쇄는 하자 화면에서만 나타납니다(`#tbRm`·`#tbPrintWrap` 의 `hidden`).

**사이드바** — 머리(`#444B55`)에 팀 선택 + 접기 버튼, 그 아래 메뉴 · 관리 · 프로필.
구분선은 셋 다 같은 규격(`--shell-line`, 1px, 좌우 15px)입니다.
접으면 팀 선택·아이콘이 숨고 꺽쇠만 가운데 남습니다.

**정렬 축(건드릴 때 함께 확인)**
- 사이드바: 아이콘 x=20.5px · 글자 x=51px — 팀 선택과 메뉴가 같은 축
- 헤더 검색 우측 = 업무패널 우측 (1422px @1440)
- 업무패널 폭 300px

**없앤 기능** — 다크 모드(`html.dark`), 배경화면(`hasbg`·`applyBg`),
달력 유리 설정(`calGlass*`), 필터 카드(`dp-fcard`)는 519차에 제거했습니다.
되살릴 일이 있으면 이 문서의 이력을 먼저 보세요.

**함정**
- CSS 주석을 열고 안 닫으면 **뒤 규칙이 통째로 사라집니다**(오류도 안 납니다).
  smoke 의 「CSS 전량 파싱」 검사가 규칙 수로 잡습니다 — 1800개 미만이면 실패.
- UI 를 지울 때는 그 안의 클래스가 **위젯·JS 에서 쓰이는지 먼저 grep** 하세요.
  필터 카드를 지웠을 때 위젯 필터와 `.dp-frow` 2·2·1 배치가 함께 사라진 적이 있습니다.
- 색·여백은 규칙만 보지 말고 **계산값을 실측**하세요. 토큰 이름이 같아도
  투명도가 달라 다르게 보이는 경우가 있었습니다.

## 1-2. 526차 — 조직 관리 현장 위치 지도

조직 관리 좌측(팀·권역 아래)에 **현장 위치 미니 지도** 카드를 넣었습니다.
지도 데이터는 `vendor/korea-geo.js` 한 파일(약 84KB)에 상수로 들어 있고 **외부 요청이 없습니다.**
통계청 SGIS 2018 시도·시군구·읍면동 경계(공공누리 제1유형)를 단순화한 것으로,
2026-07-01 전남광주 통합과 강원·전북 특별자치도 명칭을 반영했습니다(시도 16개).

카드 이름은 **현장 지도**(547차 · 그 전에는 미니 맵), 머리 배지는 지도에 찍힌 현장 수 한 숫자입니다(528차).
하단바는 없앴고 **뒤로는 지도 오른쪽 위에 얹었습니다.** 현장 점의 설명은 `data-tip` 툴팁이 맡습니다.

**547차에 바뀐 것**
- 시도 면의 툴팁은 뗐습니다 — `.okm-pv:hover` 면 강조만 남깁니다. 시군구는 **면 데이터가 없어**(경계가 `mesh` 한 덩어리)
  면을 칠할 수 없으므로, 커서 아래 시군구(`kmMuniAt`)의 **이름표만** 굵게 합니다(`.okm-ct.hov` · `data-m="시도|이름"`).
- **현장 표의 행에 마우스를 올리면** 그 현장 점이 커지고(`.okm-pin.hov`) 툴팁이 점 위에 뜹니다(`kmHoverSite`).
  ⚠ 공용 툴팁 상태 `_tipFor` 는 쓰지 않습니다 — 행 안에서 움직일 때마다 mouseover 위임이 `tipHide` 를 부릅니다.
  점과 행은 `data-sid` 로 맺습니다.
- 머리 제목(`.tm-ttl`)을 누르면 **같은 지도를 모달에 크게** 띄웁니다(`kmOpenBig` · `#okmBig` · `#mb.kmw`).
  보기 상자·확대 이력은 카드와 **같은 `S.km`** 을 씁니다 — 모달에서 확대하면 카드도 따라갑니다.
  ⚠ 지도 클릭·우클릭 처리는 `#okmSvg` 가 아니라 `.okm` 으로 듣습니다. 모달 안에서는 조상에 `modal.stop`(data-act)이
  있어 바다 클릭 판정을 `.okm [data-act]` 로 지도 안에 한정해야 합니다 — 안 그러면 바다를 눌러도 되돌아가지 않습니다.
- **548차 — 모달 확대창에 우측 현장 목록 + 끌어서 이동.** `#mb.kmw` 는 `min(900px,94vw)`, `.kmw-grid` 가 지도(최대 564px)와
  목록(260px)을 나눕니다. 목록은 권역 순·이름 순이고 자리를 못 찾은 현장은 옅게(`.off`) 적어 배지 `찍힌/전체` 와의 차이를 보여 줍니다.
  목록 행 호버는 표 행과 같은 길(`KM_ROW`)로 점을 키우고, 누르면 그 현장의 시도로 갑니다(`org.mapTo` → `kmGoSite`).
  끌기는 확대 상태(`vb.w<KM_VB0.w`, svg 에 `.pan`)에서만 듣습니다. 끄는 동안은 `viewBox` 만 바꾸고 놓을 때 `rOrgMap()` 으로
  한 번 다시 그립니다(이름표 자리가 보기 상자에 매여 있습니다). ⚠ 3px 넘게 끌었으면 뒤따르는 click 을 캡처 단계에서 삼킵니다 —
  안 그러면 놓는 순간 바다 클릭으로 한 단계 되돌아갑니다. 이동 범위는 전체 보기 상자 밖으로 반 화면까지입니다.
  `kmHoverSite` 는 카드·모달 양쪽 점을 모두 키우고 툴팁은 마지막(모달) 점에 띄웁니다.
- **549차 — 휠 줌 · 더블클릭 · 양방향 호버 · 범례.** 휠은 커서 자리를 고정한 채 1.25배씩(`kmZoomAt`), 전체보다 커지면
  전체 보기로 되돌리고 이력도 비웁니다. viewBox 만 먼저 바꾸고 손이 멈추면(140ms) 한 번 다시 그립니다.
  ⚠ **지도 안 한 번 클릭은 220ms 미룹니다**(`kmClickDefer`) — 더블클릭(2배 확대)이 오면 버립니다. 안 그러면
  시도 선택 → 시군구 진입 → 확대가 한꺼번에 일어납니다. 클릭 위임 안의 지도 분기가 통째로 이 지연 안에 들어가 있고,
  범례·뒤로 버튼은 svg 밖이라 바로 갑니다. 헤드리스로 더블클릭을 시험할 때 puppeteer 의 `clickCount:2` 는 dblclick 을
  내지 않습니다 — 클릭 둘 + `dispatchEvent(new MouseEvent('dblclick'))` 로 흉내 내야 합니다.
  점에 올리면 표 행·모달 목록 행이 밝아지고 목록은 그 자리로 굴립니다(역방향). 범례(`kmLegendHTML` · `.okm-lg`)는
  모달 지도 왼쪽 아래에 떠 있고 담당자(첫 사람 기준)·미지정 수를 적습니다. 누르면 그 사람 현장만 남깁니다(`S.kmOwn`) —
  ⚠ 필터는 카드와 공유되고 모달을 열 때 초기화합니다. 모달 폭은 `width:auto` 로 지도가 정합니다 — 지도는 창 **높이**에
  매여 있어 폭을 고정하면 양옆이 비기 때문에, 높이를 먼저 잡고 비율로 폭을 냅니다(kmBigHTML).
- **550차 — 렉 원인과 구조 변경(⚠ 지도 손대기 전에 읽을 것).** SVG 는 자식 하나만 바뀌어도 **svg 통째로** 다시 칠한다.
  점 확대·시도 :hover·시군구 이름 강조가 경계 경로와 한 svg 에 있어서, 호버마다 지도 전체(경계 수백 KB)를 다시 칠했고
  모달은 그 위에 `backdrop-filter` 흐림까지 다시 계산했다. 그래서
  ① `kmSVG` 가 svg 두 장을 낸다 — 경계 `.okm`(`will-change:transform` 으로 제 층) + 겹층 `.okm-ov`(점·지시선·호버 강조).
     **호버로 바뀌는 것은 전부 겹층에만 둔다.** 시도 면 호버는 `.okm-hv` 에 경로 `d` 를 복사해 그리고(`:hover` 규칙 없음),
     시군구 이름 강조는 `.okm-hvt` 사본에 자리·글자를 옮겨 적는다. 경계 svg 의 DOM 은 호버 중 절대 건드리지 않는다.
  ② 지도 모달은 배경 흐림을 끈다(`#mo:has(#mb.kmw)`). ③ `#htip` 도 제 층(`will-change`) — 툴팁이 움직일 때마다 바탕 층을 다시 칠했다.
  ④ 모달이 열려 있으면 뒤의 표 줄(`#siteRoot tr.hov`)은 밝히지 않는다 — 가려진 줄 때문에 바탕 층이 다시 칠해졌다.
  ⚠ 점은 겹층에 있어 `closest('.okm')` 에 안 걸린다 — svg 를 찾을 땐 `kmSvgOf(el)`(`.okm-wrap` 기준), viewBox 는 `kmSetVB` 로 두 장 함께.
  헤드리스(소프트웨어 래스터)로는 이 렉이 재현되지 않는다 — 트레이스에서 `Paint` 대상 노드가 경계 svg 인지로 확인한다.
  우측 패널은 **현장 · 담당자 탭**(`kmPanelHTML` · `S.kmTab`) — 549차의 플로팅 범례를 담당자 탭으로 옮겼고 필터 동작은 같다.
  모달 폭은 **확대 상태 기준으로 고정**(지도 800px 상한, `.okm-wrap` 에 inline width) — 549차의 비율 맞춤은 확대하면 좁아 되돌렸다.
- **551차.** 되돌아가기 버튼은 `kmBackBtn` — 이력이 있거나 **확대돼 있으면**(vb.w<KM_VB0.w) 보인다. 휠·끌기는 이력을 안 쌓아
  이력만 보던 때는 버튼이 안 생겼다. ⚠ 툴팁 커서 추종 분기는 `.okm,.okm-ov` 로만 — `.okm-wrap` 으로 잡으면 뒤로 버튼 툴팁까지
  커서를 따라간다(550차 회귀). ⚠ **닫힌 모달의 본문(#mbody)은 비워지지 않는다** — 숨은 지도의 점이 호버 대상에 잡혀 툴팁이
  보이지 않는 자리에 떴다. 모달 지도는 `#mo.open` 일 때만 다시 그리고, 점을 고를 때 `kmVisible` 로 거른다.
  패널 탭은 글자만(`.kml-t`) — 현장 · 권역 · 담당자. 권역 필터 `S.kmReg`, 담당자 필터 `S.kmOwn`, 둘 다 `kmFiltHit` 에서 함께 건다.
  계정 표 행(`tr[data-pid]`)에 올리면 그 사람의 담당 현장 점이 전부 커진다(`kmHoverOwner` · 툴팁 없음).
- **552차.** ① 클릭 220ms 지연과 더블클릭 확대는 **모달(#okmBig)에서만** — 카드는 바로 반응한다.
  ② 모달을 열 때 `S.kmSnap` 에 보기 상자·이력을 떠 두고 닫을 때(`closeModal` → `kmModalClosed`) 되돌린다. 필터도 비운다.
  ③ **현장 주소 입력** — `cfg.siteAddr[sid]`(관리자 쓰기 · 120자). 모달 현장 탭에서 자리를 못 찾은 줄을 누르거나 연필로 연다.
  줄이 입력칸으로 바뀌고 Enter 저장 · Esc/이탈 취소. `kmSiteXY` 는 게시본 주소(SITE_ADDR) → cfg 주소 → 현장명 순으로 본다.
  저장하면 `KM_XY` 기억을 비운다. ⚠ **database.rules.json 에 `cfg/siteAddr` 를 추가했다 — 규칙을 배포해야 저장된다.**
  ④ 렉 계측(헤드리스 소프트웨어 래스터, 모달 지도 위 훑기+점 호버): 549차 프레임 간격 최대 1000ms·p95 550ms·평균 61ms →
  552차 최대 16.8ms·평균 16.6ms(60fps). 모달 열기 717 → 125ms. 같은 계측 스크립트는 세션 로컬(`perf2.mjs`)이라 저장소에 없다.
- **553차.** 주소 입력은 **줄 더블클릭**(연필 버튼 제거). 찾은 줄의 한 번 클릭(시도로 이동)은 `kmClickDefer` 로 220ms 미뤄
  더블클릭과 겹치지 않게 했다. 경계 층은 `clipPath` 로 KM_VB0 우변에서 자른다 — 모달이 지도보다 넓어 meet 여백에 울릉도가
  비쳤다(경북 확대에서 빼던 정책과 같다). clipPath id 는 svg id 기준(`okmSvg-clip`·`okmBig-clip`).
- **554차.** 권역 탭은 뺐다 — 현장 탭의 분류 머리(`.kml-g`, 권역)를 누르면 그 권역만(`S.kmReg`), 다시 누르면 푼다.
  필터에 걸린 줄은 지우지 않고 가라앉힌다(`.dim`) — 누른 자리가 남아야 풀기 쉽다.
  ⚠ 휠 확대는 **한 묶음(0.8초 안의 연속 굴림)에 한 번** 이력을 쌓는다(`_kmWT`). 안 쌓으면 시도에서 휠로 들어간 뒤 우클릭·바다
  클릭이 시도를 건너뛰고 전체로 갔고, 매 틱 쌓으면 되돌리기에 틱 수만큼 걸린다.
  ⚠ 미룬 클릭(220ms)은 실행 시점에 `$('#'+svg.id)` 로 svg 를 다시 잡는다 — 그 사이 휠의 140ms 재그리기가 끼면 떼어진
  svg 로 좌표를 재 엉뚱한 시군구로 들어갔다.
- **555차.** 현장 탭의 권역 묶음은 머리줄(`.kml-g`, surf2 바탕·위아래 선)로 가르고 현장 수 배지(`tm-cnt`) + 세대 소계(`em`),
  맨 아래 합계 줄(`.kml-f`). 배지는 현장 수 전체(자리 못 찾은 것 포함) — 찍힌 수는 패널 머리의 `찍힌/전체` 가 맡는다.
- **556~557차.** 목록은 두 단계 명암으로만 가른다 — 머리줄은 옅은 라벨 줄(11px · lbl3 · 권역명·현장 수·세대 소계 전부 같은 톤),
  항목은 진한 줄. 선·바탕·배지 없음, 묶음 사이는 위 여백만. 합계 줄만 진하고 위에 실선 하나. 줄 호버는 둥근 판(사이드바와 같다).
  ⚠ 555~556차처럼 머리줄에 배지·바탕·굵은 소계를 얹으면 한 줄에 네 가지 무게가 겹쳐 난잡해진다 — 되돌리지 말 것.
- **558차.** 권역마다 옅은 음영 판(`.kml-grp` · fill 바탕 · 6px 여백)으로 묶고, 머리줄 아래 안쪽으로 들어간 구분선(`::after`, 좌우 10px).
  **숫자는 회계 서식** — `.kml-n` 한 열(52px · 오른쪽 정렬 · tabular · 같은 글자 크기)에 머리줄 소계·항목 세대수·합계가 모두 선다.
  머리줄과 항목의 좌우 padding 이 같아야(10px) 열이 맞는다 — 줄에 margin 을 주면 어긋난다(557차 회귀).
- **559차.** 패널 바깥 테두리·머리 구분선 제거(판이 묶음을 이미 보여 준다). 음영은 `rgba(120,120,128,.055)`.
  수는 **라벨 바로 뒤 배지** 한 자리로 통일 — 탭(`현장 [9/12]`, 현재 탭에만) · 권역(`중부1 [2]`) · 합계(`합계 [12]`).
  머리줄 글자는 `line-height:1` 로 세로 가운데.
- **560차.** 배지는 전부 뺐다(찍힌 수는 카드 머리 배지만 남는다). 패널 머리는 사이드바 계정·현장과 같은 **이분할 세그**(`.seg`).
  머리줄 30px + 구분선은 머리줄 아래 4px(`::after bottom:-4px`) + 항목은 7px 아래 — 머리줄 글자 중심↔선↔첫 항목 글자 중심이
  18px 씩 대칭이라 간격이 맞아 보인다. 머리줄 글자 12.5px(항목과 같다), 굵기 600.
- **561차.** 판에 위·아래 4px 을 줬다 — 머리줄 글자가 판 윗모서리에 붙어 위로 쏠려 보였다. 합계는 회계 총계 줄로 했다가
  **562차에 권역 판과 같은 꼴(음영 판 + 머리줄 하나, 숫자만 진하게)** 로 바꿨다 — 일체감. 세그 높이 30 = 줄 높이.
- **563차.** 머리줄·합계·숫자 열의 `line-height:1` 을 뺐다 — 항목과 행간(1.5)이 달라 같은 가운데 정렬에서도 글자가 위로 떠 보였다.
  담당자 탭은 현장 탭과 **같은 꼴** — 권역 판 안에 그 권역 현장의 담당자 줄(첫 사람 기준, 한 사람이 여러 권역에 나올 수 있다),
  줄 오른쪽 "n개 현장"(`.kml-s`) + 세대 합(같은 숫자 열), 미지정은 맨 끝, 합계 줄도 같다. 담당자 줄 클릭 = `S.kmOwn`, 권역 머리 = `S.kmReg`.
- **564차.** 세그 높이 38 = 권역 판·합계 줄과 같다(안쪽 4 + 줄 30 + 4).
- **565~566차.** 수는 **표처럼** — 단위 글자 없이 줄마다 숫자만(열 이름 줄은 566차에 뺐다 — 애매했다). 숫자 글자 크기는 이름과 같은
  12.5 — 크기가 다르면 가운데 정렬에서도 숫자가 떠 보인다. 줄마다는 숫자만
  두 열(`.kml-s` 현장 수 24px · `.kml-n` 세대 52px, 둘 다 오른쪽 정렬·tabular). 현장 항목은 현장 수 칸이 빈 채로 자리만 차지해
  격자가 유지된다. 패널 폭 284.
- **567차.** 열 폭을 숫자 폭에 맞췄다(현장 수 20 · 세대 44 · 사이 6) — 넓은 열은 오른쪽 정렬이라 두 열 사이가 벌어져 보였다.
  머리줄·합계의 25px 들여쓰기는 뺐다(빈 자리가 애매) — 머리줄은 판 가장자리, 항목은 점만큼 안쪽인 트리 위계로 돌아갔다.
- **568~569차.** 현장 수와 세대 사이 가운뎃점을 넣었다가 뺐다. 열은 현장 수 20 · 사이 6 · 세대 40. tabular 로 폭은 같아도 "1" 은
  먹이 가운데 몰려 오른쪽 끝이 들쭉날쭉해 보이는 건 글자 모양이라 열 폭으로는 못 고친다 — 알고 둔 것.
- **570차.** ⚠ 현장 수 칸이 머리줄·합계와 항목에서 2px 어긋나 있었다 — 두 열 사이가 `flex gap + margin` 이라 gap 이 다르면(6 vs 8) 밀린다.
  `.kml-g/.kml-f/.kml-r` 의 gap 은 8 로 같아야 한다. 열 자리는 `getBoundingClientRect` 로 재서 확인한다(눈으로는 2px 이 안 보인다).
- **573~575차 — 준공 년차 필터.** 573차 탭 → 574차 세그 아래 select → **575차 지도 오른쪽 위 도구 묶음**(`.okm-tools`: 전체 보기 + 필터 아이콘).
  필터 아이콘(`org.mapYrMenu`)은 앱 공용 메뉴 `openCtx` 를 버튼 아래에 띄운다(항목 `on` 으로 현재 값 표시 — openCtx 에 `it.on` 추가).
  값이 걸려 있으면 아이콘이 `.on`(파랑)이고 툴팁에 "준공 년차 · n년차". 담당자 탭 수도 년차 필터 안의 수만 센다.
  전체 보기 버튼은 모달에서 오른쪽으로 갔고(카드는 그대로 왼쪽), `.okm-tools .okm-back{position:static}`.
- **602차.** ① `KM_VB0.w` 2120(601차 2230 은 너무 떨어져 보였다).
  ② 우클릭 때 미뤄둔 좌클릭(220ms)을 취소한다 — 되돌린 직후 뒤늦게 확대되던 문제.
  ③ 시군구까지 들어간 상태에서 좌클릭하면 도로 시도 보기로 **축소되던** 문제 — `org.mapPv` 에서 이미 더 깊이 확대돼 있으면(`vb.w<box.w*0.98`) 무시한다.
  ④ 다중 선택 메뉴의 선택 표시는 **체크 상자**(`it.check` → `.ctx-k`) — 앱의 다른 다중 필터(`.msel-o`)와 같은 꼴. 파란 글자만으로는 잘 안 보였다.
- **601차.** ① 전체 보기 `KM_VB0.w` 2104→2230 — 경북·울산이 테두리에 붙어 있었다.
  ② ⚠ **누르고 있는 동안에는 `rOrgMap` 을 미룬다**(`_kmDown`·`_kmPend`, mouseup 에 밀린 그리기 실행). 휠 재그리기(140ms)나 다른 갱신이
  mousedown~mouseup 사이에 끼면 노드가 갈려 **click 이 아예 안 난다** — "버튼·지도가 한 번씩 안 눌리는" 증상의 원인.
  ③ 우클릭(`kmBack`)은 이력이 비어도 한 번에 전체로 가지 않는다 — 시도 안에서 확대돼 있으면 먼저 그 시도 보기로. 휠·모달 복원처럼 이력이 없는 확대에서 전체로 튀었다.
  ④ 필터 버튼 토글은 떼어진 메뉴(`document.contains`)를 무시한다.
- **600차 — 사천만 해안선이 선에서만 빠졌던 이유.** `linePath`(테두리)에 592차에 없앤 **예전 '실오라기 고리' 필터가 남아** 있었다.
  사천만 해안은 폭이 좁은 띠 고리(면적 763·둘레 1213)라 그 필터에 걸려 **면에는 있고 선에는 없었다** — 그래서 그 구간만 선이 비었다.
  `linePath` 의 고리 걸러내기를 `pathOf` 와 같은 규칙(`a<MINA && a/둘레²>THIN`)으로 맞췄다. 테두리는 곱게 줄인 면(`merged`, TOL)에서 뽑는다.
  ⚠ **두 함수의 고리 필터는 항상 같아야 한다.** 다르면 면과 선이 어긋난다.
- **599차 = 597차 그대로(598차 취소).** 598차에 끊긴 자리를 공식 해안선으로 메웠는데 **필요 없는 짓이었다** — 채워진 면의 테두리는
  이미 해안을 따라간다. 문제의 직선은 면적이 없는 군더더기 선이었고, 긴 직선을 끊는 것만으로 끝이다.
  `geo2026.mjs` 의 `COAST=` 는 남아 있지만 쓰지 않는다(608차 확인: `scripts/coast-simp.mjs` 는 이미 지워졌다 — 이 줄이 오래 틀려 있었다).
- **597차 — 최종. 직선만 지운다.** 섬 문턱은 지시대로 크게(`MINA=200 MINAP=3000` ≈ 4.5·67km²), 간략화도 원래대로(`TOL=2 TOLP=4`).
  590차 방식을 되살려 **면과 테두리를 분리**: `.okm-pv` 는 채우기만, 테두리는 `KRGEO.provl`(45단위≈7km 넘는 직선을 끊은 선)로 `.okm-pvl`·`.okm-sl` 에 그린다.
  → 공유수면 해상경계 직선이 화면에서 사라진다. 면은 자료 그대로라 시군구 판정·클릭·호버 그대로. 파일 537KB.
  ⚠ 588~596 사이 이 건으로 여러 번 왕복했다: 해안선 마스크(593·594)는 실제 육지를 잘랐고, 문턱을 내려 섬을 되살린 596은 "작은 섬 생략" 지시와 어긋났다.
  **결론은 "면은 손대지 않고 테두리에서 긴 직선만 끊는다"** — 다시 뒤집지 말 것.
- **596차 — 해안선을 곱게.** 간략화를 `TOL=1`(150m · 시군구·확대) `TOLP=1.5`(225m · 시도 외곽)로 낮추고 섬 문턱을 `MINA=10 MINAP=20`(≈0.2~0.45km²)로
  내렸다 — 594차에 문턱을 올렸더니 비토섬·신수도 같은 **실제 섬**이 사라져 "육지가 잘렸다"는 지적을 받았다. 파일 825KB(이전 550).
  재생성: `TOL=1 TOLP=1.5 MINA=10 MINAP=20 THIN=0.004 MASK=/nonexistent node scripts/geo2026.mjs …`
- **595차 — 해안선 마스크(593~594차)는 되돌렸다. 사천만 직선은 실제 경계다.**
  ⚠ **이 건으로 588·589·590·593·594 다섯 차례 헛수고했다. 다시 건드리지 말 것.**
  · 사천만·진해만·거제 앞바다를 가로지르는 직선은 **공유수면의 실제 행정경계**다 — 네이버 지도의 시·군 경계도 똑같이 그어져 있다.
    2018 SGIS·2018 통계청·2026 행정동 세 자료가 모두 같은 이유이기도 하다. 지울 대상이 아니다.
  · 국립해양조사원 해안선(SHP)으로 만든 육지 마스크(593차)는 **진짜 육지를 잘라 먹었다** — 삼천포(128.07,34.93)가 경남 면 밖으로 나갔다.
    해안선이 '선'이라 폴리곤으로 만들려면 평면 그래프 면 추적이 필요한데 끝점 잇기로 근사해 안팎 판정이 뒤집힌 자리가 생긴다.
    `scripts/coast-land.mjs` 와 `geo2026.mjs` 의 `MASK=` 는 남겨 뒀지만 **쓰지 않는다**(쓰려면 래스터 flood-fill 로 다시 만들어야 한다).
  · 재생성: `TOL=2 TOLP=4 MINA=200 MINAP=3000 THIN=0.004 MASK=/nonexistent node scripts/geo2026.mjs …`
- **594차.** 섬 문턱을 올렸다: `MINAP=3000`(전체 보기 · ≈67km² 미만 섬 생략) · `MINA=200`(확대 · ≈4.5km²). 전체 보기 고리 48 → 24개.
  ⚠ 문턱 판정은 **면적 + 동글동글함**(`a<MINA && a/둘레² > THIN(0.004)`) — 둘레만 보던 592차 방식은 들쭉날쭉한 작은 섬이 둘레가 길어
  살아남았다. 가늘고 긴 띠(사천만 해안선 등)는 이 조건에 안 걸려 그대로 남는다.
- **593차 — 공식 해안선으로 바다를 걷어냈다.** 국립해양조사원 `해안선_20251231`(2026 해안선, SHP · EPSG:5186 · 선 99,166개)을 받아
  **`scripts/coast-land.mjs`** 로 육지 폴리곤(GeoJSON)을 만들고, `geo2026.mjs` 의 `MASK=` 로 넣어 시도·시군구 면과 교집합했다.
  · coast-land.mjs: 끝점이 맞는 선을 이어 붙여 섬은 닫힌 고리(37,938개)로, 본토는 열린 사슬 42개로 얻은 뒤 끝점이 가까운 것끼리
    앞뒤 양방향으로 이어 하나로 만들고, DMZ 에서 끊기므로 북쪽으로 크게 둘러 닫는다. DP 30m 로 줄여 1.5MB GeoJSON.
    (`node scripts/coast-land.mjs` · 의존 `shapefile`·`proj4` · SHP 는 저장소 밖 원본 폴더에서 읽는다)
  · 생성: `TOL=2 TOLP=4 MINA=80 MINAP=1200 PER=400 TOLM=1 MASK=<coast-land.geojson> node scripts/geo2026.mjs …`
  · 결과: 경남 외곽의 46km 직선 → **14km 하나만 남음**(사천만 어귀). 전국에서 50 단위 넘는 직선은 경기 65(김포 한강 하구)·경남 94 둘뿐.
    시도 면적 변화는 바다를 뺀 만큼 — 인천 −6.0%, 부산 −4.5%, 전남광주 −3.2%, 나머지 −2% 이내, 내륙(서울·대구·대전·세종·충북) 0%.
  · ⚠ 남은 14km 는 마스크 쪽 한계다. 해안선은 **선**이라 면으로 만들려면 평면 그래프의 면 추적(polygonization)이 필요한데,
    지금은 끝점 잇기로 근사했다. 리아스식 해안에서 가지(부두·하구)로 잘못 들어가면 만 어귀가 육지로 이어져 남는다. 파일 656KB.
- **591차 — 사천만 건은 590차를 되돌렸다(사용자 결정).** 시도 테두리는 다시 면(`prov` path)의 stroke 로 그린다. 공유수면 직선은 자료 그대로 남는다
  — 2018 SGIS·2018 통계청 시군구·2026 행정동 **셋 다 같은 직선**이 있고(경남 외곽 1656,2740→1370,2632, 46km), 공유수면이 행정구역에 속하는
  제도 문제라 출처를 바꿔도 사라지지 않는다. `KRGEO.provl`(직선 끊은 선)과 `geo2026.mjs` 의 `linePath` 는 남아 있지만 쓰지 않는다.
  **섬 문턱: MINAP=1200(전체 보기) · MINA=80(확대).**
  ⚠ **592차: 문턱은 둘레도 본다** — `a<MINA && per<PER(400)` 일 때만 버린다. 사천만 해안선은 **고리 하나**(면적 763 · 둘레 1213)로 들어 있는데,
  면적만 보면 섬 문턱에 걸려 사라지고 만이 빈 삼각형이 된다(591차 증상). 587차의 '실오라기 고리' 필터(a<3000 && a/per²<0.003)도 같은 것을
  지우므로 껐다(`SLIV=1` 로만 켜진다). 재생성: `TOL=2 TOLP=4 MINA=80 MINAP=1200 PER=400 MASK=/nonexistent`.
- **590차 — 사천만 직선 '선만' 제거.** 면은 자료대로 두고(만은 행정구역상 동에 속한다) **테두리를 면과 분리**했다: `KRGEO.provl[pc]` 는 시도
  외곽을 선으로 뽑되 45 단위(≈7km) 넘는 직선(해상경계)은 끊은 것. `.okm-pv` 는 채우기만(stroke none), 테두리는 `.okm-pvl`, 선택 테두리
  `.okm-sl` 도 provl. 시군구 경계 mesh 도 같은 문턱으로 끊는다. 광주 외곽선도 선 path. ⚠ 면(fill)은 여전히 만을 덮으므로 현장 점의
  "시군구 안" 판정은 영향 없고, 호버 면(.okm-hv 로 복사)에는 만이 포함돼 보인다 — 자료 한계.
- **588~589차 — 사천만 직선, 결론: 자료가 그렇다(세 출처 모두).** 588차에 2018 통계청 시군구(kostat)를 육지 마스크로 써서 걷어내려 했으나
  그 자료도 사천시·고성군 면이 만을 품고 있었고(점 1450,2665 가 사천시 #17 안), **이전 SGIS 2018 지도(549차)도 같았다**(경남 외곽에
  1691,2584→1625,2608 의 직선이 있었다 — 선택 테두리가 굵어지고 확대가 되면서 눈에 띈 것). 행정구역상 공유수면이 동에 속하고 어느 자료에도
  해안선 속성이 없어 **직선만 떼어낼 방법이 없다.** 589차는 마스크 없이 2026 순수 자료로 되돌렸고(`MASK=` 를 없는 경로로 두면 꺼진다),
  실오라기 고리 제거·섬 문턱은 유지. 마스크 코드는 `geo2026.mjs` 에 남아 있다(해안선 자료가 생기면 `MASK=` 로 켠다 — 병합 **뒤** 교집합, 행정동
  단위로 먼저 자르면 위상이 깨진다. polygon-clipping 은 고리 방향을 CCW 로 뒤집는다).
  다른 시도를 골랐을 땐 제주 네모·제주 점을 안 그린다(경남 상자가 네모에 닿았다).
- **587차.** ⚠ 2026 행정동 원자료는 공유수면을 품은 동이 있다 — 588차에서 마스크로 걷어냈다. 그 동들이 겹치거나 벌어진 자리의 실오라기 고리
  (면적/둘레² < 0.003)는 `pathOf` 에서 뺀다. 섬 문턱: 전체 보기 MINAP=150(≈3.4km² 미만 섬 생략), 확대 MINA=30. 광주 외곽선은 시군구 선 색.
- **586차.** 현장이 든 시군구 면(`.okm-mp.has`)은 모든 시도·전체 보기에서 그린다(시도 네모로 현장을 먼저 거르고 `kmInPath`). 빈 면은 선택 시도에서만.
  ⚠ 584차(2018 southkorea-maps 면 + 2018 SGIS 선)는 출처가 달라 음영과 경계선이 어긋났다 — 585차부터 한 위상에서 나오므로 맞는다.
- **585차 — 경계 데이터를 2026-07-01 기준으로 전면 재생성.** 출처 GitHub `vuski/admdongkor` `ver20260701/HangJeongDong_ver20260701.geojson`
  (행정동 경계, CC BY 4.0, 원자료 통계청·행안부). **`scripts/geo2026.mjs`** 가 `vendor/korea-geo.js` 전체(prov·mesh·sub·subl·muni·dong·mpoly·gj)를 만든다 —
  `node scripts/geo2026.mjs <geojson> <이전 korea-geo.js> <출력>` (이전 파일은 bjd 법정동 색인과 시도 라벨 자리를 이어받는 데 쓴다). 의존: topojson-server/client/simplify.
  · 행정동 → topojson 위상 → 시도·시군구 merge, 시군구 경계 mesh, 광역시·광주 행정동 mesh — 같은 위상이라 이웃 면 사이 틈이 없다.
  · 두 단계 간략화: 시도 외곽 TOLP=4(전체 보기), 시군구 면·경계 TOL=2(확대). 파일 539KB(이전 390).
  · 시도 코드는 SGIS 식(11·21·22·…·36·39)으로 매핑해 앱 코드를 안 바꿨다. 2026 행안부 코드 12=전남광주통합특별시 → 36.
  · ⚠ 네모(bbox)는 본체에서 9km(60 단위) 안에 이어지는 고리만 — 백령·흑산·추자·울릉·격렬비열도는 네모 밖(면·선에는 그대로 있다).
    이 때문에 충남 상자가 0.6 을 넘어 `zoom` 문턱을 0.6→0.7 로 올렸고, 클립도 왼쪽(KM_VB0.x−120)을 자른다(백령도가 모달 여백에 걸렸다).
  · 시군구 이름은 구를 시로 합쳤다(천안시서북구 → 천안). 광역시는 구. 2026 인천은 구가 개편돼 있다(제물포·영종·서해·검단구).
  · dong 색인은 "첨단1동·첨단2동" 같은 번호 동을 "첨단동" 한 점으로도 넣는다 — 안 그러면 현장명 "첨단"이 안 잡힌다(2018 색인은 통합동이라 맞았다).
  · 584차의 `scripts/geo-muni.mjs`(2018 southkorea-maps)는 지웠다.
- **584차 — 시군구 면 데이터.** `vendor/korea-geo.js` 끝에 `KRGEO.mpoly`(시군구 폴리곤 250개, 159KB)와 `KRGEO.gj`(광주 외곽선)를 얹었다.
  출처는 GitHub `southkorea/southkorea-maps` kostat 2018 GeoJSON, 같은 투영(k·s·t)으로 바꾸고 DP 간략화 1.2(≈150m)·작은 섬 제거.
  **만드는 스크립트는 `scripts/geo-muni.mjs`**(저장소에 있다 — 553차 이전 생성 스크립트처럼 잃지 말 것). 광주(24)는 KRGEO 의 36(전남광주)에 합쳐 넣었다.
  이름은 kostat 그대로라 "천안시서북구" 식(KRGEO muni 의 "천안"과 다름) — 표시엔 안 쓰고 `data-mn` 에만.
  선택한 시도 안에서 `.okm-mp` 로 그리고 현장이 든 시군구는 `.has`(하늘색, `kmInPath` 짝수-홀수 판정). `data-act="org.mapPv"` 를 같이 줘서
  클릭(시군구 진입)·호버(겹층 `.okm-hv` 복사)가 시도 면과 같은 길로 흐른다 — 호버 면이 이제 **시군구 단위**다(547차 한계 해소).
  광주 외곽선 `.okm-gj` 는 전남광주가 보일 때 늘 그린다.
- **583차.** 현장이 있는 시도는 `.okm-pv.has`(옅은 하늘색, 네모 포함 판정). 시군구 단위는 면 데이터가 없어 불가. 툴팁의 "더블클릭으로 주소 입력" 문구 제거.
- **582차.** 제주 네모 선은 `--sep`(옅게). ⚠ 점 호버 확대는 CSS `transform:scale` 이 아니라 **r 속성**(`kmPinHov`, 원래 값은 `data-r`) —
  `transform-box:fill-box` 가 첫 프레임에 viewBox 기준으로 잡혀 점이 순간 아래로 튀었다(목록을 빠르게 훑을 때 보인다).
- **579~581차.** 카드 오른쪽 위 도구 묶음(전체 보기 + 크게 보기 `i-expand`). 580차에 잘못 뺐다가 581차에 되돌렸다 — 빼야 했던 건
  **제목 버튼**(이제 평문). 점 반지름은 배율 보정 `3.3·(1+0.35·log2 z)` px, 상한 7.5. 년차 문구는 구간형(2년 이내 · 2~3년 · 3~5년 · 5~10년 · 10년 초과),
  "준공일 미입력" 은 메뉴에서 뺐다(툴팁 문구 `KM_YR_NONE` 만 남김). 필터 버튼은 열려 있으면 닫는다(`_ctxEl.dataset.for==='kmYr'`).
  ⚠ 시군구 이름표는 **중심이 화면 안일 때만** 그리고, 자리는 색인 중심(네모 중심 — 섬 많은 군은 바다)이 아니라 **네모 안 읍면동 점의
  중앙값**(`kmMuniLabelPt`, 전부 육지)이다. 네모∩화면 안으로 밀어 넣던 방식은 그 바다 중심을 화면 가장자리까지 끌고 왔다. 목록의 현장은 권역 안에서 **준공일 최신이 위**, 준공일 없는 현장은 맨 아래, 같으면 이름순.
- **576~578차.** 아이콘 18px. 메뉴는 "전체" 아래 `{sep:true}` 구분선 — 다른 우클릭 메뉴와 같은 부품. 도구 버튼은 38×38(옆 세그와 같은 높이·윗선, `.okm-tools` top/right 0), 아이콘은 577차부터 가로 세 줄(`i-filter`, lucide list-filter). 도구 버튼은 세그와 같은 꼴(fill 바탕·테두리 없음, 켜지면 흰 판+그림자) — 테두리가 있으면 같은 38 이어도 더 커 보였다.
  년차는 **다중 선택** — `S.kmYr` 는 배열, 항목을 고르면 메뉴를 닫지 않고 같은 자리에 다시 연다(`org.mapYrMenu` 재호출). "전체"는 비움. 년차는 **담보책임기간 경과 기준**(`kmYearBand`): 준공일→오늘 경과 연수 y 로
  y<0 준공 전 · <2 2년차 · <3 3년차 · <5 5년차 · <10 10년차 · 10년 초과, 준공일 없으면 "준공일 미입력"(누락이 보여야 하므로 준공 전과 섞지 않는다).
  묶음 머리 클릭 = `S.kmYr` 필터, 권역·담당자와 겹쳐 걸린다(`kmFiltHit`). 현장 줄 툴팁 첫 줄이 "준공 yyyy-mm-dd · n년차".
  현장 목록 생성은 `kmSiteList(all,'reg'|'yr')` 하나 — 묶음 키만 다르고 줄은 같다. 준공일은 `completionDate`(게시본).
- **571차.** 모달 지도 오른쪽 36px(572차 · 처음 72) 을 패널 쪽으로 페이드(`.okm-wrap::after` 그라데이션 판). ⚠ `mask-image` 를 쓰면 안 된다 —
  경계 층과 겹층을 한 표면으로 합쳐 호버마다 지도 전체를 다시 칠한다(550차 렉으로 회귀).
- 제주 이름표는 네모 **가운데**, 다른 시도와 같은 `okm-pl` 입니다 — 아래 귀퉁이의 옅은 글자가 열 아래 페이드에 먹혀 바래 보였습니다.
- 머리 배지 축: 줄의 배지는 뒤의 삭제 버튼(폭 0 · margin-left −6px) 때문에 오른쪽 끝에서 2px 안쪽에 서므로 `.tm-h>.tm-cnt` 에 같은 2px 을 줬습니다.

**⚠ 시도는 큰 것부터 그립니다(531차).** 경계를 단순화하며 폴리곤의 구멍을 지웠기 때문에,
목록 순서대로 그리면 경기가 서울을, 경북이 대구를 덮어 그 시들이 보이지도 눌리지도 않았습니다.

축소 버튼은 **한 번에 전체 보기**로 갑니다 — 여러 시도를 옮겨 다닌 뒤 한 단계씩 되돌리면
안 듣는 것처럼 보였습니다. 한 단계 되돌리기는 우클릭·바다 클릭이 맡습니다.
지도 안의 툴팁은 요소가 커서 중앙이 무의미하므로 **커서 자리**에 띄웁니다(`tipShow` 의 `#okmSvg` 분기).

⚠ 현장 점 크기는 `kmSVG` 의 `const R=(o.r||3.3)*u` **한 곳**입니다.
`R=2.4*u` 같은 문자열로 치환을 시도하면 조용히 실패합니다 — 544차에 여러 회차 동안
크기가 안 바뀐 채 "키웠다"고 보고된 적이 있습니다. 고친 뒤 반드시 grep 으로 값을 확인하세요.

**⚠ `vector-effect:non-scaling-stroke` 은 굵기를 화면 px 로 읽습니다.** 지도 단위(`u`)로 굵기를
넘기면 `u` 배로 부풀어, 529차에는 현장 점의 흰 테가 원을 삼켰습니다. `.okm-pin`·`.okm-ld` 의
굵기는 **CSS 에서만** 주고 JS 는 손대지 않습니다. 반지름(`r`)은 반대로 지도 단위가 맞습니다.

면·선은 530차에 한 단씩 올렸습니다(`--surf3` 면 + `--sepo` 선) — 그 전에는 둘 다 흰색에 가까워
지도가 바래 보였습니다. 지도 여백은 없애서 제주 네모가 카드 모서리에 붙습니다.
축소 버튼은 지도 **왼쪽 위**입니다.
⚠ 이미 보고 있는 시도를 다시 누르면 아무것도 하지 않습니다 — 확대된 면이 화면을 거의 채워
무심코 한 번 더 눌리기 쉬운데, 그때마다 이력이 쌓여 뒤로가 두 번 필요했습니다(531차).

**계층** — 시도를 누르면 그 시도로, **보고 있는 시도를 한 번 더 누르면 그 자리의 시군구**로
확대됩니다(537차 · `kmMuniAt`). 시군구 경계상자는 `muni` 색인의 뒤 네 값입니다.
시군구까지 들어가면 그 안은 읍면동 경계가 없어(광역시만 있습니다) **읍면동 이름**으로 채웁니다.
확대 단계에 따라 아래 층이 켜집니다.
시도 경계 0.9px → 시군구 0.9배 확대에서 0.7px → 읍면동(광역시·특별시·세종만) 0.2배에서 0.5px.
이름표도 같은 순서로 늘어납니다. 자리는 화면에 크게 걸친 것부터 잡고, 이미 놓인
이름표와 부딪히면 접습니다. 현장 점은 이름표 자리 밖으로 밀리고 원래 자리를 가는 선으로 가리킵니다.
되돌리기는 **우클릭 · 바다 클릭 · 뒤로 버튼** 셋 다 `kmBack()` 으로 갑니다.

**제주는 보기 상자 오른쪽 아래 모서리 네모에 그립니다**(`KM_JD` · 좌하단 → 부산 아래 → 모서리 순으로 옮겼습니다).
⚠ 제주 항목의 중심점은 추자도까지 안고 있어 네모 위로 벗어납니다 — `kmSiteXY` 가 네모 안으로 가둡니다.
제자리에 두면 카드가 세로로 길어집니다. 본토 아래 여백이 사라져 같은 폭에서 본토가 더 커집니다 —
`KM_VB0` 높이가 3540 → 3222 로 줄었고, 카드 안쪽 238px 폭을 거의 다 씁니다(지도 높이 364px).
⚠ 제주와 관련된 계산(화면에 걸쳤는지 · 이름표 자리 · 현장 점)은 **전부 옮긴 자리 기준**이어야 합니다.

**좌표는 저장하지 않습니다(529차).** 현장명에서 `kmAuto()` 로 그때그때 찾습니다 —
현장 표의 위치 열, 위치 지정 모달, `cfg/siteGeo` 와 그 DB 규칙은 529차에 모두 걷어냈습니다.
색인을 훑는 일이라 `KM_XY` 에 이름 단위로 기억해 둡니다(현장 이름이 바뀌면 새로 찾습니다).
못 찾은 현장은 **지도에 나오지 않습니다** — 배지의 숫자와 현장 수가 다르면 그 차이입니다.

**담당자가 없는 현장은 속 빈 원**(`.okm-pin.none`)입니다.
⚠ 공통 색(`TEAM_COLOR`)과 첫 담당자 색(`OWN_PAL[0]`)이 같은 파랑이라 **색만으로는 갈리지 않습니다.**
달력이 공통 업무를 속 빈 막대로 가르는 것과 같은 문법을 535차에 지도에도 적용했습니다.

## 1-3. 527차 — 현장명으로 위치 찾기

현장명을 `korea-geo.js` 의 읍면동·시군구·시도 이름 색인과 대조해 자리를 찾습니다.
529차부터는 **사람이 찍지 않고 이것만 씁니다** — 찾은 것만 지도에 나옵니다.

**밖으로 나가는 요청이 없습니다.** 카카오·네이버 지도 API 를 붙이려면 CSP 를 열고
키·도메인을 등록해야 하는데(브라우저에서 REST 직접 호출은 CORS 로 막힙니다),
그러면 현장명이 외부 서비스로 나갑니다. 사내 자료라 그 길은 택하지 않았습니다.

**색인은 두 벌입니다.** `dong` 은 통계청 행정동 경계의 중심, `bjd` 는 **법정동 경계의 중심**입니다.
둘 다 ±0.5~2km 라 같은 등급으로 다룹니다(538차). 현장명에 쓰이는 이름은 대개 법정동 쪽입니다 —
두정동·도안동·방서동·예천동이 그렇습니다. 리(里)는 항목이 5배로 늘고 동명이의가 많아 뺐습니다.

**⚠ `bjd` 좌표의 출처와 라이선스를 확인하고 쓰세요.** 브이월드(국토교통부)의
`행정구역_읍면동(법정동)` 도형에서 뽑은 중심좌표입니다. 원 도형의 배포조건은 **CC BY-NC-ND** 이고,
NC(비영리) 조항이 사내 업무 시스템에 걸릴 소지가 있습니다. 나는 변호사가 아니니 판단은 사내
기준에 맡깁니다. 깨끗하게 가려면 회사 계정으로 브이월드에서 같은 데이터를 직접 받아
`gen12.mjs` 와 같은 방식(경계 중심점 추출)으로 다시 만들면 됩니다 — 결과 형식이 같아
`korea-geo.js` 의 `bjd` 만 갈아 끼우면 그대로 동작합니다.

**공동주택명 색인이 있으면 가장 먼저 봅니다(540차).** 현장명이 곧 단지 이름인 경우가 많은데,
'갑천1 트리풀시티'·'도안'·'대실' 같은 이름은 행정구역 어디에도 없어 지금은 권역 평균 자리로 밀립니다.
도로명주소 **건물DB**(공공데이터포털 · 무료)에는 공동주택명과 법정동코드가 들어 있으므로,
단지 이름을 법정동으로 옮기고 그 법정동 좌표를 쓰면 자리가 정확해집니다.

전국 단지를 통째로 앱에 실으면 수백 KB라 **빌드 단계로 뺐습니다** —
`node scripts/apt-index.mjs <건물DB폴더> 44 43 30 36 41` 로 필요한 시도만 뽑아
`vendor/apt-geo.js` 를 만듭니다. 파일이 없거나 비어 있으면(`window.KRAPT=null`)
그냥 건너뛰고 행정구역 이름 대조로 돌아갑니다 — **없어도 앱은 그대로 돕니다.**
⚠ 단지명은 표기가 조금씩 달라(띄어쓰기·브랜드 순서) 어절 **앞머리**가 아니라
**들어 있으면** 인정합니다. 행정구역 이름은 지금처럼 앞머리로만 봅니다.
⚠ 건물DB 의 열 순서는 배포 회차마다 달라진 적이 있습니다 — 스크립트의 `COL` 을 첫 줄과 대조하세요.

**주소가 있으면 이름보다 먼저 씁니다(539차).** 하자처리 현황 게시본이 현장 주소를 실어 보내면
`SITE_ADDR` 에 담아 매칭 문구로 씁니다 — 주소에는 시도·시군구·읍면동이 다 들어 있어 훨씬 정확합니다.
⚠ 지금 게시본에 주소 필드가 있는지는 확인하지 못했습니다. 없으면 표가 비고 현장명으로 돌아갑니다.
⚠ `S.org.sites` 에는 넣지 않습니다 — DB 규칙이 정해진 필드만 허용해 쓰기가 통째로 거부됩니다.

**⚠ 광주는 통합(36) 때문에 색인에서 통째로 빠져 있었습니다(546차).**
SGIS 시군구·읍면동 자료에서 광주광역시 코드는 `24` 인데 생성 스크립트의 대상 목록에 없어서,
광주 5개 구가 `muni['36']` 에 들어가지 않았습니다. 그 결과 '광주'가 **경기 광주시로만** 걸려
광주광역시 현장이 경기도로 갔습니다. 생성 스크립트(gen7·8·9·10 계열)에서 `24` → `36` 으로
옮기도록 고쳤습니다. **다른 통합·개편이 생기면 이 매핑을 모든 생성 스크립트에서 함께 확인하세요.**

**⚠ 갈리는 이름은 찍지 않습니다.** 후보가 100km 넘게 흩어져 있으면(경기 광주시 ↔ 광주광역시)
평균 자리는 빈 바다가 되므로 `kmAuto` 가 `null` 을 냅니다 — 틀린 자리보다 비워 두는 편이 낫습니다.
현장의 **권역만 채워져 있으면** 저절로 좁혀집니다.

**⚠ 두 색인을 합쳐도 못 찾는 이름이 남습니다.** '갑천'은 하천 이름이라 법정동에 없고,
'대실'은 리(里) 단위라 뺐습니다. 이런 이름은 아래 물러서기로 처리합니다.

**⚠ 좁히기(pcs)는 시군구·시도 히트로만 만듭니다.** 법정동을 여기 넣었더니 동명이의
(충남 두정동 ↔ 광주 두정동)가 서로를 살려 놓아 후보가 안 좁혀졌습니다(538차).
**거리 검사(40km)는 현장명에서 나온 시군구에만 겁니다** — 권역 이름에서만 걸린 시군구까지
쓰면 권역 '대전·세종' 에서 세종만 잡혀 대전 도안동이 40km 밖으로 밀려납니다.

- **매칭 문구에 권역 이름을 붙입니다**(`kmSiteHint`). '갑천1 트리풀시티' 만으로는 강원 갑천면이
  걸리지만, 권역 '대전·세종' 이 붙으면 그 시도로 좁혀집니다.
- **시도가 잡히면 시도로만 좁힙니다.** '서산 예천'이 경북 예천읍으로 가던 것을 이것이 막습니다.
- ⚠ 이름에 시군구가 함께 있으면 **그 시군구에서 40km 안의 동만** 인정합니다(537차) —
  같은 시도 안에도 같은 이름의 동이 있어(천안 ↔ 태안) 엉뚱한 곳으로 새고 있었습니다.
- 물러서는 순서는 **행정동 → 법정동 → 시군구 → 시도** 이고, 여러 곳이 걸리면 **평균 자리**에 찍습니다.
  권역 '대전·세종' 처럼 두 지역에 걸치면 둘의 중간에 떨어집니다 — 어느 한쪽으로 쏠리지 않게.
- ⚠ 권역 이름은 **시군구·시도 수준에서만** 씁니다. 읍면동·법정동에까지 대면 권역 '대전·세종' 이
  세종시 세종동을 잡아 현장 자리를 그리로 끌고 갑니다(532차).

정밀도는 실측 표본에서 행정동·법정동 0.3~2.4km, 시군구 중심 ±5km, 평균 자리 ±25km입니다.
매칭은 붙여 쓴 글자 속을 뒤지지 않고 **어절 앞머리**로만 봅니다 —
'충남천안'에서 '남천(동)'이 걸리던 것을 막기 위해서입니다.
⚠ 권역이 비어 있는 현장은 이 좁히기가 없어 엉뚱한 동명이의로 갈 수 있습니다.

## 2. 파일 구성

| 파일 | 설명 |
|---|---|
| `index.html` | 화면 구조와 전체 스타일. CSS가 전부 여기 들어 있습니다 |
| `app.js` | 모든 로직. 약 10,300줄 |
| `database.rules.json` | Firebase 보안 규칙. **필드를 추가하면 여기도 반드시 함께 수정** |
| `scripts/test/static-audit.mjs` | 배포 전 정적 감사 — FAIL 0 이어야 배포 |
| `scripts/test/smoke.mjs` | 브라우저 스모크 — `?local=1` 로 핵심 흐름을 실제로 눌러 본다 |
| `scripts/test/shot-prod.mjs` | 설정 게시 카드 실렌더 스크린샷(로컬 모드 · CHROMIUM 경로 지정) |
| `scripts/test/e2e-defect.mjs` | 가상 HCS 데이터 전 구간(업로드→등록→소비 21항목) 실브라우저 검증(615차) |
| `scripts/test/rainbow-render.mjs` | 무지개 색 원·업무 막대의 실제 브라우저 렌더링 검증(633차) |
| `scripts/test/firebase-live-e2e.mjs` | 실제 Firebase Auth·RTDB 2계정 E2E — CRUD·권한·새로고침·partial update·동시 수정(634~637차) |
| `scripts/test/run-all.mjs` | 전체 회귀 게이트 단일 실행 — 정적·Rules·브라우저·하자 E2E + 조건부 Firebase E2E |
| `scripts/font-subset.py` | Pretendard 2분할 서브셋 재생성(core·rare·pretendard.css) |
| `scripts/apt-index.mjs` | 도로명주소 건물DB → 공동주택명 색인(`vendor/apt-geo.js`) 생성 |
| `vendor/` | FullCalendar · Firebase SDK · Pretendard 서브셋 · 행정경계(korea-geo.js) 등 자체 호스팅 전부 |
| `widget-lite/` | Tauri(Rust/WebView2) 위젯 — 버전은 웹앱과 따로 |
| `manifest.webmanifest` | PWA 설치용(서비스워커 없음 — 캐시 사고 전력 때문에 안 쓴다) |
| `build-single.mjs` | 단일 HTML로 묶는 빌드 스크립트 |
| `README.md` | 사용 안내 — 앱 안(설정 > 도움말)에서 그대로 읽힌다 |
| `HANDOFF.md` | 이 문서. 배포되지 않는다(앱이 읽는 건 README 쪽) |
| `icon-192.png`·`icon-512.png` | PWA 설치 아이콘 — manifest 가 가리킨다 |
| `scripts/geo2026.mjs` | `vendor/korea-geo.js` 재생성(행정경계). 재생성 명령은 597차 항목에 |
| `scripts/coast-land.mjs` | 해안선 → 육지 폴리곤. **595차에 폐기된 접근이다**(아래 참조) |
| `scripts/site-geo-check.mjs` | 현장이 왜 지도에 안 찍히는지 진단. 사내 PC 에서 돌린다(밖으로 안 나간다) |
| `.github/workflows/smoke.yml` | push·PR 마다 정적 감사 + 브라우저 스모크 |
| `.github/workflows/widget-lite.yml` | `widget-lite/**` 가 바뀌면 위젯 exe 를 만들어 릴리스 |

(예전의 메일 스크립트 `scripts/mail-common.mjs`·`remind.mjs`·`weekly.mjs` 와 Electron `widget/` 은
기능 정리 때 삭제됐다 — 이 표는 static-audit 검사 10번이 실제 파일과 대조한다.)

608차 전수 점검 결과 **위 목록이 저장소 파일 전부이고, 지워도 되는 것은 `scripts/coast-land.mjs` 하나뿐**이다.
vendor 12개는 모두 실제로 불린다(`apt-geo.js` 는 빈 표지만 index.html 이 `onerror` 로 부르는 자리라 남긴다.
지우면 매 접속마다 404 가 하나 뜬다). `widget-lite/assets/index.html` 은 내용이 비어 보여도
`tauri.conf.json` 의 `frontendDist:"assets"` 가 가리키는 자리라 지우면 위젯 빌드가 깨진다.

## 3. 데이터 구조 — 업무 하나로 통합됨

39차와 44차를 거치며 **일정과 업무를 하나로 합쳤습니다**. 예전에는
`calapp/plans`(일정)와 `calapp/tasks`(주요업무)가 별개였지만 지금은
`calapp/tasks` 하나만 씁니다.

```
calapp/tasks/{sid}/{iid}
  text        제목
  kind        구분 ''(일반) | gather | meet | trip | etc
  prog        내용 (389차에 진행경과·처리계획을 한 칸으로 합쳤다)
  plan        옛 처리계획 — **읽어서 내용 뒤에 붙일 뿐 새로 쓰지 않는다**(taskBody 참조)
  st          0예정 1진행 2완료 3보류
  date        날짜 — 있으면 달력에 뜬다 (기한 개념은 없앰)
  end         종료일 (기간 업무)
  time, endTime  시작·종료 시각
  recur       {f, until} 반복. doneOn · skipOn · moveOn 이 딸린다
  remind      당일 아침 메일
  site        현장 id
  assignees   담당자 {uid:1}
  color, order, links, comments
```

`sid`는 그 업무가 속한 곳입니다. 담당자를 고르면 첫 담당자의 uid, 없으면 팀 id입니다.

나머지 노드는 `calapp/org`(팀·권역·현장), `calapp/people/{uid}`(소속·직급),
`calapp/prefs/{uid}`, `calapp/mentions/{uid}`, `calapp/cfg`입니다.
`calapp/trash`(삭제 30일 보관 · 383차)와 `calapp/archive`(완료 6개월 뒤 보관 · 384차)는
업무를 z(lz 압축)로 통째 담는 같은 wrap 모양입니다 — 아카이브는 달력 과거 이동·찾기 때만
통째로 한 번 읽어 합치고, 건드리면 tasks 로 되살아납니다.
`users/{uid}`는 하자처리 현황 앱과 공용이라 권한과 프로필이 들어갑니다.

`calapp/plans`와 `calapp/recur`는 폐기됐지만 **자동 이전을 위해 규칙에 남겨 뒀습니다**.
서버에 남아 있으면 로그인 시 한 번 업무로 옮기고 원본을 한 건씩 지웁니다.

## 4. 직급과 권한

직급(`rank`)은 `head`(팀장) · `lead`(공구장) · `member`(담당자)입니다.
팀장은 권역·현장 없이 팀 전체를 보고, 공구장은 권역 전체, 담당자만 현장을 개별 지정합니다.

권한(`role`)은 이와 별개로 `editor` · `viewer`입니다. 일정·업무·조직·계정 목록
조회는 누구나 가능하고, 권한 변경·메일 설정·연결 URL·가져오기는 editor만 됩니다.

## 5. 반복해서 걸린 함정 — 작업 전에 꼭 확인

- **DB 규칙에 없는 필드를 저장하면 쓰기 전체가 거부됩니다.** 새 필드를 추가하면
  `database.rules.json`에도 반드시 넣으세요. 규칙을 먼저 게시하고 앱을 올려야 합니다.
- **`users/{uid}`는 부분 쓰기가 안 됩니다.** 항상 전체를 써야 하므로 기존 값을
  보존하는 `userRecord(patch)`를 쓰세요. 빠뜨린 필드는 삭제됩니다.
- **CSS 블록을 통째로 교체하면 뒤 구간이 함께 날아갑니다.** 교체 후 '사용 클래스 vs
  정의' 전수 점검과 함수 유실 확인을 반드시 하세요. 실제로 여러 번 함수가 사라졌습니다.
- **`select`에 `data-act`를 달아도 클릭 처리에서 막힙니다.** 예전에 선택창 오작동을
  막으려고 넣은 가드 때문이며, `change` 쪽에 별도 분기를 넣어야 동작합니다.
- **버전은 한 숫자입니다(390차).** zip 이름 · `app.js?v=NNN` · `APP_VER` 이 모두 같은 회차 숫자입니다 —
  예전의 semver(4.8.1)는 회차와 무엇이 다른지 설명할 수 없어 없앴습니다. static-audit 이 두 값을 대조합니다.
- **같은 명시도면 뒤 규칙이 이깁니다.** 공통 규칙을 새로 만들 때 앞쪽에 남은 개별
  정의를 함께 지우지 않으면 옛 값이 살아남습니다.
- **사용자 삭제는 반드시 `store.trashTask()` 를 거칩니다(383차).** `putTask(sid,iid,null)` 를
  직접 부르면 휴지통을 건너뛰고 영영 지워집니다 — 그건 담당자 변경 같은 '이동'에만 씁니다.
- **같은 업무가 tasks 와 archive 두 곳에 남으면 안 됩니다(384차).** putTask·trashTask 가
  아카이브 사본을 걷어 줍니다 — tasks 에 쓰는 새 경로를 만들면 같은 처리를 반드시 넣으세요.
  또 `migrateRemote` 처럼 전 업무를 훑어 되쓰는 코드는 `arch` 플래그를 건너뛰어야 합니다.
- **FullCalendar 요일 헤더도 `fc-day-sun`/`fc-day-sat`을 가집니다.** 주말 배경을
  줄 때 `td.fc-daygrid-day`로 한정하지 않으면 헤더까지 덮습니다.
- **`table-layout:fixed`에서 열 폭을 지정해도 컨테이너가 좁으면 비율로 축소됩니다.**
- 치환 작업 전에는 **현재 값을 grep으로 확인**하세요. 앞선 작업에서 이미 바뀌어 있어
  치환이 조용히 실패한 적이 있습니다.

## 6. 검증 방법

`/home/claude/work/*.mjs`에 puppeteer 검증 스위트가 있습니다(이 zip에는 없음).
현재 35개 스위트 377항목을 통과합니다. 새 대화에서는 다시 만들어야 하지만,
검증에서 반복해 걸린 점은 기록해 둡니다.

- CSS `:hover`는 합성 이벤트로 발동하지 않습니다. `page.hover()`를 쓰세요.
- `dataset`은 `evaluate` 반환 시 빈 객체가 됩니다. `getAttribute`를 쓰세요.
- 선택 상태 항목은 배경이 그라디언트라 `backgroundColor`가 투명으로 잡힙니다.
- `box-shadow` 계산값은 `inset`이 문자열 끝에 옵니다.
- 테마는 브라우저 저장소에 남아 다음 페이지로 넘어갑니다.
- 구조를 바꾸면 검증 기대치도 함께 갱신해야 합니다.

## 7. 배포 순서

1. `database.rules.json`을 Firebase 콘솔에 먼저 게시
2. 나머지 파일을 저장소에 올림
3. 브라우저에서 강력 새로고침(Ctrl+Shift+R)
4. `scripts/`를 바꿨다면 GitHub Actions가 새 파일을 쓰는지 확인

## 7.4 CSS 수칙 (422차)
- 새 규칙은 관련 화면 구획의 **끝**에만 추가한다 — 파일 맨 뒤에 덧대지 않는다.
- 같은 셀렉터 재선언 전에 반드시 기존 선언을 검색한다. 뒤쪽 재선언은 앞의 수정을 말없이 무효화한다.
  static-audit 11번이 신규 재선언을 FAIL로 잡는다(기존 138건은 `CSS_DUP_BASELINE`에 동결).
  의도적 재선언이면 베이스라인에 `셀렉터 :: 프로퍼티` 항목을 추가할 것.
- 스모크에 반복 일정 3케이스(주간 생성 · 다음 회차 발생 · 이 날짜만 제외)가 있다 — 반복 로직을 만지면 이 시험이 지킨다.

## 7.5 — 50차에서 바뀐 것

- 월간 칩의 시간 이중 표기 제거(`displayEventTime:false`).
- 반복 전개(recurDates)가 범위 뒤 회차를 범위 안으로 옮긴 경우도 표시합니다.
- 메일 스크립트(remind·weekly)가 회차 이동(moveOn)을 반영합니다. 전개 로직은
  `mail-common.mjs`의 `expandRecur` 하나로 합쳤습니다 — **앱의 recurDates와 같은
  규칙이므로 한쪽을 고치면 반드시 다른 쪽도 함께 고치세요.** 반복 업무의 완료
  판정(doneOn)은 원래 회차일(src) 기준입니다.
- 라이브 부팅 캐시(`calapp.boot.v1`) — 로그인 직후 마지막 데이터로 먼저 그리고
  FB 값이 도착하면 덮어씁니다. 로그아웃 시 지웁니다.
- 담당자 묶음·개인 목록에서 본인 담당자 배지를 숨깁니다(공동 담당자만 표시).
- 데드코드 제거: ACT nq.plan · tk.linkOpen · tk.toPlan · set.saveUrl · acct.site ·
  빈 set.dark, 함수 ownOk · subjectName, 다크 체크박스의 data-act.

## 7.6 — 51차에서 확정된 완료 업무 정책

- **완료 업무는 지우지도 옮기지도 않는다** — 무료 플랜으로 연 단위는 충분하다고 판단.
- 목록: 완료 후 7일까지는 그대로 보이고, 그 뒤는 개인 화면에선 "지난 완료 N건 보기"로
  접히고 집계 화면에선 숨는다. **필터·검색을 켜면 지난 완료도 전부 대상**이 된다
  (상태 필터 '완료'로 찾아보는 용도 — openItems 가 taskListHTML 과 같은 규칙).
- 달력: 완료 포함 전부 표시(원래부터 그랬음 — buildEvents 에 숨김 없음).

## 7.7 — 52차 위젯 유리 모드 (진행 중, Electron 단계 남음)

- `?w=1&glass=1` — 바탕화면에 스며드는 전면 달력(카드 틀 없음, 앱 컬러의 반투명 틴트).
  **함정: html.dark 의 `color-scheme:dark` 는 루트가 투명해도 브라우저가 #121212
  캔버스를 깐다 — 유리 모드는 `color-scheme:normal!important` 필수.** FC 내부
  스택(#app·view·harness·scroller·table·--fc-page-bg-color)도 전부 투명화했다.
- 위젯 공통: 년·월 폰트 통일(.y 상속), 넘김 버튼 묶음 라운드, 좁아도 헤더 한 줄
  (년월 좌 · 컨트롤 우), 오늘 목록은 넓으면 2~3열 grid(minmax 420px).
- 위젯 설정 팝업(#wgSet, 기어 버튼): 배경 진하기·글자 크기·오늘 목록·다크.
  `calapp.wid` 에 로컬 저장, widApply() 가 적용. 진하기는 `#wgDyn` 스타일 태그에
  리터럴 규칙으로 주입(변수 방식도 되지만 명시적이라 유지).
- **검증 함정: `#fcal .fc-daygrid-day` 에 `transition:background .12s` 가 있어
  배경을 바꾼 직후 getComputedStyle 은 시작값을 돌려준다 — 400ms 뒤에 읽을 것.**
- 남은 것: Electron main.js 투명 창(Win11 acrylic 우선, 실패 시 transparent 폴백)
  + 트레이 반투명 토글 + 다중 모니터 좌표 보정(저장 위치가 꺼진 모니터면 주 모니터로).

## 7.8 — 53차 대폭 간소화 (사용자 지시)

- **주간뷰 삭제** — 월간만 남김. 월/주 세그, timeGrid 설정·CSS(wkh 포함), cal.view
  액션, S.calView 전부 제거.
- **시각은 하나만** — endTime 폐기. 폼(달력 편집기·주요업무)에서 종료 시각 칸 제거,
  fmtSpan 은 시작 시각만, cleanTask 가 더는 쓰지 않아 저장 시 옛 값도 자연 소거.
  규칙의 endTime 필드는 옛 데이터 읽기용으로만 남아 있음.
- **담당자 한 명 지정** — 다중 칩(ownPickHTML·pkSync·pk.* 액션·pk CSS) 전부 제거,
  ownSelHTML 단일 select 로. '지정 안 함'이면 팀 공통. 옛 다중 데이터는 목록에
  그대로 보이고, 폼을 열어 저장하면 한 명으로 줄어든다(의도된 동작).
- 업무 구분 라벨 **취합 → 공통**.
- **오늘 표기** — 파란 원형(23px)이 칩 시작 높이를 밀어 다른 셀과 단차를 만들던 것
  제거, 숫자 파란색·볼드만(단차 0px 확인).
- **지남 배지·좌측 띠 제거**(fc-event.late), **담당자 색 점 제거**(hasown ::before —
  사용자가 사진으로 물어본 '업무 앞의 ○'가 이것). planEvent 의 관련 클래스·owncol·
  eventDidMount 정리.
- **임의 색 칩 우클릭 삭제** — .pal-custom contextmenu 로 제거, 선택 중이었으면 첫
  칩(자동)으로 복귀.
- 사용 안내(GUIDE_HTML)의 다중 담당·점선 배지·달력 아이콘 서술 현행화.

## 7.9 — 54차 이름 변경 · 조직/현장 관리 개편 · 달력 다듬기

- 이름: **주요업무 현황 → 업무 목록**, **조직 관리 → 조직/현장 관리**(사이드바·타이틀·안내문).
- 조직/현장 관리 2열 재구성 — 좌: 팀·권역, 우: **계정 | 현장 탭**(orgseg, S.orgTab).
  현장 탭은 하자처리 현황의 현장 관리 표(mgtbl)를 그대로 이식: 권역·현장명·**세대수·
  동수·상가수·준공일** 인라인 즉시 저장(org.siteUpd change 위임). 그리드도 하자앱과
  동일한 300px | 1fr. **규칙 변경 있음 — sites 에 units/buildings/commercialUnits/
  completionDate 허용을 추가했으므로 배포 시 규칙 먼저.** 설정의 게시본 불러오기가
  이 필드들도 함께 가져온다. 하자앱 전용인 공가 토글·업데이트일 열은 제외.
  기존 현장 드래그 정렬(sti2·wireSiteDnD)은 표 전환으로 제거 — 순서는 권역순·이름순.
- 달력: 주말 음영을 중립 회색 rgba(60,60,67,.045)로(노란끼 제거), 공휴일 톤이 주말보다
  우선(뒤 선언), **마우스 hover 음영이 최우선(!important)**. 오늘 셀은 숫자 왼쪽에
  파란색 '오늘' 라벨(dhol.dtoday — 공휴일 이름과 같은 자리).
- 업무 목록 화면: 헤더 좌 '업무 목록'(대상명은 부제 tkm-sub), 업무 추가 버튼은 헤더
  우측(원래 margin-left:auto), 항목 간격 7→12px, 좌측 트리 하위 항목(공통·담당자)
  앞에 하자앱과 같은 6px 동그라미(.tks-item .std — hover·선택 시 파랑).
- 달력 편집기: [시간|반복] · [현장|담당자] 2열 배치.

## 7.10 — 55차 조직/현장 다듬기 · 모바일 전면 개편

- 현장 표: 하자앱 전용 열(공가세대·공가상가·업데이트일)을 **표기하되 회색 비활성**
  (.mg-dis, disabled) — 열이 빠지면 간격 균형이 깨진다는 사용자 판단.
- 조직/현장 탭 세그에서 팀명을 밖으로 분리, 현장 탭에선 미배정 카드도 숨김.
- 일자 패널: '업무 추가'를 파란 헤더(dp-top) 우측 + 아이콘(.dp-addtop)으로 이동,
  하단 버튼 제거. #dpList gap 9px. **검증 스크립트들이 '업무 추가' 텍스트로 버튼을
  찾던 것 → .dp-addtop 클릭으로 전부 교체함.**
- 모바일(≤960px):
  · 업무 일정 = 달력 풀스크린(#view-calendar margin:-14px, cal-card 테두리 제거,
    100dvh-토바), 헤더는 년월(좌)·넘기기/오늘(우) 한 줄.
  · 일자 패널(.dp-col)은 **하단 시트** — 날짜 탭(selDate)에 dpSheet(true), 스크림
    탭·X(.dp-x)·뷰 전환 시 닫힘. mobClose 가 시트 우선 닫기.
  · 사이드바 접기 버튼(.sbtg)은 드로어에서 숨김(mini 상태만 만들던 오류).
  · '이전 날짜 선택 표시 남음'의 정체는 **iOS sticky hover** — hover 음영을
    @media(hover:hover) 로 감싸 터치 기기 제외. (이 때문에 hover 규칙이 미디어룰
    안으로 들어가 순서 검증이 selectorText 평면 순회로는 못 찾는다 — 실질 정상.)
- 모바일 전 화면 가로 넘침 0 확인(달력·시트·업무·내업무·조직·설정), 현장 표는
  페이지가 아닌 표 내부 가로 스크롤.

## 7.11 — 56차 업무 일정 헤더 통합

- **데스크톱은 달력 위 헤더(cal-head)를 숨기고 달력이 그 높이까지 확장**
  (@media min-width:961px, body:not(.wid) 한정 — 위젯·모바일은 자체 헤더 유지).
  기능은 우측 파란 날짜 헤더(dp-top)로 이전: 날짜 텍스트(.dp-datebtn)를 누르면
  연·월 모달(cal.pick), 우측에 ‹·오늘·› 캡슐(.dp-nav, cal.prev/today/next 재사용).
  모바일 시트에선 dp-nav 숨김 + 날짜 버튼 비활성(라벨 역할만).
- 업무 N건(dpCnt)은 목록 패널 상단 좌측(.dp-lh)으로 — 우측에 '업무 추가' 텍스트
  버튼(btn bo bsm), 아래 1px 구분선. dp-top 의 + 아이콘(.dp-addtop) 제거.
  **검증 스크립트 편집기 열기는 .dp-lh .dp-add 클릭으로 재교체.**
- 달력 칩 제목 볼드 해제(400), 주말 음영은 이번 달 칸만(:not(.fc-day-other)),
  가로줄 .155→.125(대비 약 25).
- 일자 패널 카드 간격 9→5px. calendar-mockup.html 삭제(zip 에서 제외).

## 7.12 — 57차 일자 패널 재편 · 달력 플랫화

- 날짜 헤더(dp-top): 연·월 이동 버튼을 **날짜+꺾쇠만**으로 좁히고 보조문구(요일·
  공휴일·오늘, #dpDow)는 버튼 밖으로 분리. ‹·오늘·› 캡슐 배경 제거.
- **필터를 별도 카드(.dp-fcard)로 분리** — 1행 권역 select(#regFilter)·담당자
  select, 2행 검색창·'업무 추가' 버튼. 권역 칩(regSeg·.reg·cal.reg 액션·다중
  선택)은 select 단일 선택으로 대체(S.filter.reg 는 '*' 또는 [id] 하나).
  regFilter 는 change 위임에서 처리.
- 목록 패널 상단 건수 줄(.dp-lh)·dpCnt 삭제. **검증 편집기 열기는
  .dp-fcard .dp-add 클릭으로 재교체.**
- 달력을 감싸던 카드 프레임 제거(body:not(.wid) .cal-card 투명·패딩 0 — 위젯 제외
  전 해상도). fc-scrollgrid 테두리가 곧 외곽.
- 요일 평일 글자 --lbl2 → --lbl(검정). 공휴일 음영이 주말보다 우선 — **주말 규칙에
  :not(.fc-day-other) 를 붙이며 명시도가 hol 을 역전했던 것**을 hol 에도 같은
  :not 을 붙여 해결(이웃 달 공휴일 음영도 자연히 제외).

## 7.13 — 58차 미세 조정

- 우측 열 카드 간격 16→9px(.dp-col gap) — 줄어든 만큼 업무 패널(flex:1)이 커짐.
- 달력 자체 배경 — 카드 프레임 제거로 페이지 배경이 비치던 것을 .fc-scrollgrid 에
  background:var(--bg2). 셀의 알파 톤(주말·공휴일 등)은 이 흰 바탕 위에 합성된다.
- 이웃 달 숫자 opacity .6→.85, 주말 음영 .045→.035(다크 .028→.022).

## 7.14 — 59차 달력 '떠 있는 칸' 체계

- **배경 체계 재작성** — 날짜 칸을 떠 있는 흰 카드처럼: 바탕은 background-color
  (이번 달 var(--bg2), 이웃 달 rgba(255,255,255,.5) — 다크는 rgba(33,33,33,.5)),
  주말·공휴일·오늘·선택일·hover 톤은 전부 **background-image(linear-gradient)** 로
  얹어 서로 간섭하지 않는다. 음영 계층은 선언 순서로 오늘 > 공휴일 > 주말.
- 격자선은 **투명 2px + background-clip:padding-box** — 칸 사이로 페이지 배경이
  보인다. fc-scrollgrid 외곽 테두리·배경 제거, fc-theme-standard 보더 투명,
  tr:first-child border-top:0 예외도 제거(균일 간극). 요일 행 배경 #DEE3EA
  (다크 #3A3A3C), 요일 칸도 같은 간극 처리.
- 날짜 헤더 숫자(.dp-date) min-width:128px + tabular-nums — 날짜 길이가 바뀌어도
  꺾쇠·요일 문구 위치 고정.
- **검증 기대치 갱신**: 톤이 backgroundColor → backgroundImage 로 옮겨가
  v57 공휴일·v58 배경/주말 검사를 새 체계로 수정함(구조를 바꾸면 검증도 함께).

## 7.15 — 60차 대비 강화(D안) · 라벨 좌측 정렬

- 가시성 시안 4안 중 **D(A+C 결합) 채택**: 칸 사이 뒤판을 fc-scrollgrid
  background #DCDFE6(다크 #0F0F10)으로, 주말 .06 · 공휴일 .08 · 오늘 .13
  (다크 .04/.13/.22), 이웃 달 40%.
- 셀 안 공휴일 이름·'오늘' 라벨을 **왼쪽 정렬**(.dhol margin-right:auto) —
  fc-daygrid-day-number 가 내용폭이라 안 붙던 것을 flex:1 로 행 전체 폭 확보.
  라벨 없는 칸은 숫자 우측 그대로.
- 날짜 헤더 고정 폭 128→108px.
- 폰트 가시성 비교 3안(현재 Pretendard / 시스템 폰트 / Pretendard 튜닝)을 실제
  앱 렌더로 만들어 전달(폰트비교.png) — 사용자 선택 대기.

## 7.16 — 61차 달력 폰트 튜닝(3안 채택)

- 폰트 비교 3안 중 **3안(Pretendard 유지 + 달력 한정 튜닝)** 채택: 날짜 숫자
  12px/600 → **13px/700**, 공휴일 라벨 12→12.5px, 칩 11→11.5px·굵기 400→500·
  자간 -.01em, 요일 700→800. 앱 폰트 통일성은 그대로.

## 7.17 — 62차 외곽 정리 · 톤 재조정

- 달력 바깥 둘레의 뒤판 노출 제거 — 간극 보더는 칸 '사이'에만:
  td:first-child border-left:0, tr:first-child td border-top:0,
  th:first-child border-left:0. fc-scrollgrid 에 overflow:hidden 재추가
  (59차 재작성 때 빠져 radius 잘림이 안 되던 원인) → 모서리 라운드가 코너 칸을
  실제로 잘라낸다.
- **뒤판 배경을 fc-scrollgrid → .fc-col-header/.fc-daygrid-body 로 이동** —
  scrollgrid 에 칠하면 행 높이 반올림 잔여만큼 하단에 띠로 노출된다(다크 #0F0F10).
- 요일띠 #D3D9E2(다크 #424245), 주말 .06→.045(다크 .03), 오늘 .13→.15
  (다크 .25 — 선택 칸 음영과 동일 수위).
- 픽셀 실측: 코너 라운드 잘림·둘레 노출 0·칸 사이 2px 간극 유지 확인.
  하단 y920대의 옅은 회색은 노출이 아니라 이웃 달 반투명 셀 자체(흰 40% 합성색).
- 회귀 기대치 갱신: v60(주말·오늘·뒤판 위치)·v59(요일띠)·v58(주말), 알파 .045 는
  "0.043" 으로 직렬화되므로 '0.04' 포함 검사로.

## 7.18 — 63차 연·월 팝오버 · 간극 실색화 · 오늘 배지

- 연·월 선택을 모달 → **날짜 헤더 하단 팝오버(#ymPop)** 로: .dp-filtercard
  position:relative 기준 absolute, 다시 누르면 토글, 바깥 클릭(캡처 단계) 닫힘,
  연 이동 갱신은 $('#ymPop')||$('#mbody') 로 팝오버 우선. ymPickHTML 재사용.
- 날짜 헤더 보조문구(#dpDow — 요일·공휴일·오늘) 삭제(마크업·rDayHead·CSS).
- 요일 행: fc-col-header 뒤판 제거(투명) — 요일 칸 사이가 페이지색으로 분리.
- **간극을 배경이 아닌 실색 보더(#DCDFE6, 다크 #0F0F10)로 전환** — 테이블 폭·높이
  반올림 잔여에 뒤판 배경이 우·하단 1px 선으로 비치던 문제 원천 차단. 남아 있던
  선의 진짜 범인은 **scrollgrid 래퍼 td(.fc-scrollgrid-section>td)의 1px
  --fc-border-color 보더** — border:0 처리(elementFromPoint 로 확정).
  background-clip 은 더 이상 필수 아님(잔존 무해).
- 오늘 표시: 셀 음영 제거 → **숫자 배지**(.fc-day-today .dnum — 파랑 배경·흰 글자·
  radius 999, padding 2px 7px 을 **음수 마진으로 상쇄**해 행 높이·숫자행 높이 영향
  0 — 53차에 원형을 뺐던 단차 문제 회피). '오늘' 텍스트 라벨은 유지.
- 픽셀 실측: 사방 라운드·둘레 노출 0(좌우 대칭), 요일 간극 페이지색, 본문 간극
  #DCDFE6 2px. 회귀 기대치 갱신: v63 신설 8, v62 래퍼 보더·간극 보더, v60/v59
  간극·오늘 배지 기준.

## 7.19 — 64차 배지 잘림 · 대비 · 아웃라인

- 오늘 배지가 셀 오른쪽에서 잘리던 문제: 좌우 음수 마진(-7px) 중 **오른쪽을 0으로**
  두고 확장분을 왼쪽(-14px)으로 몰았다. 배지 오른쪽 끝이 day-number 의 우측
  패딩(8px) 안에 들어와 잘리지 않으면서 행 높이 영향은 여전히 0.
- 이웃 달 칸을 반투명(흰 40%) → **회색 실색 #E7E9EF(다크 #1A1A1C)**, 주말 음영은
  .045→.03. 실측 주말 248 vs 이웃 달 231 로 확실히 갈린다(전에는 246 vs 247).
- 다크에서 요일 칸 사이가 안 갈라지던 문제: 라이트는 페이지색이 그대로 보여 충분
  하지만 다크는 대비가 없어 th 의 border-left-color 를 #0F0F10 으로 명시.
- 달력 전체 1px 아웃라인 — border 는 radius/overflow 와 어긋나므로
  **box-shadow 0 0 0 1px rgba(60,60,67,.18)**(다크 흰 .14).

## 7.20 — 65차 오늘 배지 정원 · 선택 음영 복구

- 오늘 배지를 **완전한 원(20×20)** 으로: 글자를 11.5px 로 줄여 넣고, 원의 세로
  초과분(숫자 줄 19.5 대비 0.5px)만 음수 마진으로 상쇄, 가로 확장은 왼쪽(-6px)으로
  흡수. 칩 시작 위치(evTop)·행 높이 차이 0, 우측 잘림 없음.
- **오늘을 선택해도 파란 음영이 안 나오던 버그**: 주말·공휴일 규칙이 :not(.fc-day-other)
  때문에 명시도 4, sel-day 는 3이라 오늘(토요일)에서 주말 톤이 이겼다. sel-day 에
  :not(.x-none) 을 붙여 동급으로 올려 해결(선언은 뒤라 선택이 최우선).
- 회귀 스크립트 보정: 부팅 시 오늘이 선택돼 있어 오늘 셀 톤 검사에 sel-day 가
  섞인다 → 측정 전에 다른 날을 선택하는 단계를 v59·v60·v62·v63 에 추가.

## 7.21 — 66차 달 이동 시 선택일 동기화 · 오늘 아이콘

- **월 인지 문제 해결(C 개량안)**: cal.prev/next 뒤에 syncSelToView() — 보고 있는
  달에 오늘이 있으면 오늘, 없으면 그 달 1일을 선택한다. 헤더 날짜가 곧 '보고 있는
  달'이 되어 별도 월 표시가 필요 없고, 원래 달로 돌아오면 오늘이 다시 잡혀 맥락이
  복원된다. 일자 패널도 자동으로 따라간다.
- '오늘' 텍스트 버튼 → 아이콘(#i-today: 달력에 점). 세 버튼 모두 24px 원형으로 통일.
- 회귀 기대치 갱신: v53 '오늘 원형 제거' 검사는 65차 배지 도입으로 무효 →
  '배지 + 칩 단차 0' 으로 대체. v54 주말·공휴일은 backgroundImage 기준으로,
  hover 순서 검사는 @media(hover:hover) 안이라 평면 순회에서 -1 이 정상.

## 7.22 — 67차 접힌 사이드바 정리

- **접어도 프로필이 사라지던 버그**: #sidebar.mini .sb-acct{display:none} 이 원인.
  아바타만 남기고(이름·권한 배지 숨김) sb-foot 을 세로 배치로, 테마 버튼도 아래에
  전체 폭으로 유지.
- **팀 선택기가 사라져 메뉴가 위로 올라가던 문제**: mini 에서 select 대신 팀 약칭
  배지(.tsel-mini, teamAbbr() — 공통 접두 'H서비스'를 떼고 가운데를 쓴다:
  'H서비스중부팀'→'중부', '천안1팀'→'천1')를 표시하고,
  누르면 nav.toggle 로 펼쳐지며 선택창이 드러난다. 자리 높이가 유지돼 아이콘이
  올라가지 않는다.
- **팀 선택기가 조직 화면을 거쳐야 그려지던 버그**: rTeamSel 이 rOrg 안에서만
  호출됐다 → boot 와 rAll 에도 추가(사이드바 상시 요소).
- 분류 라벨(.sbsl)은 접으면 글자 대신 구분선(::after)으로. **height 를 펼친 상태와
  같은 29px 로 고정**해야 아이콘 위치가 안 밀린다(패딩만 조정하면 12px 밀림).

## 7.23 — 68차 접힘 배지 페인트 버그 · 테마 버튼

- **접힘 상태에서 팀 배지가 화면에 안 나오던 버그**: 레이아웃 rect 는 사이드바 안
  (x 12.5)인데 실제 페인트는 사이드바 밖(x 77~123, 캡처마다 다름)이었다 — 접힘
  전환 시 flex 재배치의 페인트가 어긋난 것. **.tsel-mini 를 position:absolute
  (left 5px, top 8px) 로 좌표 고정**해 해결(부모 .teamsel 은 position:relative,
  height 51px). 픽셀 실측으로 배지 글자가 x22~41(사이드바 폭 64 안)에 그려짐을 확인.
  참고: 조각 innerHTML → 정적 마크업(#tselWrap/#tselMini, 내용만 채움)으로도
  바꿨다(그 자체로는 해결되지 않았지만 구조가 단순해짐).
- 접었을 때 테마 전환 버튼은 숨김(#sidebar.mini .sb-th1{display:none}) — 펼치면 그대로.
- **app.js 캐시로 옛 코드가 도는 문제**: `<script src="./app.js?v=67">` 로 버전
  쿼리를 붙였다. 배포 때마다 숫자를 올릴 것. build-single.mjs 는 정규식으로
  스크립트 태그를 찾도록 수정(고정 문자열 매칭이면 쿼리가 붙는 순간 빌드 실패).

## 7.24 — 69차 접힘 사이드바 단순화

- 분류 라벨의 구분선(::after) 취소 — 접으면 **글자만 감추고(opacity 0) 자리(29px)는
  유지**. 아이콘 위치가 밀리지 않는 이유는 그대로.
- 프로필은 접어도 다른 메뉴와 같은 자리(왼쪽 정렬·같은 패딩) — mini 전용 중앙 정렬·
  세로 배치 규칙 제거, .sb-acct{flex:1} 만 남김. 아바타 좌표가 펼침·접힘 동일(x16).
- **팀 약칭 배지 폐기**(디자인상 어울리지 않음) — 관련 마크업(#tselMini)·teamAbbr()
  제거. 접으면 팀 선택기는 감추되 **자리 높이(51px)는 유지**해 아래 아이콘이
  올라가지 않게 했다.
- app.js 버전 쿼리 v67 → v69. 검증은 v67.mjs 폐기, v69.mjs 신설(6항목: 아이콘·
  아바타 위치 불변, 펼침 복귀, 라벨 자리, 배지 없음, 팀 자리 높이).

## 7.25 — 70차 접힘 마감

- 접으면 팀 자리의 구분선도 제거(border-bottom:0, margin-bottom:0) — 자리 높이
  51px 은 그대로라 아래 아이콘은 안 밀린다.
- 분류 라벨은 **페이드 없이 즉시 사라지게**(visibility:hidden + transition:none).
  opacity 트랜지션이면 폭이 줄어드는 동안 글자가 함께 밀려 '날아가는' 것처럼
  보였다. 전환 중간 프레임(110ms)에서 라벨 글자 픽셀 0 확인.
- app.js 버전 쿼리 v69 → v70.

## 7.26 — 71차 연·월 팝업 잘림 수정

- **연·월 팝업이 안 보이던 원인**: 붙이던 호스트 .dp-filtercard 에 overflow:hidden
  이 있어(카드 라운드용) 팝업 높이 230px 중 헤더 카드 49px 밖이 전부 잘렸다.
  DOM·좌표는 정상이라 63차 검증(존재·위치)에서는 잡히지 않았다.
- **호스트를 바깥 열(.dp-col, position:relative)로 이동**하고, top 은 헤더 카드의
  offsetTop+offsetHeight+7 로 계산해 붙인다. 검증에 elementFromPoint 히트 테스트를
  추가해 '실제로 보이는지'까지 확인(v71 7항목).
- v63 기대치도 .dp-col 기준으로 갱신. app.js?v=71.

## 7.27 — 72차 접힘 시 1px 밀림 제거

- 접으면 메뉴가 1px 위로 올라가던 원인: 팀 자리를 height 51px + border-bottom:0 +
  margin-bottom:0 으로 뒀는데, 펼침 상태는 **높이 50 + 테두리(border-box 포함) +
  margin-bottom 2 = 52** 였다. 접힘도 **height 50px + margin-bottom 2px 유지,
  구분선은 border-bottom-color:transparent 로만 감춤** → 총 점유 52px 로 일치.
- 검증 강화: v69 첫 항목을 '모든 메뉴·아바타의 y 이동량이 전부 0' 으로 바꿨다
  (허용 오차 2px 로 두면 1px 밀림을 놓친다). app.js?v=72.

## 7.28 — 73차 달력 헤더 복구 · 날짜 헤더 역할 정리

- **데스크톱 달력 헤더(cal-head) 복구**(56차에 숨겼던 것) — 좌 '2026년 8월'(누르면
  연·월 팝업), 우 ‹·오늘·›. 모바일·위젯과 같은 형식. **높이 46px + margin-bottom
  9px** 로 두면 우측 날짜 헤더 카드(45.8) + 카드 간격(9) 과 같아 달력 상단선이
  필터 카드 시작선과 맞는다(실측 127/127).
- 날짜 헤더(dp-top)는 **표시 전용** — 연·월 버튼(.dp-datebtn)·달 넘기기(.dp-nav)
  제거, 우측에 보조문구(#dpDow: 요일·공휴일·오늘) 부활.
- **달을 넘겨도 선택일은 그대로**(66차 syncSelToView 폐기). 헤더 날짜는 '선택한 날',
  달력 헤더가 '보고 있는 달'로 역할이 갈린다.
- ymOutside 가드도 .dp-datebtn → .cal-title 로. 검증: v56·v66 폐기, v73 신설(6항목),
  v57·v63·v71 셀렉터·기대치 갱신. app.js?v=73.

## 7.29 — 74차 업무 작성 폼 개편(A안)

- **구조**: 제목 고정 바(.pe-bar, 우측 × = plan.cancel) / 기간 한 줄(.pe-range —
  시작일 + '＋ 여러 날' 토글 + 종료일, 기본 접힘) / [시간|담당자] / [현장|색] /
  '자세히'(반복·반복종료·메모·리마인드·회차 이동) / 하단 고정 저장 줄.
  **높이 521 → 335px.**
- **색**: 팔레트를 펼치지 않고 점 하나(.pe-col + #peColDot). 누르면 팝오버(#colPop)
  로 그리드가 뜨고 고르면 점에 반영·자동 닫힘. 값은 S.planEdit.draft.color 에 두고
  저장 때 읽는다(더 이상 DOM 의 .sel 을 보지 않음).
- **카드 클릭 = 펼쳐 보기**(plan.open — .plan.open 에서 본문 표시), 수정은 우측
  연필(#i-pen, plan.edit). 실수로 편집 화면에 들어가지 않는다.
- **작성 중에도 '업무 추가' 버튼 유지**(.dp-add.on 강조). 새 업무 폼이 이미 열려
  있으면 제목으로 포커스만 옮긴다.
- 제목에서 Enter = 저장, Esc = 닫기(색 팝오버가 열려 있으면 그것부터).
- 달력에서 기간을 드래그해 들어오면 기간 줄이 펼쳐진 채 시작·종료가 채워진다.
- 달력 헤더 '오늘' 버튼도 점 아이콘(#i-today)으로 통일. app.js?v=74.
- 검증 v74 신설(12항목), v54 편집기 행 기대치를 [시간|담당자]·[현장|색]으로 갱신.

## 7.30 — 75차 폼 재정리 · 달력 두 건

- **월마다 칸 높이가 달라지던 것**: fixedWeekCount:false 라 5주 달은 행이 152,
  6주 달은 127 이었다 → **fixedWeekCount:true**(항상 6주).
- **연·월 팝업이 엉뚱한 곳(우측 열)에 뜨던 것**: 73차에 여는 버튼만 달력 헤더로
  옮기고 호스트는 .dp-col 그대로였다 → 호스트를 .cal-card(position:relative)로,
  top 은 .cal-title 아래로 계산.
- 작성 폼 구성: 1 기간(시작 ~ 종료, 항상 두 칸 + '~') / 2 업무 구분 | 시간 /
  3 담당자 | 색 / '자세히' / 4 현장 / 5 반복 | 리마인드(선택창) / 6 내용.
  업무 구분(peKind)은 planToTask 에 kind 로 전달.
- 색: 담당자 색 + 자주 쓰는 4색(COL_QUICK) + 점선 '+' 버튼. + 를 누르면 전체
  팔레트와 색상 선택기가 한 팝오버(#colPop)에 함께 뜬다. 고른 색은
  setPlanColor() 가 draft 에 저장하고 색 줄을 다시 그린다.
- 라벨: '지정 안 함 — 팀 공통' → '팀 공통', '현장 지정 안 함' → '-',
  '자세히(반복·메모·리마인드)' → '자세히'.
- 하단 버튼 줄(.dp-edit-f) 폐지 — 제목 바 우측에 아이콘으로: [자세히 쓰기(외부
  링크)] [삭제] [저장(파란 체크)] [닫기]. 취소는 닫기와 중복이라 제거. 폼 290px.
- 검증 v74 폐기·v75 신설(15항목), v71·v63(팝업 호스트)·v54(폼 행) 기대치 갱신.
  app.js?v=75.

## 7.31 — 76차 카드·폼 일관화

- 연·월 팝업 left 2 → -8px(버튼 글자보다 살짝 왼쪽).
- **업무 카드 개편**: 좌측 색 바(.pc) 제거 → **상태 배지(.pst)** 가 그 자리에서
  색까지 겸한다(진행 = 업무 색 12% 배경 + 같은 색 글자, 완료 = 회색). 배지를
  누르면 완료 전환. 우측 아이콘은 세로 → **가로 [리마인드][수정]**.
- **폼 제목 바도 같은 문법**: [상태 배지][제목][저장][닫기]. 저장은 파란 배경 대신
  **아이콘 색만 파랑**. 삭제·'업무 목록에서 자세히 쓰기'(이름·아이콘 #i-tasks 교체)
  는 '자세히' 하단 .pe-tools 로 내려 제목 입력 폭을 확보(197px).
- 색 줄: 6개 **균등 배치**(justify-content:space-between, 폭 168px), 담당자 색은
  그라디언트 → **사람 아이콘**(#av-person), 기본 4색은 파스텔(COL_QUICK
  #7FA8E8·#7FC9A4·#EDBB77·#E89A9A). **setPlanColor 가 .frow 전체를 다시 그려
  '색' 라벨이 사라지던 버그** → #peCols 만 outerHTML 교체(팝오버는 떼었다 다시 붙임).
- 작성 중 달력에서 기간을 드래그하면 새 폼을 열지 않고 **열려 있는 폼의 시작·종료일을
  갱신**(select 콜백에서 S.planEdit 확인).
- 기간 업무가 이어지지 않던 것은 74차 폼의 '여러 날' 토글을 켜야만 end 가 저장되던
  탓 — 75차에서 조건을 없앴다. 실측: 2일 262px(2칸), 주 경계 넘김 263+398px 로 분할.
  **74차에 만든 기간 업무는 한 번 다시 저장해야 이어진다.**
- v76 신설(14항목). app.js?v=76.

## 7.32 — 77차 기간 막대 잘림 · 상태 4단계 · 폼 폭

- **여러 날 업무 막대가 첫 칸에서 잘리던 진짜 원인**: .fc-daygrid-day-events 에
  overflow-y:auto 를 준 탓. **overflow-y 가 auto 면 x 는 스펙상 자동으로 auto**가
  되어 옆 칸으로 넘치는 막대가 잘린다. 칸 넘침은 dayMaxEvents 가 이미 처리하므로
  overflow:visible 로. (75차에 '토글 탓'이라 한 진단은 틀렸다 — 저장은 정상이었다.)
  실측 2일 262px(칸 135), 주 경계 263+398px.
- 상태 배지: 완료 토글(2단) → **예정→진행→완료→보류→예정 4단계**(plan.st).
  업무 목록과 같은 .tk-st s0~s3 스타일 재사용. 폼 배지도 누르면 순환
  (plan.stDraft — draft.st 에 담고 저장 때 반영, planToTask 는 p.st 우선).
- 폼 입력칸 폭 전부 146px 로 균일: 기간을 frow2 두 칸(시작일·종료일 라벨, '~' 제거),
  .dp-edit .frow2 는 flex 1:1, 색 칸도 flex:1. 색 버튼은 **항상 6개** — 팔레트에
  없는 색은 '+' 버튼 자체를 그 색으로 칠해 표시한다.
- 삭제 아이콘은 상단 [삭제][저장][닫기]로, '업무 목록에서 자세히 쓰기'는 자세히 줄
  우측 아이콘으로(.pe-morerow). '자세히'는 가운데 아래 화살표만(펼치면 위로 회전).
- 기본 4색 한 톤 진하게(#5B8FDD·#3FA57C·#E0A44A·#DD6B6B) — 완료 회색과 구분.
- 달력에서 기간을 드래그해도 폼을 열지 않고 S.dragRange 에 기억 + 안내 토스트,
  '업무 추가'를 누를 때 그 기간으로 연다.
- v76 폐기·v77 신설(14항목), v75 기대치 갱신. app.js?v=77.

## 7.33 — 78차 카드 배지 폐지 · 기간 선택

- **카드에서 배지를 전부 걷어냈다**: 상태 배지·pm-chip(기간·현장·담당자·리마인드)
  → 제목 앞 **색 점(.plan-c)** + 회색 보조문구 한 줄(.plan-meta:
  "진행 · 오전 10:00 · 회의 · 두정역 · 김담당"). 리마인드는 아이콘 색만(.p-ico.on).
  접힌 카드에서 업무 색은 이 점이 담당한다.
- **폼도 같은 자리**: 제목 앞 색 점(.pe-c, 고르면 즉시 갱신), 제목 x 좌표가 카드와
  동일(1269/1269). 상태는 배지 대신 '자세히' 위 첫 줄 [상태|업무 구분] 선택창(#peSt),
  둘째 줄 [시간|담당자], 색은 단독 행.
- 저장 아이콘 hover 배경(--b100) 추가.
- **달력 드래그 = 기간 선택**(S.selEnd, selRange): 헤더에 "2026. 8. 2. ~ 2026. 8. 3.",
  우측 보조문구 "일 ~ 월요일"(기간 중엔 공휴일·오늘 표기 생략), 그 사이 칸 전부
  sel-day, 패널에는 **기간에 걸친 업무 전부**(rangePlans, 중복 제거). 안내 토스트 삭제.
  '업무 추가'를 누르면 그 기간으로 폼이 열리고, 날짜를 한 번 클릭하면 기간이 풀린다.
- v77 폐기·v78 신설(13항목), v75·v54 폼 행 기대치 갱신. app.js?v=78.

## 7.34 — 79차 목록 정렬 · 담당자 우측 · 완료 체크

- 보조문구를 좌우로 나눴다: 좌(.pm-l) 상태·시간·구분·반복·현장은 한 줄 말줄임,
  **우(.pm-r) 담당자는 오른쪽 끝 정렬** — 카드를 세로로 훑을 때 담당자가 한 열에 선다.
- **목록 정렬(sortPlans)**: 완료는 아래로, 그 안에서 시간 있는 업무가 먼저(시간순),
  나머지는 날짜순 → 제목 가나다. rangePlans 가 항상 이 정렬을 거친다.
- 카드 우측에 **완료 체크 아이콘**(plan.doneToggle, .p-done.on 은 초록) — 완료↔진행만
  빠르게 토글하고 예정·보류는 폼의 상태 선택창에서. 반복 업무는 회차별 doneOn 토글.
- v79 신설(6항목). app.js?v=79.

### 80차 — 실사용 검증 기반 버그 수정·정리
- **버그**: 새 업무 폼에서 반복 선택 시 '반복 종료' 행이 안 보이던 문제(display/visibility 불일치) ·
  기간 드래그 후 단일 클릭해도 '업무 추가'가 옛 기간으로 열리던 문제(S.dragRange 폐기, selDate/selEnd 로 일원화) ·
  라이브 모드에서 calapp/prefs 미구독으로 최근 이모지가 세션마다 초기화되던 문제(구독 추가) ·
  접속 중 role 변경이 새로고침 전까지 미반영되던 문제 · 담당 현장 모달 제목 이중 이스케이프(&→&amp;) ·
  팀이 없으면 업무 목록이 안내 없이 빈 화면이던 문제.
- **동작 개선**: 일자 패널 카드 펼침 상태(S.planOpen)를 재렌더 후에도 복원 ·
  임의 색 칩 우클릭 삭제 시 저장값(PF_SEL·draft.color)도 함께 되돌림.
- **정리(데드코드)**: ACT plan.st / nq.q, S.tk.r, store.subPlans 인터페이스, #cmtIn 폴백,
  cal.pickY 모달 폴백, savePlanInline 의 by 필드, 사용 안내의 '+ 필터 저장' 문구(기능 없음).
- **메일 스크립트**: weekly.mjs 가 완료 업무를 '이번 주 일정'에 포함하던 것을 앱 미리보기와 동일하게 제외,
  st 비교를 Number 캐스팅으로 통일. remind.mjs 제목 '오늘의 플랜'→'오늘의 업무'.
- app.js?v=80.

### 81차 — 디자인 통일성 감사 (실측 기반, 원본 대비 픽셀 diff 로 회귀 0 확인)
- **아이콘 버튼 radius 를 크기별 규칙으로 통일** (24px→6 · 26px→8 · 28px→8 · 30px→9):
  tk-ico 7→8, site-pick 6→8, tm-x 6→8, mo-x 8→9, pop-x 7→6.
- **역할 배지 통일**: radius 6px 로 3곳 일원화(사이드바 5→6, 조직 표 7→6),
  계정 모달 배지를 조직 표와 동일 규격(11.5px·3px 10px)으로.
- **삭제 hover 색 토큰화**: tm-del hover 배경 rgba 하드코딩 → var(--tint-rd) (다크 모드에서 tk-del 과 동일해짐).
- **수정 폼 상단 현장 배지** font-weight 700 (목록과 통일). due-chip 의 데드 선언(fs·pad) 제거,
  margin-top 을 .tk-row1 스코프로 한정(폼 상단 배지 2px 처짐 해소).
- **빈 상태 문구 12.5px 통일**(tk/set/tm/pf/cmt-empty · 전면 안내 tk-none 13.5 는 별격 유지),
  **섹션 라벨 자간 .05em 통일**(tks-h .06→, tkf-h .04→), 소속 라벨(myorg-h) 굵기 600→700.
- 포커스 링 알파 .14 통일(th-in .12→), tk-list·nq-res 에도 공통 커스텀 스크롤바(4px) 적용.
- **데드 CSS 45개 규칙 제거**(sti2·chipbar·chip2·sg*·dp-nav·duev·pe-full·tk3 등 폐기 UI 잔재,
  .tk-meta .lnk, .pe-bar .pst, .pm-chip.rep, .tbr). tk-ico 옛 정의(20px) 삭제.
- ⚠ **sel-day 규칙의 :not(.x-none) 은 데드가 아니라 명시도 부스터** — 주말·공휴일 background-image
  규칙을 이기기 위한 것. 제거하면 토·일·공휴일 선택 강조가 사라진다(주석으로 명문화).
- 검증: 기능 회귀 19/19, 컴포넌트 실측(dmeasure) 목표값 일치, 원본 대비 픽셀 diff —
  달력 0px, 업무 목록·조직은 의도 지점만 변화. app.js?v=81.

### 82차 — 기간 선택 박스 · 필터 확장 · 업무 폼 재편
- **기간 선택을 주(행) 단위 박스로**: 칸마다 `box-shadow:inset` 을 주던 방식 폐기.
  셀 프레임의 `::after` 를 padding-box 밖 2px(격자 간극)까지 늘려 위·아래 변을 잇고,
  좌·우 변은 그 행 구간의 시작(`sel-s`)·끝(`sel-e`)에만 그린다. `markSel()` 이 행별로 끝 칸을 표시.
  표 가장자리(첫 행·끝 행·첫 열·끝 열)는 바깥에 간극이 없어 offset 을 0 으로 되돌리는 예외가 필요하다
  (안 그러면 scrollgrid 의 overflow:hidden 에 변이 잘린다).
- ⚠ **FullCalendar 가 `.fc-daygrid-day-frame::after` 를 clearfix(display:table)로 이미 쓴다.**
  `display:block` 을 명시하지 않으면 absolute + 상하좌우 offset 을 다 줘도 shrink-to-fit 되어
  2×4px 짜리 점만 그려진다. computed style 로는 border-width 가 정상으로 보이므로
  **스타일 조회만으로는 못 잡는다 — 픽셀 프로브로 발견했다.**
- **필터 패널 4행화**: 1행 업무 구분·진행 상태 / 2행 권역·담당자 / 3행 현장 / 4행 검색·업무 추가.
  `S.filter` 에 kind·st·site 추가, 새 함수 `taskFilterOk()` 가 `taskOwnOk()` + 3조건을 묶어
  달력(buildEvents)·일자 패널(dayPlans) 두 곳의 단일 관문이 된다.
  업무 구분 '일반'은 값이 빈 문자열이라 전체(`*`)와 겹치므로 옵션 값에 `_gen` 센티넬을 쓴다.
  팀-권역-담당자-현장 계층대로 권역→담당자·현장, 담당자→현장 순으로 목록이 좁혀진다
  (배정 현장이 없는 담당자는 좁히지 않는다 — 목록이 빈 채로 잠기는 것 방지).
  상위를 해제해도 하위 선택은 유지한다.
- **업무 폼(읽기·편집) 골격 통일** — 7번 요구('수정 눌러도 위치가 안 바뀌게')의 핵심.
  양쪽 모두 `[테두리][8 패딩][26 상태 아이콘][9][제목][아이콘 26·간격 2][8 패딩]`.
  읽기 카드를 `.plan-hd`(상태·제목·아이콘 한 줄) + `.plan-main`(본문·보조문구, padding-left 35) 2단으로 재구성,
  `.pe-bar` 는 좌 패딩 13→8, 아이콘 3개를 `.pe-side`(gap 2)로 묶고 `background:var(--b50)` 제거.
  실측: 제목 x 동일(1181), 아이콘 3개 x·y 차이 0.0px, 제목 세로 중심 차이 0.025px.
  ※ 제목 상자는 편집이 input(16px)·읽기가 div(20.25px)라 **상단 좌표는 2.1px 차이가 정상** — 중심으로 비교할 것.
- **진행 상태를 선택창에서 아이콘 버튼으로**: 스프라이트에 `i-st0`~`i-st3` 추가
  (예정=빈 원 · 진행=반쪽 채운 원 · 완료=체크 원 · 보류=일시정지 원, 색은 회색·파랑·초록·앰버).
  `plan.stCycle` 이 draft.st 만 바꾸고 버튼 DOM 을 제자리에서 교체한다
  (폼을 다시 그리면 입력 중인 값이 날아간다). `savePlanInline` 은 `#peSt` 대신 `pe.draft.st` 를 읽는다.
  폼 행은 [시작일|종료일] [업무 구분|시간] [담당자|색].
- **업무 색은 카드·폼의 아웃라인 색**으로. 색 점(`.plan-c`·`.pe-c`)과 `.plan:hover` 테두리 규칙 폐기,
  `setPlanColor` 가 `#dpEdit` 의 borderColor 를 갱신한다.
- **수정 버튼은 hover 시에만 노출**. 조직관리처럼 폭을 줄이면 옆 아이콘이 밀렸다 돌아와 위치가 튀므로
  **자리는 두고 opacity 만 0↔1**. 터치 기기는 `@media(hover:none)` 으로 항상 노출.
- ⚠ **위젯(rWidget)도 `.plan` 을 쓴다** — 구조 변경 규칙은 전부 `#dpList` 아래로 한정했다.
  v81 대비 실측 동일(들여쓰기 20px · 높이 59.4 · 폭 489.3/유리 492)임을 확인.
- **헤더-본문 간격 20→9px**(레이아웃 세로 간격과 동일). 뷰포트 높이 계산이 걸린 곳은
  **`.cal-wrap` 과 `.tkwrap` 두 곳** — 둘 다 `- 40px`→`- 29px`. 모바일은 `#content` 9px 14px 14px 와
  `#view-calendar{margin:-9px -14px -14px}` 로 풀블리드 상쇄값을 맞춘다.
  (tkwrap 누락은 기능 검증에 안 잡히고 픽셀 diff 로 발견했다.)
- 검증: 신규 45/45 · 스모크 20/20 · v81 대비 픽셀 diff(사이드바 0 · 상단바 0 · 고정 버튼 0,
  본문은 11px 보정 시 의도 지점만). 검증 스크립트 /home/claude/{v82,smoke82,probe,pshot}.mjs. app.js?v=82.

### 83차 — 기간 박스 세로 병합 · 상태 배지 복귀 · 자동 저장
- **기간 박스가 세로로도 합쳐진다**: 네 변을 각각 이웃 칸 선택 여부로 결정(좌우 ±1일, 위아래 ±7일=같은 요일).
  클래스 sel-l/sel-r/sel-t/sel-b (82차의 sel-s/sel-e 대체). 격자 가장자리 칸은 이웃이 없으므로 무조건 변을 둔다.
  결과는 여러 주에 걸친 하나의 계단형 도형 — 완전 내부 칸은 클래스가 하나도 붙지 않는다.
- 달력 요일띠 #D3D9E2 → **#C4CCDA**(라이트만. 다크는 #424245 그대로).
- **상태 표시를 아이콘에서 배지로 되돌림** — 업무 목록의 `.tk-st` 규격(22px·12px)과 색(s0~s3)을 그대로 쓴다.
  읽기·편집 모두 좌측 첫 칸이고 누를 때마다 예정→진행→완료→보류 순환.
  읽기 카드는 즉시 저장(반복 업무면 회차별 doneOn), 편집 폼은 draft 만 바꾸고 배지 DOM 을 제자리 교체 후 자동 저장.
  이에 따라 완료 체크 버튼(plan.doneToggle)과 i-st0~3 심볼·.p-st CSS 폐기.
- **읽기 카드 2행 구성**: 1행 `[상태 배지][제목][링크(있을 때)][알림][수정]`,
  2행 좌 `업무 구분 · 날짜 · 시간 · 반복` / 우 `현장 · 담당자`. 상태 문구는 배지와 겹치므로 뺐다.
  2행 들여쓰기는 배지 실측 폭을 `--pst-w` 로 넣어 맞춘다(pstWidth()) — 글자가 바뀌어도 어긋나지 않는다.
- **수정 버튼은 조직관리(.tm-del)와 같은 폭 접기/펼치기**로 되돌림(82차의 opacity 고정 방식 폐기 — 사용자 요청).
- **자동 저장**: 저장 버튼 폐지. 입력이 멎으면 600ms 뒤 조용히 반영(토스트 없음).
  savePlanInline 을 `planCollect()`(입력 수집·draft 갱신) / `planCommit()`(반복 전환 처리 포함 저장) /
  `planAutosave()` 로 쪼갰다. closePlanEdit 도 닫기 전에 대기 중인 입력을 확정한다. 제목이 비면 저장하지 않는다.
- ⚠ **자동 저장이 목록을 다시 그리면 입력 중인 폼이 통째로 새로 그려져 타이핑·커서가 날아간다.**
  → rDay() 가 이미 떠 있는 폼 노드를 **떼었다가 목록을 만든 뒤 도로 꽂는다**(S.planEdit.mounted 플래그).
  라이브 모드에서 남의 변경으로 재렌더가 걸려도 같은 경로로 보호된다.
- 우측 아이콘: 편집 `[삭제][알림][저장하고 닫기(체크)]` — X 는 오해 소지가 있어 체크로. 읽기 `[링크][알림][수정]`.
  리마인드 메일 선택창(#peRemind)은 알림 버튼으로 대체해 삭제.
- **업무 구분 순서 공통→일반**(TK_KIND 첫 항목이 새 업무 기본값 = KIND_DEF='gather').
  일반(빈 값)이면 본문이 진행경과·처리계획 두 칸(kindSplit) — 업무 목록 폼과 동일. peBodyHTML/peKindRefresh 신설.
  planToTask 에 `plan`·`links` 전달 추가(안 하면 base 스프레드에 덮여 저장되지 않는다).
- 폼 행: `[시작일|종료일] [업무 구분|담당자] [링크|색]` · 자세히 `현장 / [시간|반복] / 반복 종료 / 본문`.
  링크는 한 칸만 편집하고 업무 목록에서 넣은 label·나머지 링크는 보존한다.
- ⚠ **제목이 읽기/편집 사이에서 1~2px 어긋나던 원인은 input 의 UA 기본 좌측 패딩(2px)과 굵기 차이(600 vs 700)**.
  상자 좌표(getBoundingClientRect)로는 안 잡힌다 — `.pe-ttl{padding:0;font-weight:700}` 으로 해결.
- 카드·폼 아웃라인 **2px**(1.5px 은 dpr 1 에서 1px 로 내림되어 얇아 보인다).
- 검증: 신규 48/48 · 82차 잔여 24/24 · 스모크 20/20 · v82 대비 픽셀 diff
  (업무 목록·조직·설정·내 업무 **0px**, 달력은 요일띠 y116~157 외 **0px**).
  다크 모드 배지·아웃라인 토큰 정상, 폼 내부 넘침 0, 색 칩 6개 145px 반칸에 수용.
  검증 스크립트 /home/claude/{v83,v82r,smoke82,probe,pshot}.mjs. app.js?v=83.

### 84차 — 카드 색면화 · 스크롤바 규약 · 필터 접기 · 펼쳐 보기
- **업무 카드**: 아웃라인 2px→**1px**, 대신 배경을 업무 색 **20%**(`color-mix(in srgb, C 20%, var(--bg2))`).
  다크 모드도 --bg2 와 섞이므로 자동 대응. 폼(.dp-edit)도 1px 로 맞췄다(배경은 흰색 유지 — 입력칸 가독성).
- ⚠ **스크롤바 규약(앱 전체)**: 크롬은 네이티브 스크롤바를 내용 위에 겹칠 수 없다(overflow:overlay 제거됨,
  ::-webkit-scrollbar 는 항상 레이아웃 폭을 먹는다). 그래서 **폭(--sbw:4px)을 늘 확보(scrollbar-gutter:stable)하고
  그만큼 오른쪽 패딩에서 뺀다** — 스크롤 유무와 무관하게 내용 폭이 불변이고 여백 위에 겹친 것처럼 보인다.
  적용: #content(9px 16px 20px 20px) · .dp-body(12px 12px 16px 16px) · .tk-list · .nq-res(10px 6px 14px 10px) · .pf-emg,
  모바일 #content(9px 10px 14px 14px) · 위젯(10px 6px 10px 10px) · 유리(6px 2px 6px 6px).
  폭이 5·4·3px 로 섞여 있던 것을 4px 로 통일. **새 스크롤 영역은 `[data-sb]` 속성만 달면 규약이 적용된다.**
  달력 셀(.fc-daygrid-day-events)은 overflow:visible 이라 스크롤바 규칙이 데드였다 — 제거.
- **일자 패널 우측 아이콘**: 편집 버튼이 상시 노출, **알림 버튼이 hover 접기/펼치기**(83차와 반대).
  ⚠ 폼을 닫으면 커서가 카드 위에 그대로라 hover 가 즉시 켜진다 → `#dpList.nohov` 로 잠그고
  **실제 mousemove 가 오면 해제**(lockHover()). closePlanEdit·savePlanInline 양쪽에서 호출.
- **펼쳐 보기(planDetHTML)**: 내용(일반이면 진행경과·처리계획)·링크를 읽기 전용으로. 비면 '적힌 내용이 없습니다'.
  이전엔 body 가 없으면 눌러도 아무 일이 없어 cursor:pointer 가 거짓말이었다.
  펼친 상태에서 수정을 눌러도 카드·배지·제목 좌표 불변(헤더 골격이 폼과 동일).
  ⚠ 펼침 영역의 링크는 클릭이 위임을 타고 .plan-main(plan.open)으로 올라가 도로 접힌다 →
  앵커에 `data-act="lnk.open"`(빈 핸들러)을 달아 closest 가 앵커에서 멈추게 한다.
- **필터 패널 접기**: 1행 `[검색][범위 선택][꺾쇠]` 만 상시, 나머지 3행은 `.dp-fadv`(폼의 '자세히'와 같은 방식).
- **찾는 범위 (일/월/전체)** — S.dayScope. 검색어가 있고 범위가 day 가 아니면 rangePlans 가 선택 날짜를 벗어나 찾는다
  (month 는 기간이 그 달에 걸치기만 해도 포함). **검색어가 없으면 범위와 무관하게 선택 날짜만** — 빈 검색어로 전체를 쏟지 않게.
- **목록 카드 머리(.dp-lh)**: 좌 '업무 N건'(#dpCount) · 우 업무 추가 버튼(필터 카드에서 이동).
  하단 구분선은 border 가 아니라 ::after(left/right 16px)로 — 양옆을 띄운다.
- **폼 제목 밑 구분선도 같은 방식**: .pe-bar 의 border-bottom 제거 → ::after(left/right 8px), padding-bottom 8→11 로 살짝 아래.
- **수정 중 날짜 클릭이 먹지 않던 버그**: selDate 가 `if(!S.planEdit.orig)` 로 **새 업무일 때만** 시작일을 갱신했다.
  조건 제거 — 한 칸 클릭이면 시작일 갱신 + 종료일 해제 + 자동 저장. 드래그(select)도 planAutosave 로 통일.
- **수정 중 달력 즉시 반영**: buildEvents 가 편집 중인 업무를 저장본 대신 **draft 로 그린다**(같은 iid 는 건너뛰고 draft 를 push).
  planAutosave 는 600ms 저장을 기다리지 않고 planCollect+refetchCal 을 먼저 돌린다.
- 검증: 신규 33/33 · 83차 48/48 · 82차 잔여 24/24 · 스모크 20/20 ·
  v83 대비 픽셀 diff(조직·설정·내 업무 0px, 업무 목록은 스크롤바 자리 4px 띠만).
  검증 스크립트 /home/claude/{v84,v83,v82r,smoke82,probe,pshot}.mjs. app.js?v=84.

### 85차 — 가시성(대비) 전면 상향 · 업무 카드 회색 면 + 카드 밖 색 바
사용자 지적: "전체적으로 앱 자체가 연하고 대비가 낮아 가시성이 떨어진다". 실측으로 원인을 특정한 뒤 3단계로 적용.

**진단 (흰 배경 대비, 적용 전 → 후)**

| 토큰 | 쓰임 | 이전 | 이후 |
|---|---|---|---|
| `--lbl` #1C1C1E | 제목 | 17.01:1 | 그대로 |
| `--lbl2` .58 → **.70** | 보조문구·라벨 | 3.26:1 | **4.46:1** |
| `--lbl3` .45 → **.58** | 희미한 문구·빈 상태 | 2.40:1 | **3.26:1** |
| `--sep` .13 → **.20** | 구분선·카드 테두리 | 1.25:1 | 1.42:1 |
| `--sepo` #C6C6C8 → **#B0B0B4** | 스크롤바 썸 | 1.71:1 | 2.16:1 |
| `--bg` #F2F2F7 → **#EAECF1** | 카드 vs 페이지 배경 | 1.12:1 | 1.18:1 |

원인은 폰트가 아니라 **토큰 알파**였다. 참조 앱에서 가져온 iOS 계열 반투명 회색은 기본 17px 기준인데
이 앱의 보조문구는 11.5~12.5px 이고 한글은 획이 많아 같은 알파에서 훨씬 흐리게 보인다.
`--fill` .12→.15 · `--fill2` .18→.22 도 함께. 다크 모드 대응: `--lbl2` .80→.88 · `--lbl3` .60→.72 ·
`--sep` .12→.18 · `--sepo` .22→.30 · `--fill` .06→.09 · `--fill2` .11→.15.
**하자처리 현황 앱에 옮길 때 이 표를 그대로 쓰면 된다.**

- 2단계: `.card` 에 2겹 그림자(`0 1px 2px rgba(16,24,40,.05), 0 1px 3px rgba(16,24,40,.04)`).
  ⚠ `.cal-card` 는 `body:not(.wid)` 규칙이 프레임을 지우고 달력 격자가 곧 외곽이라 **그림자를 넣지 않는다**(의도).
- 3단계: 10 · 10.5 · 11px 로 쓰이던 **37개 규칙을 11.5px 하한**으로. 위젯 전용(`body.wid`)과 `#wgSet` 은 창이 작아 제외.
  ⚠ 작은 글씨를 키우면 고정 높이 상자(18px 배지·22px 아바타·26px 현장 칩)에서 잘릴 수 있다 →
  **넘침 전용 스캔(lay.mjs)** 으로 5개 화면 + 폼·팝오버까지 검사, 전부 통과. 이 스크립트는 앞으로도 폰트/패딩 변경 시 돌릴 것.
  '고대비 토글'은 사용자 판단으로 제외.

**업무 카드 — 회색 면 + 카드 밖 색 바(G4)**
- 84차의 '색 20% 배경 + 색 아웃라인'을 폐기하고 업무 목록(`.tk-item`)과 같은 **회색 면(--surf2) + 회색 아웃라인(--sep) 1px**.
- 업무 색은 인라인 배경·테두리가 아니라 **`--pc` 변수로만** 카드에 들어온다(`style="--pc:색"`).
  표현 방식은 CSS 한 규칙(`#dpList .plan::before,.dp-edit::before`)에만 있으므로 **바꿀 때 렌더 코드는 손댈 필요가 없다.**
- 채택안 G4: **카드 밖 왼쪽 4px 캡슐 · 카드와 간격 6(left:-10px) · 위아래 인셋 5 · radius 99**.
  업무 목록 `.tkc`(4px 캡슐, 인셋 9)와 같은 굵기이고 인셋만 낮춘 값. absolute 라 카드·제목·아이콘 좌표가 전혀 안 변한다.
  ⚠ 바가 일자 패널의 **왼쪽 안여백(16px) 안**에 들어간다 — 여백을 줄이면 `left:-10px` 도 함께 봐야 한다.
  ⚠ 바를 밖에 두려면 `#dpList .plan` 의 `overflow:hidden` 을 풀어야 한다(풀었음).
  모바일(400px, 하단 시트)에서도 패널 안쪽 7px 지점이라 잘리지 않고 가로 스크롤도 생기지 않음을 실측.
- 편집 폼도 같은 색 바. 배경은 흰색·테두리는 `--b300` 유지 — 회색 카드(읽는 중) ↔ 흰 폼(고치는 중) 구분.
  `setPlanColor` 는 `style.setProperty('--pc', ...)` 로 갱신.
- 시안 산출물: `card-color-mockup.html`(카드 안쪽 6안) · `card-color-mockup2.html`(카드 밖 6안) · `card-color-inset.html`(인셋 9~0).
  실제 앱 CSS·마크업을 그대로 써서 뽑았고, 모든 안이 제목 x·아이콘 좌표·카드 높이 동일함을 실측으로 확인했다.

**그 밖**
- 리마인드가 켜진 카드는 알림 아이콘을 접지 않는다(`.p-rem.on` 을 hover 접기 규칙에서 제외) — 켜 둔 상태가 안 보이던 문제.
- 날짜 헤더(`.dp-top`) 우측 문구 여백: 우측 패딩 13→17, 날짜와의 간격 10→14.
  ⚠ 사용자는 "오른쪽으로 이동"이라 했으나 그대로 하면 모서리에 더 붙는다 — "너무 붙어있음"을 여백 부족으로 읽고 **반대 방향**으로 넓혔다. 확인 필요.
- 사이드바 헤더를 제목 한 줄 `H · 주요업무현황` 으로(보조문구 `.sbs` 폐기). 브라우저 제목·로그인 화면·바로가기·설정 문구도
  `주요업무현황` · `하자처리 현황` 으로 붙여쓰기 통일.
- 업무 카드 보조문구 `--lbl3` → `--lbl2` + 굵기 500.
- 검증: 신규 35/35 · 83차 48/48 · 82차 잔여 24/24 · 스모크 20/20 · 넘침 스캔 7화면 0건.
  v84 대비 픽셀 diff 는 색조 변화라 넓게 퍼지나 **평균 변화량 3.8~6.3** 으로 레이아웃 이동은 없다(이동이 있으면 평균이 훨씬 커진다).
  검증 스크립트 /home/claude/{v84,v83,v82r,smoke82,lay,probe,pshot}.mjs. app.js?v=85.

### 86차 — 색 원 도입 · 카드/폼 다듬기 · 접힘 회귀 수정
- **업무 색 표현을 다시 바꿨다(85차 색 바 → 색 원)**: 제목 앞 20px 원(`.p-col`, 지름 = 제목 줄 높이).
  **이 원이 곧 색 변경 버튼** — 누르면 팔레트 팝오버(`#colPop`)가 원에 붙어 열린다.
  읽기 카드의 원에는 `data-pid` 가 붙어 고른 색이 **바로 저장**되고, 폼의 원은 draft 에 담고 자동 저장한다(`COL_PID` 로 분기).
  폼의 '색' 행(`colRowHTML`/`#peCols`/`.pe-cs`/`.pe-cadd`)은 폐기 — 링크가 한 줄 전체를 쓴다.
  85차의 `--pc` 변수와 `::before` 색 바는 함께 폐기.
- **상태 배지가 보조문구 줄 맨 앞으로** 이동하고 보조문구 크기(11.5px·높이 18px)로 줄었다.
  `.plan-hd .tk-st` → `.plan-meta .tk-st`. 폼에서는 갈 자리가 없어 **우측 도구줄(`.pe-side`) 맨 앞**에 둔다
  — 그래야 제목 x 가 읽기 카드와 같게 유지된다(실측 색 원 1146 · 제목 1175, 두 상태 동일).
  `.plan-meta` 는 `align-items:baseline`→`center`(배지가 inline-flex 라 baseline 이 0.7px 어긋났다).
  2행 들여쓰기는 실측(`pstWidth()`)이 필요 없어져 `padding-left:29px`(원 20 + 간격 9) 고정으로 바꾸고 함수 폐기.
- ⚠ **수정 버튼을 누르면 카드가 목록 맨 위로 튀던 문제**: rDay 가 `editorHTML + 나머지 카드` 순으로 붙이고 있었다.
  → 원래 카드 자리에 `<div id="peSlot">` 를 두고 폼(또는 떼어 둔 `keep` 노드)으로 교체한다.
  ⚠ **`editingId` 를 `draft.id` 로만 찾으면 안 된다** — 처음 여는 순간엔 draft 가 없다(planFormHTML 이 만든다).
  `draft.id || orig.id` 로 찾아야 자리를 지킨다. 목록에 없는 업무(새 업무·날짜 이동)는 예전처럼 맨 위.
- **접히는 아이콘은 아이콘 무리의 맨 왼쪽에** — 순서를 `[알림][링크][수정]` 으로 바꿨다.
  그룹이 오른쪽 정렬이라 맨 왼쪽 항목이 접히면 폭이 왼쪽으로만 줄어 **나머지 아이콘은 제자리**(실측 이동 0px).
- **편집 폼 배경을 회색(--surf2)** 으로 — 카드와 같은 면. 테두리 `--b300` 은 유지(읽는 중/고치는 중 구분).
- ⚠ **date·time 입력칸이 선택창보다 높던 문제**: 브라우저 기본 위젯이라 같은 padding 이어도 높이가 다르다
  (선택창 31 · date 33 · time 35.6). `.inp-sm[type=date],[type=time]{height:31px;padding-top/bottom:0}` 으로 고정.
- **펼침에 보여 줄 게 없으면 아예 만들지 않는다**: planDetHTML 이 빈 항목을 건너뛰고, 전부 비면 `''` 를 반환.
  카드에 `has-det` 클래스가 있을 때만 `plan.open` 액션과 `cursor:pointer` 를 준다(누르면 아무 일도 없던 헛손질 제거).
- ⚠ **사이드바를 접으면 메뉴 아이콘이 최대 4.5px 어긋나던 회귀**: `#sidebar.mini .sbsl{height:29px}` 하드코딩이
  85차에서 라벨 글자를 10→11.5px 로 올리자 실제 높이(31.25px)와 어긋났다. **높이·패딩 override 를 지우고
  `visibility:hidden` 만 남긴다** — 글자 크기를 또 바꿔도 자동으로 맞는다. 실측 이동량 전 항목 0.
  (교훈: 접힘 상태에서 자리만 유지하는 요소에 고정 높이를 쓰지 말 것.)
- 오늘 버튼 아이콘: 점(원) → **홈**(`#i-today` 심볼 교체, 3획). 사이드바 바로가기 라벨 `H · 하자처리 현황`.
- 검증: 신규 23/23 · 85차 35/35 · 83차 49/49 · 82차 잔여 24/24 · 스모크 20/20 · 넘침 스캔 0건.
  v85 대비 픽셀 diff 는 **바로가기 라벨 한 곳(y726~737)만 726px**, 나머지 화면 0px.
  검증 스크립트 /home/claude/{v86,v84,v83,v82r,smoke82,lay,probe,pshot}.mjs. app.js?v=86.

### 87차 — 아이콘 접힘 폐기 · 색 원 축소 · 간격 다듬기
- **카드 아이콘의 hover 접힘을 전부 폐기**했다. 알림·수정 모두 읽기·수정 상태에서 늘 26px 로 보인다.
  84차(수정만 접힘) → 86차(알림만 접힘, 맨 왼쪽) → 87차(접힘 없음)로 정리된 것이니 되돌리지 말 것.
  이에 따라 `#dpList .plan .p-rem` 접힘 규칙, `.p-rem.on` 예외, `@media(hover:none)` 예외,
  그리고 **`lockHover()` / `#dpList.nohov` 잠금 전체가 데드가 되어 제거**했다(막을 것이 없어졌다).
- 색 원 지름 **20 → 14px**. 원이 작아진 만큼 제목·보조문구가 함께 왼쪽으로 당겨진다
  (제목 x 1175→1169, `.plan-main` padding-left 29→23 = 원 14 + 간격 9). 읽기·수정 두 상태 모두 동일.
- 상태 배지와 보조문구 사이 `margin-right` 2→5px.
- 사이드바 바로가기의 외부 링크 아이콘: `opacity .5→1`(글자와 같은 진하기 — currentColor 라 자동으로 `--lbl2` 를 따른다),
  `margin-left 5→7px`.
- 검증: 신규 25/25 · 85차 32/32 · 83차 49/49 · 82차 잔여 24/24 · 스모크 20/20 · 넘침 스캔 0건.
  v86 대비 픽셀 diff 는 **바로가기 링크 아이콘 한 곳(82px)** 뿐. app.js?v=87.

### 위젯 실행 방법 (사용자 안내용 — widget/main.js 는 이미 완성되어 있다)
- `widget/` 폴더에서 `npm install` → `npm start` 로 바로 뜬다(Electron 개발 실행).
- 배포용 exe: `npm run dist` → `dist/업무일정위젯.exe` (포터블 · 설치 불필요).
- 위젯이 여는 주소는 `main.js` 의 `APP_URL`(기본 **`https://dongyexn.github.io/plan/?w=1`** — 배포 경로는 `/plan/` 이다. `/calapp/` 은 저장소 이름 착각으로 87차까지 잘못 박혀 있었다).
  환경변수 `CALWIDGET_URL` 로도 덮어쓸 수 있다.
- 기능: 테두리 없는 항상 위 창 · 트레이 아이콘(보이기/숨기기 · 항상 위 · 작업표시줄 숨김 · 새로고침 · 종료) ·
  `Alt+Shift+C` 전역 단축키 · 창 위치/크기 기억(userData/widget-state.json) · 중복 실행 방지 ·
  상단 26px 드래그 바(브라우저에서 열기 ↗ · 숨기기 −). 지정 origin 밖 이동은 차단하고 외부 링크는 기본 브라우저로.
- ⚠ 사내 PC 에 Node.js 가 없으면 `npm` 자체가 안 되므로, 그 경우 Node 설치가 가능한 PC 에서 `npm run dist` 로
  exe 를 한 번 만들어 배포하는 편이 낫다.

### 88차 — 카드 규칙을 업무 목록까지 확장 · 알림 아이콘 규칙 확정
- **알림 아이콘 규칙 확정**: 편집 폼에는 항상 두고, **읽기 상태에서는 리마인드가 켜진 업무에만** 나타난다.
  87차의 '읽기·수정 모두 상시 노출'은 지시 오독이었다(원문 "동작해야 읽기 상태에서도 띄우도록" = 켜져 있어야 보인다).
  나타났다 사라지는 hover 연출은 없다 — 상태에 따라 있거나 없거나 둘 중 하나다. 업무 목록도 같은 규칙(`tk.remind` 신설).
- 색 원 **14 → 11px** + `margin-left:3px`. 작아진 만큼 왼쪽으로 붙지 않게 여백을 주었고,
  **3+11+9=23 이라 제목 x(1169)·2행 들여쓰기(23px)는 그대로**다. 읽기·편집 동일.
- **업무 일정 카드의 규칙을 업무 목록(.tk-item)에 이식**:
  · 좌측 색 바(`.tkc`) 폐기 → **제목 앞 색 원**(= 색 변경 버튼, 팔레트 팝오버·즉시 저장).
    `colDotHTML(planColor(taskAsPlan(...)), iid)` 를 그대로 재사용한다 — `findPlan` 이 iid 로 전역 검색이라 두 화면이 같은 경로를 탄다.
    `setPlanColor` 의 로컬 재렌더에 `rTasks()` 를 추가해야 목록이 갱신된다.
  · 카드 면: 회색(`--surf2`) + **회색 아웃라인 1px**(펼침 상태는 `--b200` 으로 색만 바뀐다).
  · **배지·칩 규격 22px/12px → 18px/11.5px** (81차에 세운 값의 재결정 — 업무 일정 상태 배지와 맞추기 위함).
    `.tk-meta>*` 와 `.tk-row1 .due-chip` 이 한 규칙이라 두 곳이 함께 바뀐다.
  · 우측 아이콘: `[코멘트수][알림(켜짐만)][링크(있을 때만)][수정(연필·상시)][삭제]`.
    hover 로 나타나던 `.tk-ico`·`.tk-del` 의 opacity/width 연출을 걷어내고 상시 노출로.
    '수정' 텍스트 버튼(`.tk-editbtn`, 펼쳤을 때만 보이던 것)은 연필 아이콘으로 대체·폐기.
- 검증: 신규 25/25 · 85차 32/32 · 83차 49/49 · 82차 잔여 24/24 · 스모크 20/20 · 넘침 스캔 0건.
  v87 대비 픽셀 diff — **업무 목록만 4.97만px(의도한 전면 개편), 조직·설정·내 업무는 0px**. app.js?v=88.

### 89차 — 업무 목록을 업무 일정과 같은 경험으로 · 위젯을 클릭 한 번 설치로
- 색 원 **11 → 8px**. 크기를 바꿀 땐 **왼쪽 여백으로 상쇄해 `여백+지름+9 = 23` 을 지킨다**(지금은 6+8+9).
  이 값이 제목 x·2행 들여쓰기를 고정하는 앵커라, 원 크기만 바꾸고 여백을 안 고치면 전체가 밀린다.
- **색 팝오버 일체형**: 팔레트 아래 구분선을 두고 **색상환 슬라이더 + 정밀 선택기**를 같은 팝오버 안에 넣었다.
  ⚠ 슬라이더는 **끌 때 미리보기만, 놓을 때(change) 확정**한다 — input 마다 저장하면 목록이 다시 그려져 팝오버가 사라진다.
  ⚠ **팝오버는 색 원 버튼(`data-act="plan.color"`) 안에 들어 있다** → 전역 클릭 위임 맨 앞에
  `if(e.target.closest('#colPop'))return;` 가 없으면 팝오버 안쪽 클릭이 버튼까지 올라가 즉시 닫힌다(86차부터 잠재된 문제).
- **업무 목록 카드를 업무 일정 카드와 동일한 골격으로**:
  · 드래그 핸들(`.tk-grip`) 폐기 — 카드 전체를 잡아 끈다(`cursor:grab`, `draggable` 은 원래 카드에 있었다).
  · 1행 `[색 원][제목][아이콘]` (`align-items:center` — 핸들이 있던 `flex-start` 라 원과 제목 높이가 어긋나 있었다).
  · **D-day 배지·구분 칩·현장 칩·담당자 칩을 전부 폐기하고 `.plan-meta`(pm-l/pm-r)를 그대로 재사용**한다.
    2행 = `[상태 배지] 구분 · 날짜 · 시간 · 반복` // `현장 · (담당자 묶음이면 대상) · 공동 담당자`.
    클래스를 그대로 쓰므로 두 화면의 보조문구가 자동으로 같아진다. 들여쓰기는 `31px`(1행 패딩 8 + 6+8+9).
  · 펼침(`.tk-item.open`)·작성/수정 폼(`.tk-new`) 배경도 회색(`--surf2`).
  · 칩 규격 규칙(`.tk-meta>*`)은 카드에서 안 쓰게 되어 **폼 상단 요약 `.tkf-now>*` 로 옮겼다**.
- **오른쪽 열을 두 장으로**(업무 일정과 동일): `.tkcol` 안에 **제목 없는 필터 카드(.tkf-card)** + **업무 패널(.tkmain)**.
  업무 패널 머리(`.tkm-h`)는 `.dp-lh` 규격 — 좌 `업무 목록 · 분류명`, **우 `N건` + 업무 추가**, 구분선은 양옆 16px 인셋.
  ⚠ 건수를 우측에 붙이려면 `.tkm-h .btn{margin-left:auto}` 를 지워야 한다(둘 다 auto 면 여백을 나눠 가져 가운데로 뜬다).
  건수는 `listHTML` 에서 `class="tk-item ` 개수를 세어 **실제로 그려진 항목 수**를 쓴다.
- **위젯 설치를 비개발자용으로**: 명령 프롬프트·Node 없이 쓰도록 두 갈래를 준비했다.
  · `widget-install/위젯 바로가기 만들기.bat` — 더블클릭하면 엣지/크롬을 찾아
    `--app=<주소>?w=1` 바로가기를 바탕화면에 만든다(주소창·탭 없는 창). 설치되는 프로그램 없음.
  · **PWA**: `manifest.webmanifest` + `icon-192/512.png` + `<link rel=manifest>` 추가 →
    엣지 주소창의 설치 아이콘으로 '앱으로 설치'가 가능해졌다.
  · `widget-install/설치 안내.txt` — 팀원 배포용 안내문(SmartScreen 경고 대처·작업표시줄 고정·Ctrl+F5 포함).
  · Electron 위젯(`widget/`)은 그대로 둔다 — 트레이·전역 단축키가 필요할 때 쓰는 상급 경로.
- 검증: 신규 항목 실측 + 85차 32/32 · 83차 49/49 · 82차 잔여 24/24 · 스모크 20/20 · 넘침 스캔 0건. app.js?v=89.

### 90차 — 설정에서 버튼 한 번으로 위젯 설치
사용자 요구: "설정 페이지에서 버튼 하나 누르면 바탕화면에 띄워준다" 수준.
**웹 페이지는 바탕화면에 직접 파일을 만들 수 없다** — 대신 브라우저의 앱 설치(PWA)를 앱 안에서 대신 눌러 주는 방식으로 구현했다.

- `beforeinstallprompt` 를 가로채(기본 배너 차단) `INSTALL_PROMPT` 에 들고 있다가,
  설정 → 위젯 → **[바탕화면에 만들기]** 에서 `prompt()` 로 꺼낸다. 사용자는 엣지 대화상자에서 '설치'만 누르면
  바탕화면·시작 메뉴에 아이콘이 생긴다. `appinstalled` 로 상태를 갱신한다.
- 버튼 옆에 **[창으로 열기]** — 설치가 막힌 환경(사내 정책·다른 브라우저)을 위한 즉시 대안.
  `window.open(origin+pathname+'?w=1', 'calwidget', 'width=390,height=640,...')`.
- `rWidgetSet()` 이 상태별로 버튼·안내를 바꾼다: 이미 설치됨(`display-mode: standalone` 감지) / 프롬프트 있음 / 없음.
  `rCfg()` 에서 호출하므로 설정 화면을 열 때마다 갱신된다.
- ⚠ **설치 조건을 맞추려면 서비스워커가 필요하다** — 그래서 `sw.js` 를 새로 두되 **캐시를 일절 두지 않는
  패스스루 워커**로 만들었다(예전 '캐시 때문에 옛 코드가 돈다' 사고를 되풀이하지 않기 위해).
  부팅 코드의 "모든 SW 해제"는 우리 워커까지 지워 버리므로 제거하고, **옛 캐시 삭제만 남기고
  `location.protocol==='https:'` 일 때만 등록**한다(로컬 http 검증에는 등록되지 않는다).
- 89차의 `.bat` 바로가기와 `설치 안내.txt` 는 그대로 둔다 — 설치 아이콘이 안 뜨는 PC 의 우회로.
- 검증: 설정에 '위젯' 카드 노출·버튼 상태 전환·프롬프트 없는 환경에서 안내 토스트 확인,
  기존 회귀 전부 통과(25/25 · 32/32 · 49/49 · 24/24 · 20/20 · 넘침 0). app.js?v=90.

### 91차 — 위젯을 '바탕화면 달력'으로 (Desktopcal 형태)
사용자 지적: 90차의 PWA/브라우저 창은 "그냥 웹뷰창을 띄우는 느낌". 목표는 xdiarys(Desktopcal) 처럼
**벽지 위에 달력만 반투명하게 얹혀 고정되는 것**.

- ⚠ **브라우저·PWA 로는 원리상 불가능하다.** 창을 벽지 층에 붙이거나 다른 창 아래로 내리는 것은
  브라우저가 웹 페이지에 주지 않는 권한이다. 이 형태는 **Electron 위젯만 가능**하다 —
  90차의 설정 버튼(PWA)은 '전체 화면 앱'용으로 남겨 두고, 바탕화면 위젯은 `widget/` 경로로 간다.
- `widget/main.js`: `transparent:true` · `hasShadow:false` · `backgroundColor:'#00000000'` 로 바꾸고
  **주소에 `&glass=1` 을 붙여 연다**(유리 모드가 벽지가 비치는 배경을 그린다). 작업표시줄 기본 숨김.
- **표시 층을 3단으로**(트레이 메뉴 라디오): `바탕화면에 고정`(기본) / `항상 위에 표시` / `보통 창`.
  ⚠ **윈도우에서 '바탕화면에 붙이기'는 Electron API 로는 안 된다** — Progman 에 `0x052C` 를 보내
  벽지 뒤 `WorkerW` 를 만든 뒤 `SetParent` 로 창의 부모를 옮겨야 한다(`attachToWallpaper()`).
  Win32 호출은 **koffi 를 optionalDependencies 로** 두고, 없거나 실패하면 '항상 위 끄기'로 물러난다
  (완전히 벽지에 박히지는 않지만 다른 창 아래로 내려간다). koffi 가 없어도 앱은 정상 실행된다.
- **위젯 UI: 목록을 늘어놓지 않는다.** `#widPanel`(항상 보이던 오늘 목록)과 설정의 '오늘 목록' 토글을 폐기하고,
  **날짜를 누르면 그 칸 옆에 `#widPop` 업무 팝업**이 뜬다(제목·시간·현장·담당자, 완료는 취소선).
  화면 밖으로 나가면 반대쪽·위쪽으로 접어 붙인다. 팝업/달력 칸 밖을 누르면 닫힌다.
  `selDate` 가 위젯 모드에서 `S.widPop=true` 로 세우고 `rWidget()` 이 그린다.
- ⚠ **이 샌드박스에는 GUI 가 없어 Electron 창은 실행 검증을 못 했다.** 웹 쪽(팝업·유리 모드)은 실측했다.
  실기 확인 항목: ①투명 창에 벽지가 비치는지 ②'바탕화면에 고정' 시 다른 창에 가려지는지
  ③Win+D(바탕화면 보기)에서 살아 있는지 ④다중 모니터에서 위치 복원.
- 검증: 위젯 스모크 20/20(팝업 동작 포함) · 85차 32/32 · 83차 49/49 · 82차 잔여 24/24 · 86차 25/25 · 넘침 0. app.js?v=91.

### 92차 — 바탕화면 고정 본구현 (widget/desktop-pin.js)
사용자 요구는 xdiarys 홍보 이미지 그대로: **벽지 위에 반투명 달력 · 바탕화면 아이콘이 달력 위에 보이고 클릭도 됨**.
아이콘이 위에 오려면 창이 **아이콘 층(SHELLDLL_DefView) 아래**, 즉 벽지 층에 들어가야 한다.

- `widget/desktop-pin.js` 신설 — user32 직접 호출(koffi). 공개된 Win32 기법이며 외부 코드를 가져오지 않았다.
  · `Progman` 에 `0x052C` 전송 → 벽지 뒤 `WorkerW` 생성
  · `SHELLDLL_DefView` 를 가진 `WorkerW` 의 **다음 형제** WorkerW = 벽지 층 → `SetParent`
  · 형제가 없는 윈도우 11 빌드에서는 **Progman 에 직접** 붙인다(자동 대체)
  · `WS_EX_NOACTIVATE|WS_EX_TOOLWINDOW` 로 포커스·Alt+Tab 제외
  · ⚠ **부모가 바뀌면 좌표 기준이 부모 클라이언트 영역으로 바뀐다** → 가상 화면 원점(SM_X/YVIRTUALSCREEN)만큼
    빼서 `SetWindowPos` 로 되돌린다(다중 모니터에서 창이 튀는 것 방지)
  · ⚠ **탐색기 재시작 시 부모가 사라져 창이 떠 버린다** → 5초 주기로 `GetParent` 확인 후 재부착
- ⚠ **윈도우에서 자식 창은 레이어드 창이 될 수 없다** — `SetParent` 하면 Electron 의 `transparent:true` 가
  깨져 배경이 검게 나올 수 있다(빌드·GPU 설정에 따라 다르며 실기에서만 확인 가능).
  그래서 **표시 층을 4단으로** 두고 트레이에서 고르게 했다:
  `바탕화면에 고정`(아이콘이 위로 · 투명이 깨질 수 있음) / `바탕화면 위 · 맨 아래`(투명 확실 · `HWND_BOTTOM` 을
  1.5초마다 재적용 · 아이콘은 가려지고 Win+D 로 감춰짐) / `항상 위에 표시` / `보통 창`.
  **둘 중 어느 쪽이 실제로 원하는 그림인지는 실기에서 골라야 한다.**
- 트레이에 **'바탕화면 고정 진단'** 추가 — koffi 로드·Progman 탐색·붙일 층·현재 부모·창 좌표를 대화상자로 보여 준다.
  실패 시 라디오 라벨에도 '(실패 — 진단 참고)' 가 붙는다. 실기 디버깅은 이걸로 한다.
- `koffi` 를 optionalDependencies → **정식 dependencies** 로 올리고, electron-builder `files` 에
  `node_modules/koffi/**/*` 를 포함 + `asarUnpack` 지정(네이티브 바인딩은 asar 안에서 로드되지 않는다).
- 기본 창 크기 380×560 → **620×520**(바탕화면 달력이므로).
- ⚠ 상용 앱 설치 파일을 열어 코드를 가져오는 방식은 쓰지 않았다(저작권·출처 불명 코드 혼입).
  필요하면 Spy++ 로 그 앱 창의 부모·확장 스타일을 **관찰**해 알려 주면 같은 층·스타일로 맞출 수 있다.
- ⚠ 이 샌드박스에는 GUI 가 없어 **Electron 실행 검증 불가**. 웹 쪽(위젯 팝업·유리 모드)만 실측했다.

### 93차 — 위젯 실기 1차 결과 반영 (입력 불가·창 튐·로그인 막힘)
실기에서 위젯은 떴고 트레이 메뉴도 나왔다. 대신 세 가지가 드러났다 — 전부 실기에서만 잡히는 것들이다.

- ⚠ **로그인 칸에 글자를 못 친다** → 원인은 `WS_EX_NOACTIVATE`. 이 확장 스타일이 붙으면 창이 포커스를
  받지 못해 키 입력이 통째로 막힌다. **바탕화면 위젯이라고 이걸 붙이면 안 된다.**
  Alt+Tab 에서 빼는 `WS_EX_TOOLWINDOW` 만 남겼다(포커스와 무관).
- ⚠ **창이 자꾸 떠오른다** → 붙이기가 실패하는데도 5초마다 재시도하면서 `SetWindowPos(..SWP_SHOWWINDOW)` 가
  창을 계속 다시 띄우고 있었다. 두 가지를 고쳤다: ①위치 보정은 `IsChild` 로 성공을 확인한 뒤에만
  ②연속 3회 실패하면 재시도를 멈춘다(트레이 라벨에 실패 표시는 남는다).
- ⚠ **붙이기가 실패한 근본 원인**: Electron 의 프레임 없는 창은 `WS_POPUP` 이다.
  **WS_POPUP 인 채로 `SetParent` 를 하면 '소유'만 될 뿐 자식이 되지 않는다**(그래서 IsChild 가 false).
  `WS_POPUP` 을 떼고 `WS_CHILD` 를 붙인 뒤 부모를 옮기도록 고쳤다. 해제할 때는 반대로 되돌린다.
- ⚠ **위젯 안에서 로그인이 끝나지 않는다** → `setWindowOpenHandler` 가 모든 새 창을 바깥 브라우저로
  보내고 있었는데, Firebase 는 `signInWithPopup` 으로 새 창을 띄운다. 인증 도메인
  (google/googleapis/gstatic/firebaseapp/web.app/microsoftonline/live)만 **위젯 안 실제 창으로 열도록** 허용하고,
  `will-navigate` 의 외부 이동 차단에서도 같은 도메인을 예외로 뒀다.
- ⚠ **.bat 은 CRLF + CP949 로 써야 한다.** LF·UTF-8 이면 윈도우 cmd 가 첫 줄부터 깨져 검은 창이
  뜨자마자 사라진다(실기에서 확인). 모든 종료 경로에 pause, 압축 안에서 실행한 경우(main.js 없음) 선제 안내.

### 94차 — 바탕화면 고정과 키 입력은 양립하지 않는다 (구조적 제약 확정)
93차 수정으로 창은 안정됐지만(더 이상 떠오르지 않음) **여전히 글자가 안 쳐진다.**

- ⚠ **원인은 설정이 아니라 윈도우의 구조다.** `WS_CHILD` 로 벽지 창(WorkerW/Progman)의 자식이 된 창은
  **키보드 포커스를 받을 수 없다** — 윈도우는 키 입력을 최상위(포어그라운드) 창에만 보낸다.
  즉 93차에서 `WS_POPUP`→`WS_CHILD` 로 고쳐 **고정이 성공하게 된 것 자체가** 입력 불가의 직접 원인이다.
  이건 스타일 하나로 풀 수 있는 문제가 아니다(상용 앱들은 자체 입력 처리기를 따로 만든다).
- 그래서 **역할을 나눴다**: `바탕화면에 고정` = 보기 전용 / `항상 위`·`맨 아래`·`보통 창` = 입력 가능.
  · **첫 실행 기본값을 `desktop` → `top` 으로 바꿨다** — 로그인부터 해야 하는데 고정 상태로 뜨면 아무것도 못 한다.
  · 트레이 맨 위에 **'입력 모드로 (로그인·업무 작성)'** 추가 — 고정 상태에서 누르면 `top` 으로 올려 주고,
    작업이 끝나면 바로 아래 나타나는 **'↩ 바탕화면 고정으로 되돌리기'** 로 복귀한다.
  · 고정에 처음 성공하면 안내 대화상자를 한 번 띄운다(입력이 안 되는 이유와 대처를 그 자리에서 알려 준다).
  · 라디오 라벨에도 `(보기 전용 · 입력 불가)` / `(입력 가능)` 를 명시.
- 남은 선택지(사용자 판단 필요): 입력까지 되는 바탕화면 위젯을 원한다면 **`맨 아래` 모드**가 현실적인 절충이다
  (투명 유지 · 입력 가능 · 다른 창에 가려짐. 단 바탕화면 아이콘은 달력에 가려지고 Win+D 로 감춰진다).

### 95차 — '입력까지 되는 바탕화면 위젯'으로 확정
사용자 목표: 보기 전용이 아니라 **입력까지 자유로운 바탕화면 위젯**.

- ⚠ **아이콘이 달력 위에 보이는 것과 키 입력은 동시에 가질 수 없다.**
  아이콘이 위로 오려면 창이 벽지 창의 **자식(WS_CHILD)** 이어야 하는데, 자식이 되는 순간
  키보드 포커스를 못 받고 투명(레이어드)도 깨진다. 둘 중 입력을 택했다.
- **기본 모드를 `below`(바탕화면 모드)로 확정**했다. 최상위 창을 유지한 채 z-순서만 `HWND_BOTTOM` 으로
  내리는 방식이라 **반투명 유지 + 키 입력 가능 + 다른 창에 가려짐** 을 모두 얻는다.
  대신 바탕화면 아이콘은 달력에 가려진다 → 첫 실행 위치를 아이콘이 몰린 왼쪽을 피해 **오른쪽 위**로 바꿨다.
- 이 모드의 두 함정을 처리했다:
  · ⚠ **글자를 치는 중에 다시 맨 아래로 내리면 다른 창 뒤로 숨어 버린다** → `win.isFocused()` 면 z-순서를 건드리지 않는다.
  · ⚠ **Win+D(바탕화면 보기)·최소화로 감춰진다** → 1.5초 주기에서 `isVisible()` 이 false 면 `showInactive()` 로 되살린다.
- 트레이 라벨 정리: `바탕화면 모드 (반투명 · 입력 가능)` = 기본 / `바탕화면에 박기 (아이콘이 위로 · 입력 불가)`
  = 보기 전용 실험 / `항상 위에 표시` / `보통 창`.
- 남은 가능성(미구현): 자식 창에서도 입력을 받으려면 explorer 스레드에 `AttachThreadInput` 후 `SetFocus` 하는
  방식이 있다. 상용 앱이 자체 입력 처리기를 두는 이유이기도 하며 불안정하다 — 필요해지면 그때 실험할 것.

### 96차 — 기본값을 바꿔도 안 먹던 이유: 저장된 모드
95차에서 기본 모드를 `below` 로 바꿨는데도 실기에서 **여전히 글자가 안 쳐졌다.
버튼(마우스)은 되고 키보드만 안 되는 것** — 자식 창(WS_CHILD)의 전형적인 증상이다.

- ⚠ **표시 모드는 `userData/widget-state.json` 에 저장된다.** `state.mode || 'below'` 로 기본값을 바꿔도
  예전 실행에서 저장된 `mode:'desktop'` 이 그대로 이겨서 계속 '바탕화면에 박기' 로 뜨고 있었다.
  **저장되는 설정의 기본값을 바꿀 때는 항상 마이그레이션을 함께 넣을 것.**
  → `state.v` 를 두고 `v!==2` 이면 `desktop` 을 `below` 로 한 번만 옮긴다(이후 사용자가 다시 고르면 그 선택은 유지).
- 시작 시 `pin.unpin(win)` 을 먼저 호출해 **예전 실행이 남긴 WS_CHILD/부모 관계를 확실히 되돌린다.**
- 트레이 맨 위에 **현재 모드를 비활성 항목으로 표시**했다 — 어느 층에 있는지 몰라 헤매는 일이 없도록.

### 97차 — 위젯 실사용 다듬기 8건
바탕화면 모드가 자리 잡은 뒤 나온 실사용 지적들.

- **주입하던 헤더 바(#wgbar) 제거.** 대신 위젯 설정에 **'위치·크기 조정'** 토글을 넣고,
  켰을 때만 창을 옮기고 크기를 바꿀 수 있다(끄면 `setMovable(false)`·`setResizable(false)` 로 그 자리에 고정).
  웹→Electron 전달은 기존 방식대로 해시(`#move`/`#moveoff`). 켜면 창 전체를 덮는 드래그 레이어가 뜬다.
- 위젯에서 **스크롤바·검색 플로팅 버튼 숨김**(`#content{overflow:hidden}` · `.nq-fab{display:none}`).
- **글자 크기 조절 대상이 달력**이 됐다 — `--wfz` 를 날짜 숫자·요일·일정 칩·월 제목까지 확대 적용.
- **다크 모드 토글 제거**(유리 모드에서 차이가 거의 없다).
- ⚠ **여러 날 업무가 잘리던 원인**: 좁은 창이라 `@media(max-width:720px)` 의
  `.fc-daygrid-day-frame{overflow:hidden}` 이 먹고 있었다 → `body:not(.wid)` 로 위젯을 예외 처리.
  **위젯은 창이 좁아 모바일 규칙에 걸린다 — 모바일 CSS 를 손댈 땐 위젯도 함께 볼 것.**
- ⚠ **업무를 고치는 중에 다른 날을 누르면 그 업무가 그 날로 옮겨지던 문제** → 84차 #9 를 부분 되돌렸다.
  **새 업무를 쓰는 중일 때만 시작일이 따라가고**, 기존 업무의 날짜는 기간 드래그나 입력칸으로만 바꾼다.
- **위젯 팝업이 앱의 업무 패널 그 자체가 됐다(핵심).** 팝업용 목록을 따로 만들지 않고
  **`.day-panel` 노드를 `#widPop` 안으로 옮겨** 쓴다(`widMount()`). 그래서 카드·수정 아이콘·편집 폼·
  자동 저장·색 원까지 앱과 완전히 동일하게 동작한다. 유리 배경에 맞춰 색만 덮어썼다.
  실측: 팝업 안에 `.day-panel` · 카드 2건 · 수정 버튼 · `#dpEdit` 폼 정상.
- 검증: 위젯 스모크 20/20 · 85차 32/32 · 83차 49/49 · 82차 잔여 24/24 · 86차 25/25. app.js?v=93.

### 98차 — 위젯 마감 6건 + 시행착오 산출물 정리
- ⚠ **유리 모드에서 기간 선택 표시가 사라지던 원인**: `body.wid.glass` 의 셀 배경 규칙들이
  `background: … !important` **단축**이라 `sel-day` 의 `background-image`(선택 강조)까지 지웠다.
  → 유리 모드용 `sel-day{background-image: … !important}` 를 뒤에 다시 얹었다.
  **단축 속성 + !important 는 다른 규칙이 쓰던 하위 속성까지 날린다 — 유리 모드 규칙을 손댈 때 항상 확인할 것.**
- 공휴일 숫자가 유리 모드에서 흰색이던 문제 → 셀 클래스는 `.hol`(dayCellClassNames) 이다. 그 셀의 `.dnum` 만 빨갛게.
- 연·월 팝업(`#ymPop`)을 위젯에서는 설정 팝업과 같은 유리 카드로, `left:-8px`→`0` 으로 잘림 제거(폭 252px).
  내부 클래스는 `.ymp-h b` · `.ymp-m`(`.sel`) 이다.
- 위젯 상단 연·월 제목에도 알약 배경(`rgba(16,20,30,.55)`), 설정 버튼 옆에 **프로필 버튼**(`acct.open`) 추가 —
  둘을 `.cal-nav` 로 묶어 이동 버튼과 같은 배경을 공유한다.
- **시행착오 산출물 정리**(사용자 요청 — 깃허브 파일 수 줄이기):
  `widget-install/`(bat·안내문) · `manifest.webmanifest` · `sw.js` · `icon-192/512.png` 삭제,
  index.html 의 manifest/theme-color/icon 링크와 **앱 설정의 '위젯' 카드**(PWA 설치·창으로 열기) 제거,
  app.js 의 `INSTALL_PROMPT`/`rWidgetSet`/`isChromium`/`isInstalled`/`wid.install`/`wid.open` 제거.
  서비스워커는 **다시 '등록 해제 + 캐시 삭제'** 로 되돌렸다(설치 조건이 필요 없어졌고, 캐시 사고 방지가 우선).
- README 를 현재 상태로 갱신 — 제목 `H · 주요업무현황`, 배포 주소, widget 파일 구성,
  **표시 모드 표(반투명·키 입력·아이콘 가림 3열)** 와 '아이콘 위 표시와 키 입력은 양립 불가' 경고, 위치·크기 조정 잠금.
- 검증: 유리 모드 실측(선택 배경 gradient 살아 있음 · 공휴일 `rgb(255,156,147)` · 제목 알약 배경 ·
  프로필 버튼 존재 · 검색 버튼 none · 다크 토글 없음 · 연월 팝업 left 0/폭 252),
  회귀 25/25 · 32/32 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=94.

### 99차 — 위젯 실사용 2차 + 앱 공통 다듬기
- ⚠ **위젯 오른쪽이 잘리던 원인 두 가지.** ①`body.wid .cal-wrap{grid-template-columns:1fr}` —
  **1fr 의 최소폭은 max-content** 라 달력 표가 창보다 넓어졌다 → `minmax(0,1fr)`.
  ②창이 좁아 `@media(max-width:720px)` 의 `#view-calendar{margin:-9px -14px -14px}`(풀블리드)와
  높이 계산이 위젯에도 먹었다 → 그 세 규칙을 `body:not(.wid)` 로 한정.
  **위젯은 늘 모바일 폭이다 — 모바일 블록을 만들 때마다 위젯 예외를 확인할 것.**
- ⚠ **위치·크기 조정 레이어가 화면을 다 덮어 토글을 다시 끌 수 없었다** → 레이어 안내 상자에
  **'끝내기' 버튼**(`wid.moveOff`)을 넣고, 설정 팝업 z-index 를 레이어 위로 올렸다.
- ⚠ **팝업 안 업무 카드 글자가 안 보이던 원인**: 카드 색 규칙이 `#dpList`(아이디)로 걸려 있어
  `body.wid .wid-pop .plan`(클래스만) 로는 못 이겼다 — 밝은 카드에 흰 글자가 얹혔다.
  → 선택자에 `#dpList` 를 끼워 명시도를 맞췄다. '업무 추가' 버튼도 유리 배경용으로.
- **수정 중 다른 날을 누르면 폼을 닫는다**(자동 저장 후). 기존 업무의 날짜는 그대로 두고,
  새 업무를 쓰는 중일 때만 날짜가 따라간다. 남의 날 업무 폼이 목록 위에 계속 떠 있던 혼란을 없앴다.
- 그 밖: 배경 진하기 범위 20~90 → **15~100**, 연·월 제목 알약을 우측 버튼과 같은 높이(34px)로
  + 꺾쇠 색 지정, 위젯 콘텐츠 패딩 축소(하단 여백), 프로필 등 **모달을 위젯 유리 카드로**(`body.wid #mb`),
  일정 막대끼리 붙지 않게 `harness+harness{margin-top:2px}`.
- **앱 공통**: 상단바 `--th` 52 → **46px**(살짝 얇게), 업무 카드 보조문구 우측을 3px 안쪽으로(`padding-right`).
- **위젯 배포**: 앱 설정에 **'바탕화면 위젯' 카드**(내려받기 버튼 + `cfg.widgetUrl` 입력)를 뒀다.
  관리자가 `npm run dist` 로 만든 **포터블 exe** 를 릴리스에 올리고 그 주소를 넣으면, 팀원은
  버튼 → 파일 하나 → 두 번 클릭이면 끝이다(**Node 불필요 · 명령 프롬프트 창도 뜨지 않는다**).
  규칙에 `cfg.widgetUrl` validate 추가(300자) — **규칙을 먼저 게시해야 저장된다.**

### 100차 — 위젯 상호작용 재설계 + 가시성
- **날짜 칸: 첫 클릭은 선택까지, 같은 칸을 한 번 더 눌러야 업무 팝업**(`S.widPop = quiet ? false : same`).
  다른 날을 누르면 팝업은 닫힌다. `selDate(ds, quiet)` 의 `quiet` 는 '이동만' 을 뜻하고
  **오늘 버튼·연·월 이동이 이 인자를 쓴다** — 이동만 했는데 1일이 선택되며 팝업·업무 추가가 열리던 문제를 없앴다.
- **일정 막대를 누르면** 그 날 팝업을 열고 그 업무를 펼친다(`S.planOpen`). **수정 모드로는 들어가지 않는다.**
- **수정 중 다른 날을 누르면 폼을 닫는다** — 위젯에서도 확실히 닫히도록 `rWidget()` 까지 호출.
- ⚠ **유리 배경 위 가시성은 요소를 하나씩 칠하면 계속 빠진다** → `body.wid #mb` 와 `body.wid .wid-pop` 에서
  **디자인 토큰(--lbl/--lbl2/--lbl3/--sep/--fill…)을 통째로 밝은 값으로 재정의**했다.
  라벨·보조문구·구분선·아이콘이 한꺼번에 해결된다. 앞으로 유리 위 컴포넌트는 이 방식을 쓸 것.
- 팝업 헤더를 앱처럼 **좌 `2026년 8월 13일` / 우 `공휴일 · 요일 · 오늘`** 로, 좌우 여백을 아래 '업무 N건' 과 맞췄다.
- 위치·크기 조정을 **토글 → '조정하기' 버튼**으로. 누르면 설정 팝업이 닫히고 레이어가 뜨며, 레이어 안 '끝내기' 로 나온다.
- 글자 크기에 맞춰 **일정 막대 padding 도 `--wfz` 로 함께** 커지고, 표는 `width:100%!important` 로 창에 묶어
  글자를 키워도 달력이 창보다 커지지 않는다(가로 넘침 0 실측).
- README 에 **초보자용 exe 만들기 절차**(Node 설치 → 폴더 주소창에 cmd → npm install → npm run dist →
  릴리스 업로드 → 설정에 주소 입력)를 넣었다. **앱만 고쳤을 때는 exe 재배포가 필요 없다** — 위젯은 웹 주소를 불러온다.

### 101차 — 새 업무 담당자 기본값 · 칸 넘침 '+N건'
- **새 업무의 담당자 기본값 = 로그인한 본인**(앱·위젯 공통). `meOwner()` 신설 —
  로그인 uid 가 담당자 명단(roster)에 있으면 `{uid:1}`, 없으면 팀 공통(`{}`).
  업무 일정 폼(`planFormHTML` 의 새 draft)과 업무 목록 폼(`taskFormHTML` 의 `assignees`) 양쪽에 적용.
  ⚠ 로그인 전(로컬 모드·미로그인)에는 그대로 '팀 공통' 이 된다 — 검증 시 `S.user` 를 세워야 재현된다.
- ⚠ **칸에 업무가 많으면 그냥 잘려서 안 보였다.** `dayMaxEvents` 가 데스크톱에서 `false`(무제한)였기 때문이다
  → **`true`(칸 높이에 맞춰 넣고 나머지는 더보기)** 로 바꿨다. 좁은 화면은 그대로 2건.
  `moreLinkContent` 를 `+N건` 으로, 알약 배경(`--fill`)을 줘 눈에 띄게 하고 유리 모드용 색도 따로 뒀다.
- `moreLinkClick` 은 기본 팝오버 대신 **그 날짜를 골라 업무 패널(위젯은 팝업)에서 전부 보여 준다**(`return 'none'`).
- 검증: 8건을 한 칸에 넣어 `+5건` 표시·배경 확인, 로그인 상태에서 두 폼 모두 담당자가 본인으로 선택됨.
  회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=97.

### 102차 — 달력 일정 막대 높이 축소
- `#fcal .fc-event` 의 세로 패딩 1.5px → 0, `line-height:1.55` 를 명시해 높이를 **약 21px → 17.8px** 로 줄였다.
  글자 크기(11.5px)는 그대로라 가독성은 유지된다. radius 도 6→5 로 맞췄다.
  위젯의 `--wfz` 배율 규칙(`body.wid #fcal .fc-daygrid-event`)도 세로 패딩 0 으로 함께 정리.
  ⚠ 세로 패딩을 지울 때 `line-height` 를 같이 고정하지 않으면 글꼴에 따라 글자가 잘린다(scrollHeight 차이 0 확인).
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=98.

### 107차 — 위젯 실사용 3차(12건)
- ⚠ **칸마다 막대 높이가 어긋나던 원인**: 106차에 넣은 `harness+harness{margin-top:2px}` 는 **둘째 막대에만** 붙어
  첫 줄에 막대가 없는 칸과 줄이 안 맞았다. 게다가 **harness 의 margin-top 은 FullCalendar 가 인라인으로 정하는
  줄 맞춤 값**이라 CSS 로 건드리면 안 된다 → 규칙을 지우고 **막대 자체의 상하 여백(`.fc-event{margin:2px 3px}`)** 으로 준다.
  실측: 모든 칸에서 1행 y=426 · 2행 y=450 로 일치.
- ⚠ **유리 위 `select` 의 펼친 목록은 OS 가 그린다** — 배경이 흰색이라 흰 글씨가 안 보였다.
  `color-scheme:dark` + `option{background:#22252e}` 로 해결(date·time 입력도 동일). placeholder 색도 별도.
- **팝업 위치**: 아래·오른쪽 모두 창 안으로 접고 `max-height` 를 창 높이에 맞춘다(위젯은 창이 곧 화면이라 잘리면 못 본다).
- **날짜 칸 3번째 클릭에 팝업이 닫힌다**(`same && !S.widPop` — 열기/닫기 토글).
- 팝업 헤더: 날짜·요일·X 를 한 줄 중앙 정렬, 좌측 여백을 아래 '업무 N건'(16px)과 맞추고 X 는 6px 안쪽.
- **위젯 모달을 왼쪽에 붙였다**(`body.wid #mo{justify-content:flex-start}`, 모달 270px · 팝오버 300px) —
  창이 좁아 프로필 팝오버가 늘 모달 위에 겹치던 문제. 이제 앱처럼 오른쪽에 선다.
- ⚠ **카드 위쪽 패딩은 편집 폼과 맞춰야 한다**(제목 좌표 불변 규칙) → '내용이 처져 보인다'는 **아래 패딩만** 8→7 로,
  2행 `margin-top` 3→2 로 해결. 위쪽을 건드리면 회귀 검사 4e·8c 가 바로 깨진다.
- 그 밖: 설정 위젯 문구를 SmartScreen 대처 안내로 교체(주소·바로가기 보조문구는 삭제),
  상단 아이콘 순서 **프로필 → 설정**, `조정하기` 를 유리 팝업용 버튼(`.wgs-btn`)으로,
  진행 배지 hover 를 `filter:brightness` 로, 카드 우측 아이콘 여백을 좌측 색 원 여백과 맞춤,
  글자를 키워도 스크롤바가 안 생기도록 `.fc-scroller{overflow:hidden!important}`.
- 리마인드 버튼은 정상 동작 확인(누르면 draft→저장, 버튼 on).
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=99.

### 108차 — 위젯 실사용 4차 + '눈으로 보는' 검증 자동화
- ⚠ **시간이 있는 업무가 유리 배경에서 거의 안 보이던 진짜 원인**: `display` 를 지정하지 않으면 FullCalendar 가
  시간 있는 이벤트를 **점 형식(dot-event)** 으로 그린다 — **배경 없이 `--lbl`(어두운) 글자**라 어두운 위젯에서 묻힌다.
  `planEvent` 에 `display:'block'` 을 넣어 전부 색 막대로 통일했다. 덤으로 **모든 막대 높이가 18px 로 같아져
  칸마다 줄이 어긋나던 문제도 사라졌다**(실측: 전 칸 y426/y448 일치).
- **글자 크기별 막대 간격이 제멋대로이던 것**도 같은 뿌리 — `.fc-event` 에 `height:18px`(위젯은 `18px*--wfz`)를
  못 박아 크기를 바꿔도 줄 간격이 일정하다(fz s/m/l = 16/18/21px, 모든 칸 동일).
- **팝업이 창을 벗어나던 원인**: 위치 계산이 `rWidget()` 안에만 있어 **폼이 열려 팝업이 길어질 때 다시 잡히지 않았다**
  → `widPlace()` 로 분리하고 팝업에 `ResizeObserver` 를 붙여 내용이 바뀔 때마다 다시 잡는다.
- **팝업을 X 로 닫으면 열려 있던 수정 폼도 닫는다**(`wid.popClose` 에서 `closePlanEdit`).
- 팝업 날짜 헤더 좌우 여백을 아래 '업무 N건'과 **실측으로 맞췄다**(좌 19px 동일, 우 X 버튼도 19px).
- **수정 모드에서 아웃라인 색을 바꾸지 않는다**(`.dp-edit` 테두리 `--b300` → `--sep`) — 사용자 요청.
- 카드 상·하 패딩을 8px 로 동일하게(요청). 위쪽만 줄이면 폼과 제목 좌표가 어긋나 회귀가 깨진다.
- 위젯 모달 300px · 팝오버 290px 로 조정(270px 은 너무 좁았다). 선택 탭·이모지 분류 칩이 유리 배경에 묻히던 것도 색 지정.
- **검증 도구 `wvis.mjs` 신설** — 화면을 눈으로 훑는 대신 **글자마다 조상 배경을 겹쳐 실제 대비를 계산**하고
  가로 넘침·글자 잘림을 찾는다. 유리 모드처럼 배경이 반투명으로 겹치는 화면에서 눈보다 정확하다.
  이번에 시간 이벤트 대비 1.01(사실상 안 보임)을 이걸로 잡았다. 위젯을 손댈 때마다 돌릴 것.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=100.

### 109차 — 트레이 메뉴 정리 + 자동 실행 + 오늘 업무 보기
사용자 지적: 트레이 옵션에 실사용에 불필요한 것이 많다.

- **표시 층을 둘로 줄였다**: `바탕화면 모드`(기본) / `항상 위에 표시`(체크박스 하나로 전환).
  ⚠ **'벽지 층에 박기'(WS_CHILD) 모드를 완전히 뺐다** — 자식 창이 되면 키보드 포커스와 반투명을 둘 다 잃어
  위젯으로 쓸 수 없다는 94차 결론이 확정됐다. 이에 딸린 '입력 모드로'·'되돌리기'·'바탕화면 고정 진단'·
  '보통 창'·'작업표시줄에 숨기기'(이제 항상 숨김)·현재 모드 표시 줄도 함께 제거.
  `desktop-pin.js` 는 `sendToBottom`·`unpin` 만 남기고 벽지 부착 코드를 정리했다(필요하면 git 기록에서 복구).
  저장된 옛 모드는 `state.v=3` 마이그레이션으로 `below` 로 옮긴다.
- **윈도우 시작 시 자동 실행** 추가 — `app.setLoginItemSettings({openAtLogin, path: process.execPath})`.
  포터블 exe 라 실행 파일 경로를 그대로 등록한다. 위치·크기는 원래 `widget-state.json` 에 저장되므로
  **마지막에 잠근 위치·크기 그대로 다시 뜬다**(평소 창이 잠겨 있어 실수로 밀릴 일도 없다).
- **'오늘 업무 보기'** 추가 — 메인 프로세스가 페이지의 훅 `window.widToday()` 를 호출한다
  (앱 쪽에 훅을 하나 열어 두는 방식. `executeJavaScript` 로 내부 상태를 직접 만지지 않는다).
- 최종 트레이: 오늘 업무 보기 / 위젯 보이기·숨기기 // 항상 위에 표시 / 윈도우 시작 시 자동 실행 //
  새로고침 / 브라우저에서 전체 화면 열기 // 종료.
- ⚠ **이번엔 `widget/` 파일이 바뀌었으므로 exe 를 다시 만들어 배포해야 한다**(앱만 고친 회차와 다름).
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20, wvis 대비 경고 0(달력 오늘 배지 2.32 는 파란 원 위 흰 숫자 — 정상).
  app.js?v=101.

### 110차 — 배포 전 전수조사(성능·데드코드·오류·문서·파일)
- **런타임 오류 0.** 5개 화면 + 계정 모달·사용 안내·Ctrl+K·연월 팝업을 한 바퀴 돌며 수집 —
  콘솔 오류는 샌드박스의 Firebase CDN 403 뿐(실환경 무관). 경고 0.
- **성능**: 한 번의 조작에 무거운 함수가 몇 번 불리는지 계측하는 `audit.mjs` 신설.
  날짜 클릭 rDay 1 / 자동 저장 rDay 2·allTasks 5 / 달 넘기기 buildEvents 2 — 폭주 없음.
  렌더 1회 평균 rDay 0.45ms · rTasks 1.95ms · buildEvents 0.03ms, DOM 1,395 노드. **최적화 불필요.**
- **데드코드 제거**
  · JS: `'tk.due'`·`'tk.dueClear'`(89차에 D-day 칩을 없애며 도달 불가가 됨, MODAL_CB `type:'due'` 포함),
    `moveSite()`(현장 드래그 정렬이 사라진 뒤 남은 함수).
  · CSS 27규칙: `.ct` · `.p-ico.p-done.on` · `.pm-chip*`(위젯이 rWidget 을 쓰던 시절 잔재 — **97차에 팝업이
    `.day-panel` 을 옮겨 쓰게 되며 마지막 사용처가 사라졌다**) · `.sg` · 업무 목록 옛 골격(`.tk-body`·`.tk-row1`·
    `.tk-time`·`.tk-meta*`) · 위젯 옛 목록(`.wid-h`·`.wid-only`·`.wp-i/c/t/m/none`).
  · ⚠ **`.x-none` 은 이번에도 남겼다** — 데드가 아니라 명시도 부스터(81차 사고 재발 방지).
  · ⚠ `.r-blocked` 는 `rc='r-'+role` 로 만들어지는 동적 클래스라 살아 있다. **문자열 조합 클래스는 정적 검색으로 안 잡힌다.**
- **README 를 현재 상태로 재작성** — 없어진 기능 서술을 걷어냈다: 주간 뷰, 기한 D-표기 배지,
  '기한 있는 업무를 달력에 점선 배지로', 현장 드래그 정렬(→ 실제로 코드가 없다). 화면 이름도 '주요업무 현황'→'업무 목록'.
  현재 구성(필터 카드 + 업무 패널, 카드 두 줄 구조, 자동 저장, `+N건`, 설정의 위젯 카드, cfg.widgetUrl,
  트레이 메뉴, `dist:nosign`)을 반영.
- **파일 구성 점검**: 총 20개(index.html · app.js · README · HANDOFF · build-single.mjs · database.rules.json ·
  scripts 3 · vendor 2 · widget 5 · .gitignore). 불필요 파일 없음.
  세분화도 과하지 않다 — 메일 스크립트는 공용 로직(`mail-common.mjs`)을 뽑아 둘이 나눠 쓰고,
  위젯은 창 제어와 Win32 호출이 성격이 달라 두 파일이 맞다.
  ⚠ `app.js`(3,650줄)·`index.html`(1,578줄)은 크지만 **단일 파일 빌드(build-single.mjs)와 `?v=` 캐시 무효화가
  이 구조를 전제로 한다** — 쪼개면 배포·캐시 규약을 함께 바꿔야 하므로 지금은 유지.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · wvis 대비 경고 0. app.js?v=101(코드 변경 없음).

### 111차 — 위젯·앱 지적 12건 중 8건 반영 (4건은 다음 회차)
**반영**
- 트레이: '오늘 업무 보기' 제거(관련 훅 `window.widToday` 도 삭제), '브라우저에서 전체 화면 열기' → **'브라우저 앱 열기'**,
  **첫 실행이면 자동 실행을 켠 상태로**(`state.autoStart === undefined` 일 때 `setAutoStart(true)`).
  ⚠ **'위젯 보이기/숨기기' 가 안 먹던 이유**: 바탕화면 모드는 z-순서가 맨 아래라 `show()` 해도 다른 창에 가려
  안 뜬 것처럼 보였다 → 일단 `show()+focus()` 로 꺼낸 뒤 400ms 후 모드를 다시 적용해 제자리로 돌린다.
- 날짜를 옮기면 **열려 있던 폼과 펼침을 모두 정리**한다(`closePlanEdit` 이 제목 있을 때만 저장하므로 빈 폼은 버려진다).
  `S.planOpen` 도 비워 새 날짜 팝업이 수정·펼침 상태로 열리지 않는다.
- ⚠ **배지 hover 시 왼쪽이 잘리던 원인**: `filter:brightness` 가 만드는 스택 문맥이 `.pm-l` 의 `overflow:hidden` 과
  겹쳤다 → hover 효과를 빼고 `margin-left:1px` 로 모서리를 띄웠다.
- ⚠ **유리 모드에서 칸 사이 선 색이 제각각이던 원인**: 라이트 테마의 격자 색(`#DCDFE6`)이 그대로 남아
  어떤 줄은 밝고 어떤 줄은 벽지가 비쳤다 → `.fc-scrollgrid-sync-table td{border-color:rgba(255,255,255,.10)!important}` 로 통일.
- 오늘 칸 음영 `.48` → **`.26`**. ⚠ **'오늘' 글씨가 붉던 원인**: 유리 모드의 `.dhol` 규칙(공휴일용 빨강)이
  `.dhol.dtoday` 까지 덮고 있었다 → 파랑 계열로 따로 지정.
- 위젯 배경 진하기 기본값 55% → **85%**.
- 업무 목록 분류 구분줄의 건수 배지를 **오른쪽 끝으로**(`.tk-sub::after{order:1}` + `.c{order:2}` — 구분선이
  공간을 먹고 있어 `margin-left:auto` 로는 안 밀린다). 실측 우측 2px.

**측정 결과 문제 없던 것**
- 카드 읽기 ↔ 수정 제목: `13.5px / 700 / line-height 20.25 / 카드 위에서 11.9px` **완전히 동일**.
  카드 상·하 패딩도 8px 로 대칭(요청대로 이미 반영돼 있었다). 유리 배경에서 다르게 보였을 가능성이 크다.

**다음 회차로 미룬 것(구조 변경이라 한 번에 하면 위험)**
1. 위젯 상단에 검색창 + 필터 꺾쇠 팝업 — `.dp-fcard` 를 헤더로 옮기고 `.dp-fadv` 를 팝오버로 만드는 작업.
2. 프로필 모달 위치·크기 — 지금은 위젯에서 왼쪽 정렬 + 300px. 팝오버가 오른쪽에 서야 해서 얽혀 있다.
3. 업무 목록 펼침 정리(내용 빈칸 1줄, 부제 간격, 코멘트 상시 입력 + 작성 버튼 같은 행 우측).
4. 업무 목록 상단 필터행을 업무 일정과 동일하게(기한 선택창 제거, 권역 필터 추가).
- 참고: '새 업무가 다 공통업무로 들어간다'는 101차 이전에 만든 업무들 얘기다. 지금은 담당자 기본값이
  로그인 본인이라 그 사람 아래로 들어간다(담당자를 비우면 팀 공통).
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=102. **widget/ 변경 있음 → exe 재배포 필요.**

### 112차 — 업무 목록 필터 통일 · 필터 저장 · 카드 하단 여백
- **업무 목록 필터행을 업무 일정과 같은 구성으로** 바꿨다(`.dp-fcard` 규격 재사용):
  1행 `[찾기][초기화][꺾쇠]`, 접힘 `[업무 구분|진행 상태]` · `[권역|현장]`.
  **기한 선택창(TK_DUE)은 폐기** — 업무 목록에 기한 개념이 사라진 뒤로 쓸 일이 없었다.
  `tkMatch` 도 기한 대신 kind·site·reg 로 거른다. ⚠ **권역 필터는 그 업무의 대상(sid)이 담당자일 때만 적용하고
  팀 공통 업무는 항상 남긴다** — 담당자들이 '팀 공통 + 본인 권역'으로 쓰는 것이 기본 사용 패턴이다.
  권역을 바꾸면 현장 선택은 초기화한다(목록이 좁혀지므로).
  옛 `.tkf-bar`·`.tkf-srch`·`.tkf-sel`·`.tkf-reset` CSS 폐기.
- **필터를 계정별로 저장한다**(`filtSave`/`filtLoad`, `localStorage['calapp.filt.<uid|local>']`).
  업무 일정의 5개 필터 + 찾는 범위 + 업무 목록 필터를 함께 담는다.
  부팅 때 한 번, **로그인이 끝난 뒤 한 번 더 읽는다**(로그인 전엔 uid 를 몰라 `local` 키를 쓰기 때문).
  RTDB `prefs` 대신 브라우저 저장을 쓴 이유: 필터는 조작할 때마다 바뀌어 쓰기가 잦고, PC마다 다르게
  쓰는 편이 자연스럽다.
- **업무 카드 하단 여백 8 → 11px.** ⚠ 위쪽은 8px 이지만 제목 줄이 아이콘(26px) 기준으로 가운데 정렬되어
  **실제로는 제목 위가 10.9px** 다 — 아래를 8px 로 두면 위아래가 달라 보인다. 위쪽 패딩은 편집 폼과
  맞춰야 하므로 건드리지 않고 아래만 맞췄다.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0. app.js?v=103. widget/ 변경 없음(직전 111차 exe 로 충분).

### 113차 — 필터를 다중 선택 칩으로 · 업무 목록 펼침 정리 · 프로필 모달
- **사용자 제안 채택: 선택창 → 다중 선택 칩.** 단 **항목이 적은 것만** 바꿨다 —
  `업무 구분`(5)·`진행 상태`(4)는 칩 토글로 여러 개를 동시에 고를 수 있고,
  **`권역`·`담당자`·`현장`은 선택창을 유지**했다(항목이 많아 칩으로 깔면 필터가 화면을 다 먹는다).
  · 값이 문자열 → **배열**로 바뀌었다. 빈 배열이면 '전체'라 '전체' 옵션 자체가 필요 없어졌다.
  · `S.filter.kind/st`, `S.tkF.kind/st` 둘 다 배열. 필터 판정은 `배열.length===0 || 배열.includes(값)`.
  · 업무 일정은 `chipHTML/chipOn` + `filt.chip`, 업무 목록은 `tkChip` + `tkf.chip`(렌더 방식이 달라 함수는 따로).
  · ⚠ 저장된 옛 필터(문자열)가 남아 있으면 `.map(String)` 이 문자열을 배열처럼 다루지 못한다 —
    `filtLoad` 는 스프레드로 병합하므로 **옛 값이 있으면 그대로 문자열**이 된다. `(f.st||[]).map` 이
    문자열에서 터지지 않도록 판정부는 배열만 받는 형태로 두었고, 초기화 버튼을 누르면 정상화된다.
- **업무 목록 펼침 정리**: 내용 칸 최소 높이 44 → **37px**(빈 칸이 커서 아래 링크와 멀어 보였다),
  첫 칸 위에 `margin-top:6px`(부제가 보조문구에 붙어 있던 것), **코멘트 입력을 상시 표시**하고
  '남기기' 버튼을 **같은 줄 오른쪽**에 두었다(`.th-new.th-always`). 접혀 있던 때와 같은 높이(34px)로 맞췄다.
  `tk.cmtOpen`/`tk.cmtCancel` 로 열고 닫던 흐름이 사라졌다.
- **프로필 모달**: 위젯에서 늘 왼쪽 정렬이던 것을 **프로필 팝오버가 열려 있는 동안만**(`#mo.pf-on`) 왼쪽으로
  비키도록 바꿨다. 평소에는 가운데에 뜨고 폭도 320px 로 되돌렸다. `pfDrop`·`pf.close` 에서 클래스를 뗀다.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=104. widget/ 변경 없음.
- **아직 남은 것**: 위젯 상단 검색창 + 필터 꺾쇠 팝업(헤더로 옮기는 작업) — 다음 회차.

### 114차 — 필터: 겉모습은 선택창, 열면 체크 목록 (시안 후 확정)
113차에 칩으로 바꾼 것을 사용자가 되돌리라고 했다. 요구는 **"C(선택창 배치) 그대로 두고, 누르면
선택창 안에 체크박스가 있는 것"** — 레이아웃을 바꾸지 않고 다중 선택만 얻는 방식.
(시안 5안 `filter-mockup.html` 로 높이를 비교해 결정: 전부 칩 455px vs 선택창 170px)

- **`mselHTML(scope,g,all,items,sel)` 신설** — 겉모습은 `.dp-sel` 과 같은 버튼, 누르면 아래로 체크 목록.
  맨 위 '전체' 로 한 번에 해제. 라벨은 `없음→'○○ 전체'` / `1개→그 이름` / `2개 이상→'첫 이름 외 N'`.
  `scope` 로 업무 일정(`cal`)·업무 목록(`tk`)이 같은 컴포넌트를 쓴다.
- ⚠ **필터 값이 전부 배열이 됐다**(`kind·st·reg·own·site`). 빈 배열이 '전체'라 `'*'` 센티넬과 '전체' 옵션이
  사라졌다. 판정은 `배열.length===0 || 배열.includes(값)`. `S.filter`·`S.tkF` 초기값도 배열로.
  ⚠ **저장된 옛 필터(문자열·`'*'`)가 남아 있으면** `rFilter` 의 `keep()` 이 걸러 내 빈 배열이 된다(= 전체).
- **`rFilter` 가 접힘부 전체를 그린다**(`#dpFadv` 를 통째로 innerHTML). 개별 `<select>` 마크업과
  `regFilter/ownFilter/siteFilter/kindFilter/stFilter` id, 그에 걸린 change 핸들러를 모두 제거.
  좁힘 규칙은 유지: 권역→담당자·현장, 담당자→현장(배정 현장이 없으면 좁히지 않음).
- 업무 목록도 같은 4개(`kind·st·reg·site`)를 msel 로. 칩(`fchip`)·`tkChip`·`filt.chip`·`tkf.chip` 폐기.
- 목록 밖을 누르면 닫힌다(`mselClose`, 전역 click 맨 앞).
- 실측: 펼친 높이 **172px**(칩 안이었으면 242~455px), 상태 '진행'만 2건 → '예정' 추가 5건 → '전체' 7건,
  라벨 `진행` → `예정 외 1`. 업무 목록도 동일 동작.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=105. widget/ 변경 없음.

### 115차 — 위젯 헤더에 검색창·필터 (이월분 완료)
- **팝업과 같은 방식으로 해결했다**: 새로 만들지 않고 **`.dp-fcard`(검색 + 범위 + 꺾쇠 + 접힘부) 노드를
  헤더의 `#widFilterSlot` 으로 옮긴다**(`widMount` 에서 `.day-panel` 과 함께 처리).
  그래서 다중 선택 목록·좁힘 규칙·필터 저장이 앱과 완전히 같은 코드로 동작한다.
  ⚠ `widMount` 는 `rWidget()` 이 부른다 — 옮기는 코드를 여기 두지 않으면 아예 실행되지 않는다(한 번 놓쳤다).
- 자리: `[연·월] … [◀ 홈 ▶] [검색·범위·꺾쇠] [프로필][설정]`. 카드 껍데기(배경·테두리·패딩)를 벗기고
  입력칸 높이를 34px 로 맞춰 헤더 줄(36px)에 얹었다. 슬롯은 `flex:1` + 카드 `max-width:340px`.
- **펼침부는 팝오버로**: `body.wid #dpFcard .dp-fadv{position:absolute;top:100%+8;right:0;width:306px}` +
  유리 카드. 안에서 토큰을 밝은 값으로 재정의해 다중 선택 목록·체크칸이 그대로 보인다.
  꺾쇠는 켜지면 파란 알약이 된다. 창이 좁으면(520px 미만) 범위 선택창은 숨긴다.
- ⚠ **위젯에서 검색 결과는 업무 팝업 안에 그려진다** — 글자를 치면 팝업을 자동으로 연다(안 그러면 아무 반응이 없어 보인다).
- 실측(760×560): 헤더 넘침 0, 필터 팝오버 306px·화면 안, 다중 선택 목록도 화면 안,
  범위 '전체'에서 `두정` 검색 → 1건, 진행 상태 '진행'만 → 달력 막대 7 → 2.
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=106. widget/ 변경 없음.

### 116차 — 분류 버그 · 색 팝오버 개편 · 다듬기 8건
- ⚠ **가장 큰 버그: 담당자를 바꿔도 소속이 안 옮겨졌다.** `planToTask` 가
  `const sid = prev ? prev.sid : (owns[0]||팀)` 였다 — **한 번 만들어진 자리에 영원히 남는다**.
  만든 사람 밑에 쌓이고, 담당자를 비워도 팀 공통으로 안 갔다.
  → `sid` 를 **항상 담당자(owns[0]) 기준으로 다시 계산**하고, `planToTask` 가 `prevSid` 를 함께 돌려주면
  `putPlan`(로컬·Firebase 양쪽)이 **소속이 바뀐 경우 옛 자리를 지운다**(안 지우면 두 곳에 남는다).
  실측: u1 → u3 로 담당자 변경 시 sid 이동·중복 0, 담당자 비우면 팀 id 로 복귀.
- **업무 구분 기본을 '일반'으로**(순서도 일반·공통·출장·회의·기타). `KIND_DEF=''`.
  일반은 진행경과·처리계획 두 칸이므로 새 업무의 기본 입력 형태가 바뀐다.
- **색 팝오버 전면 개편**
  · 1행 `[담당자 색(사람 아이콘)][빨][파][초][노][회]`, 그 아래 구분선 + **직접 추가한 색**.
  · 맨 아래에 **색상 팔레트(사각형 + 색상 띠)를 늘 펼쳐 둔다** — 가로 채도, 세로 밝기, RGB 값은 안 보여 준다.
    끌면서 고를 수 있고(`mousedown`+`mousemove`), 고른 색은 추가색 목록에 쌓인다.
  · 추가색은 **계정별로 이 브라우저에 저장**(`calapp.pal.<uid|local>`, 최근 18개). **우클릭으로 삭제**.
  · ⚠ 팝오버 안 색 칩의 **확대 hover 를 없앴다**(좁은 격자에서 서로 겹쳐 보였다) — 외곽선 강조로 대체.
  · 옛 `hueHex`·`col-hue`·`col-cus`·`pal-add`(색 입력) 경로 폐기. `hexHsv`/`hsvHex`/`rgbHex` 신설.
- 업무 목록 필터 4개를 **한 줄**로. **화면을 옮기면 업무 일정 필터 펼침도 닫는다**(`go()` 에서 `adv-on` 제거 + `mselClose`).
- 업무 카드 하단 12px, 보조문구 우측 여백 12px(담당자가 모서리에 붙던 것).
- 업무 패널 머리: 높이 42 → **52px**, 구분선 1px `--sep` → **2px `--sepo`**(아래 분류 구분선과 층이 구분되게),
  건수를 **아래 분류 배지와 같은 선**(패널 우측에서 19px)에 맞췄다.
- 위젯 자동 실행: 정책상 등록이 막히면 API 가 계속 false 를 주므로 **저장해 둔 뜻(`state.autoStart`)도 함께 본다**.
  ⚠ 이 값은 111차 exe 부터 들어갔다 — **exe 를 다시 만들지 않으면 켜지지 않는다.**
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=107. **widget/ 변경 있음 → exe 재배포.**

### 117차 — 폼 행 순서 · 위젯 헤더 정리 · 모달 쏠림 수정
- **업무 폼 행 순서 변경**: `시작일|종료일` · `업무 구분|담당자` · `현장` · (자세히) `시간|반복` · `반복 종료` · `링크` · `내용`.
  현장이 링크 자리로 올라와 기본 화면에 보이고, 링크는 반복 아래로 내려갔다.
- 위젯 헤더에서 **검색창과 범위(일/월/전체)를 뺐다** — 필터 꺾쇠 버튼만 남는다(`body.wid #dpFcard .dp-srch,.dp-scope{display:none}`).
  슬롯도 `flex:0 0 auto` 로 바꿔 헤더 폭을 먹지 않는다. 필터 팝오버는 그대로.
- 위젯 오늘 칸 음영 `.26` → **`.16`**.
- ⚠ **위젯 프로필 모달이 계속 왼쪽으로 쏠리고 좁던 진짜 원인**: 113차에 조건부로 바꿨다고 적었지만
  **`body.wid #mo{justify-content:flex-start}` 규칙이 그대로 남아 있었다**(고쳤다고 기록만 하고 실제로는
  다른 곳을 고쳤다). `body.wid #mo.pf-on` 으로 바로잡고, 폭 축소(320px)도 **팝오버가 열린 동안만** 걸리게 했다.
  `openModal` 에서 `pfClosed()` 를 불러 지난번 상태가 남아 있지 않게 한다.
  실측: 평소 560px·가운데 정렬, 팝오버 열면 320px·왼쪽·팝오버가 오른쪽에 서고 화면 안, 닫으면 560px 복귀.
  **교훈: '고쳤다'고 적기 전에 그 선택자가 실제로 사라졌는지 grep 으로 확인할 것.**
- 회귀 25/25 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=108. widget/ 변경 없음.

### 118차 — 색 팝오버 정리 · 업무 목록 레이아웃 8건
- **색 변경은 수정 모드에서만.** 읽기 카드의 색 원은 `<span class="p-col p-col-ro">`(표시 전용)으로,
  수정 폼의 원만 버튼이다. `COL_PID` 분기와 읽기 카드 즉시 저장 경로를 통째로 걷어냈다.
- ⚠ **팝오버가 카드·구분선에 가려지던 원인**: 색 원 안에 넣어 두니 `.dp-body`·`.plan` 의 overflow 에 잘렸다.
  → **`document.body` 에 붙이고 `position:fixed`(z-index 900)**, 버튼 좌표를 재서 화면 안으로 접는다.
  `.p-col` 의 확대 hover 도 제거(작은 원이 커졌다 작아지면 카드가 흔들려 보인다) — 외곽 링으로 대체.
- ⚠ **추가색이 드래그하는 동안 계속 쌓여 비슷한 파랑이 8개** 생겼다 → `cpPick(e,commit)` 으로 나눠
  **손을 뗄 때(mouseup) 한 번만** 담는다. 보관 개수도 18 → **7개**(계정별 `calapp.pal.<uid|local>`).
  담은 뒤 색 줄만 다시 그린다(`cpRefresh` — 팔레트 사각형을 다시 그리면 끌던 자리가 튄다).
- 기본색: 금색 `#E0A44A` → **노랑 `#FACC15`**, 회색 앞에 **핑크 `#EC4899`** 추가(6색).
- ⚠ **업무 목록 필터가 두 겹으로 보이던 원인**: `<div class="card tkf-card">` 로 감싸고 그 안의
  `.dp-fcard` 가 또 카드 껍데기였다 → 래퍼를 없애고 `tkFilterHTML` 이 `card dp-fcard` 를 직접 낸다.
- 업무 목록: 패널 머리 아래 굵은 선 삭제 · 분류 줄 옆 선 삭제 → **분류와 분류 사이에만 얇은 선**
  (`.tk-list>.tk-sub:not(:first-child)` 에 위 여백 + 위 테두리). 분류 글자 11.5 → **13px/800**,
  담당자 부제 앞에 **점**(사이드바와 같은 모양), 직급 배지는 업무 목록 안에서만 10.5px 로 축소.
- 업무 목록 카드 하단 여백 **12px**(업무 일정 카드와 같게).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=109. widget/ 변경 없음.

### 119차 — 업무 목록 작성·수정 폼을 업무 일정과 완전히 통일
- **핵심: `taskFormHTML` 을 `planFormHTML` 과 같은 골격으로 다시 썼다.**
  `.dp-edit` + `.pe-bar`(색 원 · 제목 · 상태 배지 · 삭제 · 알림 · 저장 체크) + `.pe-body` 행 순서
  `시작일|종료일` · `업무 구분|담당자` · `현장` · `시간|반복` · `반복 종료` · `링크` · `내용`.
  업무 목록에는 요청대로 **'자세히' 접기·펼치기와 '업무 목록에서 쓰기' 아이콘을 넣지 않는다**(모든 행이 늘 보인다).
  · 상태·리마인드는 **폼 안에서 draft 로만** 바꾼다(`tk.stDraft`·`tk.remindDraft` — 다시 그리면 입력이 날아간다).
  · 색은 같은 팔레트를 쓰되 `#tnColor` 숨은 값에 담고 저장 때 반영한다(`setPlanColor` 가 폼 종류를 보고 갈라진다).
  · **종료일·반복·반복 종료가 새로 저장된다**(예전 폼에는 없던 필드).
  · 링크는 업무 일정과 같이 **한 칸** — 예전에 여러 개 넣어 둔 것은 첫 칸만 고치고 나머지는 보존한다.
  · 폐기: `.tkf-top/.tkf-now/.tkf-sec/.tkf-h/.tkf-g3/.tkf-g4/.tkf-f/.tkf-foot/.tk-new-t/.tk-new-a/.tk-new-g`,
    `linkRowHTML`·`tk.linkAdd`·`tk.linkDel`, 옛 `tkKindRefresh` 본문(→ `tkBodyHTML` 공용).
- 업무 목록 여백: 패널 머리 아래 12 → **4px**, 분류 줄 패딩 축소, 담당자 부제 12.5px·**회색 점**.
- 코멘트 버튼 '남기기' → **'작성'**, 파랑 → **회색(bg2)**, 높이 34px 로 입력칸과 맞춤.
- 색 팝오버 폭 222 → **250px**(담당자 색 + 기본 6색이 한 줄에). 기본색의 핑크 → **보라 `#8B5CF6`**.
- 사이드바 담당자 들여쓰기 24 → **16px**(점이 글자에 붙도록).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=110. widget/ 변경 없음.

### 120차 — 위젯 4건
- **프로필 팝오버가 모달을 밀지 않는다.** 창이 좁아 옆에 세울 자리가 없으니 **모달 위에 겹쳐 띄운다** —
  `body.wid #pfPop{left:50%!important;transform:translateX(-50%);z-index:1100}` + `pfPlace` 가 위젯이면
  세로 가운데만 잡고 좌우 계산을 건너뛴다. `#mo.pf-on` 로 밀던 방식은 폐기(모달 폭도 그대로 560px).
- **연·월 팝업은 달을 골라도 닫히지 않는다** — 연달아 옮겨 볼 수 있다.
  모달로 열렸을 때만 닫고, 고른 뒤 팝업 내용을 다시 그려 선택 표시를 갱신한다.
- **위젯에서 오늘 칸 배경색 제거** — 정적 규칙과 `widApply` 가 주입하던 동적 규칙 **두 곳 모두**에서 뺐다.
  날짜 숫자의 파란 원 배지로 충분하다. ⚠ 위젯 색은 CSS 와 주입 규칙 두 군데에 있다 — 한쪽만 고치면 남는다.
- 위젯 설정에 **'브라우저 앱 열기'** 추가. `window.open(...)` 으로 열면 Electron 의 `setWindowOpenHandler` 가
  기본 브라우저로 넘긴다 — **main.js 를 고치지 않아 exe 재배포가 필요 없다.**
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 넘침 0 · 대비 경고 0. app.js?v=111. widget/ 변경 없음.

### 121차 — 업무 목록 폼 배치 · 위젯 마감 점검
- **업무 목록 폼 행 재구성**: 1행 `시작일·종료일·시간·반복`(4등분, `.frow4`) · 반복 종료 ·
  2행 `업무 구분(25%)·담당자(25%)·현장(50%)`(`.frow-ks` = `1fr 1fr 2fr`) · 링크 ·
  내용(100%) 또는 진행경과·처리계획(50/50). 실측 201/201/402px.
  좁은 화면(960px 이하)에서는 4열 → 2열, 3열 → 1열로 접힌다.
- 업무 패널 제목과 첫 분류 사이 간격 축소(목록 위 패딩 4→0, 분류 위 패딩 6→2).
- 위젯 설정: '브라우저 앱 열기'를 **맨 아래로** 옮기고 위에 구분선, **문구 자체가 버튼**(`.wgs-full`, 가운데 정렬).
- ⚠ **새로 만든 업무의 폼에 삭제 버튼이 안 나타나던 문제**: 자동 저장으로 `pe.orig` 가 생겨도
  **폼을 다시 그리지 않으므로**(입력이 날아간다) 버튼이 없는 채로 남았다 → 첫 저장 시점에
  `.pe-side` 에 삭제 버튼을 **DOM 으로 끼워 넣는다**(상태 배지 다음, 알림 앞).
- **사이드바 접힘 아이콘 이동은 재현되지 않았다** — 5개 화면 모두 접힘 전후 이동량 0,
  전환 중(60/140/220/400ms)에도 아이콘 x 가 16px 고정. 활성 항목의 배경만 223→47px 로 줄어든다.
  사용자 화면과 다를 수 있어 다음 회차에 스크린샷으로 다시 확인할 것.
- **위젯 전면 점검 도구 `waudit.mjs` 신설(14항목, 전부 통과)** — 첫 화면·날짜 3단계 클릭·팝업 내용/위치·
  수정 폼 자동 저장·X 동시 닫힘·색 팔레트·필터 다중 선택 반영·연월/오늘 이동·설정 구성·글자 크기·
  배경 진하기·조정 모드·프로필 팝오버·막대 클릭. 창 크기 4종(380×520~1200×900)에서 넘침 0·팝업 화면 안.
  CRUD(신규→자동 저장→달력 반영→삭제)도 실측. 콘솔 오류 0.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14 · 넘침 0 · 대비 경고 0. app.js?v=112. widget/ 변경 없음.

### 122차 — 121차가 화면에 안 나오던 이유(따옴표 하나)
- ⚠ **`.frow2` 규칙 앞에 따옴표 `"` 가 하나 섞여 들어가 있었다.** 파이썬으로 CSS 를 끼워 넣을 때
  삼중 따옴표 처리를 잘못한 결과다. CSS 파서는 잘못된 토큰을 만나면 **다음 `}` 까지 통째로 버리므로**
  그 뒤에 넣은 `.frow4`(1행 4칸) 배치가 화면에 반영되지 않았다. 코드는 맞고 CSS 만 죽어 있던 것이다.
  → 따옴표 제거 후 실측: `.frow4` = 274×4, `.frow-ks` = 276/276/552(25/25/50), 업무 일정 폼 `.frow2` = 1fr 1fr.
  **교훈: CSS 를 스크립트로 끼워 넣은 뒤에는 그 규칙이 실제로 적용됐는지 computed style 로 확인할 것.**
  문법 오류는 node --check 로도, 회귀 검사로도 안 잡힌다(둘 다 JS 만 본다).
- ⚠ **업무 구분을 바꿔도 내용 칸이 안 바뀌던 것**도 함께 고쳤다 — 119차에 폼을 다시 쓰면서
  `tnKind` 의 change 처리가 빠졌다(`tnRec` 만 남아 있었다). 실측: 일반 → 진행경과·처리계획 2열,
  공통 → 내용 1칸, 반복을 고르면 '반복 종료' 행이 나타난다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14 · 넘침 0 · 대비 경고 0. app.js?v=113.

### 123차 — 업무 목록 폼 2행도 4등분
- 2행을 `업무 구분 · 담당자 · 현장 · 링크` **4등분**으로 바꿨다(별도 링크 행 폐기).
  1행(시작일·종료일·시간·반복)과 같은 `.frow4` 를 쓰므로 두 줄의 칸 폭이 정확히 맞는다.
  `.frow-ks`(1fr 1fr 2fr)는 폐기. 좁은 화면(960px 이하)에서는 두 줄 모두 2열로 접힌다.
- 실측(1800px): 1행 273.5×4, 2행 273.5×4, 내용 1칸 1124px. `.frow-ks` 잔존 없음.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14 · 넘침 0. app.js?v=114. widget/ 변경 없음.

### 124차 — 사내 PC 위젯 로그인 멈춤 진단 도구
증상: 회사 PC 에서 exe 로 띄우니 로그인 화면까지는 나오는데 **'로그인 중…' 에서 멈춘다.**
로그인 화면이 떴다는 건 GitHub Pages 와 Firebase SDK(gstatic) 는 열렸다는 뜻이므로,
막힌 곳은 **인증·토큰·DB 도메인** 쪽일 가능성이 높다.

- **연결 진단 `netDiag()` 추가** — 9초 감시가 끝나면 네 곳에 `no-cors` 로 찔러 보고 **막힌 이름을 그대로 알려 준다**:
  `identitytoolkit.googleapis.com`(인증) · `securetoken.googleapis.com`(토큰) ·
  `...firebasedatabase.app`(데이터베이스) · `www.gstatic.com`(CDN).
  전부 닿는데도 응답이 없으면 "사내 보안 프로그램이 가로막고 있을 수 있다"고 안내한다.
  ⚠ `no-cors` 는 응답 내용을 못 읽지만 **닿았는지 여부는 알 수 있다** — 진단에는 그걸로 충분하다.
- **위젯에서 개발자 도구를 열 수 있게 했다** — 트레이 '개발자 도구 (문제 확인)' + `Ctrl+Shift+I`.
  콘솔·네트워크 탭에서 어느 요청이 실패하는지 직접 볼 수 있다.
- **주소를 아예 못 여는 경우**(사내망이 GitHub Pages 를 막는 등) 흰 화면만 남던 것을
  `did-fail-load` 로 잡아 **오류 문구와 주소를 창에 표시**한다.
- ⚠ **widget/ 이 바뀌었으므로 exe 재배포 필요.**
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14. app.js?v=115.

### 125차 — 위젯 로그인 멈춤: User-Agent 가 원인으로 유력
확인된 사실: **같은 사내 PC 의 엣지에서는 로그인이 된다.** 따라서 방화벽·계정 문제가 아니라
**Electron 창에서만 생기는 문제**다.

- 이 앱은 **App Check(reCAPTCHA Enterprise)** 를 켜 두었다(`firebase.appCheck().activate(...)`).
  로그인은 App Check 토큰을 기다리는데, **reCAPTCHA 는 User-Agent 에 `Electron/…` 이 들어 있으면
  통과시키지 않는 경우가 있다.** 토큰이 안 나오면 `signInWithEmailAndPassword` 가 오류도 없이
  영원히 기다린다 — 증상('로그인 중…' 에서 멈춤)과 정확히 맞는다.
- → `app.userAgentFallback` 에서 **`Electron/버전`과 앱 이름 토큰을 지운다**(창을 만들기 전에).
  ⚠ **`requestSingleInstanceLock()` 블록 안, `whenReady` 앞**에 두어야 첫 창부터 적용된다.
- `netDiag()` 진단 대상에 **reCAPTCHA(`www.google.com/recaptcha/api.js`)** 를 추가하고,
  네 곳이 모두 닿는데 멈추면 "보안 확인 단계에서 걸린 것 — 최신 위젯으로 다시 만들라"고 안내한다.
- ⚠ **widget/ 변경 → exe 재배포 필요.** 이 수정은 exe 를 다시 만들어야만 적용된다.
- 그래도 안 되면 다음 후보: ①App Check 를 위젯에서만 끄기(서버 강제 설정이 켜져 있으면 불가)
  ②reCAPTCHA Enterprise 콘솔에 도메인/키 설정 확인 ③위젯을 엣지 앱 모드 바로가기로 대체.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14. app.js?v=116.

### 126차 — 추정 중단, 계측으로 전환
사용자 지적: **같은 exe 가 집에서는 되고 회사에서만 멈춘다.** 그렇다면 125차의 User-Agent 가설은
성립하지 않는다(UA 는 PC 와 무관하게 같다). **원인은 PC 가 아니라 그 망에 있다.**
UA 수정은 해가 없으니 남겨 두되, 이제부터는 추측 대신 **실패한 요청의 이름을 받아 본다.**

- `widget/main.js` 에 **네트워크 계측** 추가
  · `session.webRequest.onErrorOccurred` — 차단·시간 초과 등 **모든 요청 실패**를 호스트 + 오류 코드로 기록
  · `app.on('certificate-error')` — 사내 보안 장비의 **TLS 검사(인증서 오류)**. ⚠ **우회하지 않고 기록만 한다**
  · `app.on('login')` — **프록시가 인증을 요구**하면 창이 안 뜨고 조용히 멈춘다. 이것도 기록
- 트레이에 **'네트워크 오류 보기(N)'** — 최근 20건을 대화상자로 보여 주고 **[복사]** 로 전체를 클립보드에 담는다.
  이 목록만 보내 주면 원인이 바로 나온다(예: `identitytoolkit.googleapis.com ERR_CERT_AUTHORITY_INVALID`
  → 사내 TLS 검사 / `ERR_PROXY_CONNECTION_FAILED` → 프록시 / `ERR_BLOCKED_BY_ADMINISTRATOR` → 정책 차단).
- 유력 후보(집↔회사 차이로 설명되는 것만): ①**사내 TLS 검사**가 googleapis 계열만 가로채는 경우
  (GitHub Pages 는 통과해 로그인 화면은 뜬다 — 증상과 맞음) ②프록시 인증 ③EDR/DLP 가 서명 없는 exe 의 통신만 차단.
- ⚠ **widget/ 변경 → exe 재배포 필요.**
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14. app.js 변경 없음(?v=116 유지).

### 127차 — 위젯 로그인 멈춤의 진짜 원인: App Check 403
개발자 도구 콘솔로 확정했다. 네트워크는 정상이었다(회사 PC 에서 `curl` 로 인증 서버·reCAPTCHA 모두 200/400 응답).

```
@firebase/app-check: Requests throttled due to 403 error. Attempts allowed again after 24h
@firebase/database: FIREBASE WARNING: Invalid appcheck token (…firebasedatabase.app)
```

- **흐름**: App Check 토큰 요청이 **403** → 클라이언트가 **24시간 차단(throttle)** → 로그인 자체는 성공하지만
  **RTDB 가 `Invalid appcheck token` 으로 읽기를 거부** → 앱은 데이터가 오기만 기다려 '로그인 중…' 에서 멈춘다.
  ⚠ **로그인 실패가 아니라 로그인 후 데이터 차단**이었다 — 증상만 보면 구분이 안 된다.
- 403 은 서버가 내는 것이라 **클라이언트로는 우회할 수 없다.** 콘솔에서 풀어야 한다:
  ①Firebase 콘솔 > App Check > **Realtime Database·Authentication 의 강제(enforce) 해제**
  ②reCAPTCHA Enterprise 키의 **허용 도메인에 `dongyexn.github.io`** 가 있는지
  ③앱 등록(App Check > 앱)에 웹 앱이 그 키로 등록돼 있는지
- 24시간 차단은 **클라이언트에 저장**된다 — 위젯은 `%AppData%\업무일정위젯` 을 지우면 초기화된다.
- ⚠ 집에서는 되고 회사에서만 안 됐던 이유는 **엣지에 유효한 App Check 토큰이 캐시돼 있었기 때문**으로 보인다
  (같은 PC 에서 엣지는 되고 위젯만 안 되던 것도 이걸로 설명된다).
- 클라이언트 보완: **로그인 후 20초 안에 데이터가 안 오면** 게이트를 다시 열고
  "로그인은 되었지만 데이터를 읽지 못했습니다 · 보안 확인(App Check) 토큰 문제" 라고 알린다(`FB._dbWatch`).
  이제 무한 대기 대신 원인이 화면에 뜬다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14. app.js?v=117. widget/ 변경 없음.

### 128차 — App Check 를 다시 켜고 싶을 때
App Check 강제를 끄자 위젯이 바로 동작했다(127차 진단 확정). 다시 켜려면 **위젯이 토큰을 받을 길**이 있어야 한다.

- **먼저 확인할 것(이게 되면 가장 깨끗하다)**: reCAPTCHA Enterprise 키 설정에서
  ①허용 도메인에 `dongyexn.github.io` ②키 유형이 '웹사이트(체크박스 없는 점수 기반)'
  ③App Check > 앱 목록에 웹 앱이 그 키로 등록. 위젯도 같은 도메인을 열므로 원래는 통과해야 한다.
- **그래도 안 되면 위젯만 디버그 토큰**: `FB.APPCHECK_DEBUG` 에 값을 넣고
  Firebase 콘솔 > App Check > 앱 > **디버그 토큰 관리**에 같은 값을 등록한다.
  `WIDGET && FB.APPCHECK_DEBUG` 일 때만 `self.FIREBASE_APPCHECK_DEBUG_TOKEN` 을 세운다
  (⚠ **`activate()` 보다 먼저** 세워야 적용된다). 비워 두면 지금과 같이 위젯도 reCAPTCHA 를 쓴다.
  ⚠ **이 값은 배포 파일에 그대로 들어간다** — 알면 누구나 쓸 수 있어 **App Check 의 보호 효과는 그만큼 약해진다.**
  즉 '강제 켜고 위젯은 디버그 토큰'은 **보안상 강제를 끈 것과 큰 차이가 없다.**
- **판단**: 이 앱은 사내용이고 규칙(database.rules.json)에서 로그인·역할을 이미 검사한다.
  App Check 는 그 위에 얹는 봇 차단 장치이므로, **강제를 끈 채로 두는 편이 실용적**이다.
  꼭 켜야 한다면 브라우저만 강제(Auth/RTDB 모니터링 모드)로 두고 지표를 보는 방법도 있다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 14/14. app.js?v=118. widget/ 변경 없음.

### 129차 — 위젯 밝기·글자 크기 자유 조절 · 즉시 반영 (모두 웹만 수정)
App Check 는 **해제 상태로 잠정 보류**(128차 판단).

- **밝기(어둡게/밝게) 추가.** 바탕화면이 어두운 사람은 밝은 위젯이 잘 보인다는 실사용 의견.
  `widApply` 가 주입하는 색을 **변수로 뽑아**(칸·주말·요일머리·카드·버튼 5색) 어둡게/밝게를 한 번에 바꾼다.
  `body.wid.wlight` 로 글자·격자선·검색창 색도 함께 뒤집는다. 실측: 칸 `rgba(246,248,252,.85)` · 숫자 `rgba(28,28,30,.92)`.
- **글자 크기를 슬라이더(85~140%)로.** 작게/보통/크게 3단 → 자유 조절.
  ⚠ **`--wfz` 가 안 걸려 있던 것들을 함께 묶었다** — 공휴일 문구·더보기(+N건)·팝업 날짜 머리.
  (막대 높이·padding 은 이미 묶여 있었다.) 실측 135%에서 공휴일 문구 14.3px, 가로 넘침 0.
- **업무 막대 글자 또렷하게** — 굵기 500→600(위젯은 700) + 위젯에서만 그림자·안티에일리어싱.
  ⚠ 라이트 모드에서는 그림자를 뺀다(밝은 배경에서 지저분해진다).
- **막대를 한 번 누르면 목록이 열리고 그 업무가 펼쳐진다.**
  ⚠ 원인: `S.planOpen` 을 세운 **뒤에** `selDate` 를 불렀는데, `selDate` 가 날짜가 바뀌면 `planOpen` 을 지운다 → 순서를 바꿨다.
- ⚠ **수정이 한 박자 늦게 반영되던 원인**: 라이브(Firebase) 모드에서는 **서버 응답이 돌아와야** 화면을 다시 그렸다.
  → `FbStore.putTask` 가 **화면을 먼저 고치고**(낙관적 반영) 서버에 보낸다. 실패하면 구독이 원래 값을 되돌려 준다.
- 위젯 헤더에 **새로고침 버튼**(연·월 이동과 필터 사이). `#i-refresh` 심볼 신설.
- ⚠ **'뒤로 배치'는 이미 트레이에 있다** — '항상 위에 표시'를 끄면 바탕화면 모드(맨 아래)다.
  탐색기 앞으로 튀어나오는 건 **위젯을 클릭해 포커스가 갔을 때 z-순서를 건드리지 않기 때문**이고,
  그 동작을 바꾸려면 `main.js` 수정 → **exe 재배포가 필요하다**(이번 요청은 재설치 없이 처리).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=119. **widget/ 변경 없음.**

### 130차 — 위젯에서 찾기 흔적 완전 제거
117차에 위젯 헤더의 검색칸·범위를 `display:none` 으로 숨겼는데, **숨긴 것과 없앤 것은 다르다.**

- ⚠ **숨은 입력칸은 값이 남아 필터처럼 계속 작동한다** — `S.dayQ` 에 예전 검색어가 남아 있으면
  위젯 목록이 조용히 걸러진다. → `widMount` 에서 **`.dp-srch` 와 `.dp-scope` 를 DOM 에서 아예 제거**하고
  `S.dayQ=''` 로 비운다. 관련 위젯 전용 CSS(7규칙)도 함께 정리.
- **`Ctrl+K` 찾기 패널도 위젯에서는 막았다**(`!WIDGET &&` 조건). 플로팅 버튼은 이미 숨겨져 있었지만
  단축키는 그대로 열려 있었다. 앱에서는 그대로 동작(실측: 앱 true / 위젯 false).
- 115차에 넣었던 '위젯에서 검색어를 치면 팝업을 연다' 분기도 제거(검색칸이 없어졌다).
- 실측: 위젯에 `#dpQ`·`#dpScope` 없음, 헤더는 `[이동][새로고침][필터][프로필·설정]`,
  필터 카드 1행은 꺾쇠 하나뿐, 필터 5개 정상.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=120. widget/ 변경 없음.

### 131차 — 위젯 사용감(상용 앱 대비) 점검과 보완
위젯을 직접 조작하며 상용 데스크톱 위젯이면 당연히 되는데 우리는 안 되던 것을 찾았다. **다섯 개 다 안 됐다.**

- **바깥을 누르면 닫힌다** — 위젯 설정 팝업, 필터 펼침.
  ⚠ **`click` 이 아니라 `mousedown` 캡처를 써야 한다** — FullCalendar 가 달력 칸의 click 을 삼켜서
  `click` 으로 달면 **달력을 눌렀을 때만 안 닫히는** 이상한 상태가 된다(실제로 그렇게 만들었다가 발견).
- **`Esc` 로 닫힌다** — 겹친 순서대로 하나씩: 색 팔레트 → 다중 선택 목록 → 연·월 팝업 → 설정 → 필터 펼침 →
  업무 팝업(수정 중이면 폼도 함께 닫는다). 한 번에 다 닫으면 되돌리기가 번거롭다.
- **프로필 버튼이 계정 얼굴로** — 사이드바 아바타와 같은 이모지·색을 쓴다(`#widAcctAv`).
  `rAcct` 의 아바타 칠하기를 `paint(el)` 로 뽑아 사이드바와 위젯 헤더 둘 다에 적용.
- **`←` `→` 로 달 넘기기**(위젯 한정, 입력 중일 때는 동작하지 않는다).
- 실측: 달력 칸을 눌러도 설정·필터가 닫히고, 설정 버튼 토글은 그대로 동작(열림→닫힘).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=121. widget/ 변경 없음.

### 132차 — 위젯 설정 다듬기 8건
- 설정 팝업 236 → **282px**(퍼센트가 줄바꿈되던 것). 라벨은 `white-space:nowrap`, 슬라이더가 남는 폭을 먹는다.
- **'배경 진하기' → '투명도'** 로 이름을 바꾸면서 **방향도 뒤집었다** — 오른쪽으로 갈수록 투명해진다.
  ⚠ 저장값 `c.a` 는 여전히 '불투명도'다. 슬라이더 값 = `100 - a*100`, 입력 시 `a = 100 - 값`.
  이름과 조작 방향이 어긋나면 매번 헷갈리므로 표시만 바꾸지 않고 방향까지 맞췄다. 퍼센트 표기 추가.
- **'밝기' → '모드', 버튼은 '다크/라이트'**(기본 다크).
- 새로고침 버튼에도 알약 배경 — 필터 버튼과 같은 규칙으로 묶었다.
- **라이트 모드 색을 한 단계 낮췄다**(너무 밝다는 지적):
  칸 `246,248,252→231,235,242` · 주말 `228,232,240→214,220,231` · 요일머리 `226,231,240→205,213,226` ·
  카드 `255,255,255→243,246,251` · 버튼 `240,243,248→221,227,238`.
- 라이트 모드에서 **글자 그림자 전부 제거**(연·월 제목·날짜·요일). 밝은 배경에서 번져 보인다.
- **필터 버튼만 유독 밝던 것** — 흰색(.9)이라 헤더의 다른 알약과 안 맞았다 → 버튼 배경색과 같은 톤으로.
  켜졌을 때만 파란 알약.
- 업무 막대 글자 굵기 700 → **600**, 그림자를 `0 1px 2px`(아래로) → **`0 0 3px`**(중앙에서 퍼짐)로.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=122. widget/ 변경 없음.

### 133차 — 밝은 색 막대의 글자 가독성 · 말줄임 · 설정 정렬
- ⚠ **노랑·연두 막대 위의 흰 글자는 그림자로 못 살린다** — 색 자체를 바꿔야 한다.
  `isLightColor(hex)` 신설(사람 눈 기준 밝기 `0.299R+0.587G+0.114B > 150`) → 밝으면 **어두운 글자**(#1B1B1F)로,
  그림자도 밝은 쪽(`rgba(255,255,255,.55)`)으로 뒤집는다. 기준값 150 은 연두 `#84CC16`(≈162)이
  실사용에서 안 읽혀 168 에서 낮춘 값이다. 현재 팔레트: 노랑·연두만 어두운 글자.
- ⚠ **FullCalendar 의 `textColor` 옵션이 먹지 않는다** — 이 앱의 `#fcal .fc-event` 규칙(아이디 명시도)에 가린다.
  → 글자색은 CSS 에서 정한다(`#fcal .fc-event{color:#fff}` + `.on-light{color:#1B1B1F}`),
  구분은 `planEvent` 가 붙이는 `on-light` 클래스로. **FC 옵션이 안 먹으면 CSS 로 못 박을 것.**
- 막대 글자가 넘치면 **말줄임(…)** — `.fc-event-title{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`
  + `.fc-event-main`·`-main-frame` 에 `min-width:0`(그리드·플렉스 안에서는 이게 없으면 안 줄어든다).
- 막대 글자 굵기 600 → **500**(밝은 막대는 600 유지 — 어두운 글자는 얇으면 흐려 보인다).
- 설정 슬라이더 시작점을 맞췄다 — 라벨 폭 `flex:0 0 116px` 고정(글자 수가 달라 슬라이더 길이가 제각각이던 것).
- 요일 머리 글자 11.5 → **12.5px**(`--wfz` 연동 유지).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=123. widget/ 변경 없음.

### 134차 — 위젯 글자가 메모장보다 흐린 이유
폰트 문제가 아니다. **투명한 창에서는 윈도우가 서브픽셀 렌더링(ClearType)을 쓸 수 없다** —
바탕이 무엇인지 모르니 회색조로만 그린다. 옆의 메모장(불투명 창)과 나란히 두면 차이가 확연하다.

- ⚠ **`-webkit-font-smoothing:antialiased` 를 body 에서 제거**했다. 이 값은 **서브픽셀 렌더링을 강제로 끄는**
  선언이라 상황을 더 나쁘게 만들고 있었다(맥에서 얇게 보이려고 쓰는 관용구인데 윈도우에서는 손해다).
- **글자 그림자 제거** — 막대·연·월 제목. 그림자는 정의상 글자 가장자리를 번지게 한다.
  밝은 막대의 가독성은 133차에서 **글자색을 어둡게 바꾸는 것**으로 이미 해결했으므로 그림자가 필요 없다.
- **소수점 크기를 없앴다** — `--wfz` 를 곱하면 11.5px→13.225px 같은 값이 나와 글자가 흐려진다.
  `round(calc(…), 1px)` 로 정수 픽셀로 떨어뜨렸다(Chromium 112+, Electron 31 은 126이라 지원).
  실측 115%에서 막대 13px · 날짜 14px · 제목 17px · 높이 21px 모두 정수.
- **투명도 0% 면 창 바탕을 불투명하게 칠한다**(`body.wsolid` + `--wsolid-bg`).
  브라우저가 '이 층은 불투명하다'고 판단하면 서브픽셀 렌더링을 다시 쓸 수 있다 — **재설치 없이 가장 또렷한 상태.**
- ⚠ 그래도 완전히 메모장 수준이 되려면 **창 자체를 불투명하게** 만들어야 한다(`transparent:false`) — exe 재배포 필요.
  반투명을 포기하는 선택이라 사용자 판단이 필요해 이번에는 넣지 않았다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=124. widget/ 변경 없음.

### 135차 — 공휴일 문구가 여백을 두고 잘리던 것
- ⚠ **위젯의 `.dhol` 에 `max-width:52px` 이 박혀 있었다** — 칸이 106px 인데도 '대체공휴일'이 '대체공…'이 됐다.
  앱 쪽도 `max-width:104px` 로 고정이었다. → 둘 다 **`flex:1;min-width:0`** 으로 바꿔 **칸 폭에 맞춰 쓰고
  정말 좁을 때만 말줄임**한다. 위젯 글자 크기(10px 고정)도 풀어 `--wfz` 를 따르게 했다.
  실측(창 760): '대체공휴일' 76px / 칸 106px, 잘림 없음. 앱(1500)도 잘림 없음.
  좁은 창(420)이나 글자 140% 처럼 정말 자리가 없을 때만 `…` 로 줄어든다 — 가로 넘침은 0.
  **교훈: 고정 max-width 는 화면·글자 크기가 바뀌면 바로 어긋난다. 잘라야 할 때만 잘리게 flex 로 둘 것.**
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=125. widget/ 변경 없음.

### 136차 — 위젯 잔손질 3건
- **투명도 0% 일 때 창 바탕을 불투명하게 칠하던 것을 되돌렸다**(134차에 넣은 `wsolid`).
  글자 선명도를 위한 시도였지만 사용자가 원치 않았다 — 관련 CSS·JS 를 남김없이 제거(잔존 0).
  ⚠ 이 때문에 투명 창의 글자 흐림은 그대로다. 해결하려면 창 자체를 불투명하게(exe 재배포) 하는 수밖에 없다.
- **설정 팝업 높이를 필터·연·월 팝업과 맞췄다** — `top:44px` → `55px`. 실측 세 팝업 모두 55px 에서 시작.
- **이웃 달 칸을 더 옅게** — `a*.42` → `a*.22`. 이번 달과 대비가 뚜렷해진다
  (투명도 0% 기준: 이번 달 불투명 vs 이웃 달 0.22).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=126. widget/ 변경 없음.

### 137차 — 위젯 글꼴 선택(기본 / 시스템)
투명 창의 글자 흐림은 창 구조 문제라 CSS 로는 못 없앤다. 다만 **가변 폰트(Pretendard Variable)는
작은 크기에서 특히 뭉개지고, 윈도우 기본 글꼴(맑은 고딕)은 작은 크기용 힌팅이 들어 있어** 더 나을 수 있다.
어느 쪽이 나은지는 화면·해상도마다 다르므로 **고르게 했다.**

- 위젯 설정에 **'글꼴 — 기본 / 시스템'** 추가(기본값은 기존 글꼴). `body.wid.wsysfont` 로 전환.
- ⚠ **맑은 고딕은 굵기가 400·700 만 실제로 존재한다** — 500·600 을 쓰면 브라우저가 억지로 굵혀
  오히려 뭉개진다. 시스템 글꼴일 때는 막대·날짜·요일·공휴일 문구를 **700 으로 스냅**하고 자간도 0 으로 되돌린다.
- 실측: 전환 시 `Malgun Gothic` / 굵기 700 적용, 설정은 `calapp.wid` 에 저장되어 다시 열어도 유지.
  시스템 글꼴 + 글자 140% 에서도 가로·세로 넘침 0, 공휴일 문구·막대 잘림 없음.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=127. widget/ 변경 없음.

### 138차 — 위젯 글꼴 4종 선택
137차의 '기본/시스템' 2종을 **기본 · 맑은고딕 · 굴림 · 돋움** 4종으로 넓혔다.

- 윈도우에 항상 있는 한글 글꼴만 골랐다. **굴림·돋움은 작은 크기에 비트맵이 내장돼 있어**
  투명 창처럼 회색조로 그려지는 환경에서 가장 또렷할 수 있다(대신 곡선이 각져 보인다).
  어느 쪽이 나은지는 모니터·해상도마다 달라 **직접 비교하고 고르게 두는 것이 맞다.**
- 항목이 넷이라 한 줄에 안 들어간다 → **라벨을 위에 두고 아래 줄을 통째로 쓰는 `.wgs-c`** 배치 신설.
  실측 버튼 폭 62px 씩, 글자 잘림 없음.
- ⚠ **137차에 저장된 `font:'sys'` 는 '맑은고딕'으로 자동 이전**한다(`c.font==='sys' ? 'malgun' : 'app'`).
  저장되는 설정값을 바꿀 땐 마이그레이션을 함께 — 96차에서 같은 실수를 했다.
- 굵기 700 스냅은 기본 글꼴이 아닐 때 모두 적용(굴림·돋움도 400·700 만 존재).
- 실측: 네 글꼴 전환 시 `Pretendard Variable / Malgun Gothic / Gulim / Dotum` 정상 적용,
  굴림 + 글자 140% 에서도 가로·세로 넘침 0.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=128. widget/ 변경 없음.

### 139차 — 상태 2단계화 · 배지 위치 · 목록 정리 · 조직 팀 열
- **상태를 `진행`·`완료` 둘로 줄였다**(예정·보류 폐기, 옛 데이터는 진행으로 본다).
  ⚠ 저장값 숫자는 그대로 두고(`s1`·`s2` 색 규칙 재사용) **읽을 때 변환**한다 —
  `ST_LBL=['진행','진행','완료','진행']`, `stOf()` 는 2 아니면 1.
- **날짜가 지나면 자동으로 완료로 본다**(`stEff(it)`). 지난 뒤 손으로 '진행'을 고르면 `stKeep:true` 가 붙어 그대로 유지된다.
  ⚠ 이건 **표시 계산**이지 저장값이 아니다 — 화면·필터·집계가 모두 `stEff` 를 거치게 바꿨다(`stOf` 직접 사용 금지).
  규칙에 `stKeep` validate 추가 → **규칙 먼저 게시할 것.**
- **상태 배지를 1행 아이콘 왼쪽으로**(리마인드가 있으면 그 왼쪽). 업무 일정·업무 목록 카드 모두.
  배지 라운드를 알약 → **5px**(업무 막대와 같은 값).
- 업무 목록: **업무가 없는 분류는 머리·구분선까지 통째로 감춘다**. 펼쳤을 때 **내용이 비면 그 칸을 그리지 않는다**.
  팀·권역·담당자 패널의 직급 배지 10.5px 로 축소.
- 위젯 글꼴을 **`◀ 이름 ▶` 넘김**으로(4칸 나열 폐기). `WID_FONTS` 배열 하나로 목록·라벨·순환을 모두 처리.
- **조직/현장 관리에 '팀' 열 추가**(이름 다음, 직급 앞). 팀을 옮기면 **권역·담당 현장을 비우고** 안내를 띄운다 —
  다른 팀 항목이 남아 있으면 목록이 어긋난다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=131. widget/ 변경 없음.

### 140차 — 조직 표 정리 · 상태 아이콘 시안
- **업무 패널 머리의 건수 배지 삭제**(분류별 건수로 충분).
- 조직/현장 관리
  · 팀 열 폭 112 → **142px**(잘림 해소, 실측 126px 셀렉트가 다 들어감).
  · 팀 선택에 **'미배정'** 추가 — 팀에서 빼면 아래 '팀 미배정 계정' 카드로 내려간다.
  · ⚠ **담당 현장이 여백을 두고 '+1'** 이 뜨던 원인: 코드가 **무조건 3개로 잘라** 놓고 나머지를 세고 있었다.
    → 전부 그리고 넘칠 때만 CSS 가 접게 했다(`site-chk` 는 이미 `flex-wrap`).
  · 현장 배지: 파랑 → **회색**(`--fill`/`--sep`), 라운드 알약 → **5px**(업무 막대와 동일), 높이 26→24px.
  · **현장 탭 표(.mgtbl)의 머리를 계정 표(.utbl)와 같은 규격으로** 통일(크기·색·대문자·자간·여백·구분선).
- **진행 상태 아이콘화 시안 5안**(`status-mockup.html`) — 실제 카드 안에서 진행·완료를 나란히 비교.
  폭 실측: A 글자 배지 36px · B 아이콘만 22px · C 아이콘+글자 52px · D 체크박스 19px · E 점선원↔체크 22px.
  ⚠ 결정 전에는 코드에 손대지 않는다(114차 교훈).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=132. widget/ 변경 없음.

### 141차 — 진행 상태를 글자 배지 → 아이콘 (E안 채택)
시안 `status-fx-mockup.html` 에서 **E(튀어오름 + 체크 그리기)** 채택. 진행 원은 **테두리가 아니라 채워진 회색**.

- **`stIcon(st, attrs)`** 신설 — 진행: 채워진 회색 원 + **흰 삼각형** / 완료: 초록 원 + **흰 체크**.
  `attrs` 를 넘기면 누를 수 있고, 없으면 `disabled`(표시 전용, 내 업무 화면).
  쓰는 곳 5군데: 업무 일정 카드·폼, 업무 목록 카드·폼, 내 업무 목록. 옛 `.tk-st` 규칙은 전부 폐기.
- **누를 때 효과**: 아이콘이 살짝 눌렸다 커지고(`transform`), 완료 원이 차오른 뒤 체크가 그려진다(`stroke-dashoffset`).
  ⚠ **transform·opacity·선 길이만 움직인다** — 화면을 다시 계산하지 않아 카드가 많아도 느려지지 않는다.
  그림자·색 자체를 애니메이션하면 매 프레임 다시 그려야 하므로 피했다.
  `@media(prefers-reduced-motion:reduce)` 로 **윈도우 '애니메이션 효과 끄기'** 사용자에게는 자동으로 빠진다.
- 다시 그리지 않는 자리(폼)에서는 `stxSet(el, st)` 로 상태·효과만 갱신한다.
- ⚠ 카드 목록에서 토글하면 **완료가 아래로 정렬**돼 첫 카드가 다른 업무로 바뀐다 —
  검증할 때는 `data-pid` 로 그 카드를 다시 찾아야 한다(처음에 잘못 읽고 '동작 안 함'으로 오판했다).
- 읽기 카드 토글에도 `stKeep` 을 붙였다(날짜가 지난 뒤 손으로 '진행'을 고르면 유지).
- 검증 스크립트 5종의 `.tk-st` 참조를 `.stx`(`dataset.on`)으로 모두 옮겼다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=133. widget/ 변경 없음.

### 142차 — 상태 아이콘 크기·색 조정, 분류 건수 배지 제거
- 아이콘 22 → **20px**(내부 그림 20 → 17px). 수정·삭제 아이콘 옆에서 혼자 커 보이던 것.
- 색을 한 톤 옅게 — 진행 `--lbl3`(rgba(60,60,67,.58)) → **`#C2C6CE`**, 완료 `--gn` → **`#4CC26A`**.
  다크 테마용 값(`#4B4F58` / `#3BA85C`)도 함께 뒀다.
  ⚠ 진행 회색이 진하면 **완료보다 눈에 먼저 들어와** 목록에서 뭘 봐야 할지 헷갈린다.
- **분류 구분줄의 건수 배지(`.tk-sub .c`) 제거** — 140차에 지운 것은 패널 머리(`.tkm-c`)뿐이었다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=134. widget/ 변경 없음.

### 143차 — 상태 아이콘 마무리 · 조직 표 정리 · 담당자 색
- **진행 아이콘을 테두리 원으로**(채운 원 → `fill:none;stroke`), 색은 **옆 수정·삭제 아이콘과 같은 `--lbl3`**.
  삼각형은 채운 상태 유지. 완료(초록 원 + 흰 체크)는 그대로.
  수정 아이콘과의 간격 4 → **1px**(실측 3px 떨어짐 — X 와의 간격과 비슷해졌다).
- 조직/현장 관리
  · 담당 현장 **`+` 버튼을 배지와 같은 24×24 정사각형**, 라운드도 5px 로 통일(26px·8px 이었다).
  · 이름 열 164 → **180px**.
  · **공가세대·공가상가 토글이 실제 데이터를 보여 준다** — 게시본 가져오기에서 `vacantUnits`·`vacantCommercial` 을
    함께 받고, 표에서는 **색은 그대로 두되 누를 수 없게**(`.mg-ro`) 한다. 회색으로 죽이던 `.mg-dis` 대신 새 클래스.
    ⚠ 원본은 하자처리 현황이다 — 여기서 고치면 두 앱의 값이 어긋난다.
  · **팀 미배정 표에 '팀' 열 추가** — '이 팀에 추가' 버튼 대신 위 표와 같은 선택창으로 어느 팀이든 보낸다.
    미배정이 없으면 카드가 통째로 감춰진다(기존 동작 유지, 실측 확인).
- ⚠ **담당자 색이 프로필 색과 달랐다** — `ownColor()` 가 명단 순서로만 색을 뽑아, 프로필을 노랑으로 해도
  업무는 보라로 떴다. → **프로필에 고른 색(`avColor`)이 있으면 그 색을 쓴다**(없을 때만 순서 색).
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=135. widget/ 변경 없음.

### 144차 — 자동 실행이 재부팅 뒤 안 뜨던 원인
증상: 트레이의 '윈도우 시작 시 자동 실행'이 **체크돼 있는데도** 재부팅하면 위젯이 안 뜬다.

- ⚠ **원인: 포터블 exe 는 실행할 때 `%TEMP%` 에 풀려서 돌아간다.** `process.execPath` 는 그 **임시 경로**이고,
  자동 실행에 그 경로를 등록해 두면 재부팅 뒤 폴더가 없어 아무것도 실행되지 않는다.
  체크 표시는 남아 있어(레지스트리 항목 자체는 존재) 정상처럼 보인다 — 그래서 알아채기 어려웠다.
- → electron-builder 가 원본 exe 경로를 **`PORTABLE_EXECUTABLE_FILE`** 환경변수로 넘겨 준다.
  `exePath()` 를 만들어 등록·조회 양쪽에서 쓴다(`getLoginItemSettings` 도 같은 `path` 로 조회해야 정확하다).
- 등록한 경로를 `state.autoPath` 에 남기고, **켜져 있는데 경로가 달라졌으면 실행할 때 다시 등록**한다
  (exe 를 옮기거나 이름을 바꾼 경우).
- 트레이에 **'자동 실행 상태 확인'** 추가 — 등록 여부와 등록된 exe 경로를 보여 주고 복사할 수 있다.
- ⚠ **widget/ 변경 → exe 재배포 필요.** 이 수정은 새 exe 부터 적용되고,
  **새 exe 를 한 번 실행해야** 옛 임시 경로가 지금 경로로 교체된다.
- app.js 변경 없음(?v=135 유지).

### 145차 — 문서 폴더 자리 잡기 · 위젯 자동 업데이트 · 완료 취소선 제거
- **위젯이 처음 실행될 때 `문서\H 주요업무현황\HPlanWidget.exe` 로 자기를 복사**하고, 자동 실행도 그 자리로 등록한다
  (`settleHome()`). 다운로드 폴더에 두고 지워도 다음 부팅부터는 이 자리에서 뜬다.
  ⚠ `npm start` 개발 중에는 복사하지 않는다(경로가 `.exe` 가 아니면 건너뛴다).
- **자동 업데이트**
  · ⚠ **electron-updater 는 portable 대상을 지원하지 않는다.** GitHub API 도 쓰지 않았다(사내망 차단 우려).
    대신 **이미 열려 있는 앱 페이지**에 훅(`window.widInfo()`)을 두고 **관리자가 설정에 적은 최신 버전**을 읽는다.
  · 앱 설정에 **'위젯 최신 버전'**(`cfg.widgetVer`) 입력칸 추가. 규칙에 validate 추가 → **규칙 먼저 게시.**
  · 위젯은 시작 20초 뒤 한 번, 이후 6시간마다 확인 → 새 버전이면 `HPlanWidget.new.exe` 로 **미리 받아만 둔다.**
  · ⚠ **실행 중인 exe 는 스스로 덮어쓸 수 없다** → 트레이가 '지금 업데이트 (v…)'로 바뀌고, 누르면
    `cmd /c ping(대기) & move /y & start` 로 **꺼진 뒤 바꿔치고 다시 실행**한다. 몰래 바꾸지 않는다.
  · 릴리스 주소는 한 번 리디렉션되므로 `download()` 가 `Location` 을 따라간다.
- **완료 업무의 취소선 제거**(달력 막대·업무 일정 카드·업무 목록). 완료는 아이콘과 흐린 글자색으로만 표시.
  달력 막대는 `opacity:.72` 로 대체.
- ⚠ **widget/ 변경 → exe 재배포 필요.** 이번 exe 부터 자동 업데이트가 동작하므로 **이 배포가 마지막 수동 배포**다.
- 회귀 24/24 · 33/33 · 49/49 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=136.

### 146차 — 부팅 시 자동 업데이트(PC 카카오톡 방식)
- **받아 둔 새 버전이 있으면 켜질 때 스스로 갈아탄다** — `takeUpdateOnBoot()` 를 `whenReady` 맨 앞,
  **창을 만들기 전에** 부른다(창을 만든 뒤 하면 화면이 한 번 떴다 사라져 깜빡인다).
  갈아탈 게 있으면 `createWindow()` 를 건너뛰고 그대로 종료·재실행한다.
- 받아 둔 버전은 `state.pendingVer` 에 적어 둔다. 부팅 때 **그 값이 지금 버전보다 높을 때만** 갈아탄다.
  ⚠ **낮거나 같으면 받아 둔 파일을 지우고 값을 비운다** — 관리자가 버전을 잘못 적었을 때
  매 부팅마다 교체를 반복하는 무한 고리를 막는다.
- 교체 절차를 `swapAndRestart(src)` 로 합쳤다(부팅 자동 / 트레이 수동 둘 다 같은 길).
- 트레이는 그대로 **'업데이트 확인' ↔ '지금 업데이트 (v…)'** 로 바뀌고, 그 아래 **현재 버전**을 비활성 항목으로 보여 준다.
- 알림 문구를 '다음에 컴퓨터를 켤 때 저절로 적용됩니다'로 바꿨다(수동으로 눌러야 하는 것처럼 읽히던 것).
- ⚠ **widget/ 변경 → exe 재배포 필요.** app.js 변경 없음(?v=136 유지).

### 147차 — 업데이트 구조 재검토(실사용에서 터질 것들)
146차 구조를 다시 훑어 **실제로 사고가 날 지점 다섯 개**를 고쳤다.

- ⚠ **한글 폴더 이름이 `cmd` 에서 깨질 수 있다** — `문서\H 주요업무현황` 을 `cmd /c move` 로 옮기고 있었다.
  → **cmd 를 버리고 Node 로 옮긴다.** 윈도우는 **실행 중인 exe 를 지울 수는 없어도 이름은 바꿀 수 있다** —
  현재 파일을 `.old` 로 밀어내고 새 파일을 제자리에 놓은 뒤 `app.relaunch({execPath})` 로 다시 띄운다.
  대기용 `ping` 도 필요 없어졌다.
- ⚠ **받다 만 파일로 바꿔치면 위젯이 아예 안 뜬다**(게다가 자동 실행 대상이라 복구가 번거롭다).
  → `.part` 로 받고 **크기 검증(20MB 초과) 후에야** 정식 이름을 준다. 교체 직전에도 한 번 더 확인한다.
- ⚠ **교체 성공 전에 `pendingVer` 를 비우고 있었다** — 실패하면 그 버전을 영영 잊는다.
  → **성공했을 때만** 비운다. 실패하면 원래 파일을 되돌리고 지금 버전으로 뜬 뒤 다음 기회에 재시도.
- ⚠ **두 벌이 돌아다니는 문제** — 다운로드 폴더의 exe 를 계속 열면 업데이트는 문서 폴더 쪽에만 적용돼
  '업데이트했는데 그대로'가 된다. → **다른 자리에서 실행되면 문서 폴더 파일로 넘겨주고 종료**(`handOverToHome`).
  경로가 같으면 아무것도 하지 않아 고리에 빠지지 않는다.
- 수동 '지금 업데이트'는 **되묻는다**(작업 중일 수 있다). 실패하면 그 사실을 알린다.
- 시작할 때 `.old` 찌꺼기를 지운다.
- 교체 절차는 실제 파일로 재현해 검증(새 파일로 바뀜·받다 만 파일 거름 확인).
- ⚠ **widget/ 변경 → exe 재배포 필요.** app.js 변경 없음(?v=136 유지).

### 148차 — 회사 PC 에서 exe 를 못 만드는 문제 → 깃허브가 대신 만든다
사내망이 통신을 검사해 회사 PC 에서는 `npm install` 이 `UNABLE_TO_GET_ISSUER_CERT_LOCALLY` 로 막힌다.
집 PC 로 만들어 옮기는 방법이 있지만, **아예 로컬에서 만들 필요가 없게** 하는 편이 낫다.

- **`.github/workflows/widget.yml` 추가** — `windows-latest` 러너에서 `npm install` → `npm run dist` →
  **Releases 에 자동 업로드**. `workflow_dispatch` 를 넣어 **깃허브 웹의 Run workflow 버튼**으로 실행한다
  (git 명령·로컬 Node 불필요). `widget-v*` 태그를 밀어도 돈다.
- 버전은 `widget/package.json` 에서 읽어 릴리스 태그 `widget-v<버전>` 으로 쓴다 — 웹에서 그 숫자만 고치면 된다.
- `permissions: contents: write` 가 있어야 릴리스에 파일을 올릴 수 있다.
- README 를 '방법 1 깃허브 빌드(권장) / 방법 2 내 PC' 로 나누고,
  `UNABLE_TO_GET_ISSUER_CERT_LOCALLY` 대처(사내 인증서 지정 · `--use-system-ca` · 임시 `strict-ssl false`)도 적었다.
- ⚠ 파일이 하나 늘었다(워크플로). 저장소 파일 수를 줄여 온 방침과 어긋나지만,
  **회사 PC 에서 배포가 아예 막히는 문제를 푸는 값이 더 크다**고 판단했다.

### 149차 — 워크플로 경고 정리
깃허브 빌드가 성공했는데 노란 경고가 하나 떴다: *Node.js 20 is deprecated…*

- **실패가 아니라 안내**다. 우리가 쓴 남의 액션들이 예전 실행 환경을 지정해 둔 것뿐이고,
  결과물(exe)과 릴리스 업로드에는 영향이 없다. 판단 기준은 **작업 옆의 초록 체크와 Releases 의 파일**이다.
- 그래도 눈에 거슬리니 `actions/checkout@v4→v5`, `actions/setup-node@v4→v5` 로 올리고
  빌드용 Node 도 20 → **22(LTS)** 로 맞췄다. `softprops/action-gh-release@v2` 는 그대로 둔다(경고 대상 아님).
- ⚠ 워크플로 파일만 바뀌었다 — **웹 파일·exe 재배포와 무관**하다.

### 150차 — 위치·크기가 저장되지 않던 원인, 부팅 시작 정리
- ⚠ **설정(state)을 창 만들 때야 읽고 있었다.** `let state = {}` 로 두고 `createWindow()` 안에서 `loadState()` 했는데,
  그 전에 도는 **부팅 자동 업데이트(`takeUpdateOnBoot`)와 자동 실행 경로 확인이 빈 설정을 보고 있었다.**
  → **모듈 맨 위에서 한 번 읽는다.** 이 한 줄 때문에 **145~147차의 부팅 자동 업데이트가 실제로는 한 번도 동작하지 않았다.**
- ⚠ **위치·크기가 저장되지 않던 원인**: `moved`·`resized` 만 듣고 있었다.
  조정 모드의 **드래그 레이어(`-webkit-app-region:drag`)로 옮길 때는 이 이벤트가 오지 않는 경우가 있다.**
  → 끄는 동안 계속 나오는 **`move`·`resize` 를 함께 듣고 400ms 잠잠해지면 저장**한다.
  더해서 **조정을 끝내는 순간**(`setLocked(true)`), **창을 닫기 직전**(`close`), **프로그램 종료 전**(`before-quit`)에도
  현재 자리를 확정 저장한다.
- 부팅 직후는 다른 프로그램과 겹치므로 **업데이트 확인을 20초 → 90초**로 미뤘다.
  ⚠ 위젯이 뜨는 속도 자체는 대부분 **윈도우가 시작 프로그램을 늦게 띄우는 것 + 웹 주소를 받아오는 시간**이라
  프로그램 쪽에서 더 줄일 여지는 크지 않다. (윈도우 설정 > 앱 > 시작 프로그램에서 '시작 영향'을 확인할 수 있다.)
- 저장·복원 흐름을 파일로 재현해 검증(자리·모드·자동 실행 값 복원, 창 만들기 전 `pendingVer` 조회).
- ⚠ **widget/ 변경 → exe 재배포 필요.** app.js 변경 없음.

### 151차 — 삭제 반영이 늦던 원인 · 폼에서 상태 아이콘 제거
- ⚠ **삭제가 한 박자 늦게 보이던 원인: 순서였다.** `store.delPlan(...)` 을 먼저 부르고 그다음 `S.planEdit=null` 이라,
  저장소가 비워진 상태에서 다시 그릴 때 **이미 없는 업무의 편집 폼이 화면에 남아 있었다.**
  → **폼부터 닫고**(대기 중인 자동 저장도 취소) 지운 뒤 그린다.
  더해서 **라이브 모드에서도 `rWidget()` 을 부른다** — 예전에는 `if(!S.live)` 안에만 있어 위젯 팝업이 갱신되지 않았다.
  실측: 삭제 확인 후 250ms 에 폼·카드·데이터가 모두 사라짐.
- 업무 목록 삭제(`tk.del`)도 같은 순서로 맞췄다.
- **수정 폼에서 상태 아이콘을 뺐다** — 카드에서 누르면 되고, 폼 도구줄은 `[삭제][알림][저장하고 닫기]` 로 단순해진다.
  ⚠ 업무 목록 폼의 `#tnSt` 는 **저장할 값을 담는 자리로 숨겨 두었다**(지우면 저장 때 상태가 날아간다).
- 검증 스크립트 4종의 폼 배지 관련 검사를 새 구조에 맞게 옮겼다(폼 배지 순환 검사는 카드 토글 검사로 대체).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0. app.js?v=137. widget/ 변경 없음.

### 152차 — 위젯 설정 정리
- **값(%)을 슬라이더 쪽으로 붙였다** — 라벨 칸(112px)을 `justify-content:space-between` 으로 만들어
  이름은 왼쪽, 값은 **오른쪽 끝**에 서게 했다(슬라이더와 10px 간격).
- **글꼴 넘김 버튼의 배경 제거** — 값과 화살표만 남는다.
- **모드(다크/라이트)도 글꼴과 같은 `◀ 값 ▶` 형식**으로 통일(세그먼트 버튼 폐기, 배경 없음).
  둘뿐이라 어느 쪽 화살표를 눌러도 번갈아 바뀐다.
- **'위치·크기 조정'을 아래쪽 전체폭 버튼으로** 내렸다 — 구분선 아래 `[위치·크기 조정]` · `[브라우저 앱 열기]`
  두 줄이 문구 자체가 버튼인 형태로 나란히 선다.
- 결과: 설정은 **슬라이더 2줄 + 넘김 2줄 + 아래 버튼 2개**로 정리됐다(높이 256px 유지).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 대비 경고 0. app.js?v=138. widget/ 변경 없음.

### 153차 — 자리·크기가 끝내 복원되지 않던 진짜 원인
- ⚠ **`STATE_FILE` 을 모듈 로드 시점에 굳히고 있었다.** `app.getPath('userData')` 는 **준비(`whenReady`) 전에는
  다른 폴더를 가리킬 수 있다** — 그러면 **저장은 A 폴더, 읽기는 B 폴더**가 되어 '저장이 안 되는' 것처럼 보인다.
  150차에 `loadState()` 를 모듈 맨 위로 올리면서 이 함정을 정통으로 밟았다.
  → **경로를 함수(`stateFile()`)로 바꿔 필요할 때 묻고**, `state` 는 **`whenReady` 맨 앞**에서 읽는다.
  경로가 갈릴 때 어떤 일이 생기는지 파일로 재현해 확인했다(예전 방식 복원 실패 → 새 방식 복원 성공).
- 창이 뜬 직후 **저장해 둔 자리로 한 번 더 맞춘다**(`ready-to-show`) — 윈도우가 화면·배율에 맞춰
  초기 좌표를 손보는 경우가 있다. `blur` 에서도 자리를 남긴다.
- **진단 강화** — 트레이 '자동 실행 상태 확인'에 **설정 파일 경로 · 저장된 자리·크기 · 현재 버전**을 함께 보여 준다.
  다음에 같은 증상이 나오면 이 창만 보면 어디가 끊겼는지 바로 안다.
- **부팅 속도**: 화면 부르기(`loadURL`)를 **가장 먼저** 하고, 파일 찌꺼기 정리·자동 실행 재등록은 **3초 뒤로** 미뤘다.
  ⚠ 남은 지연은 대부분 **윈도우가 시작 프로그램을 늦게 띄우는 것**과 **웹 주소를 받아오는 시간**이라
  프로그램에서 더 줄일 여지는 크지 않다(두 번째 부팅부터는 브라우저 캐시가 있어 빨라진다).
- **버전 규칙**: 앞으로 `widget/` 이 바뀌면 `widget/package.json` 의 `version` 을 **내가 함께 올려서 준다.**
  이번 회차는 **1.0.3**. (릴리스 태그·앱 설정의 '위젯 최신 버전'도 같은 값으로 맞추면 된다.)
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0 · 대비 경고 0.
- ⚠ **widget/ 변경 → exe 재배포 필요(v1.0.3).**

### 154차 — 자리·크기 문제: 추측 중단, 기록으로 전환
150·153차에 원인을 두 번 짚었는데 증상이 그대로다. **추측을 멈추고 증거를 남긴다.**

- **진단 기록 파일 추가** — `문서\H 주요업무현황\widget-log.txt`. 트레이 **'진단 폴더 열기'** 로 바로 간다.
  적는 것: **시작**(설정 파일 경로 · 읽어 온 자리 · 실행 파일 · 버전),
  **창 생성**(요청한 자리 vs 실제 자리 — 윈도우가 손댔는지 여기서 드러난다),
  **표시 후 재적용 결과**, **자리 저장**(무엇이 계기였는지 `move/resized/blur/close/quit/조정 종료/주기`),
  **저장 실패 사유**, **종료**. 최근 200줄만 남긴다.
  이 파일 한 장이면 **저장이 안 되는 것인지, 저장은 되는데 복원이 안 되는 것인지, 복원 뒤 윈도우가 바꾸는 것인지**가 갈린다.
- **이중 안전장치 — 5초마다 실제 자리를 확인해 바뀌었을 때만 저장한다.**
  ⚠ `-webkit-app-region:drag` 로 창을 끌면 `move`·`moved` 가 오지 않는 경우가 있다(이번 증상의 유력한 후보).
  이벤트에 기대지 않고 주기적으로 확인하면 그 경우에도 남는다. 값이 그대로면 파일을 쓰지 않으므로 부담이 없다.
- 저장은 **바뀐 값일 때만** 기록한다(같은 값이면 건너뜀 — 로그와 디스크 쓰기 둘 다 절약).
- ⚠ **확인 부탁 사항**: 지금 쓰는 exe 가 **1.0.2** 라면 150·153차 수정이 들어 있지 않다.
  이번 **1.0.4** 로 다시 만들어 배포한 뒤, ①위치를 옮기고 ②재부팅 ③`widget-log.txt` 를 보내 주면
  어느 단계에서 끊기는지 바로 특정된다.
- 기록 회전(200줄 유지)과 중복 저장 방지를 실제 파일로 재현해 검증.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15.
- ⚠ **widget/ 변경 → exe 재배포 필요(v1.0.4).**

### 155차 — 버전이 바뀌면 자동 빌드 · 설정에 위젯 버전 표시
- **워크플로 자동 실행 조건 추가** — `push` 에 `paths: ['widget/package.json']`.
  버전이 담긴 파일이 올라오면 **버튼을 누르지 않아도** 빌드가 돈다(Run workflow 버튼도 그대로 남긴다).
- ⚠ **같은 버전으로 두 번 도는 것을 막았다** — `gh release view widget-v<버전>` 으로 이미 있으면
  이후 단계를 전부 건너뛴다. 버전을 올리지 않고 위젯 파일만 고쳤을 때 헛돌거나
  **기존 릴리스를 덮어써 팀원이 업데이트를 감지하지 못하는 상황**을 막는다.
- **설정 > 버전 · 오류 기록 밑에 위젯 최신 버전을 함께 보여 준다**(`#widgetInfo`).
  값이 없으면 '아직 지정되지 않았습니다'로 알려 준다. 앱 버전(`APP_VER`)도 **위젯과 같은 1.0.4** 로 맞췄다.
  ⚠ 이 값은 두 곳(`rCfg`·설정 화면 렌더)에서 함께 갱신한다 — 한쪽만 두면 화면을 옮겨 다닐 때 옛 값이 남는다.
- **앞으로의 규칙**: `widget/` 이 바뀐 회차마다 내가 `widget/package.json` 과 `APP_VER` 을 함께 올려서 준다.
  올리기만 하면 깃허브가 exe 를 만들고 릴리스에 올린다. 남는 일은 **앱 설정에 그 버전을 적는 것 하나**다.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=139.

### 156차 — 최신 버전도 자동으로 (설정에 적을 필요 없음)
- ⚠ 145차에 **깃허브 API 를 피하려고** 관리자가 설정에 버전을 적게 했는데, 그 손질이 남아 있었다.
  **API 없이도 알아낼 방법이 있다** — 깃허브의 `…/releases/latest/download/<파일>` 은 늘 최신 릴리스로 넘겨주고,
  **넘어가는 주소 안에 태그(`widget-v1.0.5`)가 들어 있다.**
  → `latestUrlOf()` 로 그 주소를 만들고, `resolveLatest()` 가 **넘어가는 주소만 받아 버전을 뽑는다**(파일은 받지 않는다).
  이미 열려 있는 릴리스 도메인만 쓰므로 사내망 문제도 없다.
- 설정의 '위젯 최신 버전'은 **비워 두는 것이 기본**이 됐다(자동 확인). 특정 버전으로 묶고 싶을 때만 적는다.
  `widInfo()` 는 이제 **주소만 있으면** 값을 돌려준다(버전은 빈 문자열 가능).
- 자동 확인이 실패하면(주소 형태가 다르거나 응답이 없으면) **설정에 적힌 값으로 물러난다** — 기록에도 남긴다.
- 주소 변환·버전 추출 규칙을 따로 검증(릴리스 주소 → latest 주소, `widget-v1.0.5` → `1.0.5`).
- 버전 **1.0.5**(위젯·앱 함께). 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0.
- ⚠ **widget/ 변경 → 파일만 올리면 자동 빌드된다(v1.0.5).**

### 157차 — 자동 빌드가 안 돌던 이유
- ⚠ **`on.push` 에 `tags` 를 함께 적으면 '태그를 밀 때만' 도는 것이 된다** — 브랜치에 파일을 올리는 것은
  대상에서 빠진다. `paths` 를 넣어도 소용없다. 155차에 `paths` 와 `tags` 를 같이 둔 것이 원인이었다.
  → **`branches: ['**'] + paths`** 로 바꿨다(태그 방식은 버렸다 — 웹에서 파일만 올리는 흐름에는 태그가 필요 없다).
  `branches` 를 `['**']` 로 둔 것은 기본 브랜치 이름(main/master)을 가리지 않기 위해서다.
- ⚠ **이번 한 번은 버튼으로 돌려야 한다** — `widget/package.json` 은 이미 1.0.5 로 올라가 있어
  이 워크플로만 고쳐 올려도 '바뀐 파일' 조건에 걸리지 않는다. Actions > Run workflow 로 한 번 실행하면 된다.
  다음 회차부터는 버전 파일이 함께 바뀌므로 저절로 돈다.
- 주석의 3번 단계도 '설정에 버전 적기' → '끝'으로 고쳤다(156차에 자동 확인이 되었다).

### 158차 — 손으로 적고 누르던 것 두 개 제거
- **'위젯 최신 버전' 입력칸 삭제**(156차에 자동 확인이 되면서 쓸모가 없어졌다).
  `cfg.widgetVer` 관련 저장·표시·규칙을 모두 걷어냈다. `widInfo()` 는 **주소만** 돌려준다.
  '버전 · 오류 기록' 밑 문구도 **'위젯은 켜질 때 최신 버전을 스스로 확인해 갱신합니다'** 로 바꿨다.
- **조직·현장을 로그인할 때 자동으로 맞춘다.** 사용자 지적대로 두 앱이 같은 파이어베이스를 보므로
  사람이 '불러오기'를 누를 이유가 없다. `org.import` 본문을 **`orgPull(auto)`** 로 빼고,
  로그인 4초 뒤 **관리자 계정에서만** 조용히 한 번 돈다(쓰기 권한이 있어야 반영되므로 관리자면 충분하다).
  ⚠ **자동 모드는 게시월이 바뀌었을 때만 적용한다** — `cfg.orgSrc` 에 반영한 게시월을 남겨 두고 비교한다.
  같은 게시월이면 아무것도 하지 않아 매번 목록을 갈아엎지 않는다.
  ⚠ 자동 모드에서는 확인창·안내 토스트를 띄우지 않는다(`fail` 을 빈 함수로 바꿔치기).
- 설정 카드 이름을 **'하자처리 현황과 맞추기'**, 버튼을 **'지금 맞추기'** 로 바꿨다.
  버튼은 남겨 뒀다 — **게시본을 새로 올린 직후 기다리지 않고 반영**하고 싶을 때가 있다.
  ⚠ 완전히 없애지 않은 이유: 게시월이 같은데 내용만 바뀐 경우는 자동으로는 안 잡힌다.
- 규칙에 `cfg.orgSrc` validate 추가(`widgetVer` 자리 대체) → **규칙 먼저 게시.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=141. widget/ 변경 없음(v1.0.5 유지).

### 159차 — 팀·권역·현장을 하자처리 현황에서 **실시간으로** 읽는다
사용자 지적: 복사해 두지 말고 **같은 경로를 그대로 읽으면 된다.** 맞는 말이라 구조를 바꿨다.

- **`report/<게시월>/_dash` 를 직접 구독한다**(`bindReportOrg`). 저쪽에서 현장이 바뀌면 **곧바로 따라간다.**
  ⚠ 게시본은 **월별로 나뉘어** 있어 `reportIndex` 로 최신 월을 찾고, 그 달이 바뀌면 **구독을 옮긴다**(`ORG_RM`·`ORG_OFF`).
  인덱스를 못 읽으면 최근 24개월을 훑어 한 번만 붙는다.
- 변환은 `orgFromDash(teams, sites)` 하나로 모았다(가져오기와 같은 규칙 — 권역은 **이름을 그대로 id 로**).
- ⚠ **`calapp/org` 는 지우지 않고 '사본'으로 남긴다.** 게시본을 읽을 권한이 없는 계정·오프라인을 위해서다.
  원본을 읽고 있으면(`ORG_LIVE`) 사본 구독은 무시하고, **관리자가 접속해 있을 때 사본을 갱신**해 둔다.
  이 이중 구조가 없으면 권한이 없는 팀원에게 현장이 통째로 사라진다.
- ⚠ **원본을 읽는 동안에는 이 앱에서 조직·현장을 고칠 수 없다**(`orgSave` 가 막고 안내한다) —
  고쳐도 다음 갱신에 덮어써지므로 조용히 실패하는 것보다 낫다.
- 158차의 '로그인 4초 뒤 자동 맞추기'는 필요 없어져 제거. 설정 카드는 **'하자처리 현황 연동'** 으로 바꾸고
  지금 어느 게시본을 읽고 있는지 보여 준다. '다시 읽기' 버튼은 남겼지만 연동 중이면 안내만 한다.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=142. widget/ 변경 없음.

### 160차 — 자리·크기가 매번 줄어들던 진짜 원인 (로그로 확정)
사용자가 보내 준 `widget-log.txt` 한 장으로 끝났다. **추측 세 번보다 기록 한 번이 빨랐다.**

```
창 생성 · 요청={…858×653} · 실제={…687×524}     ← 0.801배
창 생성 · 요청={…1073×1179} · 실제={…859×653}   ← 0.801배
자리 저장(blur) {…687×524}                       ← 줄어든 값이 저장됨
표시 후 재적용 · 결과={…687×524}                 ← 이미 오염돼 의미 없음
```

- ⚠ **원인 ①: 배율이 다른 모니터.** 창 좌표가 `x=-2997`(보조 모니터)이고 그 모니터가 **125%** 배율이라,
  만들 때 준 크기가 **한 번 더 나뉘어 0.8배(=1/1.25)** 로 작아진다. **초기화가 아니라 매번 0.8배씩 축소**였다.
  858→687, 1073→859 — 비율이 정확히 0.801로 일치한다.
- ⚠ **원인 ②: 되돌리기 전에 저장이 끼어들었다.** 창이 뜨자마자 `blur` 저장이 **줄어든 값을 저장해 버려서**,
  뒤이은 '표시 후 재적용'이 **이미 오염된 값**으로 되돌리고 있었다. 그래서 재적용이 아무 일도 하지 않았다.
- **수정**: ①읽어 온 값을 `want` 로 **따로 들고** 있다가 창이 뜬 뒤 그 값으로 되돌린다(어긋나면 120ms 간격 3회 재시도).
  ②`restored` 가 될 때까지 **어떤 저장도 하지 않는다.** 5초가 지나면 안전하게 풀어 준다.
  되돌림 결과를 로그에 남겨(원함 vs 결과) 다음에 또 어긋나면 바로 보인다.
- 축소가 반복되는 상황과 수정 후 값이 유지되는 것을 **모의로 재현해 검증**했다.
- ⚠ **widget/ 변경 → v1.0.6.** 로그의 실행 파일이 한때 `HPlanWidget.new.exe` 였는데,
  이는 받아 둔 새 파일을 직접 실행한 흔적이다 — **문서 폴더의 `HPlanWidget.exe` 를 쓰면 된다**(자동 교체가 처리한다).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15. app.js?v=143(APP_VER 1.0.6).

### 161차 — 수동 업데이트가 '바꾸지 못했습니다'로 끝나던 이유
- ⚠ **윈도우가 실행 중인 exe 의 이름 바꾸기를 허용하는 건 맞지만, 포터블 실행기가 파일을 붙들고 있으면 막힌다**
  (EPERM/EBUSY). 147차에 `cmd` 를 버리고 Node 로만 바꾼 것이 이 경우를 놓쳤다.
- **두 갈래로 만들었다.**
  · ① 빠른 길 — 지금 그 자리에서 이름 바꾸기(되면 바로 다시 실행).
  · ② 우회로 — 막히면 **우리가 꺼진 뒤에 대신 바꿔 줄 도우미**를 띄우고 종료한다.
    도우미는 우리 PID 가 사라질 때까지 기다렸다가 파일을 옮기고 다시 실행한다.
  ⚠ 도우미는 **PowerShell `-EncodedCommand`** 로 띄운다 — `cmd` 는 한글 경로가 명령창 인코딩에서 깨질 수 있는데,
  이 방식은 UTF-16 으로 넘기므로 `문서\H 주요업무현황` 이 안전하다(왕복 보존을 따로 검증).
- 실패 사유를 `widget-log.txt` 에 남기고, 안내창에도 **어디를 보면 되는지** 적었다.
- ⚠ 사용자는 안 되니까 받아 둔 `HPlanWidget.new.exe` 를 **직접 실행**했다 — 그래서 로그의 실행 파일이 그것이었다.
  이제 그럴 필요가 없다.
- **v1.0.7**. 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15. app.js?v=144.

### 162차 — 백업 (162차 진단의 1순위)
- **관리자 PC 의 폴더 하나에 주 1회 `hplan_YYMMDD.json`.** 설정 > 백업 카드(관리자에게만 보인다).
- ⚠ **브라우저는 아무 폴더에나 못 쓴다** — 사용자가 고른 폴더에만 쓸 수 있고(File System Access),
  그 권한도 브라우저를 껐다 켜면 다시 물을 수 있다. → **폴더가 없거나 권한이 풀리면 내려받기로 물러난다.**
  조용한 자동 백업에서는 권한을 다시 묻지 않는다(팝업이 갑자기 뜨면 곤란하다).
- 고른 폴더 손잡이는 **IndexedDB 에만 담을 수 있다**(문자열로 못 바꾼다) → `bkIdb()` 로 저장·복원.
- 담는 것: `org` · `people` · `tasks` · `cfg` + 버전·시각. 되돌릴 때는 **people·tasks 만** 되돌린다 —
  ⚠ 팀·권역·현장은 159차부터 하자처리 현황이 원본이라 되돌려도 곧 덮어써진다.
- 마지막 백업이 **7일 넘으면** 로그인 6초 뒤 관리자에게 한 번 알린다(`bkCheck`).
- 실측: 실제로 내려받아 `hplan_260804.json` 생성 확인, 백업 → 삭제 → 되돌리기로 **업무 수 7 → 6 → 7 복원** 확인.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=145. widget/ 변경 없음.

### 163차 — 백업을 자동으로 (위젯이 대신 쓴다)
사용자 지적: **"이 정도 왔으면 자동화를 염두에 두고 만들어야 한다."** 맞는 말이라 사람이 누르던 것을 없앴다.

- ⚠ **브라우저는 아무 폴더에나 못 쓴다** — 그래서 162차에는 폴더를 고르게 했다.
  **위젯(Electron)에는 그 제약이 없다** → **백업 주체를 위젯으로 옮겼다.**
  앱은 창구(`window.bkExport()`)만 열어 두고, 위젯이 받아서
  `문서\H 주요업무현황\backup\hplan_YYMMDD.json` 으로 저장한다. 끝나면 `window.bkNote()` 로 앱에 알린다.
- 주기: 시작 2분 뒤 한 번, 이후 6시간마다 확인하고 **마지막 저장이 7일 넘었을 때만** 쓴다.
  최근 **12개**만 남기고 오래된 것부터 지운다(`hplan_` 패턴만 건드리므로 다른 파일은 안전 — 실측 확인).
- ⚠ **관리자 계정이고 `S.tasks` 가 비어 있지 않을 때만** 내준다 — 로그인 전이나 자료 수신 전에 **빈 백업으로 덮어쓰는 사고**를 막는다.
- 트레이에 **'지금 백업하기'** 추가(결과를 창으로 알린다). 앱의 폴더 지정·IndexedDB 손잡이 코드는 **전부 제거**(위젯이 하므로 불필요).
  위젯을 쓰지 않는 관리자를 위해 '내보내기'(내려받기)와 '되돌리기'는 남겼다.
- **v1.0.8**. 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 15/15 · 넘침 0. app.js?v=146.

### 164차 — 위젯을 '제품'으로 다루기 시작
사용자 정리: **팀원은 브라우저를 열지 않는다. 위젯이 곧 제품이다.** 그 전제로 우선순위를 다시 잡았다.

- **헤더에 알림·내 업무 버튼 추가**(필터 오른쪽). 새로고침은 **설정 안 전체폭 버튼**으로 내렸다.
  ⚠ 헤더는 좁다 — 자주 안 쓰는 것은 설정으로, 매일 쓰는 것만 헤더에 둔다.
- **알림 팝오버** — 받은 부름(멘션)을 목록으로. 누르면 그 업무가 있는 날로 이동하고 **읽음 처리**된다.
  버튼에 **빨간 점**을 달아 화면을 안 봐도 알 수 있게 했다(`rMention` 이 함께 갱신).
  ⚠ 지금까지 멘션은 **사이드바 배지로만** 표시됐다 — 위젯에는 사이드바가 없으니 **없는 기능이나 마찬가지였다.**
- **내 업무 팝오버** — 이번 주 내 일정 + 미완료 업무. 누르면 그 날짜로 이동하며 그 업무가 펼쳐진다.
  기한 없는 업무는 달력에 없으므로 그렇게 안내한다.
- 팝오버는 **바깥 클릭(mousedown 캡처)·Esc·같은 버튼 다시 누르기**로 닫힌다(설정·필터와 같은 규칙).
- **연결이 끊겨도 버틴다** — `did-fail-load` 에서 **10초 간격 20회(약 3분) 스스로 재접속**한다.
  ⚠ 예전에는 오류 문구만 남기고 끝이라, 잠깐 끊기면 사람이 새로고침해야 했다.
- **자기 복구** — 켤 때 `HPlanWidget.exe` 가 없으면 `.new.exe` → `.old` 순으로 되살린다(크기 검증 통과분만).
  교체가 절반만 끝나 죽으면 실행 파일이 사라지고 **자동 실행까지 끊기는** 상황을 막는다. 세 경우를 재현해 검증.
- **`읽어주세요.txt`** 를 폴더에 만들어 둔다 — 위젯이 안 뜰 때 팀원이 스스로 할 일과 **브라우저 주소**를 적어 둔다.
  ⚠ 메모장에서 한글이 깨지지 않게 BOM 을 붙인다.
- **위젯 검증 15 → 21항목**(헤더 구성·알림·내 업무·닫힘 규칙·설정 버튼·백업 창구).
  ⚠ 실사용 비중과 반대로 브라우저 화면 검증이 훨씬 두꺼웠다 — 무게중심을 옮기는 중이다.
- **v1.0.9**. 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=148.

### 165차 — 설정 정리 · 상단바에 알림·사용 안내
- **상단바 오른쪽에 [알림][?] 두 버튼 추가**(`.tbr`). 알림은 받은 부름이 있으면 **빨간 점**,
  누르면 멘션 목록이 뜬다. 사용 안내는 설정에서 옮겨 왔다. ⚠ **위젯에서는 상단바가 없으므로 감춘다.**
- 설정에서 뺀 것: **다크 모드 토글**(사이드바에 이미 있다) · **사용 안내**(상단바로) ·
  **위젯 버전 문구** · **자동 백업 상태 문구** · **위젯 파일 주소 입력칸**.
- 카드 재편: `표시` 카드를 없애고 **`백업 · 기록`** 으로 합쳤다(내보내기 · 되돌리기 · 버전·오류 기록).
  `연결` 카드도 없애고 **하자처리 현황 바로가기를 `하자처리 현황 연동` 카드로** 옮겼다(표기도 붙여쓰기로 통일).
  결과: **메일 발송 / 백업 · 기록 / 하자처리 현황 연동 / 바탕화면 위젯** 네 장.
- ⚠ **위젯 파일 주소를 고정 상수(`WIDGET_URL`)로 바꿨다** — `…/releases/latest/download/HPlanWidget.exe` 는
  **늘 최신 릴리스를 가리키므로** 관리자가 적을 필요가 없다. 설정에 값이 남아 있으면 그것을 우선하되(비상용),
  입력칸은 없앴다. 내려받기 버튼과 위젯 업데이트 확인이 같은 주소를 쓴다.
- 위젯 내려받기 보조문구를 실제 크롬·엣지 흐름에 맞게 고쳤다:
  **다운로드 시 '유지' 선택 후 '삭제 옆 옵션'에서 '그래도 계속' 선택.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0. app.js?v=149. widget/ 변경 없음(v1.0.9).

### 166차 — 알림 팝오버 · 직급 2종 추가 · 설정 잔손질
- **알림을 모달 → 팝오버로.** 상단바 알림 버튼 아래에 붙는다(`#appAlertPop`). 위젯 팝오버와 **같은 줄 모양(`.ws-row`)** 을 쓴다.
  바깥 클릭·닫기 버튼으로 닫히고, 항목을 누르면 팝오버가 닫히며 그 업무로 간다.
  ⚠ 모달은 화면을 덮어 다른 일을 못 하게 만든다 — 알림처럼 훑고 지나가는 것에는 맞지 않는다.
- 하자처리 현황 연동의 **상태 문구 삭제**(무슨 게시본을 읽는지는 평소에 알 필요가 없다).
- **업무 목록 패널 머리 오른쪽에 '업무 추가'** 를 항상 둔다. 예전에는 담당자를 고른 상태에서만 나왔다.
- ⚠ **펼친 카드에 내용이 없어도 여백이 컸다** — 접힘용 아래 여백(12px)과 펼침 영역 여백이 **두 번 겹쳤다.**
  `.tk-item.open .plan-meta{padding-bottom:4px}` 로 겹치는 쪽을 줄였다(실측 4px). 빈 묶음은 아예 안 그린다.
- **직급에 '안전'·'원가' 추가** — `RANKS` 는 다섯이 됐다.
  · **권역·현장을 모두 비운다**(팀 전체를 보는 자리). `rankUses` 를 `member/lead` 기준으로 다시 썼다.
  · `coverSites` 도 담당자·공구장 외에는 **팀 전체 현장**을 보게 정리했다.
  · **정렬 순서를 `RANK_ORD` 하나로 모았다** — 공구장 · 담당자 · **안전 · 원가** · 팀장.
    ⚠ 예전에는 세 곳에서 `rank==='lead'?0:1` 를 각각 쓰고 있었다 — 직급이 늘 때마다 세 곳을 고쳐야 했다.
  · 직급 배지도 `rankLabel()` 로 바꿔 **직급이 늘어도 자동으로 표시**된다.
  · 규칙에 `safety`·`cost` 허용 추가 → **규칙 먼저 게시.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0. app.js?v=150. widget/ 변경 없음(v1.0.9).

### 167차 — 직급 차례를 하나로
- 직급 순서를 **팀장 · 공구장 · 담당자 · 안전 · 원가** 로 맞췄다.
- ⚠ **선택 목록(`RANKS`)과 정렬 순서(`RANK_ORD`)를 따로 두고 있었다** — 하나를 고치면 다른 하나를 잊는다.
  `RANK_ORD` 를 없애고 **`RANKS` 배열의 차례가 곧 정렬 순서**가 되게 했다(`rankOrd` 가 `findIndex`).
  이제 직급을 넣거나 순서를 바꿀 때 **고칠 곳이 한 줄뿐**이다.
- `rankOf` 의 기본값은 여전히 `member` 다(배열 첫 항목이 아니라 명시값) — 순서를 바꿔도 안전하다. 실측 확인.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21. app.js?v=151. widget/ 변경 없음.

### 168차 — 알림 팝오버 다듬기 · 직급 칸 비우기 · 업무 추가 자리
- **알림 팝오버를 다시 만들었다.** 폭 352px, 말풍선 꼬리, 제목 옆 **건수 배지**, 항목마다
  **아바타 · 이름 · 시각(오른쪽 끝) · 내용(두 줄까지) · 어느 업무인지**. 빈 상태는 아이콘 + 한 줄.
  ⚠ 이름과 문구만 있으면 **어디를 봐야 할지 알 수 없다** — 업무 제목을 함께 보여 준다.
  알림이 없으면 '모두 읽음' 버튼도 감춘다.
- ⚠ **쓰지 않는 칸에 '팀 전체'·'권역 전체 · N개 현장' 같은 안내를 넣고 있었다** — 값이 들어 있는 것처럼 보인다.
  → **그냥 비운다.** 팀장·안전·원가는 권역·현장 둘 다, 공구장은 현장만 빈 칸이다(공구장의 권역은 그대로).
  `autoSitesHTML()` 폐기. 내 프로필의 권역 칸도 '해당 없음 · <직급>'으로 바꿨다.
- ⚠ **팀 전체·권역을 보다가 '업무 추가'를 누르면 엉뚱한 곳에 생겼다** — 버튼에 박아 둔 값을 쓰고 있었다.
  → **지금 고른 자리**를 기준으로 정한다(담당자면 그 사람, 아니면 팀 공통).
  ⚠ 게다가 **폼은 그 자리가 화면에 보일 때만 그려진다** — 팀 전체·권역을 보고 있었다면 **선택도 팀 공통으로 옮긴다.**
  그러지 않으면 '눌렀는데 아무 일도 안 일어나는' 것처럼 보인다. 세 경우(팀 전체·권역·담당자) 모두 실측.
- **새 업무 폼에 취소 버튼**(X) 추가 — 저장하지 않고 닫는다. 예전에는 삭제 버튼이 기존 업무에만 있어 빠져나갈 길이 없었다.
  폼을 열면 제목 칸에 바로 커서가 간다.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=152. widget/ 변경 없음.

### 169차 — 위젯 헤더·팝오버 다듬기
- **알림·내 업무 버튼을 프로필·설정과 같은 알약 안으로** 넣었다. 헤더가 `[이동] [필터] [알림·내업무·프로필·설정]` 세 덩어리가 된다.
  ⚠ 같은 성격의 버튼이 배경 있는 것과 없는 것으로 섞여 있으면 **급조한 인상**을 준다.
- **팝오버의 X 버튼을 없앴다**(위젯 설정 · 알림 · 내 업무). 바깥 클릭 · Esc · 같은 버튼 다시 누르기로 닫힌다.
  ⚠ 업무 팝업의 X 는 **남긴다** — 그건 수정 폼까지 함께 닫는 역할이 있다.
- 설정 아래 버튼 세 개를 같은 규격으로 맞추고 **위에 구분선 한 줄**을 뒀다.
  ⚠ `line-height` 로 높이를 맞추면 글자가 잘린다(대비 검사가 잡아냈다) → `flex` + `min-height` 로.
- **옆 팝오버 목록을 다시 짰다.** 한 줄 = `[날짜 알약][제목 + 부제]` 로 **알림과 내 업무가 같은 뼈대**를 쓴다.
  · 날짜 알약은 오늘이면 파랑, 기한이 지났으면 빨강.
  · 내 업무: 묶음 제목에 건수, 부제에 **구분·시각** 또는 **현장·구분**. 헤더에 전체 건수.
  · 알림: 아바타 · 보낸이 · 시각(오른쪽) · 내용 두 줄 · **어느 업무인지**. 맨 아래 '모두 읽음'.
  · 비어 있을 때는 아이콘 + 한 줄.
- 설정에서 **'팀 · 권역 · 현장 다시 읽기' 행 삭제**(159차부터 실시간으로 읽으므로 누를 일이 없다).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=153. widget/ 변경 없음.

### 170차 — 잔손질 다섯
- 설정 아래 버튼 **세 개를 같은 모양으로**(배경 + 테두리). 위 구분선은 그대로.
- 알림·내 업무 팝오버의 **제목 여백을 설정 팝업과 같게**(`12px 14px 10px`).
- 첫 묶음 제목('이번 주 일정')과 위 구분선 사이 간격을 벌렸다(`.wl-h:first-child` 위 여백 2 → 9px).
- **업무 일정의 새 업무 폼에도 취소(X)** — 업무 목록과 같은 자리·같은 뜻.
  ⚠ 여기는 **자동 저장**이 도는 자리라 그냥 닫으면 안 된다 →
  `plan.discard` 가 **대기 중인 저장을 취소하고, 이미 저장됐으면 지운다.**
  첫 저장으로 삭제 버튼이 끼워질 때 **취소 버튼을 제거**해 둘이 겹치지 않게 했다.
  실측: 저장 전 취소 → 아무것도 안 남음 / 저장 후 삭제 → 원래 개수로 복귀.
- 위젯 **주말 칸에 붉은 끼**(다크 `62,48,54` · 라이트 `238,224,226`).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=154. widget/ 변경 없음.

### 171차 — 팝오버 여백이 안 맞던 이유
- ⚠ **`.wid-pop` 자체가 `padding:10px` 을 갖고 있었다.** 그 위에 헤더 여백(12px)이 얹혀 **제목이 22px 아래**에 섰고,
  설정 팝업(12px)과 나란히 놓으면 확연히 깊었다. 헤더 여백만 맞춰 놓고 '같게 했다'고 적은 것이 잘못이다.
  → 옆 팝오버는 **컨테이너 여백을 0 으로** 두고 헤더가 여백을 갖게 했다. 실측 **설정 13px · 알림 13px 동일**.
  **교훈: 여백을 맞출 때는 조상의 padding 까지 더해서 잰다.**
- 주말 칸 붉은 끼를 **이전과 지난 회차의 중간**으로(다크 `62,48,54`→`57,52,61` · 라이트 `238,224,226`→`228,222,229`).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 대비 경고 0. app.js?v=155. widget/ 변경 없음.

### 172차 — 설정 팝업도 같은 뼈대로 · 글자 굵기 조절
- **위젯 설정 팝업을 알림·내 업무 팝오버와 같은 뼈대로** 바꿨다.
  껍데기(배경·테두리·라운드·그림자)를 맞추고, `.wp-h`(구분선 있는 제목) + `.wgs-b`(본문) 두 층으로 나눴다.
  실측 제목 위 여백 **13px — 세 팝업이 모두 같다.**
- **글자 굵기 조절 추가**(글자 크기 아래). `◀ 가늘게 · 보통 · 굵게 ▶`.
  기준 굵기에 **더할 값**(`--wfw` = -100 / 0 / +100)을 두고 `font-weight:calc(500 + var(--wfw))` 식으로 건다 —
  요소마다 기준이 달라도(막대 500 · 날짜 700 · 제목 800) **상대 관계가 유지된다.**
  ⚠ **맑은 고딕·굴림·돋움은 400·700 만 실제로 있다** — 그 사이 값을 주면 억지로 굵혀 뭉개진다.
  그래서 시스템 글꼴에서는 **가늘게=400 / 보통·굵게=700** 두 단계로만 붙인다(실측 확인).
  ⚠ **인라인 변수 값은 선택자로 고를 수 없다**(`--wfw: -100` 처럼 공백이 붙는다) → `wfw-l`·`wfw-b` 클래스도 함께 단다.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=156. widget/ 변경 없음.

### 173차 — 글자 굵기 조절 철회
- **172차에 넣은 글자 굵기 조절을 뺐다.** 사용자 판단: 효용이 없다.
  `--wfw`·`WID_FWS`·`wfw-l/b` 클래스·시스템 글꼴 예외 규칙까지 **남김없이 되돌렸다**(잔존 0).
  설정은 다시 **투명도 · 글자 크기 · 글꼴 · 모드** 네 줄이다.
  ⚠ 172차의 **팝업 뼈대 통일은 유지**한다 — 그건 효과가 있었다.
- **교훈: 되돌릴 때는 켜는 쪽뿐 아니라 그 기능 때문에 만든 예외 규칙까지 함께 지운다.**
  (시스템 글꼴의 굵기 예외는 굵기 조절이 없으면 필요 없다.)
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=157.

### 174차 — 리마인드 알림 (만들다 만 기능 완성)
시안에서 **①부팅 직후 오늘 업무 · ④부름** 두 가지만 채택. 오후 5시 알림은 넣지 않았다.

- ⚠ **지금까지 리마인드는 종 아이콘만 켜질 뿐 아무 일도 하지 않았다** — 있는 것처럼 보이는데 없는 기능이었다.
- **역할을 갈랐다**: 앱 페이지가 **무엇을 알릴지** 정하고(로그인·자료 수신·알림 끄기까지 여기서 판단),
  위젯은 **띄우기만** 한다. 창구는 `window.bootBrief()` · `window.newMentions(seen)` · `window.notiGo(payload)`.
  ⚠ 브라우저 탭은 닫히면 끝이라 **트레이에 상주하는 위젯만** 이 일을 할 수 있다.
- **오늘 업무 — 하루 한 번.** `state.briefDay` 에 날짜를 적어 두고 같은 날은 건너뛴다(재부팅을 여러 번 해도 한 번).
  자료가 늦게 올 수 있어 **45초 · 150초 두 번 시도**한다. 완료된 것은 빼고, 없으면 아예 안 띄운다.
  줄은 3개까지, 시각이 있는 것부터(없는 것은 뒤로 — `'99:99'` 로 밀어낸다).
- **부름 — 알린 id 를 `state.notedMentions` 에 최근 100개 기억**해 두 번 띄우지 않는다. 30초마다 확인.
- 알림을 누르면 창을 앞으로 내고 `notiGo` 로 그 날짜·업무를 연다(부름은 읽음 처리까지).
- ⚠ **`app.setAppUserModelId` 가 없으면 윈도우 알림에 'electron.app…' 으로 뜬다** — 추가했다.
- 위젯 설정에 **알림 토글**(기본 켜짐, 이 PC 에만 저장). 끄면 창구가 `null`·`[]` 을 돌려주므로 위젯은 아무것도 하지 않는다.
- ⚠ 검증 스크립트의 설정 행 선택자를 `.wgs-r > span` 으로 좁혔다 — 토글 안의 `<span>` 까지 세고 있었다.
- **v1.1.0**. 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=158.

### 175차 — 상단바 정리 · 사용 안내 문서화 · 달력 색 · 읽은 부름 남기기
- 위젯 설정에서 **알림 토글을 '모드' 아래로**.
- **상단바의 알림·`?` 버튼에 각각 배경**(따로 떨어진 두 개). 제목 글자를 **사이드바 제목과 같은 14px** 로.
  본문과 헤더 사이를 **9 → 12px**. ⚠ `.cal-wrap`·`.tkwrap` 의 높이 계산(`- 29px`)도 함께 고쳐야
  아래 여백이 20px 로 유지된다 — 한쪽만 고치면 하단이 17px 로 줄어든다(검증이 잡아냈다).
- **앱 알림 팝오버를 위젯과 같은 줄 모양(`.wl-al`)으로** 통일. 전용 `.al-*` 규칙은 폐기.
  ⚠ 두 화면이 다르게 생기면 **배우는 데 두 번 든다.**
- **사용 안내를 README 와 같은 내용·차례로 다시 썼다**(6장 구성) — 문서형 레이아웃(`.doc`)으로,
  머리글 계층·인용 상자·코드 표기를 갖췄다. README 에도 "둘 다 고칠 것"을 못박아 뒀다.
- **달력 색**: 위젯은 **공휴일을 주말과 같은 칸 색**으로(쉬는 날이라는 뜻이 같다).
  앱은 **주말에도 붉은 끼**를 넣고 **공휴일을 그와 같은 톤으로 연하게**(예전 `rgba(255,59,48,.08)` 은 혼자 튀었다).
  다크 테마용 값도 함께 뒀다.
- ⚠ **읽은 부름을 지우고 있었다** — 무슨 부름이었는지 되짚을 수 없었다.
  → `read` 시각만 남기고 **지우지 않는다**(`mentionRead`). 배지는 **안 읽은 것만** 세고,
  목록에는 읽은 것도 **흐리게(opacity .5)** 남는다. 위젯 알림도 읽은 것은 다시 띄우지 않는다.
  규칙에 `mentions/$mid/read` 추가 → **규칙 먼저 게시.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=160. widget/ 변경 없음(v1.1.0).

### 176차 — 사용 안내를 README 로 (하자처리 현황 방식 이식)
- 참고: 하자처리 현황은 **저장소의 `README.md` 를 그대로 읽어** 보여 준다.
  `marked` 로 렌더 → `DOMPurify` 로 살균 → **h2 단위로 쪼개 왼쪽 목차 탭**. 같은 방식으로 옮겼다.
  ⚠ 두 라이브러리는 **`vendor/` 에 함께 둔다**(사내망 CDN 차단 대비) — 처음 열 때만 지연 로드한다.
  ⚠ 받은 글은 **반드시 살균 후** 넣는다.
- 앱 안에 박아 두던 `GUIDE_HTML` 을 **폐기**했다. ⚠ 이제 **문서와 안내가 어긋날 수 없다** — 같은 파일이다.
- **README 를 사용자 우선으로 다시 짰다**: 소개 · 처음 쓰는 분께 · 업무 일정 · 업무 목록 ·
  내 업무와 알림 · 팀·권역·현장 · 문제가 생기면 · 위젯 · 알림 · 백업 → 그다음 `---` 아래가 **관리자·개발자용**.
  exe 만드는 법도 백업 장에서 관리자 영역으로 옮겼다.
- 상단바 아이콘을 **위젯 헤더의 알약(`.cal-nav`)과 같은 문법**으로 — 한 덩어리 안에 둘, 버튼 34 → **27px**,
  아이콘 18 → 15px. 빨간 점에는 알약 색 테두리를 둘러 겹쳐도 또렷하다.
- ⚠ **단일 파일 빌드(`dist/`)에는 README.md 가 없다** — 열면 안내 토스트가 뜬다. 보류 목록에 적어 둔다.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=161. widget/ 변경 없음.

### 177차 — 우클릭 메뉴 · 툴팁 · 단체연차
하자처리 현황의 두 장치를 이 앱에도 들였다.

- **우클릭 메뉴**(`openCtx`/`closeCtx`, `.ctxmenu`) — 참고 앱과 같은 시각 문법.
  ⚠ **입력칸 안이거나 글자를 고르는 중이면 기본 메뉴를 그대로 둔다**(잘라내기·붙여넣기를 뺏으면 안 된다).
  붙인 곳: **달력 날짜 칸**(업무 추가 · 이동 · 휴무일 지정/해제) · **업무 카드**(완료↔진행 · 수정 · 제목 복사 · 삭제).
  삭제 항목은 붉게(`.dg`).
- **툴팁**(`#htip`, `[data-tip]`) — 420ms 머무르면 뜬다. 위가 좁으면 아래로 뒤집고 화면 밖으로 안 나간다.
  ⚠ 기존 `title` 속성과 겹치면 **둘 다 뜬다** → 툴팁을 단 곳은 `title` 을 지웠다.
- **단체연차 등 팀 휴무일** — `calapp/offdays/<날짜>` 에 이름 한 줄. 관리자만 쓴다(규칙 추가 → **규칙 먼저 게시**).
  · `holOf()` 가 **팀 휴무일을 공휴일보다 앞세운다** → 칸 색·`hol` 클래스가 그대로 붙는다.
  · 글자만 `.dhol.off` 로 구분(주황) — **칸 색은 같게, 문구로 구분**한다는 결정을 그대로 옮겼다.
  · ⚠ **달력 '칸'의 내용·클래스는 이벤트만 새로 받아서는 안 바뀐다** → `calRerender()`(refetchEvents + render).
  · 로컬 모드에도 같은 창구를 뒀다(`LocalStore.putOffday`).
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=162. widget/ 변경 없음.

### 178차 — 휴무일을 한 번에 넣기
사용자 지적: **단체연차는 한 해에 한 번, 며칠씩 몰아서 정한다.** 우클릭만으로는 손이 많이 간다.

- 설정에 **휴무일 카드**(관리자에게만) — 시작일·종료일·이름을 넣고 **한 번에 지정**한다.
  ⚠ **주말과 공휴일은 건너뛴다** — 이미 쉬는 날에 또 칠하면 목록만 지저분해진다.
  건너뛴 일수를 알림에 함께 알린다. 한 번에 60일까지.
- 지정된 날은 **연도별로 묶어** 보여 주고 **지난 날짜는 흐리게**. 각 줄에서 바로 지운다.
- ⚠ **CSS 블록 하나가 저장되지 않은 채로 지나갔다** — 앞 회차에서 한 스크립트 안의 마지막 치환이 실패해
  **파일 쓰기 자체가 실행되지 않았고**, 그 앞의 성공한 치환까지 전부 날아갔다(우클릭 메뉴·툴팁 스타일).
  기능은 돌아 검사에 안 걸렸고, **모양만 빠져 있었다.**
  **교훈: 한 스크립트에 여러 치환을 담을 때는 실패하면 앞의 것도 함께 사라진다 — 반영 여부를 grep 으로 확인할 것.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=163. widget/ 변경 없음.

### 179차 — 되돌리고 다듬기
- **설정의 휴무일 카드를 철회**(178차). 사용자 판단: **우클릭만으로 충분하다.**
  목록·일괄 지정 코드와 스타일을 남김없이 지웠다(잔존 0).
- 우클릭 날짜 메뉴를 둘로 줄였다: **이 날짜에 업무 추가 / 휴무일로 지정…**.
  · '단체연차로 지정'과 '다른 휴무일로 지정…' 을 **하나로 합쳤다** — 창에서 이름을 적고(기본값 `단체연차`) 정한다.
  · ⚠ **'이 날짜로 이동'은 없앴다** — 우클릭하려면 이미 그 칸을 가리키고 있고, 왼쪽 클릭이 곧 이동이다. 겹치는 기능이었다.
- **한 줄만 받는 모달을 좁게**(`#mb.narrow`, 380px). 휴무일 창이 쓸데없이 넓었다.
  ⚠ **모달 폭 클래스를 열 때 지운다** — 안 지우면 **앞서 연 모달의 폭이 다음 모달에 남는다**(사용 안내가 380px로 떴다).
- **남은 기본 툴팁(`title`)을 전부 `data-tip` 으로** 옮겼다(22곳). ⚠ 둘이 함께 있으면 **노란 기본 툴팁이 같이 뜬다.**
- 상단바의 알림·안내 버튼을 **각각 따로 떨어진 알약**으로.
- **사용 안내 목차를 8장으로 줄였다**(12 → 8): 소개 · 시작하기 · 업무 다루기 · 내 업무와 알림 ·
  바탕화면 위젯 · 팀·권역·현장·권한 · 백업 · 개발·배포(관리자).
  ⚠ **목차가 길면 읽기 전에 지친다** — 잘게 나뉜 장을 h3 로 내려 묶었다. 모달 폭도 980 → **1180px**.
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=164. widget/ 변경 없음.

### 180차 — 툴팁 손보기
- ⚠ **툴팁이 남아 있던 경우들** — 세 가지를 고쳤다.
  · **누른 뒤에도 떠 있었다** → `mousedown` 에서 즉시 감춘다(누르고 나면 화면을 가린다).
  · **가리키던 요소가 사라져도 떠 있었다**(화면을 다시 그리면 생긴다) → `_tipFor` 를 기억해 두고
    **0.7초마다 그 요소가 아직 화면에 있는지 확인**해 없으면 치운다. 실측: 요소 제거 후 사라짐.
  · **기다리는 420ms 사이에 마우스가 떠났는데도 떴다** → 띄우기 직전에 `isConnected`·`:hover` 를 다시 본다.
- 옆 요소로 옮겨 갈 때 **앞의 툴팁을 먼저 감추고** 새로 띄운다. 같은 요소 안에서 움직이는 것은 무시한다.
- ⚠ **문구에서 군더더기를 뺐다** — '알림 — 나를 부른 코멘트' → **'알림'**, '내 업무 — 이번 주 일정과 미완료' → **'내 업무'**,
  '완료 · 눌러서 진행으로' → **'완료'** 등 9곳. **툴팁은 이름표지 설명서가 아니다.**
- 회귀 24/24 · 33/33 · 47/47 · 24/24 · 20/20 · 위젯 21/21 · 넘침 0 · 대비 경고 0. app.js?v=165. widget/ 변경 없음.

### 181차 — 전체 재검토 반영 · 자동 완료 정책 재설계 · 찾기 헤더 이전
- **버그(핵심): 표시용 자동 완료(stEff)가 저장 경로로 새던 것.** planCollect·taskFormSave 가
  `st:stEff(...)` 로 상태를 수집해, 지난 업무의 폼을 열었다 닫기만 해도 st:2 가 기록되고
  **시작일을 미래로 미루면 '완료'로 저장**됐다(헤드리스 재현으로 확정). 둘 다 `stOf(저장값)` 으로 교체.
  taskFormSave 가 편집마다 stKeep:true 를 켜 자동 완료를 조용히 꺼 버리던 것도 함께 제거(#tnSt 스팬 폐기).
- **버그: 반복 업무가 목록·집계·내 업무·미니 달력에서 영구 완료 취급.** stEff 가 원 시작일 기준이라
  매주 반복은 첫 회차가 지나면 카운트에서 빠지고 7일 뒤 '지난 완료'로 접혔다 → stEff 에 반복 분기(자동 완료 제외).
- **버그: 옮긴 회차(moveOn)의 완료가 달력 칩에 안 보임** — planEvent 가 doneOn 을 표시일로 조회 → isDone(occSrc) 로.
- **자동 완료 정책: 팀 공통 단발 업무만.** 담당자 업무는 자동 완료 대신 **아침 확인(morningReview)** —
  그날 처음 열 때(로컬은 부팅, 라이브는 첫 tasks 스냅샷 뒤) 지난 미완료 담당자 업무를 모달로 띄워
  체크한 것은 완료, 안 한 것은 **보류(st=3, 회색 원 안 −)** 로 저장. '나중에'면 다음에 다시 묻는다.
  하루 1회(localStorage `calapp.mrv.<uid>`), 위젯 창에서는 띄우지 않는다.
  ⚠ stOf 가 3 을 보존하도록 바뀌어 **옛 4단계 시절의 st=3 데이터가 보류로 되살아난다**(의도 — 규칙은 원래 0~3 허용이라 배포 순서 무관).
  ST_PICK 에 ['3','보류'] 추가 — 필터로 보류만 모아 볼 수 있다. 상태 아이콘을 누르면 보류→완료.
- **찾기 FAB 폐지 → 상단바 검색창.** FAB 이 우측 패널 하단 버튼('이 회차 옮기기')을 가렸다.
  알림 버튼 왼쪽에 검색창 모양 버튼(.tb-srch, Ctrl K 표기) — 누르면 기존 패널이 **헤더 아래 우측**에 뜬다
  (bottom 앵커 → top:calc(--th+8px)). 모바일(≤960px)은 아이콘만. nq-fab CSS·마크업·wid/print 참조 전부 제거.
- **현장 표 숫자 잘림**: 세대수 1200 이 '120'으로 보이던 것 — 숫자 입력 min-width(56/48/52px)·열 배분 조정, 준공일 132→120px.
- README 현행화(자동 완료·아침 확인·찾기 위치). app.js?v=189.
- ⚠ 남긴 것: rptRows(주요 업무 보고)는 여전히 반복을 전개하지 않는다(weekly 메일과 불일치 — 정책 결정 필요),
  planCommit 의 반복 전환 분기는 set() 전체 교체라 사실상 불필요(무해해서 유지), roster() 무캐시.

### 182차 — 보류함(D+B) · 아침 확인 모달 개편 · 상태 아이콘 정정
- **아침 확인 모달**: 하단 버튼(나중에·확인) 폐지 → 우상단 X(openModal 에 foot 을 넘기지 않으면 has-x 가 붙는다).
  항목마다 왼쪽 상태 아이콘=완료, 오른쪽 회색 버튼=[오늘로 이동][보류]. **누르는 즉시 저장**(mrvApply)하고 그 줄만 지운다.
  다 비면 모달이 스스로 닫힌다. 모달을 **연 시점에** mrv 키를 오늘로 적어 하루 한 번만 뜬다(X 로 닫아도 오늘은 다시 안 뜸,
  처리하지 않은 것은 진행 상태 그대로라 다음 날 다시 묻는다). 안내 문구는 white-space:nowrap 으로 두 줄 고정.
- **보류 아이콘**: 테두리 원+선 → **완료와 같은 방식**(채워진 원에서 모양을 파냄, 마스크 `stx-mn`), 색만 회색.
- **보류함 D안 — 달력 일자 패널 아래**(#holdCard): 제목은 위 카드와 같은 .dp-lh-t(13px 회색), 카운트 배지는
  업무 목록 트리와 같은 회색 알약(.hold-c)을 우측에. 핸들·꺾쇠·설명 문구 없음, 172px 넘으면 내부 스크롤, 0건이면 카드째 hidden.
  rDay 끝에서 rHold()+wireHoldDnD() 호출(빈 목록 조기 반환 경로에도 넣을 것).
- **끌어 놓기**: 행 draggable → #fcal 의 dragover/drop 을 직접 듣는다(FullCalendar Draggable API 미사용).
  놓으면 date 변경 + **보류 해제(st:1, stKeep:true)**, 기간 업무는 기간 길이 유지. 날짜 칸은 .hold-over 로 표시.
  ⚠ 터치에서는 HTML5 드래그가 안 먹는다 — 행을 누르면 업무로 이동(hold.go)하는 경로가 정식 수단.
- **보류함 B안 — 업무 목록**: 미니 달력과 트리 **사이**에 독립 카드(.tks-hold), 고르면 우측에 보류만 모아 보여 준다(sel==='hold').
  ⚠ **tkSel() 의 valid 검사에 'hold' 를 넣지 않으면 고르는 즉시 teamall 로 되돌려진다**(처음에 이걸로 한참 헤맸다).
  보류함 화면에서는 '업무 추가' 버튼을 감춘다(담을 sid 가 없다).
- **위젯**: 내 업무 팝오버의 '미완료 업무' 아래에 '보류함' 섹션(mineHolds, 날짜 알약 .wl-d.hold 앰버, widMore.hold 로 더보기).
  이때 **mineTasks() 에서 보류를 뺐다** — 안 그러면 미완료와 보류함에 같은 업무가 두 번 나온다. bootBrief 도 같은 기준이라 보류는 알림에서 빠진다.
- 헤더 찾기 버튼의 'Ctrl K' 표기 제거(단축키 자체는 유지). app.js?v=190.

### 183차 — 리마인드 폐지 · 보류 자동화 · 보류함 다듬기
- **리마인드 일괄 제거**: 발송 경로(weekly.mjs·mail-common)에 remind 가 없어 기능이 이미 죽어 있었다.
  달력 카드·업무 카드의 벨 버튼, 두 편집 폼의 벨 토글, ACT 4개(plan.remind/plan.remindDraft/tk.remind/tk.remindDraft),
  cleanTask·정규화·planCollect·taskFormSave 의 remind 필드, CSS(.pe-rem)까지 전부 삭제(cleanTask 22개).
  ⚠ 규칙(database.rules.json)의 remind 는 남겨 둔다 — 기존 데이터에 남아 있는 값이 거부되면 안 된다.
- **업무 카드 읽기 상태의 삭제(×) 버튼 제거**(.tk-del CSS 포함). 삭제는 수정 폼의 휴지통으로만.
- **아침 확인 모달**: [보류] 버튼 폐지 → **창을 닫으면 남은 업무가 자동으로 보류**(mrvHoldRest, closeModal 진입점에 연결).
  MRV_ON 플래그로 한 번만 돈다(다 처리해 저절로 닫힐 때는 넘길 것이 없으므로 먼저 내린다). 남는 버튼은 [오늘로 이동] 하나.
- **보류함(달력)**: 제목 '보류된 업무 N건'(카운트 배지 폐지), 그 자리에 **[내 업무만 보기]** 토글(S.holdMine, holdItems ⇄ mineHolds).
  높이는 **옆 달력 4주차 줄에서 시작**하도록 holdFit() 이 실측해 맞춘다(#fcal 의 4번째 tr top ~ .dp-col bottom).
  rHold 와 resize 에서 호출하고, 위젯·좁은 화면(≤960px)에서는 높이를 풀어 준다.
  ⚠ '내 업무만'을 켠 채 내 보류가 0건이면 카드가 사라져 토글을 다시 끌 수 없다 — 팀 보류가 있으면 카드는 남기고 빈 문구를 띄운다.
- 헤더 찾기 버튼의 margin-right 제거 → .tbr 의 gap(6px) 으로 통일(알림·사용 안내 버튼과 같은 간격).
- 좌측 트리 카드 안쪽 여백을 보류함 카드와 같은 9/10px 로(항목이 좁아 보이던 문제). app.js?v=191.

### 184차 — 보류함 다듬기(문구·정렬) · hidden 버그
- **[hidden] 이 먹지 않던 버그**: `.hold-card{display:flex}` 가 UA 의 `[hidden]{display:none}` 을 덮어,
  보류 0건에도 카드가 남고 그 탓에 holdFit() 이 높이를 풀어(hidden 이면 early return) **4주차 정렬도 안 걸렸다**.
  `.hold-card[hidden]{display:none}` 추가. ⚠ display 를 지정한 요소에 hidden 속성을 쓰면 늘 같은 문제가 생긴다.
- 문구 통일: '보류함' → **'보류한 업무'**(달력 카드 제목·업무 목록 좌측 버튼·업무 패널 제목·위젯 섹션).
- 보류한 업무 카드(.tks-hold)의 오른쪽 여백을 10 → 14px 로. 아래 트리는 data-sb 규약상 스크롤바 자리 4px 을
  늘 확보하므로, 그만큼 더 주어야 카운트 배지가 같은 x 에 선다.
- 직급 배지(.rk) 12 → 11px, 안쪽 여백 축소.
- 업무 패널의 '팀장' 묶음 안에서는 이름 옆 직급 배지를 생략(묶음 이름이 곧 직급이면 같은 말을 두 번 하는 셈).
  판정은 `rn!==rankLabel(rk)` — 권역 묶음의 공구장 배지는 그대로 남는다. app.js?v=192.

### 185차 — 스크롤바 규약 변경(오버레이) · 하자 관리 분류
- **⚠ 규약 변경 — 모든 스크롤바는 자리를 차지하지 않는다.** 예전 규약(scrollbar-gutter:stable + padding 에서 4px 빼기)을 폐기하고
  `*{scrollbar-width:none}` · `*::-webkit-scrollbar{width:0;display:none}` 로 **네이티브 스크롤바를 전부 감췄다.**
  더 볼 내용이 있다는 신호는 기존 위·아래 페이드(.sb-fade-t/.sb-fade-b, fadeOne)가 그대로 맡는다.
  스크롤바 몫으로 더해 두었던 4px 을 되돌렸다(#content 20, .dp-body/.tk-list/.hold-body 좌우 16, .nq-res 10, .tks-hold 10).
  ⚠ **앞으로 새 스크롤 영역을 만들 때 좌우 패딩에서 스크롤바 몫을 빼지 말 것.** 막대를 그리지 않으므로 .sb-on 클래스와
  scroll 리스너의 타이머도 함께 제거(페이드 갱신만 남음).
- **사이드바에 '하자 관리' 분류 + '하자 현황'(view-defect)** 추가. 지금은 안내만 띄우는 자리(rDefect).
  하자처리 현황 업로드 포털이 게시한 자료를 읽어 붙일 곳이며, **무엇을 어떤 주기로 읽을지는 아직 정하지 않았다.**
  아이콘은 #i-defect(집+점검) 새로 추가. app.js?v=193.

### 186차 — 다크 달력 색 정리
- 다크 요일 머리 배경 `#424245` → **`#2A2A2A`**(카드 표면색 --surf2 와 같은 값 — 달력이 주변 UI와 한 덩어리로 보인다).
- 다크 주말·공휴일 배경을 **한 색으로 통일**: 흰색 3%(주말) / 붉은색 13%(공휴일) 두 갈래를 없애고
  채도를 뺀 `rgba(214,166,166,.10)` 하나로. 토·일이 서로 달라 보이던 것과 공휴일만 유난히 붉던 것을 함께 정리.
  ⚠ 같은 선택자의 **다크 규칙이 위·아래 두 벌 있었다**(위 규칙은 아래에 덮여 아무 일도 안 하던 죽은 줄) — 위쪽을 지웠다.
- 위젯 유리 모드 요일 머리의 일·토에 글자색 부여(#FF9C93 / #9DC3FF) — 전부 흰색이라 평일과 구분되지 않았다.
- 검토했으나 채택하지 않음: 칸 배경을 위젯처럼 푸른 기 도는 #22262F 로 올리는 안(A안). 앱 카드 톤과 어긋나 캘린더만 도드라진다. app.js?v=194.

### 187차 — 하자처리 현황 이식 1단계(뼈대)
- 하자처리 현황 앱은 **같은 Firebase 프로젝트(report-c29a1)** 를 쓴다 — 별도 인증·프로젝트 연결이 필요 없다.
  게시본 경로: `reportIndex`(게시월 인덱스) · `report/{YYYY-MM}`(_dash·_meta·{현장id}) · `plans` · `analysis` · `siteConfig`.
  현장 KPI 는 `report/{rm}/{sid}/kpi`, 미처리 목록은 `ulz`(LZ-String 압축·PII 마스킹) — 목록을 쓰려면 lz-string 이 필요하다.
- 이번 차수에 넣은 것(뼈대만):
  - 사이드바 '하자 관리' 아래 **권역(소분류) → 현장** 목록(rDefectNav). 지금은 조직/현장 관리의 현장을 쓴다.
  - 현장 클릭 시 S.dfSid 지정 후 defect 화면(대시보드는 dfSid='').
  - 하자 관리 화면에서만 상단바에 **기준월 칩(#tbRm)·인쇄 버튼**(dfTopbar). 인쇄는 window.print().
  - 프로필 옆 역할 배지 12 → 11px. README 에 하자처리 현황 절 추가.
- 아직 없는 것: 게시본 읽기·대시보드 KPI·현장 탭·처리계획 쓰기·차트. **2~4단계 계획은 대화 기록 참조.**
- ⚠ 조직/현장 관리의 공가세대·공가상가 토글은 이미 disabled(.mg-ro) 다 — 값이 false 라 회색으로 보일 뿐이며,
  게시본 siteConfig 를 읽기 시작하면 켜진 상태로 표시된다. app.js?v=195.

### 188차 — 하자처리 현황 2단계(게시본 읽기·대시보드)
- **이미 게시본을 읽고 있었다**: `bindReportOrg()` 가 `reportIndex` 로 최신 게시월(ORG_RM)을 찾아
  `report/{rm}/_dash` 를 구독하고 S.org(팀·권역·현장)를 게시본 기준으로 채운다. 그래서 사이드바 권역·현장이 곧 게시본이다.
- 이번 차수에 더한 것 — `DF.cache[rm]` 에 집계를 담아 대시보드·현장 화면을 그린다.
  - 팀 합계: `report/{rm}/_dash/wks` **마지막 줄**(주차 누계) → 접수 r · 처리 res · 미처리 r-res · 장기미처리 d30+d60.
  - 현장별: `report/{rm}/{sid}/siteWks` 마지막 줄. ⚠ **kpi 노드를 읽지 말 것** — 목록(ul·ulz)이 함께 있어 현장당 수십~수백 KB.
  - 화면: KPI 카드 5개(.df-kpi) + 권역으로 묶은 현장별 표 · 현장 화면은 그 현장 KPI + 최근 10주 표(sun 컷만).
  - 표 행 클릭 → 그 현장으로 이동. 로컬 모드·미게시면 안내 카드.
- 디자인은 **이 앱의 언어로** 옮겼다(카드·모서리 8px·mgtbl 규격·색 토큰). 하자처리 현황 앱의 CSS 를 가져오지 않았다.
- 사이드바: 관리·프로필을 스크롤 영역(.sbsc) **밖으로** 빼 항상 아래 고정. 권역 소분류는 꺾쇠로 접힘(S.dfFold).
  현장 항목은 아이콘 대신 회색 점 · 글자 11.5px(소분류와 동일) · 간격 축소. 명칭 '하자 현황' → '하자처리 현황'. 아이콘 렌치로 교체.
- 남은 것(3~4단계): 현장 탭(장기미처리·공가·상가·상세), 처리계획 쓰기(plans), 미처리 목록(ulz — lz-string 필요), 차트, 인쇄 레이아웃. app.js?v=196.

### 189차 — 하자처리 현황 3단계(현장 탭·처리계획)
- 현장 화면에 탭 4개(.df-tabs): **종합 / 장기미처리 / 공가 / 상세 현황**. 원본 앱의 5탭 중 공가세대·상가를
  '공가' 하나로 합쳤다 — 게시본 kpi 의 v* 필드가 세대·상가를 나누어 담는지 확인하지 못했다(⚠ 확인 후 분리할 것).
- **현장 kpi 는 그 현장을 열 때만 읽는다**(DF.kpi 캐시). 대시보드에서는 절대 읽지 않는다 — 목록(ul·ulz)이 무겁다.
  종합=top(공종별)·주차별, 장기미처리=topLt 상위 5, 공가=v*, 상세=monthly+주차별.
- **처리계획 쓰기**: `plans/{sid}/processingPlan/{encodeURIComponent(기준월@공종)}` — 하자처리 현황과 **같은 자리**라
  이 앱에서 쓴 내용이 그쪽 화면에도 그대로 뜬다. 키 인코딩은 `.`→`%2E` 까지 원본과 동일해야 한다(다르면 다른 칸이 된다).
  한 칸 안에 위=지난달(읽기)·아래=이번 달(입력), 입력 멈춘 뒤 700ms 에 저장.
- 사이드바 소분류·현장 글자 12px, 하자 아이콘을 막대 차트로 교체. app.js?v=197.
- ⚠ **아직 실 데이터로 못 돌려 봤다** — 로컬 모드에는 게시본이 없어 가짜 캐시로 렌더 모양만 확인했다.
  배포 후 하자처리 현황 앱의 숫자와 대조할 것(특히 v* 공가 필드와 topLt 정렬).

### 190차 — 사이드바 분류 재편 · 주차별 추이(무라이브러리 차트)
- 사이드바: '업무 일정' → **'업무 관리'**, '업무 현황' 분류 폐지, **주요 업무**를 업무 관리 아래로 이동(분류 3개 → 2개 + 관리).
- 하자 화면 상단바의 기준월 배지(.tb-rm)를 다른 버튼과 같은 규격으로(높이 30px·1px 테두리·8px 라운드).
- **주차별 추이 차트를 SVG 로 직접 그렸다**(dfTrendHTML) — 막대=미처리, 선=장기미처리, 최근 12주(일요일 컷만).
  ⚠ **Chart.js 를 들이지 않았다.** 이 앱의 외부 라이브러리는 FullCalendar·Firebase 뿐이며 그 방침을 유지한다.
  데이터는 `_dash/wks`(이미 읽고 있음) — 추가 요청 없음. 좌표 계산은 dfTrendHTML 안에 닫혀 있다.
- app.js?v=198.

### 191차 — 하자 차트 이식(Chart.js 도입) · wks 필드 오독 수정
- ⚠⚠ **버그: 게시본 주차 행의 필드를 잘못 읽고 있었다.** `_dash.wks`·`siteWks` 의 한 줄은 capWks 산출
  `{m,w,cumR,cumRes,u,lt,lt60}` 인데 190차까지 `r·res·d30·d60·sun` 으로 읽어 **모든 숫자가 0** 이었다
  (로컬에 게시본이 없어 가짜 캐시로만 확인해 놓쳤다). dfRows 를 실제 필드로 교정 · 주차 라벨은 `M월 N주`.
  ⚠ 하자처리 현황의 `capWks`(app-core.js)가 이 구조의 기준이다 — 바뀌면 여기도 함께 고칠 것.
- **Chart.js 도입**(vendor/chart.umd.min.js·chartjs-plugin-datalabels.min.js·lz-string.min.js 복사).
  방침 변경: 원본과 같은 모습을 목표로 하므로 같은 라이브러리를 쓴다.
  주차별 추이 = 누적 막대 3단(60일↑ / 30~59 / 30 미만)+누계 접수·처리 선 2개·오른쪽 축, 색은 --ch-* 토큰(원본 값).
  기존 SVG 자작 차트는 폐기. 다크 모드는 축·글자색만 토큰으로 바꾼다.
- 사이드바 하자 아이콘 = 추이 선그래프(D안).
- 남은 이식: 미처리·장기미처리 **목록 모달**(ulz+LZString — 라이브러리는 이미 넣음), 도넛(공종별 분포),
  전월 대비 실적, 주요 이슈, 피벗·엑셀(xlsx 950KB — 도입 여부 미결), 인쇄 레이아웃. app.js?v=199.

### 192차 — 목록 모달·엑셀·도넛 이식 (하자처리 현황 앱은 관리자 전용, 사용자는 이 앱만 쓴다)
- 방침 확정: **하자처리 현황 앱은 업로드·게시용 관리자 전용**으로 남기고, 팀원은 이 앱에서만 본다.
  따라서 뷰어 기능은 **전부** 이쪽으로 옮긴다(라이브러리 포함).
- 도입한 vendor: chart.umd.min.js · chartjs-plugin-datalabels.min.js · lz-string.min.js · **xlsx.full.min.js(950KB, defer)**.
  ⚠ 배포 시 vendor 폴더가 빠지면 차트·목록·엑셀이 모두 죽는다.
- **미처리/장기미처리 목록 모달**(recOpen): `report/{rm}/{sid}/ulz` 를 LZString 으로 풀어 표로 — 실패 시 kpi.ul(캡 300건) 폴백.
  검색·지연구간 칩(60↑/30~59/30 미만)·열 머리 정렬·엑셀 내보내기. 열 구성은 원본 REC_COLS 와 같은 순서로 맞췄다
  (양쪽에서 내보낸 엑셀이 같아야 한다). 표시는 3,000행까지(그 이상은 엑셀로).
  KPI 카드의 미처리·장기미처리에 「목록 보기」 버튼 — 현장 화면에서만 나온다(대시보드는 전 현장 ulz 라 무겁다).
- **공종별 도넛**(dfDonutDraw) — 원본과 같은 Chart.js doughnut·cutout 58%. 팔레트는 DF_PAL 로 옮겨 왔다.
- 남은 이식: 피벗, 전월 대비 실적, 주요 이슈 카드, 우클릭 메뉴, 무엇이든 찾기(NLQ), 인쇄 레이아웃, 분석 의견. app.js?v=200.

### 193차 — 주요 이슈·전월 대비·분석 의견·인쇄·찾기 확장
- **주요 이슈**: `report/{rm}/_dash/insightsHTML` 을 그대로 표시. ⚠ 남이 만든 HTML 이므로 **DOMPurify 로 씻어서** 넣는다
  (vendor/purify.min.js 를 이때 처음 로드했다 — 예전에는 파일만 있고 안 읽고 있었다).
- **전월 대비 실적**: kpi.prev(total·res·unr·lt) vs 금월. 미처리·장기미처리는 **줄어야 좋은 값**이라 증감 색을 뒤집는다.
- **분석 의견**: `analysis/{sid}/{기준월}` 읽기·쓰기(처리계획과 같이 열람자도 쓸 수 있다). 700ms 디바운스 저장.
- **인쇄**: 하자 카드 테두리 제거·KPI 5열 고정·표 축소·탭/버튼/입력칸 숨김(@media print).
- **찾기 확장**: 기존 헤더 찾기(nq)에 '하자처리 현황 · 현장'과 '하자' 그룹 추가.
  ⚠ 하자 건 검색은 **이미 불러온 목록(DF.list)** 안에서만 한다 — 전 현장 ulz 를 미리 받으면 수 MB 다.
  현장을 한 번 열면 그 현장이 검색 대상에 들어온다. 결과를 누르면 그 현장 하자 화면으로 이동(nq.site).
- 남은 이식: 피벗, 우클릭 메뉴, 목록 열 숨김·너비 조절, 대시보드 통합 목록. app.js?v=201.

### 194차 — 피벗 · 우클릭 · 팀 전체 목록
- **피벗**(PIV): 목록 모달의 [피벗] 버튼 → 행·열 선택(문자 열만), 값은 건수. 행은 합계 내림차순, 마지막 줄은 합계.
  현재 필터·검색이 걸린 결과(recCompute)를 그대로 집계한다.
- **우클릭**: 하자 표·목록 표 어디서나 '표 전체 복사'(탭 구분 — 엑셀에 그대로 붙는다), 현장 화면의 공종 행이면
  '"<공종>" 미처리 목록'. ctxFor 맨 앞에 분기를 넣었다(기존 업무 카드·달력 분기보다 먼저 걸린다).
- **팀 전체 목록**: 대시보드 KPI 의 목록 보기 → 전 현장 ulz 를 **차례로** 받아 합치고 '현장' 열을 앞에 붙인다(REC.withSite).
  ⚠ 현장 수만큼 통신하므로 진행 표시를 띄운다. 한 번 받은 현장은 DF.list 에 남아 다음부터 즉시 열린다.
  엑셀·검색·정렬·피벗 모두 현장 열을 포함한 recCols() 기준으로 동작한다.
- app.js?v=202.

### 195차 — 목록 열 조작(마무리) · 하자 이식 완료
- **열 머리 우클릭**: '이 열 숨기기' · '이 열 필터 해제' · 값 목록(최대 20개, 여러 개 고르면 OR).
  값 필터가 걸린 열에는 머리에 '필터' 배지. 숨긴 열이 있으면 상단에 [숨긴 열 N개] → 누르면 전부 복원.
  ⚠ 원본과 같은 규칙: **숨김은 표시에만 적용**되고 검색·정렬·엑셀은 전체 열(recCols) 기준이다.
- **열 너비 드래그**: 머리 오른쪽 경계(.rz)를 끌어 조절(최소 50px, REC.w 에 보관). document 레벨 mousedown 위임.
- **AI 분석 생성은 이식하지 않는다** — 이 앱은 게시본만 읽으므로 생성 기능이 필요 없다(결과인 주요 이슈·분석 의견은 읽고 쓴다).
- 이로써 하자처리 현황 **뷰어 기능 이식은 일단락**. 남은 차이: 스냅샷 내보내기(관리자 앱 전용), 업로드·게시(관리자 앱 전용). app.js?v=203.

### 196차 — 하자 화면 전면 재이식(원본 화면·기능 일치)
- **문제**: 195차까지의 이식이 원본과 크게 어긋났다 — 주요 이슈 카드의 SVG 아이콘이 화면 크기로 터지고
  (`.ic` 계열 CSS 미이식), 대시보드에 전월대비 실적·도넛 2종·월별 표가 없고, 현장별 표는 6열 축약본,
  현장 화면의 주차별 표는 siteWks 를 잘못 읽어 라벨이 비었다. 또 app.js 파일 맨 앞에 지난 패치 사고로
  고아 dfWeekHTML 조각이 붙어 있었다(제거).
- **원칙**: 화면 구성·기능은 원본(하자처리 현황)을 그대로 따르고, 색·모서리(8px)·글자·간격만 이 앱 토큰을
  쓴다. CSS 는 원본 클래스명을 유지하되 전부 `#view-defect` 범위로 가둬 앱 다른 화면과 충돌하지 않는다.
- **데이터 계약(단일 출처 = 게시본)**: 대시보드는 `_dash(wks·am·insightsHTML)` + 현장별
  `kpi/weekly`(가벼운 부분 경로 — calcW 행이라 금월말·전월말 스냅샷이 정확히 들어 있다).
  현장 화면은 `kpi 전체 + siteWks + siteAm + vac` 를 병렬로 읽는다. 게시 때 인코딩된 맵 키는
  dfDec(deepDecKeys 동치)로 복원한다. ⚠ 예전처럼 siteWks(cumR·cumRes)로 표를 만들면 전월값·주차
  라벨이 없다 — 표·전월대비는 반드시 kpi/weekly 로.
- **대시보드**: KPI 5장(kc 톤 bl/sk/ms/wh + 우상단 목록 보기) · 주차별 추이(누적막대+라인, 데이터라벨·
  페이드) · 전월대비 실적(가로 막대) · 현장별/공종별 미처리 도넛(가운데 합계 + 2열 범례 hover 연동) ·
  주요 이슈(insightsHTML 그대로, `.ic` CSS 이식) · 월별 하자처리 현황 12열 표 · 현장별 하자처리 현황
  11열 표(정렬·합계·업체별 축 — 업체별은 누르는 순간 전 현장 kpi 를 받는다, dfAllKpi).
  집계 범위는 원본과 같이 '인수 전 현장' 권역 제외(dfDashSites) — 팀 전체 목록 보기도 같은 범위.
- **현장 화면**: 탭 종합/장기미처리/공가세대/공가상가(hasCommercial)/상세 현황. 종합=추이+전월대비+
  공종 도넛+종합 분석 의견(aib 상자·공동 작성 textarea). 장기미처리=비율 현황 누적 바(전월/금월)+
  상위 5개 공종 표(지난달/이번 달 처리계획 pp-stack)+공종·업체 축 표(정렬 dfSortPanel·상위 15 접기).
  공가=현황 스트립(vrow, 공가 수는 게시 vac 읽기 전용)+상위 5개 표. 상세=월별 12열·주차별 13열 표
  (연도 선택, 월 그룹 mcell·월 경계 mend).
- **목록 모달**: 지연구간 칩에 건수, 공가세대 칩(있을 때만), 표시 건수 500/1000/5000/전체, 표 복사(TSV),
  결과 N/전체 N. 공종·업체 셀 클릭 → 그 값으로 미리 걸러 연다(rec.list data-trade/data-co/data-vac).
- **함정 기록**: `'...">'.repeat(n)` 은 마지막 문자열에만 걸린다 — colgroup 이 통째로 깨졌다. 괄호 필수.
- **검증**: 원본 report 앱을 같은 시드(4현장 1,040행)로 띄워 fb2Publish 동일 구조의 게시본을 추출,
  이 앱에 주입해 화면별 스크린샷을 원본과 대조(대시보드·탭 4·목록·다크). 상호작용(정렬·축 전환·목록
  진입·처리계획 저장·연도 변경) 무오류 확인. static-audit FAIL 0 · WARN 0. app.js?v=204 · APP_VER 1.8.0.

### 197차 — 원본 대조 피드백 반영(연동·모달·사이드바·서식)
- **처리계획·분석 의견 연동**: ① kpi 가 캐시돼 있으면 plans·analysis 를 건너뛰던 로딩 게이트를 고쳤다
  (셋 중 하나라도 없으면 로딩). ② 원본(fb2SubSite)처럼 열어 둔 현장의 plans/{sid}·analysis/{sid}를
  **on('value') 실시간 구독** — 입력 중인 칸은 덮지 않는다. ③ 종합 분석 의견은 textarea 가 아니라
  원본과 같은 **AI 산출 HTML 렌더**(.aib/.ail/.ait, DOMPurify)로 바꿨다. 생성·수정은 하자처리 현황에서.
  df.ana 쓰기 경로는 제거(이 앱의 쓰기는 처리계획뿐).
- **공가세대·공가상가 토글**: 실제 출처는 게시본이 아니라 **siteConfig/{sid} 리프(showVacant·hasCommercial)**다
  — 원본 fb2SubSiteConfig 와 같은 규칙으로 구독해 S.org.sites 에 덮어쓴다(dfSubSiteCfg). 현장 관리 표의
  토글도 없는 필드(vacantUnits)에서 이 필드로 교체. 현장 탭(공가세대/공가상가)도 이 값이 실시간으로 정한다.
- **테마 전환**: 차트는 그릴 때의 토큰 색을 굽는다 — applyTheme 에서 하자 화면이면 rDefect() 재렌더.
- **목록 모달**: 원본 #mb.wide 와 동일하게 **88vw×88dvh**, 내부 flex 로 표가 남는 높이를 다 쓴다.
  'N건까지 표시 …' 안내 문구 삭제.
- **사이드바**: 하자처리 현황 아이콘을 선형→**막대형 차트**로(i-defect 심볼 교체). 권역 소분류는
  **기본 접힘**, 열어 둔 현장의 권역만 자동으로 편다(누르면 그 선택이 우선). 현장 점(.dot)의 우측 여백
  제거 — 점-이름 간격은 gap 8px 만.
- **현장 관리 숫자**: 세대수·동수·상가수를 #,##0 서식 텍스트 입력으로(콤마 제거 후 저장, 저장 시 재서식).
- **카드 제목**: .ct(회색)/.st(검정) 혼용을 **검정(--lbl)으로 통일**하고 크기를 14→15px 한 단계 위로.
- 검증: siteConfig 병합·탭 반영(s1 공가세대 off/공가상가 on)·분석 HTML 렌더·처리계획 값 왕복·다크 즉시
  재렌더·모달 1320×836(=88vw×88dvh@1500×950)·현장 관리 서식, 콘솔 오류 0. app.js?v=205 · APP_VER 1.8.1.

### 198차 — 제목 규격·피벗 파리티·상단바 스코프·자동 높이
- **레이아웃 제목 15px 앱 전체**: .tbt(상단바)·.set-h(설정)·.tm-h(조직/현장 관리)·.dp-lh-t(캘린더 우측)·
  .rp-week(주요 업무)를 15px·--lbl 로 통일(하자 .ct/.st 는 197차에 완료). set-h·tm-h 의 소문자 캡션풍
  (12px·uppercase)은 폐기.
- **상단바 기준월·인쇄가 다른 화면에 남던 버그**: .tb-rm{display:inline-flex}·.tb-nav{display:flex} 가
  hidden 속성을 이겼다 — `#tbRm[hidden],#tbPrintWrap[hidden]{display:none!important}` 로 고정.
  하자 관리에서만 보인다. 찾기(nq)는 원래 앱 전체(업무·코멘트·하자 현장·불러온 하자 목록) 검색이며
  모든 화면에서 동작 확인.
- **피벗 전면 재작성(원본 파리티)**: 행 칩 최대 3개(계층 트리·들여쓰기·그룹 행)·열 1개·값 선택
  (건수/평균 지연일)·%(비중) 병기 토글·헤더 클릭 정렬(라벨/각 열/합계)·합계 행·열·'(빈값)'·접수월 필드·
  피벗 상태에서 엑셀 누르면 피벗 표를 내보낸다(피벗_{제목}_{기준월}). 추가 메뉴는 openCtx 재사용.
  원본과 남은 차이: 칩 드래그 재정렬(FLIP)·우클릭 드릴다운 — 제거 후 재추가로 대신한다.
- **엑셀(목록)**: 원본 rec.export 와 동일 규격 — 파일명 `미처리목록_{제목}_{기준월}.xlsx` · 시트 '미처리' ·
  '엑셀로 내보냈습니다' 토스트. 필터·정렬 반영, 표시 건수 제한은 미적용(전체).
- **처리계획 칸 자동 높이**: 원본 autoResize 이식(dfPlanFit) — 입력·렌더 직후·실시간 수신 시
  scrollHeight 로 재계산, 숨김 상태면 측정 생략.
- 검증: 캘린더 상단바 기준월·인쇄 display:none, 하자에서 표시 · 찾기 전 화면 동작 · 3줄 계획 62px ·
  피벗 2단 행/평균 지연일/% 병기/정렬, 콘솔 오류 0 · audit FAIL 0 WARN 0. app.js?v=206 · APP_VER 1.8.2.

### 199차 — 공가 입력·기준월 변경·연도 셀렉터·인쇄·스냅샷
- **기준월 변경**: 상단바 기준월 칩을 누르면 reportIndex(게시월 인덱스 — 뷰어 선택기 전용 노드)를 읽어
  게시된 달 목록을 띄운다. 선택하면 S.dfRmSel 로 그 달의 게시본을 본다(최신은 ✓·'최신' 표기).
  하자 모듈의 데이터 경로는 전부 dfRm()=S.dfRmSel||ORG_RM 을 쓴다.
- **추이 연도 셀렉터**: 게시본 wks 는 게시 연도뿐 — 다른 해는 kpi/weekly(전 기간)를 wks 형으로 재구성
  (dfWksOfYear: 일요일 컷+기준월 말일 컷 / 대시보드는 dfDashWksOfYear: 컷 union carry-forward 합산).
- **공가 수 입력**: 원본 vmodal 과 같은 입력(미분양·미키불출·합계 자동)을 이식, report/{rm}/{sid}/vac 에
  쓴다. ⚠ 공가 수의 원 소스는 하자처리 현황 로컬(S.cmt) — 재게시하면 그쪽 값으로 덮인다(모달에 명시).
- **분석 의견**: .ait 에 원본 규격 들여쓰기(ul 20px·섹션 제목 15px) 적용, 'AI 분석은 …에서 생성' 부제 삭제.
  도넛 범례의 현장명에서 '힐스테이트' 접두어 생략(전체 이름은 툴팁).
- **인쇄(dfPrint)**: 원본 doPrint 의 화면식 재현 — 항상 밝은 색으로, 페이지마다 인쇄 헤더(팀명·제목·기준일).
  현장: 1p KPI+추이+전월대비/도넛+월별 → 장기미처리 → 공가세대 → 공가상가 → 공종별 → 분석(5~6쪽).
  대시보드: KPI+추이 → 전월대비/도넛+이슈/도넛 → 월별 → 현장별(4쪽). 페이지는 프레시 마크업으로 만들어
  화면 밖(#dfPrintPages)에 차트를 새로 그린 뒤 찍는다 — calapp 은 탭을 하나만 렌더하므로 원본처럼 라이브
  카드를 옮길 수 없다. 처리계획은 plan-print 텍스트로 복제, 빈 단은 숨김. 원본의 '보고서 양식(rpt)' 인쇄
  픽커·글꼴 선택은 이식하지 않음(화면식만) — 남은 차이로 기록.
- **스냅샷**: 설정 카드 '하자 관리 스냅샷' — 게시월 자료 전체(대시보드·전 현장 kpi/추이/도넛/공가/목록·
  처리계획·분석)를 LZ 로 박은 단일 HTML. 열면 로그인 없이 하자 화면만 뜨고 쓰기는 차단(토스트).
  구현 함정 둘: ① 인라인 시 </script> 리터럴이 스크립트를 조기 종료 — '</scr'+'ipt>' 로 조립.
  ② String.replace 의 '$' 치환 규칙이 벤더 압축 코드의 $' 를 만나 문서를 복제·훼손 — 함수 치환으로 회피.
  CSP 메타는 단일 파일에서 스스로를 막으므로 제거. 폰트(woff2)는 미포함 — 시스템 폰트로 폴백.
- 검증: 기준월 메뉴·연도 옵션·공가 저장 payload({미분양,미키불출})·부제 제거·현장 인쇄 5쪽/헤더 5/
  plan-print 6/차트 렌더·대시보드 인쇄 4쪽(월별 6행·현장별 3행·라이브 표 복원)·스냅샷 단독 부팅
  (defect 전용·차트·현장 진입·분석 렌더, 오류 0)·A4 PDF 416KB. audit FAIL 0 WARN 0.
  app.js?v=207 · APP_VER 1.9.0.

### 200차 — 아이콘·보고서 양식 인쇄 픽커·피벗 드래그/드릴다운 (원본 파리티 완성)
- **사이드바 아이콘**: 사용자 참조 이미지 스타일 — L자 축 + 채운 막대 3개 + 상승 지그재그 화살표.
  시안 8종(A~H)을 실측 크기로 비교해 G안 채택, i-defect 심볼 교체(막대 fill 은 currentColor).
- **인쇄 픽커(원본 openPrintPick 이식)**: 인쇄 버튼 → '화면 그대로 / 보고서 양식' 선택 모달
  (#mb.wide-pick 620px · 썸네일 · 보고서 글꼴 기본/맑은 고딕/바탕, localStorage 'calapp.rptFont').
  화면 그대로 = dfPrint(199차), 보고서 양식 = **dfPrintReport**.
- **보고서 양식(rpt) 전체 이식**: 원본 rptDashboard/rptSite 를 게시본 데이터로 포트 —
  calc→DF.kpi(=calc 전체), dashSites→dfDashSites, moSnaps→dfMoSnaps*, tblMetrics→dfMetrics,
  S.cmt 계획→dfPlanGet(지난달=dfPrevMonth), S.ana→DF.ana, dashCoAgg→dfDashCoAgg(대시 표와 공용 추출).
  구성 동일: 대시보드 2쪽(종합/추이 SVG/이슈/월별 + 분포 도넛/현장별/업체별, 밀도 dense·dense2 자동),
  현장 3쪽(+장기미처리·공가세대 처리계획 표·AI 분석 재서식 rptAI). rpTrend/rpDonut SVG·rptFit 실측 축소·
  rpt CSS(고정 문서색·f-sys/f-serif)·@page 덮기(rptPageCSS)·#rptRoot 화면 밖 조립 전부 원본 그대로.
  ⚠ #rptRoot{position:fixed;left:-10000px} 줄이 .rpt 블록 밖에 있어 추출에서 빠졌던 것 복원.
- **피벗 칩 드래그 재정렬**: 원본 pvFlip(FLIP 보간)·pvCommitOrder 이식 — 행 칩 draggable,
  드래그로 끝난 제스처는 클릭 제거로 치지 않게 pvDragMoved 가드.
- **피벗 우클릭 드릴다운**: 행경로(data-rp)+열값(data-cv) → '해당 목록 보기'가 REC.vals 로 필터를 걸고
  목록 모드로 전환('(빈값)'→'(미기재)' 매핑). '__month' 같은 피벗 전용 키는 recCompute 값 필터가
  pivCell 로 값을 뽑아 처리. '값 복사' 병행.
- 검증: 픽커 2옵션/글꼴 3종/wide-pick · 대시 보고서 2쪽 7섹션 SVG(PDF 472KB) · 현장 보고서 3쪽
  (처리계획 텍스트·AI 재서식, PDF 497KB) · 1쪽 렌더 육안 대조(원본 규격 일치) · 드래그 커밋
  (rows 순서·표 머리 갱신) · 드릴다운(2조건 필터 69/87건) · 콘솔 오류 0 · audit FAIL 0 WARN 0.
- **이로써 원본과 남은 의도적 차이는 업로드·게시·AI 분석 생성(관리자 앱 전용) 뿐이다.**
  app.js?v=208 · APP_VER 2.0.0.

### 201차 — 실사용 대조 보정(목록 머리·화면식 인쇄·보고서·분석 서식)
- **목록 모달 머리 재구성**: 원본과 같은 한 줄 — 제목 · (우측) 돋보기 검색창 · 결과 N/전체 N ·
  표 복사/엑셀/피벗(피벗 중엔 '목록')/닫기. 검색 칸이 #mt 로 올라가 본문만 다시 그리므로 커서가 튀지
  않는다. 밴드 줄에 **공가상가 칩** 추가(공가세대와 함께, 있을 때만), 숨긴 열 버튼도 이 줄로.
  #mb.dfwide 에서는 우상단 X 를 숨긴다(머리에 닫기가 있다).
- **화면식 인쇄(dfPrint) 원본 규칙에 맞춤**: 대시보드 쪽 묶음을 원본과 동일하게 —
  1쪽 KPI·추이·전월대비/현장도넛·이슈/공종도넛, 2쪽 월별+현장별(총 2쪽).
  ⚠ `#dfPrintPages{width:auto}` 로 두면 캔버스가 뷰포트 폭으로 그려진 뒤 페이지 폭에 눌려 **차트가 잘리고
  주차 라벨이 겹친다** — 화면·인쇄 모두 720px(A4 인쇄영역 192mm) 고정으로 바꿔 그 폭에서 리사이즈되게 했다.
  추이 x축은 카테고리 폭에 따라 글자 크기를 줄이고(10→7) autoSkip·maxRotation 0 을 켰다(화면에도 적용).
  인쇄 도넛 범례는 1열로 편다(화면 규칙이 뒤에 있어 !important 필요), 카드 밀도 조정으로 1쪽에 담김.
- **보고서 양식**: ① 처리계획 셀이 좌우로 깨지던 원인 — 이 앱의 **전역 `.plan`(일정 카드) 규칙이
  보고서 `td.plan` 으로 새어 들어왔다**(원본엔 없는 클래스 충돌). `.rpt td.plan` 에 display/border/background
  등을 명시해 차단. ② 공종·유형별 도넛이 비던 원인 — 게시본 `ul` 은 상위 300건 캡이라 분모(전체 미처리)와
  표본이 어긋난다. 분모를 표본 합으로 잡고 캡션에 '(표본 N건)' 표기. ③ 글꼴에서 **바탕 삭제·돋움 추가**.
- **종합 분석 의견 서식**: 199차에 넣었던 `font-weight:800·15px` 규칙이 원본보다 굵게 만든 원인 —
  원본 규격(14px/1.7, ul disc 24px, li 8px 간격·마커 --b500, >div 여백만)으로 교체.
- **처리계획 빈 칸**: textarea `rows="1"` — 내용이 없으면 한 줄(32px).
- **공종별 표 접기**: '펼치기'를 배지(파란 pill)로.
- 검증: 모달 머리(피벗↔목록 토글·카운트 숨김·칩) · 대시 인쇄 2쪽/차트 정상/범례 1열 · 현장 인쇄 5쪽 ·
  보고서 3쪽(처리계획 세로 스택·도넛 채움) · 글꼴 3종(기본/맑은 고딕/돋움) · 배지 · 빈 칸 32px ·
  콘솔 오류 0 · audit FAIL 0 WARN 0. app.js?v=209 · APP_VER 2.0.1.

### 202차 — 실인쇄물 대조 보정(제목·목록·피벗·보고서)
- **레이아웃 제목 14px 회복**: .tbt/.set-h/.tm-h/.dp-lh-t/.rp-week 는 14px 로 되돌리고, **하자 관리 카드
  제목(.ct/.st/.lm-ttl)과 목록 모달 제목(.rl-ttl)만 15px** 유지.
- **목록(미처리 보기)**: 머리에서 '표 복사' 버튼 삭제(우클릭 '표 전체 복사'가 같은 일을 한다 — 목록 표에서는
  필터·표시건수를 반영한 recCopy 로 연결). 표 규격을 원본에 맞춤 — 머리 가운데 정렬·색 --lbl·1.5px --sepo
  밑줄·높이 34px, 본문 가운데 정렬·vertical-align top(접수내용만 왼쪽). 장기미처리 목록에서는 '30일 미만'
  칩을 숨기고, 피벗 중에는 표시건수를 감춘다(원본과 동일).
- **피벗**: 기본 조합을 원본과 동일하게 — 팀 전체는 행 현장 × 열 공종, 한 현장은 행 시공업체›공종(열 없음).
  칩 마크업도 원본식(라벨 + × 버튼, 추가는 '+')으로 바꾸고 드래그는 행이 2개 이상일 때만 활성.
- **도넛 범례**: '힐스테이트'가 앞·중간·뒤 어디에 붙어도 떼는 dfShortSite() — 갑천1 트리풀시티 힐스테이트 등.
- **화면식 인쇄**: 현장 KPI 첫 카드를 원본 인쇄와 같이 '현장규모 N세대 / 개동·준공일'로(화면은 현장명 유지),
  @page 우하단 쪽번호 추가.
- **보고서**: 글꼴에서 돋움 삭제(기본·맑은 고딕). 처리계획 표의 값 글자를 머리와 같은 7.5px 로 낮추고,
  0.5px 라 축소 인쇄에서 사라지던 지난달/이번 달 구분선을 1px 점선으로. 공종·유형별 도넛 캡션은
  '(표본 N건)' → '· 최근 N건'(게시본 미처리 목록이 상위 300건까지만 실리는 데서 온 표기).
- **주요 이슈 카드 펼치기**: 게시본은 insCleanHTML 로 상세(.insd)와 data-act 가 제거된 정적 HTML 이라
  이 앱에서는 펼칠 수 없다. 오해를 부르던 '펼치기' 툴팁만 제거. **게시 쪽에서 상세를 함께 실어야 가능**.
- app.js?v=210 · APP_VER 2.0.2.

### 203차 — 실규모 재현 검증 · 목록 정렬 · 유형 분포 전체화
- **⚠ 검증 시드가 작아 인쇄 결함을 못 잡고 있었다.** 3현장·26주 시드로는 재현되지 않던 문제들이
  실사용 규모(12현장·30주·미처리 3만+)에서만 나타났다. `shots/pub-big.json` + `big.mjs` 로
  **실규모 시드 하네스**를 만들어 이후에는 이 규모로 인쇄를 확인한다.
  재현·수정한 것: ① 추이 데이터라벨이 6자리에서 겹침 → dfMoDLCfg 가 자릿수까지 보고 크기를 줄이도록
  ② 인쇄 도넛 범례 2열이 좁은 폭에서 서로 겹침 → 1열·6.5pt·행높이 11px 로 펴서 카드 높이도 낮춤
  ③ 그 결과 대시보드 인쇄가 3쪽 → **원본과 같은 2쪽**, 현장 6쪽.
- **목록 정렬**: 팀 전체는 현장별로 받아 오므로 그대로 두면 현장 순으로 묶인다 — **접수일 최신순** 정렬을
  기본으로 넣었다(31,527건 첫 행 2026-07-26 확인).
- **보고서 공종·유형별 도넛**: kpi.ul 은 게시본 폴백(상위 300건 캡)이고 **전체 목록은 ulz** 에 실린다.
  dfList(ulz)를 미리 받아 전체 미처리 기준으로 집계하도록 바꾸고 '표본/최근 N건' 문구를 없앴다.
- **피벗**: 칩 선택 영역을 원본과 같이 회색 박스(pv-bar 배경·테두리)로, 칩은 흰 배경·30px·× 버튼,
  추가는 30px 정사각 '+'. 합계 행을 sticky bottom 으로 고정하고 행 라벨·합계 열도 좌우 고정.
- 검색창 미리보기 글씨 볼드 해제(::placeholder font-weight:400).
- app.js?v=211 · APP_VER 2.0.3.

### 204차 — 도넛 범례 붕괴·피벗 표 규격·배경화면
- **도넛 범례가 도넛 위로 겹치던 원인**: 원본은 범례가 기본 1열(flex)이고 자리가 있을 때만 `.lg-2col`
  (2열)을 붙이는데, 이식본은 항상 2열 grid(--lgr)여서 창이 좁아지면 범례 칸이 90px 로 찌그러져 넘쳤다.
  원본 규칙대로 기본 1열 + `dn-side` 폭 620px 이상이고 항목 8개 이상일 때만 2열. `.opsr>.card{min-width:0}`
  로 카드도 줄어들게 해 화면이 넘치지 않는다. 인쇄는 항상 1열.
- **피벗 표**: 열이 많으면 좁히지 말고 가로 스크롤(`width:max-content;min-width:100%`, 열 최소 78px·
  행 라벨 180px), 머리 800 굵기·1.5px 밑줄, 합계 행 음영(surf3)+sticky.
- **목록**: 접수내용 열만 좌측 정렬·줄바꿈 허용(나머지는 가운데).
- **조직/현장 관리**: 제목 옆 '· 팀명' 삭제(사이드바 팀 선택기와 중복). ⚠ 라벨만 지우고 `t` 참조를 같이
  지워 rOrg 가 죽었던 실수 — 스모크가 잡았다.
- **사이드바**: 현장 앞 점 6px → 5px.
- **앱 배경화면(설정)**: 이미지를 골라 localStorage 에만 저장(4MB 제한) — 데이터베이스·팀원 화면과 무관.
  본문 뒤에 깔고 그 위를 배경색으로 덮어 대비를 지키며(덮는 정도 슬라이더), 사이드바는 유리로. 인쇄 제외.
- **레이아웃 개편은 시안만 제출**(`shots/layout-proposal.png`) — 플로팅 사이드바, 블록 분리(헤더/기능
  버튼/팀/메인 분류/관리/프로필), 페이지 헤더 제거·기능 버튼 사이드바 이동. 승인 후 반영.
- app.js?v=212 · APP_VER 2.1.0.

### 205차 — 레이아웃 개편 반영(플로팅 사이드바·페이지 헤더 제거)
- **사이드바가 블록 묶음**이 됐다. `#sidebar` 는 투명·간격(8px)만 갖고, 그 안의 여섯 블록
  (`.sbh` 헤더 / `.sb-tools` 기능 버튼 / `.teamsel` 팀 / `.sbsc` 메인 분류 / `.sb-bottom` 관리 /
  `.sb-foot` 프로필)이 각각 유리 카드다. 구분선(sb-foot::before, teamsel border)은 블록 간격이 대신한다.
- **페이지 헤더 삭제**: `#topbar{display:none}` — 화면 이름은 사이드바 선택 상태로 안다. 모바일(≤900px)
  에서는 메뉴 버튼 때문에 남긴다. 헤더 우측 기능부(`.tbr`)는 **DOM 을 통째로 사이드바 기능 블록으로 옮겼다**
  (id·구조 유지 → dfTopbar·알림 팝오버·찾기 토글이 그대로 동작). 버튼은 **아이콘만**(찾기 글자 숨김).
- **접힘(mini) 상태**: 기능 블록이 '+' 버튼 하나로 줄고, 누르면 사이드바 오른쪽에 아이콘 팝업이 뜬다
  (`nav.tools`, 바깥 클릭이면 닫힘). 모바일 드로어에서는 접힘이 무의미하므로 팝업 없이 펼친다.
- 본문(`#main`)도 카드가 됐다(배경화면을 넣으면 유리 위에 뜬다). 모바일에서는 여백·모서리를 없앤다.
- app.js?v=213 · APP_VER 2.2.0.

### 206차 — 접힘 상태 다듬기·본문 카드 제거·배경 자동 축소
- **접어도 분류가 보인다**: `.sbsl` 에 긴 이름/짧은 이름을 함께 심어(`sbsl-long`/`sbsl-short`)
  접히면 '업무'·'하자'·'관리'로 줄이고 가운데 정렬. 아이콘(.nvi)·프로필도 접힘 상태에서 가운데로 모은다.
- **접기/펼치기 흔들림**: 폭(#sidebar width)만 애니메이션하고 라벨·아이콘 칸의 transition 을 없앴다.
  전에는 .sbsl opacity·.nil opacity·.nic background 가 각자 .22s 로 따라 움직여 글자가 늘었다 줄었다 했다.
  로고 블록도 opacity 0 대신 display:none — 자리를 차지한 채 사라져 헤더가 밀리던 것.
- **기능 버튼**: 블록 안에서 가운데 정렬.
- **팀 이름**: 'H서비스중부팀' → '중부'(teamShort — '…서비스(가운데)팀' 꼴이면 가운데만).
- **본문 카드 제거**: #main 배경·테두리·그림자를 없애고 #content 패딩도 0 — 화면을 꽉 채운다.
  여백은 #app 의 10px 뿐(모바일은 12px 패딩).
- **배경 이미지 4MB 제한 → 자동 축소**: 브라우저 로컬 저장이 보통 5MB 남짓이고 dataURL 은 원본보다
  약 33% 커져서 걸어 둔 제한이었다. 이제 긴 변 2560px·JPEG 0.86 으로 줄여 저장하므로(넘치면 0.7 재시도)
  큰 사진도 그냥 고르면 된다.
- app.js?v=214 · APP_VER 2.2.1.

### 207차 — 배경화면 실동작·사이드바 구분선·캘린더 프레임
- **배경이 안 보이던 원인**: `#app{background:var(--bg)}` 가 body 뒤 배경 레이어를 덮고 있었다.
  `body.hasbg #app{background:transparent}` 로 열고, 배경을 켜면 **토큰(--bg2/--surf2/--surf3/--glass3)을
  반투명으로 바꿔** 카드 전체가 한 번에 유리가 되게 했다(다크도 별도 값). 배경 자체는 saturate .88 ·
  contrast .92 로 눕히고, `#app::after` 에 SVG feTurbulence 그레인(opacity .055, overlay)을 얹었다.
- **분류 구분선**: `.sbsl:not(:first-child)` 위에 1px 선 — 접었을 때도 묶음이 보인다.
  접힘 라벨은 축약하지 않고 '업무 관리' 그대로(줄바꿈 허용) — 205차에 잘못 줄였던 것 원복.
- **하자 기능 버튼 위치**: `#tbRm`·`#tbPrintWrap` 에 order 8/9 — 하자 관리에서만 생기는 버튼이
  기존 버튼 오른쪽에 붙는다.
- **본문이 하단까지**: `#content` 를 flex 로 두고 활성 뷰가 남는 높이를 차지, `.cal-wrap`·`.tkwrap` 의
  `height:calc(100dvh - var(--th) - 32px)` (없어진 헤더 높이를 빼던 계산)를 `flex:1` 로 교체.
- **캘린더**: 본문 카드를 없앤 뒤 외곽이 사라져 `.cal-card` 테두리·배경·모서리를 되살렸고,
  연·월 버튼과 이전/오늘/다음 묶음에 위젯처럼 채움 배경을 넣었다.
- app.js?v=215 · APP_VER 2.2.2.

### 208차 — 배경 정책 정리·달력 프레임·접힘 안정
- **달력**: 카드로 감싸지 않고 **격자(#fcal)에만 테두리**(1px --sepo, 8px 모서리)를 줘 배경과 구분한다.
- **배경 정책**:
  · 사이드바는 배경을 켜면 **한 장의 유리판**이 된다(블록 사이로 배경이 새 보이던 문제) — 내부 블록은
    투명 + 상단 1px 구분선.
  · **하자 관리 화면(#view-defect)은 항상 불투명** — 수치를 읽는 화면이라 배경이 비치면 안 된다.
  · 설정의 '배경 흐리기'를 없애고 **레이아웃 투명도(40~100%)** 로 바꿨다(--app-card-alpha 로 토큰 알파 조절).
  · **카드마다 걸던 backdrop-filter 를 걷어냈다** — 하자 화면처럼 카드·표·차트가 많은 화면에서 눈에 띄게
    느려지던 원인. 흐림은 사이드바에만 남긴다.
  · 배경 전환은 body::before 를 상시 두고 opacity .45s 로 페이드.
- **팀 이름**: 펼치면 원래 이름 그대로, 접으면 줄인 이름(teamShort)만 작은 칩으로.
- **접기/펼치기 위치 고정**: 접힘 상태에서도 블록 패딩 8 + 항목 패딩 8 = 아이콘 x 16px 로 펼침과 동일
  (mini 폭 64 = 16+32+16 이라 자연히 가운데). 전에는 mini 에서 패딩을 4/0 으로 바꿔 아이콘이 튀었다.
- 업무 목록·캘린더 래퍼에 `overflow:hidden`·`height:100%` — 열이 화면 밖으로 밀리지 않는다.
- 설정 문구 정리: '팀원 화면·데이터에는 영향 없음' 삭제, '지금 게시월을 문서로 굳히기' →
  '현재 게시월 기준으로 스냅샷 추출'. 사이드바 현장 점 5px → 4px.
- app.js?v=216 · APP_VER 2.2.3.

### 209차 — 달력 위젯 규격·사이드바 분리 유지·스크롤 범위
- **달력은 위젯과 같은 카드 프레임**(.card 규격)으로 되돌렸다. 208차에 격자에만 테두리를 준 것은
  잘못 이해한 것.
- **요일 헤더의 흰 줄**: `.fc-col-header-cell` 의 좌측 테두리가 `2px solid transparent` + background-clip
  padding-box 라 뒤 카드 배경(흰색)이 그대로 비쳤다. 본문 격자와 같은 #DCDFE6 으로 맞췄다(헤더 하단선도).
- **사이드바는 배경을 켜도 블록을 합치지 않는다** — 208차에 한 판으로 합쳤던 것을 되돌리고, 각 블록에만
  흐림을 건다.
- **접힘 팀 칩이 안 보이던 이유**: 파일 뒤쪽에 남아 있던 옛 규칙
  `#sidebar.mini .teamsel>*{display:block;visibility:hidden}` 이 새 규칙을 덮고 있었다. 정리.
- **스크롤 범위**: `#content{overflow:hidden}` — 페이지 자체는 스크롤하지 않는다. 캘린더·업무 목록은
  각 레이아웃(카드) 안에서만 스크롤하고, 세로로 긴 화면(하자·조직·설정·주요 업무)만 자체 스크롤을 켠다.
- **찾기 패널 위치**: 헤더 기준(`top:var(--th); right:16px`)이라 엉뚱한 곳에 떴다 — 버튼이 사이드바로
  옮겨졌으니 사이드바 오른쪽(`left:var(--sw)+18px`, 접히면 --swm 기준)에서 뜬다.
- app.js?v=217 · APP_VER 2.2.4.

### 210차 — 업무 목록 높이 사슬·사이드바 배경판·차트 모션
- **업무 목록이 화면 밖으로 밀리던 진짜 원인**: 높이 사슬이 `#tkRoot` 에서 끊겨 있었다
  (뷰는 flex:1 인데 그 안의 #tkRoot 가 콘텐츠 높이). `#tkRoot{flex:1;min-height:0;display:flex}` 를 넣고
  `.tkcol`·`.tk-split`·`.tkmain` 을 height 100%·min-height 0 으로 이어 붙여 **각 카드 안에서만** 스크롤한다.
  (측정: tkwrap 486→880px, tkmain 132→817px, 페이지 스크롤 없음)
- **사이드바에 배경이 비치던 것**: 블록을 합치지 않고, 사이드바 뒤에 앱 배경색 판(`#sidebar::before`,
  inset -6px)을 깔아 사진이 블록 사이로 보이지 않게 했다. 블록 분리는 그대로.
- **달력**: 위젯과 같은 배치를 쓰되 카드 프레임(테두리·배경·그림자·패딩)은 두르지 않는다.
- **차트 모션 최적화**: 첫 진입에 추이 1 + 도넛 2가 동시에 520ms 씩 움직여 끊겼다. 기본 지속을 280ms
  (도넛 260ms)로 줄이고, 첫 그림이 끝나면 `DF.warm` 을 세워 **이후 재렌더·탭 전환에는 애니메이션을 끈다**.
  측정: 대시보드 재렌더 81ms · 현장 진입 203ms · idle 62fps.
- app.js?v=218 · APP_VER 2.2.5.

### 211차 — 사이드바 판·접힘 중앙정렬·달력 격자 투명·모션 원복
- **사이드바 배경**: 의사요소(::before) 판은 상황에 따라 안 깔렸다 — **사이드바 자체에 불투명 배경**
  (var(--bg), 6px 패딩, 둥근 모서리)을 주고 그 위에 블록을 얹는 방식으로 바꿨다. 블록 분리는 유지.
- **접힘 정렬**: 모든 블록(헤더·기능·팀 칩·분류·관리·프로필)의 내용물을 가운데로. 뒤쪽에 남아 있던
  `justify-content:flex-start` 규칙과 `.sb-acct{flex:1}` 이 왼쪽으로 밀던 원인.
- **달력 격자**: 칸 사이(가로·세로)와 요일 헤더 구분선을 **투명 + background-clip:padding-box** 로 되돌려
  뒤 배경이 그대로 비친다(위젯과 같은 방식). 210차에 #DCDFE6 으로 칠했더니 진한 헤더 위에서 흰 줄처럼
  보였다. 카드 프레임도 없어 달력이 곧 외곽.
- **차트 모션 원복**: 210차에 넣은 DUR 단축(280/260)과 DF.warm(재렌더 시 애니메이션 끄기)을 되돌려
  520ms 원래 동작으로. 첫 진입 부담은 다른 방법(렌더 순서·지연)으로 다시 볼 것.
- app.js?v=219 · APP_VER 2.2.6.

### 212차 — 배경 반투명 적용 범위 정리
- **사이드바 뒤판 삭제**: 211차에 깐 불투명 판(사이드바 자체 배경)을 없앴다 — 블록만 카드로 남는다.
- **달력은 배경을 켜면 위젯 유리 톤**: #fcal 을 한 장의 판(--bg2)으로 두고 셀 배경은 투명,
  격자선은 rgba(60,60,67,.14), 요일띠는 rgba(120,130,150,.14). 위젯의 glass 규칙과 같은 방식.
- **반투명에서 빼는 것들**: 모달(#mb)·버튼(.btn·cal-nb·cal-title·tb-ic·tb-srch·sb-more)·목록 표(.rec-tbl)·
  피벗 표·컨텍스트 메뉴·팝오버·찾기 패널·하자 관리 화면. 이들 스코프에서 토큰(--bg2/--surf2/--surf3)을
  불투명 값으로 되돌려 상속으로 한 번에 처리했다(다크 별도).
- **접힘 분류 라벨**: 줄바꿈되던 '업무 관리' → 접히면 '업무'·'하자'·'관리' 한 낱말만(nowrap).
- 사이드바 첫 분류 위 여백 축소(padding-top 2px).
- app.js?v=220 · APP_VER 2.2.7.

### 213차 — 요일 헤더 흰 줄 근본 원인·사이드바 판·달력 판
- **요일 헤더의 흰 줄(3회차 만에 원인 확정)**: 헤더 셀 사이를 `border-left:2px solid transparent`
  + background-clip:padding-box 로 둬서, 그 자리에 **뒤에 있던 흰 판(#fcal/카드 배경)이 그대로 비쳤다**.
  색을 바꿔 칠하는 것으로는 해결되지 않는다(진한 헤더 위에서 어떤 색이든 줄로 보임) — **헤더 셀의
  좌우 테두리를 아예 없애** 요일띠를 한 장으로 만들었다. 본문 격자선도 투명 대신 고정색
  (가로 rgba(60,60,67,.12) / 세로 .10)으로 바꿔 같은 현상을 막았다.
- **달력 판**: #fcal 에 배경(--bg2)·모서리·overflow hidden 을 상시 부여해 배경 유무와 관계없이
  같은 톤(위젯과 동일 방식)으로 보인다. 배경을 켜면 셀은 투명, 판만 유리.
- **사이드바**: 블록 사이로 배경 사진이 비치지 않도록 사이드바 영역을 앱 배경색으로 덮되
  **모서리를 없애(border-radius:0)** 판이 카드처럼 보이지 않게 했다. 블록 분리는 그대로.
- app.js?v=221 · APP_VER 2.2.8.

### 214차 — 화면(HTML) 캐시 문제·달력 불투명
- **⚠ 스타일이 반영되지 않던 구조적 원인**: 캐시 버스터는 `app.js?v=NNN` 뿐인데 **CSS 는 index.html 안에
  들어 있다.** 브라우저가 index.html 을 캐시하면 앱(JS)만 새로 받고 화면은 옛 스타일 그대로다 —
  '고쳤는데 아무것도 안 바뀐' 상황이 여기서 나온다. ① index.html 에 no-cache 메타 3종 추가
  ② 설정의 버전 줄에 **실제 로드된 화면 버전**을 함께 표시(`버전 2.2.9 · 화면 v222 · 기록된 오류 N건`) —
  이 숫자가 zip 의 ?v= 와 다르면 캐시된 화면을 보고 있는 것이므로 강력 새로고침이 필요하다.
- **달력**: 배경 투명도의 영향을 받지 않도록 스코프에서 토큰을 불투명으로 되돌렸다(위젯처럼 제 색 유지).
- app.js?v=222 · APP_VER 2.2.9.

### 215차 — 설정 화면 배경 가독성
- 설정 2열 중 좌측에 카드가 하나뿐이라 배경 사진이 그대로 드러났다 — **앱 배경화면 카드를 좌측으로** 옮겨
  두 열의 높이를 맞췄다.
- 배경 위에서 카드가 흐려 글자가 묻히던 문제: 기본 투명도를 80→**90%**, 슬라이더 하한 40→60%로 올리고,
  카드에 밝은 테두리(rgba(255,255,255,.6))와 그림자를 넣어 경계를 또렷하게 했다.
- app.js?v=223 · APP_VER 2.3.0.

### 216차 — 사이드바 투명·피벗 원본 CSS·토글 렉·기준월 토글 (⚠ 상당수가 무효였음 — 217차 참조)
- **사이드바 블록 사이는 배경이 그대로 비친다**(판 제거). 카드 테두리를 흰색으로 바꿔 우측 일정 패널의
  파란 날짜 헤더 둘레에 **흰 테두리**가 생겼던 것 — 원래 테두리 색으로 되돌리고 그림자만 남겼다.
- **피벗 표는 원본 CSS 를 그대로 옮겼다**(border-collapse:separate, 셀 우/하 1px, padding 7px 14px,
  머리 가운데·sticky, 합계 열 --bt1 볼드 + surf2, 합계 행 sticky bottom).
- **사이드바 접기/펼치기 렉**: 폭이 0.22초 동안 계속 바뀌며 Chart.js 가 매 프레임 리사이즈된다 —
  토글 동안 `responsive=false` 로 멈추고, 끝난 뒤 한 번만 `resize()`.
- **기준월 메뉴**: 열려 있으면 다시 눌러 닫힌다(토글).
- **하자 화면 간격**: 카드 패딩 16/18, 카드·KPI·탭·2열 간격을 모두 12px 로 통일(앱 기준).
- 설정 카드 순서 원복(메일 / 백업·기록 → 앱 배경화면 → 스냅샷 → 위젯).
- app.js?v=224 · APP_VER 2.3.1.

### 217차 — 216차 '고쳤다'가 전부 무효였던 원인 규명과 근본 수정
**교훈: 216차 항목들은 기록만 남고 렌더 검증이 없었다 — 선언 순서·이벤트 순서 때문에 전부 무효였다.**
- **게시월 토글이 안 되던 진짜 이유**: 메뉴 닫기가 mousedown **캡처**라, 버튼을 다시 누르면
  ①mousedown 에서 먼저 닫히고 ②click 의 df.rm 은 '이미 닫힘'을 보고 다시 열었다 — 계속 열려 있던 것.
  openCtx 에 anchor(연 버튼) 인자를 추가해 그 버튼 위 mousedown 은 닫지 않고 click 토글이 닫는다.
- **하자 간격 통일이 무효였던 이유**: 통일 규칙이 원본 이식 CSS 보다 **앞**이라 같은 명시도의 뒤 규칙에
  전부 밀렸다(§5의 함정 그대로). 앞쪽 공통 블록을 지우고 **뒤쪽 개별 정의값을 직접 12px 로** 고쳤다
  (KPI gap·mb, tnav mb, sh mb, ltrmom-head mb, 카드 패딩 16/18).
  추이 카드 제목 아래 6px 는 원본의 의도적 인라인 예외 — 유지.
- **파란 날짜 헤더 흰 테두리의 잔존 이유**: 216차에 되돌린 건 흰색 테두리뿐, `.card` 의 회색 테두리+흰 배경이
  그라디언트 둘레에 밝은 1px 로 남는 **구조**가 원인(픽셀 스캔으로 확정). `.dp-filtercard` 에서
  테두리를 없애고 그라디언트를 카드 배경으로 올렸다(.dp-top 은 배경·라운드 제거).
- **사이드바 토글 차트 재렌더의 진짜 원인**: `responsive=false` 는 **이미 붙은 ResizeObserver 를 막지 못한다**
  (옵션만 바꿔선 attach/detach 가 안 일어난다 — 216차 방식 자체가 무효). duration 을
  `()=>DF.noAnim?0:520` 함수로 바꿔(옵션은 그릴 때마다 lazy 로 읽힘) 토글 동안 플래그만 세우면
  옵저버가 몇 번을 발화하든 즉시 상태로 그려진다 — 크기만 따라오고 솟는 모션 0(캔버스 시계열로 검증).
  도넛도 `()=>DF.noAnim?0:1000`. 첫 진입·탭 전환 애니메이션은 그대로.
- **피벗 합계 열 우측 고정**(원본 동작): td/th.pv-tot 에 sticky right + 좌측 경계선(--sepo).
  교차 z: 머리 좌/우 4 · 본문 좌/우 2 · 합계행 좌/우 3. 가로 스크롤은 pv-scroll 이 원래 담당.
- **페이드가 sticky 표 머리를 흐리게 하던 것**: 마스크를 `--sbft`(위)/`--sbfb`(아래) 오프셋 기반으로 바꾸고,
  fadeOne 이 **직계** `:scope > table > thead` 와 `tr.pv-totrow` 높이를 재서 넣는다 — 페이드가 머리
  아래·합계행 위에서 시작한다(직계 한정: #content 가 안쪽 일반 표를 잘못 잡지 않게).
- **달력 = 위젯 동일화**: 210차에 벗긴 카드 프레임이 반복 지시가 관철되지 않던 원인 — 위젯 규격
  (라운드 8 · 패딩 11px 12px · 테두리·배경·그림자)을 데스크톱에 복원하고 폰트 12px ·
  숫자 패딩 3px 5px 1px 까지 위젯 값 그대로. 모바일 풀블리드는 별개 유지.
- 검증: 정적 감사 FAIL 0 · WARN 0(위임 액션은 closest/change/부정형 가드 경로까지 수집해야 오탐이 없다),
  스모크 14항목 통과(6뷰·42셀·위젯 42셀·토글 왕복·콘솔 0).
- app.js?v=246 · APP_VER 2.3.2. (직전 배포 zip 이 v245 인데 내부가 ?v=224 로 어긋나 있었다 —
  **zip 이름만 올리고 내부를 안 맞추면 §의 화면 캐시 문제를 그대로 재현한다**. 세 값은 반드시 함께.)

### 218차 — 217차 피드백 6건 반영
- **달력 카드 제거**: 217차에 '위젯과 동일'을 카드 프레임 복원으로 **잘못 해석**해 씌운 것 — 사용자 지시로
  벗겼다(테두리·배경·그림자·패딩 0). 판은 #fcal 자체(배경·라운드 8)가 담당. 위젯과 맞춘 값은
  글자 12px·숫자 패딩 3px 5px 1px 만 유지. ⚠ 앞으로 '위젯과 동일'에 카드 프레임은 포함되지 않는다.
- **사이드바 블록 사이 투명**: 216차에 '판 제거'라 기록했지만 `body.hasbg #sidebar{background:var(--glass3)}`
  한 줄이 남아 사이드바 전체가 한 장의 유리였다(또 하나의 기록-실제 불일치). 그 줄을 지우고
  유리(반투명 --glass3 + blur 18px)는 **각 블록**(#sidebar>*)에만 — 블록 사이로 배경이 그대로 비친다.
  '카드마다 blur 금지' 주석은 하자 화면(카드 수십 장) 맥락이고, 블록 6개 수준은 체감 부담 없음.
- **현장 요약 탭 추이 카드 아래가 넓던 이유**: 현장 탭 본문은 `.as{gap:12px}` 로 묶이는데 공용 추이 카드가
  mb12 를 갖고 있어 **gap 12 + margin 12 = 24px 이중 간격**(opsr 는 인라인 margin-bottom:0 으로 이미
  상쇄돼 있었고 추이 카드만 누락). `.as>.mb12{margin-bottom:0}` 으로 상쇄 — 대시보드(래퍼 없음)의
  mb12 는 그대로 산다(실측: 현장 12px · 대시보드 12px).
- **피벗 가로 스크롤 수단**: 전역 스크롤바 숨김 규약 때문에 가로로 넘친 표를 넘길 방법이 없었다.
  ① .pv-scroll·.rec-wrap 만 예외로 가로 스크롤바 8px(세로는 webkit width:0 으로 규약 유지 —
  scrollbar-width 는 축 분리가 안 되고 auto 가 아니면 크롬이 webkit 의사요소를 무시하므로 auto 로 풀었다)
  ② 세로 여지가 없고 가로만 넘칠 때 휠을 가로로 변환(wheel 위임, Shift·트랙패드 가로 입력은 기본 동작).
  ⚠ headless 검증 환경은 스크롤바를 아예 그리지 않아 ①의 시각 확인은 불가 — ②를 보험으로 두고
  ②의 동작(dispatch wheel → scrollLeft 이동)으로 검증했다.
- **미처리 보기 페이드 제거**: 목록(rec-wrap)·피벗(pv-scroll)은 fadeOne 에서 제외(사용자 지시).
  217차의 sticky 오프셋(--sbft/--sbfb) 로직은 다른 sticky 표 컨테이너를 위해 유지.
- **검색창 확장**: 하자 관리 밖에서는 기준월·인쇄가 빠져 기능 블록이 널널하다 — dfTopbar 가
  .sb-tools 에 df-on 을 토글하고, df-on 이 아니면 검색 버튼이 flex:1 로 남는 폭을 채우며 '찾기'
  텍스트를 보인다(실측 32→152px). 접힘 팝업(mini)은 아이콘형 유지.
- 검증: 정적 감사 FAIL 0 · WARN 0, 스모크 14항목(달력 기대치는 카드 미장착으로 갱신 — §6 '구조를
  바꾸면 기대치도 함께'), hasbg 스크린샷으로 블록 사이 배경 투과 확인.
- app.js?v=247 · APP_VER 2.3.3.

### 219차 — 위젯 회귀 규명(잠복 버그 2건) · 배경 대비/그레인 슬라이더
- **위젯에 배경 사진·밝은 판이 깔리던 원인 ①**: applyBg 가 위젯 가드 없이 body.hasbg 를 붙였다 —
  위젯 창도 같은 localStorage 를 읽으므로 **사용자가 배경화면 기능을 켠 순간** hasbg 토큰(밝은 --surf2,
  body::before 사진, 밑칠)이 위젯을 덮쳤다. v245 로 A/B 재현해 **v245 부터 있던 잠복 버그**임을 확정
  (218차까지의 변경과 무관). applyBg 첫 줄에서 위젯이면 hasbg 를 떼고 반환한다
  (⚠ WIDGET 상수는 아래쪽 const 라 TDZ — location.search 직접 검사).
- **년월·월넘김 버튼이 '투명'해진 원인 ②**: `#view-calendar .cal-title{background:var(--fill)}` 등
  세 규칙이 **ID 명시도**로 위젯 전용 진한 채움(body.wid .cal-title 의 rgba(16,20,30,.55), glass 캡슐)을
  덮고 있었다 — #view-calendar 는 위젯도 같이 쓰는 화면이다. 어두운 바탕 투명 창 위에서 --fill(연회색
  .15)은 거의 안 보인다. 세 규칙을 body:not(.wid) 한정으로 좁혔다(본 앱 --fill 유지 실측).
  **⚠ #view-calendar 스코프로 스타일을 넣을 땐 위젯 침투 여부를 반드시 확인할 것 — ID 가 위젯 규칙을
  전부 이긴다.**
- **설정 · 앱 배경화면 카드에 슬라이더 2개 추가**: '배경 대비'(밑칠 --app-bg-dim, 0~60%, 기본 18 —
  기존 고정값과 동일)와 '그레인'(--app-grain, 0~15% step .5, 기본 5.5 — 기존 .055 와 동일).
  localStorage calapp.bgDim / calapp.bgGrain, applyBg 가 적용·슬라이더 동기화. 그레인 의사요소
  (#app::after) opacity 를 var() 로 변수화. range 는 기존 bgAlpha 와 같은 click 위임 경로.
- 검증: 위젯 glass/일반 hasbg 차단·채움 복원(계산값 rgba(16,20,30,…)), 본 앱 년월 --fill 유지·배경 기능
  정상, 슬라이더→변수→로컬 저장→의사요소 반영 실측. 정적 감사 FAIL 0 · WARN 0(위젯 침투 잔존을
  금지 패턴에 추가), 스모크 14항목.
- app.js?v=248 · APP_VER 2.3.4.

### 220차 — 위젯 잔존 배경의 진짜 원인 · 배경 슬라이더 정리 · 앱 달력 = 위젯 칸 색
- **위젯에 '배경이 남아 있던' 원인(219차에서 못 잡은 나머지)**: 유리 모드 투명화 목록에 **#fcal 자신이
  빠져 있었다**. 213차에 `#fcal{background:var(--bg2)}` 를 상시 부여하면서 목록에 넣지 않아,
  유리 창인데도 셀 뒤에 흰색(라이트)·#212121(다크) 판이 깔렸다(계산값으로 확정). 목록에 #fcal 추가.
  ⚠ **#fcal 에 배경을 주는 규칙을 손댈 땐 body.wid.glass 투명화 목록도 함께 볼 것.**
- **배경 대비·그레인 슬라이더 철회**(219차에 넣은 것 — 사용자 지시): 밑칠 .18 · 그레인 .055 는 CSS
  고정값으로 되돌렸다. **JS 는 `--app-bg-dim`·`--app-grain` 을 더는 세우지 않는다** — 인라인으로 세우면
  CSS 기본값을 덮으므로, 값을 CSS 에 두려면 JS 쪽 setProperty 를 반드시 함께 지워야 한다(감사 항목에 추가).
- **레이아웃 톤 밝기 추가**(--app-card-lum, 70~130% step 5, 기본 100): hasbg 토큰의 각 RGB 채널에
  `calc(채널*var(--cl))` 로 곱한다(rgb 가 범위를 자동 클램프). 카드·패널·사이드바 블록·달력 칸이 한 번에
  밝아지거나 가라앉는다. 배경화면을 켰을 때만 적용.
- **앱 달력 칸을 위젯 유리 달력과 동일하게**: 배경을 켜면 평일 rgba(24,28,38,.55) · 주말 rgba(52,56,68,.5) ·
  다른 달 rgba(24,28,38,.23) · 요일머리 rgba(13,16,24,.67) — body.wid.glass 와 같은 값(톤 밝기도 연동).
  판(#fcal·cal-card)은 투명. 214차의 '달력은 제 색 유지(--bg2:#fff 되돌림)'는 이걸로 대체됐다.
  **⚠ 날짜 숫자 색은 `.fc-daygrid-day-number` 가 아니라 그 안의 `.dnum` 이 갖는다** — 컨테이너에만
  색을 주면 --rd/--bt1 이 남아 어두운 칸에서 안 읽힌다(처음에 이 실수로 주말 숫자가 어두운 빨강으로 남았다).
  격자선·요일머리 글자·공휴일 라벨(.dhol)까지 유리 세트와 같은 값으로. 배경을 끄면 전부 원래 흰 달력.
- 검증 함정 재확인: **#fcal 셀에 transition:background .12s 가 있어 슬라이더 직후 읽으면 보간 중간값**이
  나온다(§7.7) — 검증에서 450ms 대기 필요. 커스텀 프로퍼티 상속 반영에도 한 프레임이 든다.
- 검증: 위젯 glass/일반 + 앱 배경 on/off 계산값 실측(23항목), 정적 감사 FAIL 0 · WARN 0, 스모크 14항목.
- app.js?v=249 · APP_VER 2.3.5.

### 221차 — 배경 처리를 디뉴어 방식으로 · 달력 유리 설정 · 그림자 분리
- **레이아웃 톤 밝기(--app-card-lum) 철회**(220차에 넣은 것) — hasbg 토큰을 원래 리터럴로 되돌렸다.
- **배경화면 처리 = 디뉴어 앱의 로그인 배경 방식**: 단색 밑칠을 걷어내고 ①위→아래 어두운 그라디언트
  `linear-gradient(180deg,rgba(8,8,12,.40),rgba(8,8,12,.62))` + 바깥 비네트
  `radial-gradient(125% 125% at 50% 42%, transparent 52%, rgba(0,0,0,.42))` (body::after)
  ②안쪽 비네트 `radial-gradient(118% 96% at 50% 44%, transparent 42%, rgba(0,0,0,.5))` (#app::after)
  ③**정적 필름 그레인 캔버스**(#bgGrain, opacity .17 · mix-blend-mode:overlay) — 타일 노이즈
  (180px·spread 92) + 먼지 입자를 **한 번만** 그린다(리사이즈 시 150ms 디바운스로 재렌더).
  ⚠ 매 프레임 그리면 저사양 PC 에서 그대로 부담이 된다 — 애니메이션 없음이 원본 방식이기도 하다.
  사진 자체의 filter(saturate/contrast)는 제거 — 레이어로만 다스린다.
- **설정에 달력 투명도·유리 톤 추가**(위젯 설정과 같은 두 축, 같은 구현): `calapp.calGlass{a,tint}`,
  기본 투명도 15%(a .85)·톤 100%. **값은 calGlassApply() 가 #calDyn 스타일 태그에 리터럴 규칙으로
  주입한다** — 위젯과 같은 이유로 var() 상속이 FullCalendar 셀에서 갱신되지 않기 때문(widApply 주석 참조).
  ⚠ **칸 색의 단일 출처는 이 주입 규칙이다.** index.html 에 같은 값을 또 적으면 두 곳이 갈라진다
  (감사 금지 패턴에 추가). 년월·월넘김 버튼 채움 rgba(16,20,30,a*.9)도 여기서 함께 만든다.
- **다크에서 이웃(다른) 달 칸 색이 바뀌던 것**: 주입 규칙에 `html.dark body.hasbg …fc-day-other` 를
  같은 값으로 함께 넣어 테마와 무관하게 고정(실측: 라이트=다크 동일).
- **그림자 분리**: `body.hasbg .card` 의 그림자가 .cal-card 에도 걸려, 판이 투명한데 그림자만 네모로 떠서
  달력과 위 버튼을 한 덩어리로 감싼 것처럼 보였다 — .cal-card 는 none, **#fcal(격자)과 년월·넘김 버튼에
  각각** 그림자를 건다.
- **사이드바 프로필 구분선**: 버튼 왼쪽 끝(left:0)에 붙어 있던 것을 프로필과 테마 버튼의 가운데로
  (margin-left:6px 이므로 left:-3px), 높이는 위아래 5→7px 로 살짝 축소.
- 검증: 17항목(그레인 렌더·비네트 레이어·두 슬라이더 반영/저장·다크 이웃월 동일·그림자 분리·구분선 좌표),
  정적 감사 FAIL 0 · WARN 0. **⚠ 감사 오탐 주의**: 철회된 슬라이더 검사 패턴이 새 캔버스 `#bgGrain` 과
  겹쳐 걸렸다 — `<input … id="bgGrain">` 로 한정했다.
- app.js?v=250 · APP_VER 2.3.6.

### 222차 — 달력 설정 팝업 이전 · 격자 아웃라인 · 데드코드 정리
- **년월 꺽쇠가 안 보이던 것**: 꺽쇠는 `.cal-title-c` 가 따로 색(--lbl3)을 갖는다 — 배경을 켜면 버튼이
  어두운 채움(rgba(16,20,30,…))이 되는데 꺽쇠만 어두운 회색으로 남았다. 주입 규칙에
  `.cal-title-c{color:rgba(255,255,255,.72)}` 추가(위젯은 `body.wid .cal-title .icn` 로 이미 처리돼 있었다).
- **달력 격자 아웃라인**: 카드로 감싸는 게 아니라 **#fcal 자체에 1px 테두리**(기본 --sep, 배경을 켜면
  rgba(255,255,255,.26)). `body.wid #fcal{border:0}` 으로 위젯은 예외(창 자체가 경계).
- **달력 설정 팝업으로 이전**: 월넘김 캡슐 오른쪽에 ⚙ 버튼(.appo — 앱 전용 표시 클래스 신설,
  위젯은 자체 wid.set 을 쓴다) + `.tb-pop` 문법의 #calSet 팝업. 투명도·유리 톤 슬라이더를 설정 화면에서
  여기로 옮겼다(설정에는 안내 문구만 남김). 배경이 꺼져 있으면 팝업 하단에 안내가 뜬다.
  ⚠ **닫기는 click 이 아니라 mousedown 캡처** — FullCalendar 가 달력 칸의 click 을 삼켜 click 위임으로는
  달력 위를 눌러도 안 닫힌다(openCtx 와 같은 함정, 실제로 이 버그를 검증에서 잡았다). 여는 버튼
  (#calSetWrap 안)은 제외해야 토글이 깨지지 않는다(219차 게시월 토글과 같은 이유). Esc 도 닫는다.
- **넘김/설정 캡슐 채움 통일**: `.cal-nav:first-child` 로 한정돼 있어 뒤에 붙인 설정 캡슐만 맨몸으로
  떠 보였다 — `.cal-nav` 전체로 넓히고 #calDyn 주입 규칙도 같이 맞췄다.
- **데드코드·중복 정리**(요청 점검):
  · `--sbft/--sbfb`(217차 sticky 오프셋)는 218차에 목록·피벗을 페이드에서 제외하면서 **대상이 0개가 되어
    완전한 데드코드**였다(전 화면 순회로 확인) — JS 측정과 CSS 마스크를 원래 형태로 되돌렸다.
  · `<symbol id="i-plus">` 가 **두 번 정의**돼 있었다(뒤 정의가 무시됨) — 하나 제거.
  · 미사용 심볼 `i-refresh`, 미사용 id `sbToolsIn`·`calSetNote` 제거.
  · 오탐으로 확인한 것(그대로 둠): `wid.goTask`(문자열 변수로 전달), `view-tasks`(동적 `'view-'+view` 참조),
    `sbAcct`(CSS·접근성용), FullCalendar 런타임 클래스들.
- 점검 결과 이상 없음: 배경 on/off × 라이트/다크 4조합에서 6뷰 가로 넘침 0 · 콘솔/페이지 오류 0,
  테마 토글 왕복에도 이웃월 칸 불변, `#calDyn`·`#bgGrain` 중복 생성 없음.
- ⚠ **감사기 자체의 낡은 검사도 함께 지울 것** — 217차 항목(--sbft 존재 강제)이 남아 이번 정리와
  충돌해 FAIL 을 냈다. 또한 주석에 남은 변수명이 금지 패턴에 걸리므로 '실제 사용'(setProperty/var())으로
  좁혀야 한다.
- app.js?v=251 · APP_VER 2.3.7.

### 223차 — 하자 인쇄 재점검(요청): 실버그 4건 수정 · 중복 1건 정리
인쇄 4경로(화면식/보고서 × 대시보드/현장)를 합성 게시본으로 실제 PDF 까지 떠서 점검했다.
- **추이 차트가 빈 채로 인쇄되던 근본 원인**: 화면용 `#dfPrintPages .main-chart-card{height:230px}` 가
  뒤쪽의 `#view-defect .main-chart-card{aspect-ratio:16/4;min-height:256px}` 에 **밀려 무효**였다
  (216차와 같은 '뒤 규칙이 이긴다' 함정). 그래서 인쇄 상자(720px) 안에서 카드가 aspect-ratio 로 폭을
  역산해 **1024px 로 넘쳐** 그려졌고, 인쇄 미디어로 넘어가는 순간 696px 로 줄며 차트가 리사이즈
  애니메이션 중간 상태로 찍혔다. `#view-defect #dfPrintPages …{height:196px}` 로 명시도를 올리고
  인쇄 규칙(196px)과 값을 일치시켜 **리사이즈 자체를 없앴다**(좌표 실측: 화면·인쇄 모두 720×196).
- **인쇄물 가장자리에 어두운 그림자**: 221차에 추가한 안쪽 비네트(`body.hasbg #app::after`)가 인쇄
  숨김 목록에 빠져 있었다 — 목록에 추가(4개 레이어 전부 display:none 실측).
- **화면식 인쇄 동안 `DF.noAnim=true`**: 인쇄용 차트를 즉시 완성 상태로 그린다 — 남는 미세 리사이즈
  (패딩 차이)에도 애니메이션 없이 붙는다. 대기 700→260ms 단축. done 에서 해제.
- **보고서 인쇄가 조용히 죽던 경로**: 사전 로드(dfLoadPlans/dfLoadAna/dfList/dfAllKpi)가 하나라도
  reject 되면 async 가 중단돼 **보고서가 아예 안 떴다**. 각각 try/catch — 네트워크 실패여도 이미 받아 둔
  자료로 조립한다.
- **월별 표 'undefined월' 방어**: `dfMoSnapsSite` 가 dash 쪽과 달리 월 숫자(m)를 파생시키지 않아 주간
  레코드에 m 이 없으면 라벨이 깨진다 — `{m:월키 파생,...w}` 로 방어(레코드 값이 있으면 그것이 이긴다).
  1차 원인은 합성 시드의 m·wn 누락이었고 실게시본 레코드에는 있다 — **⚠ 합성 주간 시드는 반드시
  week·m·wn·sun 을 포함할 것**(검증 함정).
- **rpt 인쇄 @media 블록이 완전 동일하게 2개** 있었다 — 1개 제거, 감사에 개수 검사 추가.
- 검증 함정: 헤드리스에서 window.print 를 no-op 으로 바꾸면 **폴백 타이머(1500ms)가 미리보기 상태를
  걷어가** PDF 캡처가 검게/비게 나온다 — 실브라우저는 print() 가 블록이라 무관. 캡처는 폴백 전에.
- 결과: 4경로 PDF 정상(밝은 색 강제·쪽 분리·표·차트·월 라벨), 페이지/콘솔 오류 0.
- app.js?v=252 · APP_VER 2.3.8.

### 224차 — A4 설정값 전수 실측(요청) · 쪽번호 복원 · 유령 띠 제거
**원본(하자처리 현황) 대조는 원본 zip 이 이 환경에 없어 보류 — 원본이 오면 픽셀 대조를 이어서 한다.**
calapp 에 이식된 A4 설정값들을 96dpi 로 전 쪽 실측해 검토했다(설정값 ↔ 실측):
- 화면식: @page 8mm/9mm · 720px 상자 → 전 쪽 여백 좌9.5 우10.3 상8.5 하12.4mm, 내용폭 720px 균일 —
  내용폭 725.7px(=192mm) 한도 안. 초기 실측의 '726px 초과'는 글자 안티앨리어싱 번짐(≤3px)이었다.
- 보고서: .page 794×1123(=A4@96dpi 정확) · .sheet 595×zoom1.333 · 패딩 36px(≈12.7mm) →
  전 쪽 좌12.4 우13.2 상11.9 하6.3mm 균일, 쪽번호(1/2) 하단 6.3mm — 설계와 정합. rptFit(dense→
  dense2→AI 폰트 8.25→6.25 축소)도 확인.
- **실버그 ① 화면식 쪽번호가 한 번도 안 찍혔다**: `@page @bottom-right{counter(page)}` 는 **크롬이
  지원하지 않는다** — 설정만 있고 출력엔 없었다. DOM 쪽번호로 복원: 각 논리 쪽을 min-height 1050px
  (< A4 내용높이 1063px) 박스로 세우고 .sp-pgn(우하단, 7pt #8a8a8e)을 주입, dfPrint 가 'n / N' 을 채운다.
  ⚠ **min-height 는 반드시 상시 규칙** — @media print 안에만 두면 인쇄 순간 박스가 늘어나며
  레이아웃이 변한다(그 실수를 이번에 직접 밟았다).
- **실버그 ② 마지막 쪽 하단 유령 띠(#EAECF1, 13px)**: df-printing 인쇄 규칙이 #app/#main/#content 만
  흰색 강제하고 **body 는 빠져** 있어, 문서 끝과 물리쪽 경계 사이 잔여에 --bg 가 찍혔다(색으로 역추적).
  `body.df-printing, html:has(...)` 배경 흰색 추가로 해결 — 전 쪽 잡음까지 소거돼 내용폭이 정확히 720 로
  균일해졌다.
- 검증 함정 추가: pdftoppm 96dpi 캔버스는 795×1124(반올림) — 794×1123 로 비교하면 오탐.
  헤드리스에서 print 폴백이 PDF 캡처 중 정리를 실행할 수 있다(§223) — 캡처 전 타이머 무력화.
- a4-audit.mjs(전 쪽 여백·경계 침범 자동 실측)를 검증 스위트에 추가. 4경로 PDF · 여백 플래그 0.
- app.js?v=253 · APP_VER 2.3.9.

### 225차 — 원본(하자처리 현황) 소스 확보 · 대시보드 픽셀 정렬
원본 zip(report-v2026-08-04.2, /home/claude/work/orig)이 확보돼 소스 단위 대조가 가능해졌다.
사용자 스샷 규모(현장 12·주 31·접수 78만)로 calapp 을 렌더해 원본 스샷과 나란히 맞췄다.
- **'도넛·범례·바·숫자가 다 크다'의 근본 원인 — 범례 2열 조건을 내가 임의로 넣은 것**:
  원본은 범례가 **항상 lg-2col**(lgRows 가 --lgr=ceil(n/2) 주입, CSS grid-auto-flow:column 이 좌열부터
  세로 채움 — 12현장이면 6행×2열). 220~222차에 '폭 620 이상일 때만 2열' 토글을 넣어 1열 12행이 되며
  ① 도넛 카드가 세로 팽창 ② opsr 행높이 동반 팽창 ③ mom(justify:space-between)의 행간까지 벌어져
  전부 커 보였다. 원본 CSS(1184~1201행)와 값 일치로 복원(1col 기본 패딩 16/24·pct48, 2col 축소 예외,
  .canv flex:0 0 210px, .l display:contents 래퍼) + 원본 768px 모바일 분기 이식.
  실측: opsr 카드 260px(원본 min-height 그대로)·범례 2열·행 22px·도넛 210px·mom행 63px.
- **추이 차트 원본 동일화 7건**: x축 tick font 10 고정(가변 7~10 제거), tooltip
  `position:'aboveAll'+yAlign:'top'+caretPadding:6`(+aboveAll positioner 를 dfChartInit 에 등록 —
  원본 app-core.js 774), 라인 hover 세트(pointHoverBorderWidth 3·흰 테두리·hoverBorderWidth 3.5),
  막대 pointStyle:'rectRounded'(툴팁 마커), 라벨 textShadow, 라벨 display 에 innerWidth>768 조건 3곳.
- **mom 사소 정렬**: prev 바 #C7DDF6 고정색·트랙 --bg·radius 99px·hover filter·delta column+gap3.
- **⚠ 원본 스샷과 코드의 알려진 불일치(사용자 확인 대기)**: 원본 화면 스샷엔 추이 막대 위 총합·라인
  끝 라벨이 **안 보이지만 원본 소스에는 있다** — 원본의 라벨 페이드($la)가 initial 애니메이션에서만
  시작되므로 연도 변경 등 재렌더 후엔 라벨이 영영 0 로 남는 특성 때문으로 판단. calapp 은 소스대로
  라벨을 표시한다(첫 렌더 기준). 지우려면 total 라벨 제거가 아니라 표시 임계 조정으로.
- 검증 함정: ① headless 의 큰 clip screenshot 은 재합성 중 가속 캔버스를 비운다 —
  **captureBeyondViewport:false** 필수(도넛·추이가 빈 캡처로 나왔던 원인) ② rDefectDash 는 DF.kpi 가
  아니라 **d.wk[sid] 주간 원본**에서 dfStFromWeekly 로 직접 계산 — 합성 시드는 wk 를 채워야 한다
  ③ print-full 의 C→D 는 print no-op 폴백(1500ms)이 D 렌더를 걷어가는 경합이 있어 C 정리 후 타이머
  전부 클리어(3회 반복 결정성 확인).
- 게이트: 정적 감사 FAIL 0 · WARN 0(원본 정렬 9항목·재유입 금지 2항목), 스모크·verify-r9·sweep·
  print-full×3·a4-audit 전부 통과.
- app.js?v=254 · APP_VER 2.4.0.

### 226차 — 원본 우클릭·필터행 전면 이식 · 공가 실시간 연동 · 배경/사이드바 사용자 지시
지시 6건: ①미처리 목록 필터행 ②원본 우클릭 전부 이식 ③공가 모달 DB 연동 ④설정 달력 잔재 삭제
⑤배경 밝기 슬라이더+기본 어둡기 완화 ⑥사이드바 블록 흰색 고정.

**①② 목록 필터행·엑셀식 열 메뉴(rlMenu) — 원본 app-data.js 944~1010·app-boot 1214~1243 이식**
- REC 에 filterRow/filters 신설(초기 선언에도 포함). recBase 파이프라인에 열별 텍스트 필터 결합.
- recBodyHTML 머리 아래 .rl-frow(필터행) — **타이핑 중엔 recRowsOnly 로 tbody 만 교체**(전체 재렌더는
  입력 포커스가 죽는다). rec.filter/rec.menuSearch 는 input 위임(클릭 위임으로는 못 받는다).
- 열 머리 우클릭 → rlMenu: 필터행 표시/숨기기(끄면 filters 초기화)·이 열 숨기기·숨긴 열 모두 표시(n)·
  검색·값 체크리스트(CAP 400)·**날짜 트리**(값의 70%↑ 이 날짜면 연/월/일 3단, 삼상 체크)·해제/적용.
  적용은 Set→REC.vals 객체 변환, 전체 선택이면 필터 삭제. 닫기: 바깥 mousedown + Esc(캡처).
- 본문 셀 우클릭: "값으로 필터 / 제외 / 셀 값 복사 / 표 전체 복사"(REC.vals + filterRow=true).
  피벗 드릴다운에도 filterRow=true(원본 동일). 기존 간이 th 메뉴(값 20개)와 recVals 는 제거.
- **0건이어도 표(머리·필터행)는 유지** — 표째 없애면 열 머리 우클릭으로 필터를 해제할 길이 사라진다.
- 원본 ③⑤⑥ 대응: 대시 현장행·**사이드바 현장 항목(.df-site[data-sid] — 컨테이너 무관 클래스 매칭)**
  → 현장 열기·미처리/장기 목록·셀/표 복사, AI(.ait) → 분석 의견 복사.
  ⚠ openCtx 는 null 항목을 거르지 않는다 — 조건부 항목은 .filter(Boolean) 필수(안 하면 메뉴 통째 죽음).
- **열 리사이즈 핸들(rl-rz)은 이식 범위에서 제외** — 우클릭·필터행 지시 밖의 별도 대형 기능.
- CSS: 원본 690~718(.rl-frow sticky top:34px·.rl-menu 244px·rl-tree) 이식 + @media print 숨김.

**③ 공가 연동 — '연동 안 됨'의 원인 2가지**
- dfSubSite 가 plans/ana 만 구독하고 **vac 은 1회 캐시** → 하자처리 현황에서 수정해도 calapp 미반영.
  → 구독 키를 rm+'/'+sid 로 바꾸고 `report/{rm}/{sid}/vac` on('value') 실시간 구독(dfDec 로 디코딩,
  공가/상가 탭이면 즉시 rDefect).
- calapp 저장이 **평문 키**('미분양') — 게시본은 fbEncKey 인코딩 키라 같은 노드에 두 형태가 섞였다.
  → dfEncKey(원본 fbEncKey 동일) 추가, vacSave 가 인코딩 키로 update(검증: %EB%AF%B8%EB%B6%84%EC%96%91 실측).

**④⑤⑥** 설정 달력 잔재 행 삭제. 배경 어둡게 레이어 완화(그라디언트 .40/.62→**.16/.30**, 비네트
.42→.26, 안쪽 .5→.28) + body::before 에 brightness(--app-bg-bri) — 설정 슬라이더 #bgBri(55~140%,
calapp.bgBri). 사이드바 블록은 hasbg 에서 **#fff 불투명 고정 + 블러 해제**(기본 규칙의 blur 20px 까지
꺼야 GPU 낭비가 없다), 다크+배경에서도 흰 블록(블록 내부 토큰을 라이트로 강제).

**검증 함정(재발 방지)**
- 목록 검증 시드는 `S.live=true; FB.db={…스텁}` 필수 — dfList 는 live 가드가 캐시보다 앞.
  **window.FB 새 객체 대입은 무효**(모듈 스코프) — 반드시 FB.db 프로퍼티 대입.
- **필터행이 열리면 열 폭이 재배치** — th 좌표는 매번 재조회(고정 좌표 재사용 시 다른 열 메뉴가 열린다).
- 대시 표·AI 카드는 스크롤 아래 — 우클릭 검증 전 scrollIntoView({block:'center'}) 필수.
- 사이드바 트리는 권역 접힘이 기본 — 검증은 S.dfFold[rid]=false 로 펼친 뒤(로컬 시드는 rid='').
- verify-r2 기대치를 새 사양(#fff·블러 없음)으로 갱신, verify-r10(28항목) 신설.
- 감사: 신규 필수 17·금지 3 추가, rec.filter 는 클래스 매칭 위임이라 감사기 수집 라인도 추가.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r5·r7·r9·r10·sweep·print-full×3·a4-audit 전부 통과.
- app.js?v=255 · APP_VER 2.4.1.

### 227차 — 사이드바 흰색 취소 · Ctrl+P 브라우저 인쇄 이식 · 목록 열 폭 고정
- **⑥ 취소**: 226차 '사이드바 블록 흰색 고정'을 사용자 지시로 철회 — 원래 유리
  (`var(--glass3)` + blur 18px)로 복원, html.dark 라이트 토큰 강제 규칙도 제거.
  verify-r2·r10 기대치와 감사 필수 항목을 유리 기준으로 되돌림.
- **Ctrl+P(브라우저 인쇄) 경로 이식** — 원본 index.html 882~1010: 그동안 전용 인쇄 상자
  (#dfPrintPages)만 다듬고 이 경로가 통째로 빠져 있어, 사용자가 Ctrl+P 를 누르면 화면 레이아웃
  (도넛 210·2열 범례)이 그대로 눌려 들어갔다. `body:not(.df-printing):not(.rpt-on)` 스코프로
  사이드바/상단바 숨김·카드/KPI/표(6.5pt)/추이(카드 210px·cw 140px)/도넛 155px·범례 1열/mom 축소.
- **Ctrl+P 머리(.df-printhdr)**: #view-defect 안·#defectRoot **앞** 정적 마크업(재렌더에도 생존)
  + dfUpdatePrintHdr(팀명·제목(대시/현장)·기준월 말일) + **beforeprint 에서 머리 채움 +
  DF.noAnim=true + 전 차트 resize/update('none')**(인쇄 순간 리사이즈 애니메이션 중간 프레임 방지,
  afterprint 에서 해제). 표시 규칙은 df-printing·rpt-on 제외 스코프 — 전용 인쇄·보고서는 자체 머리.
  ⚠ 원본 doPrint 는 차트 재그리기 없이 print() 직행 — 실브라우저는 인쇄 레이아웃을 먼저 계산해
  대개 무사하지만, calapp 은 beforeprint 재그리기로 결정성을 확보했다.
- **전용 인쇄 도넛 축소 상시화**: `#view-defect #dfPrintPages .dn-side …`(155px·범례 1열)를
  @media print 밖 상시 규칙로 — print 한정이면 미리 렌더가 210px·2열로 그려졌다가 인쇄 순간
  리사이즈되고(223차 함정), 좁은 카드(≈350px)에서 범례가 도넛을 덮었다.
- **목록 열 폭 고정**: REC_COLS 에 원본 기본 폭 w(동/호 62·접수일 132·공종 96·업체 120·지연일 78,
  접수내용 w:null=잔여) + REC_SITE_COL 152 + No 46px, head th 는 `REC.w[c.k]||c.w`,
  `.rec-tbl{min-width:1180px;table-layout:fixed}` — **필터행이 열려도 열 폭·위치가 불변**
  (fixed 없이는 auto 레이아웃이 재계산해 표가 출렁였다).
- 검증 함정: ① getComputedStyle 은 조상 display:none 이어도 자신 값(flex)을 반환 —
  가시성은 getBoundingClientRect 크기로 판정 ② 대시 추이는 **d.wks**(팀 주간
  {m,w,u,lt,lt60,cumR,cumRes}), KPI·도넛·현장표는 **d.wk[sid]** 에서 dfStFromWeekly —
  합성 시드는 둘 다 채울 것(wks 비우면 추이 차트 자체가 안 생긴다) ③ 컴팩션·중단(aborted) 이후엔
  zip 이 아니라 **작업 사본이 최신 의도**일 수 있다 — diff 로 판정하고 덮어쓰지 말 것
  (이번 회차에서 사본에만 있던 Ctrl+P·열 폭 작업을 zip 재수화로 날릴 뻔했다).
- verify-r11(13항목: Ctrl+P 머리·숨김·도넛155·범례1열·잉크·현장 제목·달력/전용 인쇄 배제·
  fixed·폭 96·필터행 토글 후 불변) 신설, 감사 필수 9항목 추가.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r5·r7·r9·r10·r11·sweep·print-full×3·a4-audit 통과.
  Ctrl+P PDF 2쪽·전용 인쇄 PDF 1쪽 시각 확인.
- app.js?v=256 · APP_VER 2.4.2.

### 228차 — 추이·도넛을 원본과 diff-0 으로 ("코드를 그대로 갖다붙여라")
사용자 지적: 추이·도넛 요소가 여전히 원본과 다르다. 원인은 **원본에 없는 임의 '개선'**들이었다 —
옵션을 골라 맞추는 접근 자체가 잔차를 남긴다. 이번부터 차트 코드는 **원본 문자 그대로 + 명시된
어댑터 최소**가 원칙이고, 편차는 주석으로 전부 명기한다.
- 걷어낸 임의 추가: ① dfMoDLCfg 의 자릿수 축소 while 루프(큰 수에서 라벨이 원본보다 작아짐)
  ② 추이 tick/축제목 12·13 고정(원본은 모바일 9·10 반응형 _tkSize/_atSize)
  ③ 도넛 `animation:{duration:fn}` 커스텀(원본은 **미지정** — 기본 회전·원호 이징까지 달라진다)
  ④ 도넛 데이터셋 pointStyle:'circle' 누락 ⑤ 도넛 툴팁 caretPadding:32 누락
  ⑥ 추이 툴팁 콜백 `(ctx.parsed.y??ctx.parsed??0)` 원본식 ⑦ onComplete 의 console.warn.
- 남긴 어댑터(각 1곳·주석 명기): 추이 DUR 함수형(DF.noAnim — 216차 사이드바 토글 근거),
  도넛 `...(DF.noAnim?{animation:{duration:0}}:{})`, 범례 data-tip 병기(calapp 툴팁),
  dfDonutDraw 의 방어(빈 데이터·Number 캐스팅).
- **검증 방식 전환**: 눈대조가 아니라 **런타임 옵션 전수 대조**(verify-r12, 23항목) —
  차트 인스턴스에서 데이터셋 시그니처(rectRounded·stack·order·hover 세트·라벨 offset/stroke/shadow),
  스케일(grace 25%·niceFitRange·tick 크기), 툴팁(aboveAll·caret 6/32·pointStyle),
  moDLCfg 산식 표, 도넛(cutout 58%·padding 14·centerText·datalabels off·기본 애니),
  범례(--lgr·.l 구조·data-tt·hover↔조각 활성 연동)를 원본 기대값과 직접 비교.
- 감사: 필수 5(circle·caret32·_atSize·moDLCfg 원본식·툴팁 콜백) + 금지 2(자릿수 루프·
  커스텀 duration 재유입), 구 'DF.noAnim?0:1000' 필수 항목은 spread 어댑터 검사로 교체.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r10·r11·r12·sweep·print-full×3·a4-audit 통과.
- app.js?v=257 · APP_VER 2.4.3.

### 229차 — 사용자 지시 10건(공통 동기화·문구·간격 미세 조정) + 위젯 팝업 조사
- **① 업무구분 '공통' → 담당자 자동 '공통'**: kindOwnerSync(kindId,ownId) — change 위임에서
  tnKind→tnAsg, peKind→peOwners 배선(value 'gather'면 담당자 ''). ② 노출 문구 '팀 공통'→'공통'
  (담당자 옵션·빈 목록 문구 — 주석은 유지). ③ 처리계획 라벨 '지난달/이번 달'→'전월/금월'
  (dfPrevPlanTop·dfPlanCell·비교 셀). ⑤ 피벗 항목에 '공간'(space).
- **④ 달력 테두리 반투명**: calGlassApply 의 #calDyn 에 hasbg 규칙 —
  `#fcal{border-color:rgba(255,255,255,.28*tint)}` + `.fc-scrollgrid` 링 .22*tint.
- **⑥⑦ 공가 카드**: 수치 .vseg-v 26→**22px**(상단 KPI .kv 와 동일), 카드에 vac-stat 클래스 부여
  후 `.vac-stat .sh{margin-bottom:8px}`(기본 12).
- **⑧⑨⑩ 사이드바**: 접기버튼 `.sbtg{margin-right:-3px}`(우간격 15→12px).
  분류 라벨 위 간격 동일화 — `.sbsc` padding-top 8→**7**, `.sb-bottom` padding-top 6→**7**.
  ⚠ 사용자 문면은 "관리 줄이고 업무 늘려"였지만 실측·스샷은 반대(업무 8 > 관리 6) —
  체감상 라벨 이름이 뒤바뀐 표현으로 판단, 의도(둘을 살짝만 움직여 동일)에 맞춰 7/7 로.
  알림 팝업 `.sb-tools .tb-pop` left +8→**+18px**(검색 nq-panel 과 동일 간격).
- **⑪ 위젯 알림/내업무/설정 팝업 '높아짐'** — v254 zip 과 픽셀 실측 비교(alert 142 · mine 290 ·
  set 352px)로 **웹 코드 동일 확인 = 회귀 아님**. wfz 배율 규칙 수도 동일. 실데이터 건수·위젯 창
  높이(max-height:calc(100dvh-76px) 비례)·exe 쪽 요인 후보 — 재현 조건 요청으로 보고.
- 검증 함정: 처리계획 셀(pp-cell)은 별도 'plan' 탭이 아니라 **lt 탭 상위5 표·vac 탭 공가 표 안**
  — 시드는 S.dfTab='lt' + kpi.topLt/topLtPrev + DF.sw/DF.sam 까지.
- verify-r13(11항목) 신설, 감사 필수 11·금지 2 추가.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r10·r11·r12·r13·sweep·print-full×3·a4-audit 통과.
- app.js?v=258 · APP_VER 2.4.4.

### 230차 — 229차 오판 교정(사진 근거) · 위젯 팝업 62px · 공가 흐름 규명 · 부팅 색 깜빡임 · 공통색
- **⑨ 재교정(사과 건)**: 229차 실측이 틀렸다 — 사진 판독 + 텍스트 상단(Range) 기준 재실측으로
  '관리' 위가 8px 더 넓음을 확인. 원인은 패딩이 아니라 **`.sbsc>.sbsl:first-child{padding-top:2px}`
  축소 규칙이 `.sb-bottom` 첫 라벨('관리')에는 안 걸린 것**. 229차의 sbsc 7px/sb-bottom 7px
  변경을 원복(8/8)하고 `.sb-bottom>.sbsl:first-child` 를 셀렉터에 추가 — 텍스트 기준 13=13.
  ⚠ 교훈: 라벨 간격은 박스(getBoundingClientRect(el))가 아니라 **텍스트 상단(Range)** 으로 재고,
  first-child 류 예외 규칙부터 의심할 것.
- **위젯 알림/내업무/설정 '위로 올라감'**: v250 실측에서도 연월 팝업 62px(제목 아래 +8, JS 인라인)
  vs 세 팝업 55px — 웹 코드 변화는 없었으나 사용자 기준(연월과 동일)에 맞춰 **top 62px 로 정렬**
  (wid-side max-height 도 -83px 로 보정). #wgSet 주석의 '같은 높이'가 실제로는 7px 어긋나 있었다.
- **공가 '반영 안 됨' 규명(코드 아님·절차)**: 원본 openVacEdit 은 S.cmt + **lsSave(localStorage)** 에만
  저장하고, FB 의 report/{rm}/{sid}/vac 은 **'사내 게시'를 다시 눌러야**(report 노드 통짜 교체) 갱신된다.
  calapp 구독(226차)은 게시 즉시 실시간 반영 — 뷰어 쪽 결함 없음. 사용자 안내로 처리.
- **부팅 시 달력 색 깜빡임**: 부팅 캐시(calapp.boot.v1)가 accounts 를 저장하지 않아 첫 렌더가
  인덱스색(OWN_PAL) → users 도착 후 지정색으로 덮이며 ~1초 깜빡임. 캐시에 accounts 포함
  (저장·복원·users 콜백 bootCacheSave) — 마지막 지정색으로 즉시 그린다.
- **공통 업무색**: '#3E71D2' 는 계정 자동색 OWN_PAL[0]과 동일해 구분 불가. 자동색 10종이 유채색을
  전부 쓰므로 공통은 **무채 슬레이트 '#64748B'**(TEAM_COLOR) — 흰 점 표시와 이중 구분.
  대안(짙은 남색 #1F2B4C)은 파랑 계열이라 배제.
- verify-r13 확장(13항목: 텍스트 기준 간격·공통색·부팅 캐시 accounts), 감사 필수 5·금지 2 추가.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r10·r11·r12·r13·sweep·print-full×3·a4-audit 통과.
- app.js?v=259 · APP_VER 2.4.5.

### 231차 — **원본 인쇄본 대조 체계 확립** · 인쇄 레이아웃 전면 교정 · 위젯 보완
사용자 지적: "추이·도넛 비율이 여전히 다르다, 원본 코드를 읽고는 있나, 그 앱으로 뽑은 샘플을 내놔라."
**정곡이었다** — 그전까지 원본 *소스*만 읽고 calapp 인쇄본만 보며 판단했을 뿐, **원본 앱을 실제로
인쇄해 대조한 적이 없었다.** 이번에 기준본 생성 절차를 만들었고, 앞으로 인쇄를 건드리면 반드시 돌린다.

**원본 기준본 만드는 법(gen-orig.mjs / gen-site.mjs)**
- 로그인 게이트 우회: `hideCover()` 직접 호출.
- 대시: `S.teams/S.teamId/S.sites(**teamId** 필드!)/S.rm/S.def[sid]=원시행` → `rDash()`.
- 현장: 위 + `S.view='site';S.sid=sid;S.tab='overview'` + **뷰 전환은 클래스 `.act` 토글**
  (`#view-dashboard`에서 제거, `#view-site`에 추가) → **`rSite(sid)` 는 인자 필수**.
- `window.print=()=>{}` 로 막고 `doPrint()` → `emulateMediaType('print')` → `page.pdf()`.
- calapp 쪽은 dfPrint() 경로('화면 그대로')로 같은 수치를 넣어 뽑고 pdftoppm 96dpi 로 픽셀 비교.

**대조로 드러난 실제 차이(전부 교정)**
1. **인쇄 폴백 타이머 버그** — `setTimeout(done,1500)` 이 인쇄 대화상자보다 먼저 터져 **2쪽 이후가
   통째로 사라졌다**(현장 인쇄가 1쪽만 나옴). dfPrint·보고서(rpt) 두 경로 모두 60초로 늘리고
   `_done` 재진입 가드 추가. afterprint 가 오면 즉시 정리되므로 지연은 무해.
2. **인쇄 차트 규칙이 임의값이었다** — 원본 index 951~ : 추이 카드 **210px·padding 10/12/12·.cw 140px**,
   도넛 **155px·gap 12·범례 7.5pt·열 8/1fr/42/36**. calapp 은 196/8·11·10·도넛 130·6.5pt 였다.
   전부 원본 값으로 교체 → 인쇄본 추이 카드 높이 **209px 로 원본과 일치**(픽셀 실측).
3. 인쇄 상자는 720px 폭이라 주차 라벨이 자동 회전 → 인쇄 분기에서만 `maxRotation:0,autoSkip`.

**사용자 지시(가독성 조정 — 인쇄 전용, 화면은 원본 유지)**
- 도넛 가운데: centerText valueSize 16→**13**, labelSize 11→**8** (키가 `pr*` 일 때만).
- 추이 축 제목 13→**9pt**, 눈금 12→**8pt**(`_pr` 분기). 범례 6.5→**5.8pt**, **항목 간격 18→8px**, 마커 .62배.
- 전월대비 실적 현황 전체 축소(행 2/3px·라벨 6.8pt·막대 6.4pt·배지 4.6pt+**nowrap**·델타 열 52px).
  ⚠ 배지가 두 줄로 접히던 원인은 델타 열(46px) < 내용 → 폭 확보 + nowrap 이 정답.
- 장기미처리 비율 현황 동일 수준 축소 + 범례를 추이 범례와 동일 규격, **막대~배지 간격 16→7px**.
- 도넛 범례 **퍼센트 잘림**: 열 36px < 내용 41px → **44px**로. (원본 값 그대로 쓰면 calapp 폰트에서 잘린다)
- 공가 KPI 축소(수치 18→15·라벨 14→10·세그 패딩 2px) + 제목~본문 8→4px, 행 간격 10→6px.
- **공가 행 쏠림**: `.vrow{margin:0 -20px}` 는 화면 카드 패딩 기준 — 인쇄 패딩(11px)과 어긋나 행이
  카드보다 16px 넓어 오른쪽으로 밀렸다. 인쇄 스코프에서 `margin:0 -11px`.
- 처리계획 전월/금월 **상단정렬(A안 채택)** — 라벨 8px/pt3 vs 본문 9.33px/lh14 로 첫 줄이 어긋났다.
  양쪽 `line-height:14px`, 라벨 padding-top 0, `align-items:flex-start`. (B안=중앙정렬 샘플도 제시)

**위젯 보완**
- **아침 점검(morningReview) 이 위젯에서 아예 막혀 있었다**(`if(WIDGET)return;`). 가드 제거하고
  `#mb.mrv-wid` 전용 폭(340px)·목록 max-height·글자 축소로 좁은 창에 맞춤. 위젯 첫 렌더가 늦어 지연 1400ms.
- 위젯 '내 업무' 미완료 줄에 진행 아이콘(`wid.st`) 추가 — **줄 오른쪽 끝**(사용자 지시).
  stxSet 애니메이션 후 putTask → widSideRender·refetchCal. 줄 클릭(이동)과 분리(.wl-row 래퍼).

**검증 함정 추가**
- 업무 담당자 필드는 **`assignees`**(owners 아님) — 시드 잘못 쓰면 mrvList/mineTasks 가 0건.
- 로컬 모드 시드는 **`store._d.tasks` 와 `S.tasks` 가 같은 객체**여야 한다. S.tasks 만 갈아끼우면
  putTask 가 옛 _d 를 쓰면서 다른 건이 사라진다.
- 인쇄 PDF 캡처 전 차트 resize/update + 대기 3.5초 — 덜 그려진 채 찍히면 막대 색·값 라벨이 빠진다.
- 현장 추이 시드는 **wks 형식**(m,w,u,lt,lt60,cumR,cumRes) — weekly(week,wn,r,res,d0..) 를 그대로
  DF.sw 에 넣으면 차트가 이상하게 그려진다.
- verify-wid.mjs(7항목) 신설, 감사 필수 11·금지 4 추가.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r10·r11·r12·r13·wid·sweep·print-full×3·a4-audit 전부 통과.
- app.js?v=260 · APP_VER 2.4.6.

### 232차 — 공통 업무 2안 완결 · 인쇄 잔여 조정
- **공통 업무 표시 = 2안(윤곽선형) 확정**. 230차에 색(#64748B)만 바꿨더니 **예전에 색을 지정해 둔
  공통 업무는 여전히 파란 꽉 찬 막대**로 남아 구분이 안 됐다(사용자 지적).
  이벤트 생성부에서 `team`이면 `backgroundColor:'transparent'` + `borderColor:planColor(p)`,
  `on-light` 클래스는 계정 업무에만. CSS `#fcal .fc-event.team{background:#fff;box-shadow:inset 0 0 0 1.5px currentColor}`
  (다크·배경 모드 대응 포함). **색 지정 여부와 무관하게 공통은 전부 속 빈 막대**가 된다.
- **공가 인쇄 페이지 누락 방어**: 페이지 조립이 한 번 실패하면 그 뒤 페이지까지 통째로 날아갔다.
  공가세대·공가상가 페이지를 각각 try/catch 로 감싸고 vacU·DF.vac 널 기본값 보강, 실패 시 콘솔 경고.
- 인쇄 추가 축소(사용자 지시): 종합 분석 의견 `.aib` 패딩 9/11 · `.ail` 6.5pt · `.ait` 7.5pt/1.5 ·
  제목 8pt · 문단/리스트 여백 축소. 추이 라인 **점 반지름 4→2.2, 테두리 2→1.2(인쇄만, `_pr` 분기)**.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r10·r11·r12·r13·wid·sweep·print-full×3·a4-audit 통과.
- app.js?v=261 · APP_VER 2.4.7.

### 233차 — 공통 막대 테두리색 · 달력 띠 색 통일
- 공통 업무 테두리를 **기존 파랑(#3E71D2)** 으로 되돌림(TEAM_COLOR). 구분은 '속 빈 막대'가 하므로
  색이 계정과 같아도 겹치지 않는다. ⚠ CSS 함정: `border:1.5px solid!important` 처럼 shorthand 로 쓰면
  **색이 currentColor(흰색)로 리셋**돼 테두리가 사라진다 — `border-style/border-width` 만 지정해
  FullCalendar 가 인라인으로 준 업무색이 살아나게 할 것.
- 달력을 두르는 띠 = **년·월 버튼 배경색**과 동일하게. 기본 화면 `#fcal{border:1px solid var(--fill)}`,
  배경 켠 상태는 calGlassApply 에서 버튼과 같은 `rgba(N, a*.9)`.
- verify-r13 기대치 2건 갱신(공통색·달력 띠), 감사 3건 갱신.
- app.js?v=262 · APP_VER 2.4.8.

### 234차 — 문구 통일 · 색 원 2안 · 모바일 달력 · 오늘 칩 · 수정 중 잠금
- **'팀 공통' 잔존 2곳**(업무 카드 담당자 자리·추가 토스트)을 '공통'으로. 나머지는 전부 주석.
  전수 점검은 주석 제거 후 한글 리터럴만 뽑아 동의어 짝(전월/지난달, 팀 공통/공통 등)으로 대조.
  함께 발견: 보고서 표 머리 **'전월 대비' 9곳 → '전월대비'**(다수 표기)로 통일.
  ⚠ '미완료'(업무)와 '미처리'(하자)는 도메인이 달라 **의도적 구분** — 통일 금지.
- **공통 업무 색 원**도 달력 막대와 같은 규칙으로: colDotHTML(c,pid,**team**) — team 이면
  `background:transparent + inset 0 0 0 1.5px 업무색`, CSS `.p-col-team{background:#fff}`.
  호출 3곳(일정 카드·그날 목록·업무 항목)에 `!planOwners(p).length` 전달.
- **오늘 표시**: 원(20px) → **라운드 사각형**(border-radius 6px·height 18·padding 0 6px, 막대와 통일).
- **모바일 달력**(≤620px): 막대 높이 18→15px·글자 12→10.5pt·여백 축소, 오늘 칩·날짜 숫자·+n 링크도 축소.
  ⚠ 위젯용 규칙(2380행대)이 뒤에 있어 오늘 칩은 `!important` 필요.
- **수정 중 목록 잠금**: 폼이 열린 동안 실시간 수신이 들어오면 목록이 재정렬돼 **폼이 다른 자리로 튀었다**.
  `tkFormOpen()`(tkEdit·tkNew·planEdit) + `tkHold()` 를 만들어 shEditing 검사 5곳을 모두 대체.
- verify-r14(7항목) 신설. 검증 함정: 색 원은 **달력 우측 그날 목록(#dpList)** 에만 있고,
  업무 화면 목록은 **S.org.teams + S.tk={t,m:'team'}** 시드가 없으면 아예 비어 폼이 안 열린다.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r10·r13·r14·wid·sweep·print-full·a4-audit 통과.
- app.js?v=263 · APP_VER 2.4.9.

### 235차 — 날짜 칸 넘침 표시(+n건) → 5안(우측 정렬 미니)
- 기존: 회색 알약 + 파란 글씨가 막대 왼쪽에 어긋나 붙어 **줄 리듬이 끊기고** 업무색과 경쟁했다.
- 채택(사용자 선택 5안): `moreLinkContent` **'외 n건 ›'**, `.fc-daygrid-day-bottom{justify-content:flex-end}`,
  링크는 배경·알약 제거하고 11px·var(--lbl2), hover 시에만 파랑. 위젯 유리 모드는 흰색 85%.
  모바일(≤620px)은 9.5px + 우측 여백 4px.
- 시안 6안(현재/막대형/점선/색점/우측미니/날짜옆배지)을 실제 칸 스타일로 렌더해 비교 제시했고,
  향후 유사 UI 결정도 같은 방식(HTML 시안 → 캡처)으로 진행할 것.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r14·wid·sweep·print-full 통과.
- app.js?v=264 · APP_VER 2.5.0.

### 236차 — 사이드바 펼침·다크 완료 초록·안내 제거·인쇄 이동 + 주요 업무 시안 준비
- **사이드바 펼침 이상**: 접을 때는 자연스러운데 펼칠 때 글자가 갑자기 나타남 — 원인은 `.sbsc`의
  `overflow-x:hidden` 이 mini 에서 잘린 내용을 확 보여줌. 메뉴 항목(`.nvi .nil`)과 분류 라벨
  (`.sbsl .sbsl-long`)에 opacity 전환(.18s/.06s, 펼침 시 .08s delay)을 걸어 부드럽게 페이드.
- **다크 모드 완료 버튼 회색**: `html.dark .stx .stx-done{color:#454D5C}` 가 `.stx.on .stx-done`
  보다 명시도가 높아 초록이 덮였다. `html.dark .stx.on .stx-done{color:#3BA85C}` 추가.
- **사용 안내 버튼 제거**: sb-tools 영역의 `data-act="set.guide"` 버튼 전체 삭제.
  `openReadme` 함수 자체는 설정 등에서 쓸 수 있으므로 유지.
- **인쇄 버튼**: 주요 업무(report) 내 `rpt.print` 버튼 제거 → 사이드바 sb-tools 에 `#sbPrintWrap`
  인쇄 아이콘 추가(검색 앞), `sb.print` 액션 등록. report·defect 뷰에서만 노출(`sbPrintSync`).
- **주요 업무 시안 구조 확인**: 현재는 권역별 카드 안에 완료·예정 2열 그리드. 사용자 요청 —
  완료와 예정을 레이아웃 분리하되 권역별 세로 시작점은 맞추기 + 디자인 개선.
  → 시안은 대화가 길어져 **다음 세션에서** 구체적으로 진행할 예정.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·r2·r14·wid·sweep·print-full 통과.
- app.js?v=265 · APP_VER 2.5.1.

### 237차 — 주요 업무 페이지 4안(완료/예정 분리 + 권역 탭) 구현
- **rReport 전면 교체** — 시안 4안 기준: 완료·예정을 독립 카드(2열 그리드)로 분리, 권역은 탭으로 전환.
  각 카드 상단에 3px 색 띠(초록/파랑) + 체크/시계 아이콘 배지 + 우측 기간 표시.
  헤더: 화살표(이전/이번/다음 주기) + "완료 8/6~12 · 예정 8/13~19" 기간 한 줄(KPI 칩 기각).
  탭: 밑줄 인디케이터 + 건수 배지(선택 탭은 파란 톤).
  테이블: uppercase 헤더·hover 하이라이트·담당자 칼럼 세로선·12.5px 본문.
- **rptRows 담당자 필터 추가** — 원래는 `S.tasks[sid]`로 전체를 탐색했는데, 업무가 팀 id 아래
  저장돼 담당자 id 로는 빈 결과가 나왔다. 담당자의 팀을 찾아 그 팀의 tasks 에서 assignees 로 필터.
- `S.rptReg`(선택 권역 탭) 상태 추가, `rpt.tab` 액션 등록.
- @media print: 탭 숨기고 전 권역 2열 펼침.
- 인쇄 시 탭 전체 펼침은 아직 미구현(탭 활성 권역만 인쇄) — 다음 세션에서 처리.
- 게이트: 감사 FAIL 0·WARN 0, 스모크·sweep·print-full 통과.
- app.js?v=266 · APP_VER 2.5.2.

### 238차 — 종합검사(데드코드·버그) · 주요 업무 월별 현장 뷰 · README 전면 재작성
- **버그: 차단 계정 복구 불가** — 계정 표가 roster() 만 써서 blocked 를 걸러, 차단하면 표에서
  사라져 UI 로 되돌릴 수 없었다(콘솔행). rOrg 에서 관리자에게만 차단 계정을 표에 합류시켜
  같은 자리에서 권한을 되돌릴 수 있게 했다. roster() 자체는 그대로(담당자 선택엔 차단 제외 유지).
- **버그: gotoTask 펼침 무효** — go() 가 화면 전환 때 S.tkOpen 을 접는데 gotoTask 가 그 앞에서
  세팅해 지워졌다(찾기·월별 뷰에서 이동해도 카드가 안 펼쳐짐). 세팅을 go() 뒤로 옮겼다.
- **버그: rptRows 단일 가방 탐색** — pid 가 든 첫 가방 하나만 훑어, 팀 id·담당자 uid 두 자리에
  나뉘어 저장되는 업무(planToTask 는 첫 담당자 uid 아래 저장) 중 일부가 주간 보고에서 빠졌다.
  전 가방을 훑어 assignees 로 모으게 재작성. 도달 불가 중복 분기(`if(!it)`)도 제거.
- **데드코드 제거** — ST_LBL · TK_ST 상수, `.rp-card` 인쇄 CSS.
- **사용 안내 진입점 복원** — set.guide 핸들러·뷰어는 살아 있는데 버튼이 유실돼 있었다(감사 WARN).
  사용자 지시대로 **설정 > 도움말 카드**에 열기 버튼을 넣었다(미사용이던 #i-help 심볼 사용).
- **주요 업무: 월별 현장 뷰 신설** — 헤더 세그먼트 [주간 보고|월별 현장]. 월 이동(◀ 오늘 ▶),
  권역 탭(주간과 S.rptReg 공유), 현장별 카드에 그 달 업무를 상태 아이콘·담당자·날짜로 나열.
  반복 업무는 recurDates 로 그 달 회차만 전개, 완료는 회차별(doneOn·occSrc). 업무 없는 현장은
  「업무 없음」으로 흐리게 남긴다(사용자 지시 — 커버리지 확인용). 현장 미지정·공통은 맨 아래 별도 카드.
  줄 클릭 → gotoTask. 상태 S.rptMode·S.rptYm, 액션 rpt.mode·rpt.mon·rpt.go, CSS rpm-*.
  기한 없는 업무는 월 화면에 넣지 않는다(업무 목록 담당).
- **README 전면 재작성** — 현행 기준. 낡은 서술 삭제(코멘트·멘션·종 알림·별도 '내 업무' 화면·
  remind 필드·상단바 ? 버튼), 주요 업무 두 보기·설정 표·sid 규칙("집계는 assignees 로") 문서화.
- 게이트: node --check · 감사 FAIL 0 · WARN 0 · puppeteer 로 월별/주간/설정/사용 안내/행 클릭 이동 확인.
- app.js?v=296 · APP_VER 2.9.0.

### 239차 — 더보기 글자 크기 · 위젯 링크 열림 버그 · 고위험 구분
- **더보기(「외 N건 ›」) 글자를 막대와 같게** — 기본 11→12px, 위젯 11.5→11px(*wfz, 막대 fc-event-title 과 동일),
  모바일 9.5→10.5px. 900px 구간은 이미 12px 로 막대와 같아 그대로.
- **버그: 위젯에서 업무 링크를 누르면 탐색기가 열림** — open crate 5.x 의 윈도우 구현은
  `cmd /c start "" "<주소>"` 인데, **cmd 는 따옴표 안에서도 `%…%` 를 환경변수로 치환**한다.
  한글 경로가 퍼센트 인코딩된 원드라이브·쉐어포인트 링크(`%ED%9E%90…`)는 그 자리가 지워져 주소가 깨지고,
  주소로 인식되지 않은 문자열을 셸이 경로로 보아 **탐색기가 떴다**.
  `win32::ShellExecuteW` 를 직접 링크해 문자열을 그대로 셸에 넘기는 `open_url()` 을 만들고,
  hpw-open 과 트레이 '브라우저 앱 열기'가 이를 쓴다(실패 시 예전 방식으로 한 번 더 시도).
  폴더를 여는 '진단 폴더 열기'는 open::that 그대로 — 거기서는 탐색기가 맞다.
  ⚠ **Rust 컴파일은 이 환경에서 검증하지 못했다**(툴체인 없음) — Actions 빌드 로그를 확인할 것.
  widget-lite 1.1.0 → **1.2.0**. tauri.conf.json 이 1.0.0 으로 어긋나 있던 것도 함께 맞췄다
  (워크플로는 Cargo.toml 만 읽지만 문서 규칙상 두 값은 같아야 한다).
- **업무 구분에 '고위험' 추가**(TK_KIND 두 번째 — 일반 다음). 고위험 업무는 **제목 앞 빨간 느낌표**:
  달력 막대는 `.fc-event.risk` ::before 로 그리고 **공통 업무의 삼각형보다 우선**한다
  (공통 여부는 속 빈 막대로 이미 드러나므로 겹칠 때 고위험을 살린다).
  목록·일자 패널·월별 현장은 `riskMark()` 가 `.risk-b` 배지를 넣는다.
  ⚠ DB 규칙의 kind 는 길이 10 이하 문자열 검증뿐이라 규칙 변경은 필요 없다.
  ⚠ kindSplit 은 그대로 — 진행경과/처리계획 두 칸은 '일반'만 쓴다(요청 범위 밖이라 건드리지 않음).
- 게이트: 감사 FAIL 0 · WARN 0 · puppeteer 로 폰트 12px 일치·고위험 저장/표시·링크 URL 무손상 확인.
- app.js?v=297 · APP_VER 2.9.1 · widget-lite 1.2.0.

### 240차 — 더보기 글자 굵기 · 고위험 표식 2-C안(윤곽 원) 확정
- **더보기(「외 N건 ›」) 굵기 700 → 500** — 막대 글자(.fc-event, font-weight:500)와 같게. 900px 구간도 함께.
  239차에 크기만 맞추고 굵기를 놓쳐 여전히 도드라져 보였다.
- **고위험 표식을 2-C안(윤곽 원 안 느낌표)으로 확정.** 시안 6종 + 색 변형 4종을 실제 달력에 렌더해
  비교한 뒤 사용자가 고름. 채택 이유: 색 막대 배경은 담당자가 자유롭게 고르므로 **흰색·검은색 등
  고정색은 어떤 색에서는 반드시 대비가 무너진다.** currentColor 로 그리면 막대 글자색을 따라가고,
  밝은 색 막대(on-light)에서 글자가 어두워질 때 표식도 함께 뒤집힌다(노랑 막대로 확인).
- ⚠ **글자 '!' 로 그리지 않는다** — 글리프가 baseline 에 맞춰져 원 안에서 위로 뜬다(사용자 지적).
  SVG 아이콘을 `--risk-ic` 변수에 담아 **mask 로 깔고 background-color 로 색을 준다** —
  느낌표가 원의 정중앙에 온다. 달력 막대(::before)와 목록 배지(.risk-b)가 같은 변수를 쓴다.
  riskMark() 는 이제 빈 span 을 낸다(안에 글자를 넣으면 안 된다).
  목록·일자 패널·월별 현장은 배경이 늘 카드라 색을 빨강(다크 #F87171)으로 고정한다.
- 게이트: 감사 FAIL 0 · WARN 0 · 앱 12px/500 · 위젯 11px/500 로 막대와 일치 확인, 라이트·다크 렌더 확인.
- app.js?v=298 · APP_VER 2.9.2 · widget-lite 1.2.0(239차의 링크 수정 — **아직 미배포 상태였다**).

### 241차 — 더보기 글자 잘림·간격
- **「외 N건 ›」의 위·왼쪽이 잘리던 문제** — 이 링크에 line-height 지정이 없어 위 계통에서 **1(=글자 크기와 같은 값)**
  이 내려왔다. 줄 상자에 여유가 없으니 한글 위쪽 획이 상자 밖으로 나가 렌더 배율에 따라 잘렸다.
  `line-height:1.35` 를 못 박아 해결(12px → 줄 상자 16.2px). 위젯·모바일은 상속으로 함께 고쳐진다.
- **막대와 너무 붙어 보이던 간격** — day-bottom padding-top 1px + 막대 아래 margin 2px = 3px 로,
  막대 사이 간격(4px)보다 좁았다. padding-top 을 3px 로 올려 5px 로 벌렸다(막대 사이보다 한 칸 넓게).
  900px 구간 0 → 2px, 620px 구간 0 → 2px 도 같은 취지로 함께.
  ⚠ 사용자가 "완료 막대 아래는 안 그런다"고 본 것은 완료 막대가 opacity .72 라 경계가 흐려
  간격이 상대적으로 넓어 보였기 때문 — 실제 값은 두 경우가 같다.
- `#fcal .fc-event.done{opacity:.5}` 중복 선언 제거(바로 다음 줄 .72 에 즉시 덮여 무효였다).
- app.js?v=299 · APP_VER 2.9.3.

### 242차 — 「외 N건」이 좁은 화면·위젯에서 왼쪽에 붙던 문제
- 900px 이하 구간이 `.fc-daygrid-day-bottom` 의 **display 를 block 으로 덮고 있었다.**
  기본 규칙(414행)의 `justify-content:flex-end` 가 함께 무효가 되어 **좁은 화면과 위젯에서만** 왼쪽으로 갔다
  (위젯은 창이 좁아 늘 이 구간에 든다 — 그래서 "우측 하단인 줄 알았는데 좌측인 경우가 있다"가 나왔다).
- ⚠ **text-align:right 로는 안 돌아온다** — 링크 자신이 display:block 이라 글자 정렬이 박스에 먹지 않는다.
  display 를 덮지 않고 flex 를 그대로 이어 쓰는 쪽으로 고쳤다(margin·padding 만 구간별로 조정).
- 확인: 1500 / 860 / 위젯 420 / 모바일 560px 네 폭에서 모두 셀 오른쪽 끝(여백 2~7px)에 붙는다.
- app.js?v=300 · APP_VER 2.9.4.

### 243차 — 위젯 전 환경 점검(6개 폭 × 글자 85/100/140%)
검사: 위젯 기본·유리·넓게 / 브라우저 / 좁은 창 / 모바일 6개 환경, 위젯 글자 크기 3단계.
**런타임 오류·콘솔 오류 0**, 화면 전환·팝업·내 업무·찾기·위젯 설정·키보드 좌우 이동 모두 정상.
고친 것 —
- **고위험 표식이 위젯 글자 크기를 안 따랐다.** 11px·13px 로 못 박아 두어 위젯을 140% 로 키우면
  글자 15px 옆에 표식만 11px 로 남고, 85% 에서는 글자 9px 옆에 11px 로 튀었다.
  ⚠ **위젯에 들어가는 치수는 px 로 못 박지 말 것**(--wfz). em 으로 바꿔 세 배율에서 정확히 비례한다.
- **위젯 '내 업무' 패널의 메타 순서가 섹션마다 반대였다** — 공통 줄은 `구분 · 현장`,
  미완료·보류 줄은 `현장 · 구분`. 앱 본체(업무 목록·일자 패널)가 구분을 앞에 두므로 그쪽으로 통일.
  아침 확인 창도 같은 이유로 `날짜 · 구분 · 현장` 으로 맞췄다.
- **위젯 '내 업무'에서 같은 업무가 두 번 나왔다** — teamTasks() 가 "팀 가방(sid)에 있으면 공통" 으로
  판정했는데, **업무 목록 화면에서 담당자를 지정해 만든 업무도 팀 가방에 남는다.**
  그래서 공통 섹션(sid 기준)과 미완료 섹션(assignees 기준) 양쪽에 걸렸다.
  공통 판정을 **assignees 가 비었는지**로 바꿨다 — 달력 막대의 공통(속 빈 막대) 판정과 같은 규칙이다.
  ⚠ 원칙 재확인: **집계는 assignees 로 하고 sid 에 기대지 않는다.**
- **주간 보고 표에 고위험 표식이 없었다**(월별 현장·목록·달력에는 있었다) — rp-t 에 riskMark 추가.
남긴 것(판단 필요) —
- **반복 업무가 '내 업무'·업무 목록에서 원본 시작일로 뜬다.** mineTasks() 가 회차를 전개하지 않아
  매주 반복 업무는 늘 과거 날짜로 남는다(달력은 회차가 정상). 회차 전개 + 회차별 완료 판정이 필요해
  설계 변경 규모 — 사용자 판단 후 착수.
- **메일의 공통/담당 분리도 sid 기준**(app.js mailItems, scripts/weekly.mjs). 같은 이유로 담당자 있는
  업무가 '공통'에 들어갈 수 있다. 앱과 스크립트를 함께 고쳐야 해 이번엔 손대지 않았다.
- app.js?v=301 · APP_VER 2.9.5.

### 244차 — 반복 업무가 목록에서 첫 회차 날짜로 뜨던 문제
- 243차에 남겨 둔 항목. 사용자 확인 결과 팀에서 반복은 거의 안 쓰고 **매월 14일·28일** 정도인데,
  매월 반복은 시작일이 한참 과거라 목록·내 업무에서 **늘 지연**으로 보였다(달력만 회차가 정상).
- `taskDate(sid,iid,it)` 추가 — 반복이면 **아직 안 끝난 회차 중 오늘에서 가장 가까운 것**을 낸다.
  지난 회차가 남아 있어도 다음 회차가 더 가까우면(주기의 절반을 넘겼으면) 다음 회차를 본다 —
  ⚠ **완료 체크를 건너뛰는 업무가 영영 지난 날짜에 묶이지 않게.** 반복이 아니면 it.date 를 그대로 낸다.
- 적용: 업무 목록 카드(날짜 배지 dueInfo·메타 날짜), 위젯 내 업무 세 섹션(공통·미완료·보류),
  mineTasks·mineHolds·집계 보조의 정렬. 회차 전개·완료 판정 자체는 그대로라 설계 변경이 아니다.
- 확인(오늘 8/15 기준): 매월14 미완료 → 8/14(지연) · 매월14 이번 회차 완료 → 9/14 ·
  매월28(7/28 미완료) → 8/28 · 일반 업무 → 그대로. 위젯·브라우저 모두 달력 회차와 일치.
- ⚠ 남은 것: **메일의 공통/담당 분리는 여전히 sid 기준**(app.js mailItems, scripts/weekly.mjs).
- app.js?v=302 · APP_VER 2.9.6.

### 245차 — 메일 발송 기능 제거
사용자 결정: 주간 메일은 쓰지 않는다. 관련된 것을 전부 걷어냈다.
- 지운 파일: `scripts/weekly.mjs` · `scripts/mail-common.mjs` · `.github/workflows/mail.yml`.
- app.js: mailItems·mailHTML·mailPreview·saveMailCfg·mailRecipients·weekRange·weekLabel,
  `mail.preview` 핸들러, `set.mail` 위임, rCfg 의 토글 반영 제거(rCfg 는 rBk 만 남았다).
  ⚠ **teamIds·isTeamSid 는 남겼다** — 업무 목록이 '개인 묶음인지' 가릴 때 쓴다(메일 전용이 아니었다).
- index.html: 설정의 '메일 발송' 카드(도움말 카드가 왼쪽 첫 카드가 됐다), `.ml-pv`·`.mlp-*`·`#mb.mlw` CSS.
- database.rules.json: `cfg/mail` 검증 블록 삭제. ⚠ DB 에 남아 있는 옛 `calapp/cfg/mail` 값은
  **읽는 곳이 없어 무해**하다(백업에도 그대로 실린다). 규칙상 이제 그 경로에 새로 쓸 수는 없다.
  ⚠ 백업 되돌리기는 cfg 를 쓰지 않으므로 규칙 변경과 충돌하지 않는다.
- static-audit.mjs 도 지운 두 스크립트를 더 읽지 않게 고쳤다(안 고치면 ENOENT 로 죽는다).
- 저장소 Secrets(FIREBASE_SERVICE_ACCOUNT·BREVO_API_KEY·MAIL_FROM)는 더 쓰지 않는다 — 지워도 된다.
- 243차에 남겨 둔 '메일의 공통/담당 분리가 sid 기준' 항목은 **이 제거로 함께 없어졌다.**
- 게이트: 감사 FAIL 0 · WARN 0, 설정 화면·사용 안내·백업 데이터 생성 확인, 오류 0.
- app.js?v=303 · **APP_VER 3.0.0**(기능 하나가 빠져 메이저를 올린다).

### 246차 — 정밀 검토(데드코드·불일치)
감사 통과 상태에서 감사가 못 잡는 것들을 따로 훑었다. 찾은 것 —
- **주간 보고에 반복 업무가 통째로 빠져 있었다.** rptRows 가 it.date/it.end 만 보는데 반복 업무의
  date 는 첫 회차(과거)라 어느 주기에도 안 걸렸다. **달력·월별 현장에는 나오는데 보고서에만 없었다.**
  recurDates 로 회차를 펼치고 완료도 회차별(doneOn)로 보게 고쳤다. 표의 날짜도 그 회차 날짜를 쓴다.
  ⚠ 이번 주기의 **미완료**가 어느 표에도 안 나오는 것은 설계 그대로다(한 일 / 할 일 두 표).
- **미사용 상태 필드 `S.dfRep` 제거** — 선언뿐이고 읽는 곳이 없었다.
- **CSS 인접 중복 선언 4건 정리** — `.rpt .page`(break-after 와 page-break-after 를 두 규칙으로 나눠 둠),
  `.rec-tbl th`(position:sticky 재선언), `.rec-tbl td.wide`(뒤 줄이 min-width 260→220 을 덮어 260 이 죽어 있었다),
  `.tk-new`(margin-bottom 같은 값 두 번). ⚠ 미디어쿼리 오버라이드는 정상이므로 건드리지 않았다.
확인해서 문제 없던 것 —
- cleanTask 전 필드 왕복(백업→복원) 무손실, 저장 필드 ↔ database.rules.json 양방향 일치,
  앱이 쓰는 DB 경로 15개 모두 규칙에 존재.
- id 참조 정합(동적 생성분 포함), data-act 짝, 죽은 함수·CSS 클래스 0.
- 6개 환경 × 위젯 글자 3배율 순회 오류 0, 인쇄 모드(탭 숨김·2열) 정상, build-single 정상.
- 위젯 스텁(window.newMentions·bootBrief·bkExport·bkNote) 유지, 상태값 정규화(stOf/stEff) 안전.
- taskDate 성능 0.4ms, until 이 지난 반복은 시작일로 폴백(회차가 없으니 그 이상은 무의미).
남긴 것 —
- `store.migrateRemote()` 의 옛 경로(calapp/plans·calapp/recur) 이관 코드는 그대로 뒀다.
  이미 옮겨진 환경에서는 읽고 바로 빠지지만, **아직 안 옮긴 기기가 있을 수 있어** 제거는 위험하다.
- app.js?v=304 · APP_VER 3.0.1.

### 247차 — 공휴일 문구 위치 · 달력 이벤트 범위 보강
- **오늘이면서 공휴일인 칸에서 공휴일 문구가 가운데로 밀렸다**(사용자 보고: 광복절).
  `.dhol` 이 `flex:1;margin-right:auto` 라 '오늘' 배지와 공휴일 문구 **둘 다** 그 규칙을 받아
  공간을 나눠 갖고 서로를 밀어냈다. dayCellContent 에서 겹칠 때만 '오늘' 에 `wnm` 을 붙이고
  `#fcal .dhol.dtoday.wnm{flex:none;margin-right:0;}` 로 자리를 양보시켰다 → '오늘 광복절' 이 왼쪽에 나란히.
  ⚠ `:has()` 나 `:only-of-type` 은 쓰지 않았다(WebView2 버전 편차·같은 태그 형제 문제).
- **buildEvents 가 범위를 CAL.view 에서 읽던 것을 FullCalendar 가 넘겨주는 info.start/end 로** 바꿨다.
  달을 넘길 때 뷰 상태가 갱신되기 전에 계산되면 그 달 반복 회차가 통째로 빠질 수 있는 구조였다.
  ⚠ info.end 는 배타적(다음 날)이라 하루 빼서 쓴다. 인자 없이 부르면 종전대로 visibleRange() 로 떨어진다.
  ⚠ **사용자가 보고한 '다음 달에 반복이 안 뜬다' 는 재현하지 못했다** — 브라우저·위젯에서 버튼으로
  3개월 이동, 목록 폼·달력 폼 저장 경로 모두 정상이었다. 위 보강은 예방 차원이고, 원인은 별도 확인 필요
  (반복 종료일이 과거로 들어갔거나, 시작일이 기대와 다른 경우를 먼저 볼 것).
- app.js?v=305 · APP_VER 3.0.2.

### 248차 — 안 쓰이는 파일·죽은 셀렉터 정리
- **`scripts/test/smoke.mjs` 삭제.** puppeteer 가 있어야 도는데 회사 PC 는 npm 이 막혀 실제로 돌린 적이
  없었고, 검사 내용(화면 6개 전환·콘솔 오류 0·달력 42칸)은 세션마다 하는 검증에 모두 포함된다.
  ⚠ 다시 필요해지면(집 PC·Actions 자동 검사) 이 항목을 근거로 되살릴 것 — 셀렉터는 당시 기준으로 유효했다.
  README 의 구성 목록에서도 뺐다. `static-audit.mjs` 는 의존성 없이 돌고 실제로 문제를 잡으므로 유지.
- **죽은 id 셀렉터 2개 제거** — `body.snap` 규칙의 `#tbBell`·`#tbHelp`. 상단바 알림·도움말 버튼은
  이미 없어진 요소다(도움말은 245차에 설정으로 옮겼다). ⚠ 죽은 CSS 검사는 **클래스만** 보므로
  id 셀렉터는 감사에 안 걸린다 — 이번처럼 따로 훑어야 한다.
- vendor 12개는 전부 실제로 참조·사용 중임을 확인(LZString·marked·DOMPurify·XLSX·ChartDataLabels·
  Chart·firebase appCheck 까지 호출 지점 있음). 지울 것 없음.
- app.js?v=306 · APP_VER 3.0.3.

### 249차 — 업무 목록: 카드 머리를 탭으로 · 필터에서 권역 제거
- **시안 3안 중 1안 채택**(탭이 머리 줄을 대신한다). 제목("팀 업무"/"담당 업무")을 지우고 그 자리에
  주요 업무 화면의 탭(.rp-tabs/.rp-tab/.rp-tcnt)을 그대로 넣었다.
  ⚠ **밑줄은 카드 양 끝까지 가지 않는다**(사용자 지시) — 밑줄을 .tkm-tabh(머리 줄)가 그리고
  좌우 패딩 16px 을 유지해 안쪽에서 끊는다. 탭 묶음 자신은 border-bottom 없음.
- 탭 구성 — A(팀 업무): 전체 · 공통 업무 · 팀장/안전/원가 각 사람. B(담당 업무): 전체 · 권역별 · 권역 미지정.
  상태는 `S.tkA`·`S.tkB`, 액션 `tk.tabA`·`tk.tabB`. **두 카드는 서로 영향을 주지 않는다.**
  ⚠ data-act 는 리터럴로 적었다 — 문자열 조합이면 정적 감사가 발신처를 못 찾아 WARN 이 뜬다.
- **집계·목록을 assignees 기준으로 바꿨다.** `tkTargetItems(id)` 신설 —
  팀 id 면 그 팀 가방의 담당자 없는 업무, 담당자면 **모든 가방에서** assignees 에 그 사람이 있는 업무.
  ⚠ 전에는 taskCount·openItems·taskListHTML 이 전부 저장 위치(sid) 기준이라,
  **업무 목록 화면에서 담당자를 지정해 만든 업무가 그 담당자 목록에서 빠져 있었다**(카운트도 0).
  각 항목은 제 sid 로 그린다 — 수정·삭제 경로가 그 값이다.
- **왼쪽 패널의 대상 목록 제거**(탭과 완전히 중복). 작은 달력 · 보류한 업무 · '팀 전체'만 남기고
  tkwrap 폭을 320 → 264px 로 줄여 목록에 줬다. `.tks-item .std`(하위 항목 점) CSS 도 함께 제거.
  ⚠ isTeamSid·teamIds 는 이 정리로 마지막 사용처가 사라져 삭제했다.
- **업무 추가**: 팀 전체 화면에서는 화면을 옮기지 않고 팀 업무 카드 안에 폼을 연다
  (A 탭이 담당자면 그 사람 자리, 아니면 팀 공통 자리). 담당 업무 카드의 추가 버튼은 뺐다 — 대상이 모호하다.
- **필터에서 권역 제거**(사용자 지시 — 탭이 대신한다). S.tkF.reg 삭제, tkMatch 의 권역 분기 삭제,
  옛 저장값의 reg 는 filtLoad 에서 버린다. ⚠ 달력 화면 필터(S.filter.reg)는 그대로 둔다 — 거기엔 탭이 없다.
- 게이트: 감사 FAIL 0 · WARN 0, 6개 환경 오류 0, 라이트·다크·좁은 창 렌더 확인, 반복·주간 보고 회귀 확인.
- app.js?v=307 · APP_VER 3.1.0.

### 250차 — 249차 지적사항 반영(구분선·탭 라벨·필터 위치·오른쪽 추가 버튼)
- **탭 밑줄이 카드 양 끝까지 갔다**(249차에 잘못 만든 것). ⚠ **border-bottom 은 패딩 바깥(요소 경계)에
  그려져 좌우 패딩으로 줄일 수 없다** — `::after`(left/right:16px)로 그려 안쪽에서 끊는다.
- **활성 탭의 파란 줄과 회색 밑줄이 어긋나 두 줄로 보였다.** 원인 둘:
  ① `.tkm-h{align-items:center}` 때문에 탭이 머리 높이의 가운데에 떠 있었다 → `align-items:stretch`,
     업무 추가 버튼만 `align-self:center`.
  ② 반응형의 `.tkm-h{padding:13px 16px 11px}` 가 내 `.tkm-tabh{padding:0 16px}` 를 **뒤에서 이겼다**
     → `.tkm-h.tkm-tabh` 두 클래스로 명시도를 올렸다. 이제 파란 줄과 회색 선이 같은 자리다.
- **탭 라벨을 직급으로만**(사용자 지시) — 같은 직급이 둘 이상일 때만 이름을 덧붙인다.
- **담당 업무 카드에 업무 추가 복원.** 폼이 어느 카드에 뜰지 `S.tkNewSide`('a'·'b')로 기억한다.
  B 에서 만든 업무는 팀 자리에 담고 담당자는 폼에서 고른다(권역 탭만으로는 사람이 정해지지 않는다).
- **왼쪽 '팀 전체' 카드 삭제, 검색·필터를 그 자리(왼쪽 칸 맨 아래 — 작은 달력·보류한 업무 다음)로 이전.**
  좁은 칸(264px)이라 고급 필터는 한 줄 셋이 아니라 세로로 쌓는다
  (`.tkside #tkFcard .dp-fadv .dp-frow{flex-direction:column}`).
  ⚠ "그 자리"는 **없앤 카드가 있던 자리**를 뜻한다 — 처음에 맨 위로 올렸다가 되돌렸다.
- app.js?v=309 · APP_VER 3.1.2.

### 250차 — 업무 목록 카드를 한 줄·열 고정으로(시장조사 후 1안 채택)
사용자 지적: 패널이 쓸데없이 넓어 보인다. **원인은 폭이 아니라 배치였다** —
2행 카드의 메타를 좌우 양끝(pm-l / pm-r)으로 밀어 두어 칸이 넓을수록 가운데가 비고,
행마다 현장·담당자 글자 수가 달라 열이 맞지 않았다.
- 조사 근거: Asana 는 리스트뷰를 "스캔하기 쉽게 격자에 가까운 구조"로 개편했고, Linear 는 체인지로그에
  "모든 행에서 날짜가 제대로 정렬되도록" 고친 항목을 실었다 → **열 정렬이 스캔의 핵심**.
  Notion 은 반대로 제목+속성 요약을 좌측에 붙인다. 폭은 WCAG 1.4.8(80자, CJK 40자) 기준으로 상한을 두는 게 통상.
  필터는 항목이 적으면 상단, 많으면 사이드바 — 우리는 셋뿐이라 상단이 맞다(249차에 왼쪽 아래로 내린 것을 되돌렸다).
- **taskItemHTML 을 한 줄 그리드로 재작성.** `.tk-row{grid-template-columns:8px 1fr 116px 52px auto}` —
  [색 원][제목][현장][날짜][조작]. 행 높이 62 → 40px.
- ⚠ **열을 늘리지 말 것.** 처음엔 구분·담당까지 열로 뒀다가 되돌렸다 — 2열 split 에서 카드가 440px 뿐이라
  제목이 죽었다. **구분은 '일반'이 대부분이라 같은 글자만 반복**되고(고위험은 제목 앞 표식이 이미 알린다),
  **담당자는 목록이 담당자별로 묶여 있어 소제목과 겹말**이다. 둘 다 제목 뒤 `.tk-sub-i` 로 흐리게(공동 담당만).
- 목록 맨 위에 열 이름 줄(`tkHeadRowHTML`). 탭 구분선에 바짝 붙이고 첫 분류의 위 선은 지운다
  (안 그러면 머리가 두 겹으로 보인다 — 사용자 지적).
- 좁아지면 현장 열부터 숨긴다(1180px 이하).
- 검색·필터 줄: `#tkFcard` 를 grid 2열로 — **검색 50% · 필터 셋이 나머지 50% 를 균등 분배**. 접기 버튼 제거(늘 펼침).
- 탭 높이 44 → 50px(padding 15px, 13.5px).
- app.js?v=310 · APP_VER 3.2.0.

### 251차 — 250차를 실사용 화면(스크린샷)으로 다시 보고 고친 것
⚠ **250차는 시드 4~5건으로만 확인하고 넘겼다.** 실제 화면(업무 28건·현장명 12자·배경 사진)에서 문제가 드러났다.
회차마다 **실운용 규모로 렌더해 눈으로 볼 것**(현장 12개·업무 수십 건).
- **열 이름 줄 제거.** `.tk-list` 가 스크롤되면 머리가 위로 밀려 **반쯤 잘린 채** 남았다(sticky 가 아니다).
  열이 셋뿐이라 이름 없이도 자명하다 — sticky 로 붙이는 대신 아예 뺐다.
- **목록에 페이드를 걸지 않는다**(사용자 지적: "왜 하단에 페이드가 걸리는데").
  `.tk-list` 는 스크롤 신호용 페이드(26px) 대상이었는데, 줄 높이를 40px 로 줄이고 나니
  **마지막 줄이 반쯤 지워졌다.** fadeOne 의 제외 목록에 tk-list 추가(rec-wrap·pv-scroll 과 같은 취급).
- **항목마다 두르던 테두리·배경 제거.** 한 줄짜리 행에 카드 테두리를 두니 알약이 줄줄이 뜬 꼴이었다.
  행 사이 얇은 선(inset box-shadow)만 남기고 배경은 hover·펼침에만 — 리스트 뷰의 통상 방식.
  ⚠ border 가 아니라 inset box-shadow 다 — border 를 쓰면 행 높이가 1px 씩 늘어 열이 어긋난다.
- **날짜 색은 지난 것(over)만 빨강.** 3일 이내(soon) 노랑까지 물들여 목록이 신호등처럼 보였다.
- 현장 열 116 → 150px(실제 현장명이 '힐스테이트 고덕 스카이시티' 처럼 길다).
- app.js?v=311 · APP_VER 3.2.1.

### 252차 — 251차에서 시안을 임의로 훼손한 것을 되돌림
⚠ **채택된 시안(1안)을 그대로 만들지 않고 내 판단으로 열을 빼서 다시 만들었다.** 하지 말 것.
시안과 달라져야 할 이유가 생기면 **고치기 전에 말할 것.**
- **열 이름 줄 복원** — 251차에 "스크롤에 밀려 잘린다"는 이유로 지웠는데, 답은 제거가 아니라
  `position:sticky;top:0` 이었다. 목록을 내려도 맨 위에 붙어 있다.
- **구분·담당 열 복원**(제목 뒤에 흐리게 붙이던 것을 되돌림). 제목 뒤에는 시각·반복만 남긴다.
- **색 원과 제목 간격** — `.p-col` 이 `margin-left:6px` 를 갖고 있어 열 안에서 오른쪽으로 밀려
  제목에 붙었다. `.tk-row .p-col{margin-left:0}` 로 지우고 grid gap 을 12px 로.
- ⚠ **열 개수는 창 폭이 아니라 카드 폭(컨테이너 질의)으로 정한다.**
  창이 1720px 이어도 사이드바(220)+달력 칸(264)이 먼저 먹고 남는 폭을 2열로 쪼개 카드는 590px 뿐이다.
  창 폭으로 재던 첫 시도에서 **28건 중 24건의 제목이 잘렸다.**
  `.tkmain{container-type:inline-size}` + `@container tkpanel (min-width:520/610/700px)` 로
  현장 → 구분 → 담당 순으로 붙인다. 지원하지 않는 엔진에서는 기본(제목·날짜)로 떨어져 안전하다.
  두 칸 → 한 칸 전환 기준도 1200 → 1400px 로 올렸다(1200 에서 카드가 375px 였다).
- 검증: 1280·1500·1600·1720·2400px 다섯 폭에서 **제목 잘림 0**(현장은 1720px 에서 4건 말줄임).
- app.js?v=312 · APP_VER 3.2.2.

### 253차 — 열 정렬이 실제로 맞는지 재보고 고침
사용자 지시: "테이블처럼 각 내용이 헤더 열에 맞춰 배치되는지 다시 확인." 재 보니 **안 맞았다.**
- 원인: 마지막(조작) 열이 `auto` 였다. **링크가 있는 행만 아이콘이 하나 더 붙어**(링크·상태·수정 = 3개,
  없으면 2개) 그 열 폭이 46 ↔ 72px 로 달라지고, grid 는 행마다 독립이므로 **앞의 모든 열이 26px 씩 밀렸다.**
  헤더는 조작 열이 비어 폭 0 이라 가장 크게 어긋나 있었다.
- 조치: 조작 열을 **84px 로 못 박고** `.tk-acts{justify-content:flex-end}` 로 오른쪽 정렬.
  컨테이너 질의 4단계 모두 같은 값으로 맞췄다.
- 검증(1280·1600·1720·2400px): 헤더 포함 모든 행의 열 시작 위치·폭이 **완전히 동일**.
  예) 1720px → 구분 696/46 · 현장 754/140 · 담당 906/62 · 날짜 980/50 · 조작 1042/84.
- ⚠ **행마다 개수가 달라지는 것을 auto 열에 두지 말 것** — 각 행이 별개 grid 라 열이 어긋난다.
- app.js?v=313 · APP_VER 3.2.3.

### 254차 — 업무 목록 구조 재배치(사용자 지시 일괄)
- **업무 추가를 패널 머리에서 빼 검색 줄로.** 목록 위는 `tkSearchHTML()` — 검색(남는 폭 전부) + 업무 추가.
  ⚠ `.dp-fcard` 가 `flex-direction:column` 이라 그대로 두면 버튼이 검색 아래로 내려간다 → row 로 못 박았다.
- **필터 셋은 왼쪽 칸(보류한 업무 아래)에 별도 카드**(`tkFilterHTML`). 접기 없이 세로로 세운다.
- **보류한 업무에서 돌아갈 길** — 단일 화면 머리에 `← 전체 업무`(.tkm-back). 왼쪽 보류 카드도
  다시 누르면 teamall 로 돌아오는 토글이다(data-id 를 상황에 따라 바꾼다).
- **날짜 열 52 → 72px** — 기간 업무('8/16–8/18')가 안 들어갔다.
- **현장·담당 열은 값이 있을 때만 그린다.** 공통 업무처럼 둘 다 없는 묶음에서 빈 열이 자리만 먹었다.
  판정은 `tkColsOf(list,hideOwn)` 가 **데이터로** 하고, 목록 래퍼 클래스(.tkl/.tkl-s/.tkl-w/.tkl-s-w)로
  그리드를 고른다. ⚠ 열 판정은 **카드 하나에 하나** — 묶음마다 다르면 열이 어긋난다.
  ⚠ 담당자별 소제목이 있는 화면에서는 담당 열을 쓰지 않는다(겹말).
- **탭 구분선과 표 사이 간격 4px·0px 로 축소**(계속 지적받던 것). tkm-tabh margin-bottom 0,
  열 이름 줄 padding-top 0, 첫 분류의 위 선 제거.
- ⚠ **두 칸 배치는 1800px 초과에서만.** 1720px 에서도 사이드바(240)+왼쪽 칸(264)을 빼면 카드가 581px 뿐이라
  열을 다 넣으면 제목이 11자에서 잘렸다(실측). 그 아래는 한 칸으로 세운다 — 1720px 에서 카드 1174px·제목 650px.
- 조작 아이콘 패딩 제거(24px 고정), 열 폭 76px.
- 검증: 1280·1720·2400px 제목 잘림 0~1건 · 현장 잘림 0 · 헤더 포함 열 정렬 일치 · 6개 환경 오류 0 ·
  보류 진입/복귀/토글 동작 확인.
- app.js?v=314 · APP_VER 3.3.0.

### 520차 — 519차 지적 6건 일괄(필터 실종 근본 원인 · 하자 표 2건 · 완료 회색 · 열 폭 · 외 N건)
- **업무현황 왼쪽 칸(주간/현장별 전환·미니 달력·보류함·검색·필터)이 통째로 사라져 있던 근본 원인** —
  `miniCalHTML` 의 `</div>` 하나 초과. 브라우저가 `.tkside` 를 조기에 닫아 `.tkcol` 이 `.tkwrap` 밖으로
  밀려나고 왼쪽 칸이 높이 0 으로 접혀 있었다. "업무현황엔 필터가 없다"는 519차의 진단은 이것의 증상이었다.
  ⚠ 템플릿 조각을 고치면 **`<div` 여닫이 수를 세라** — 검증 스위트에 div 균형 검사를 넣어 잡았다.
- **`.msel{position:relative}` 복원** — 519차 필터 카드 정리 때 함께 지워져, 드롭다운(.msel-pop 의
  left:0;right:0)이 가장 가까운 배치 조상까지 올라가 붙었다. 업무현황에서는 그게 문서 전체라
  **화면 밖(맨 아래·전체 폭)에 그려져 "안 열림"으로 보였다.** 캘린더·위젯은 calFiltWrap 이 받아 주고 있었을 뿐.
- **드롭다운이 아래에서 잘리면 위로 편다** — `filt.msel` 이 열 때 overflow 조상(왼쪽 칸)의 하단과 재서
  모자라면 `.msel.up`(top:auto;bottom:100%+5px). 담당자·현장 목록이 86·70% 노출로 잘리던 것 → 100%.
- **대시보드 업체별 하자처리 현황: 시공업체 클릭 → 팀 전체 미처리 목록(그 업체만) 복원** —
  상위 10 접기 개편 때 rowH 를 새로 쓰면서 rec.list 링크가 빠졌던 회귀. 현장 페이지의 표(dfAggRowHTML)와
  같은 `data-act="rec.list" data-co` 방식. 합계·(미기재)·그 외 접기 행은 링크 없음.
- **현장 페이지 공종별/업체별 표의 마지막 전월대비를 건수로** — 장기미처리 비율의 %p 증감(+1.2p)으로
  표기하던 것을 대시보드와 같은 dfDeltaParts(lt−plt) 건수 배지로 통일(사용자 지시 "다른 곳들과 동일하게").
- **완료 업무는 업무명만 옅게** — `.tk-item.s2 .tkc`(권역·현장·날짜)와 wdone(이름 묶음 전체 완료 시
  이름 옅게, 337차) 폐기. stOfRow·allDone 계산도 함께 제거(전용이었다).
- **업무 목록 날짜 열 68 → 80px(전 변형)** — 기간(8/26–8/28 · 82px)이 호버로 나온 수정 버튼과 겹치던 것.
  tkl-nr-w 만 80px 로 고쳤던 389차 처방을 나머지 그리드(기본·s·w·s-w·nr·반응형 4곳)에 적용. 제목(1fr)이
  12px 양보한다. 실측: 기간 날짜 끝 ↔ 수정 버튼 9px.
- **캘린더 '외 N건' 칸 아래 11px 탈출 수정** — `calFitRows` 의 더보기 줄 높이(lh)를 문서상 첫
  `.fc-daygrid-day-bottom` 에서 쟀는데 **빈 칸의 것(2px)** 이 걸려 막대를 하나 더 넣고 있었다.
  링크가 실제로 든 day-bottom(18px)에서 재도록 수정 + 줄 높이가 다를 때를 대비해 가장 낮은 칸 기준으로.
- 검증: puppeteer(playwright-core) 스위트 신설 — 실운용 규모 시드(권역 3·현장 12·담당 8·업무 40+)로
  캘린더 외 N건(5개 칸 실측 여유 11px) · 완료 회색 범위 · 기간/수정 버튼 간격 · 업무현황 필터 4종 노출
  100% · 캘린더 5종 100%/위젯 3종+2종 98~100% · 하자 표 행 HTML/클릭 델리게이션(모달 제목까지) 전부 통과.
  static-audit FAIL 0 · WARN 0.
- app.js?v=520 · APP_VER 520.

### 521차 — 위젯 회귀 3건(519차 셸 개편의 위젯 미반영)
- **위젯에 앱 헤더가 떴다** — 519차에 헤더를 `#topbar` 에서 새 `#appHdr` 로 옮기면서
  위젯 숨김 목록(`body.wid …{display:none}`)에 넣지 않았다. 추가.
  ⚠ 셸 요소를 새로 만들면 **위젯 숨김 목록과 df-printing 규칙에 함께 등록**할 것.
- **위젯 배경 흰색** — 519차 본문 판 `#main{background:var(--bg)}` 이 유리 모드 투명 목록에 빠져
  있었다. `body.wid.glass #main` 추가. 셸 패딩(위 44px 헤더 자리·아래 14px)도 위젯에서는 제거
  (`body.wid #app{padding:0;gap:0}`) — 헤더를 숨겨도 44px 띠가 남았다.
- **필터 팝업 꼬리 위치 불일치** — 꼬리는 `.tb-pop::before{right:46px}` 공통값인데 46px 는
  상단바 팝업(알림 등) 기준이라 필터에서는 옆 버튼(‹⌂›) 아래를 가리켰다.
  `.cal-filt-pop::before{right:10px}` 로 여는 꺽쇠 밑에 둔다(앱·위젯 공통, 실측 오차 1.5px).
- 검증: 위젯(글라스) — 헤더 없음·#main 투명·가장자리 0px·드롭다운 5종 98~100% 노출,
  앱 — 꼬리 중심 1012.5 vs 버튼 중심 1011. static-audit FAIL 0 · smoke FAIL 0 · 자체 스위트 전부 통과.
- app.js?v=521 · APP_VER 521.

### 522차 — 위젯 마감 4건 + 전반 점검
- **위젯 달력 테두리 제거** — 232차 달력 프레임(`#fcal` 의 1px 테두리 띠)과 FC 자체 외곽선
  (`.fc-scrollgrid`)이 유리 위젯에서 상자로 보였다. `body.wid.glass` 에서 둘 다 0 — 칸 사이 선(td)은 남는다.
- **필터 팝업 꼬리 흰색** — 꼬리(::before)는 `--bg2` 기본색이라 다크 유리 팝업 위에 흰 마름모로 떴다.
  `body.wid` 에서 팝업과 같은 유리 색으로.
- **팝업 여는 높이 통일(간격 8px)** — 연·월 팝업이 위젯에서 제목 알약을 4px 덮었다.
  ⚠ 원인: `offsetTop` 은 offsetParent 기준이라 포함 블록이 다른 위젯에서 어긋난다 —
  openYMPick 이 **붙인 뒤 화면 좌표로 실측 보정**하도록 수정(포함 블록이 무엇이든 맞는다).
  앱·위젯 모두 알약 아래 8px = 필터 팝업과 동일.
- **위젯 업무패널 머리 위 여백 10 → 14px**(사용자 지시 — 상단에 3~5px 더).
- **전반 점검** — 전 화면 순회 콘솔·페이지 오류 0 · 필터 실적용(33→6건, 전부 완료 행) ·
  현장별/보류함 진입 · 달 왕복 후 외 N건 유지 · 위젯 420px 셸 확인 · static-audit / smoke FAIL 0 ·
  wdone 계열 잔재 grep 0.
- app.js?v=522 · APP_VER 522.

### 523차 — 위젯 유리 톤 회귀의 진범: 파비콘 줄의 잉여 `">`
사용자 지적 "유리톤이 예전과 다르다 — 주말이 원래 이 색이었나?"
- **원인**: `<head>` 의 파비콘 `<link>` 끝에 `">` 두 글자가 중복돼 있었다(51x 파비콘 교체 때 유입,
  배포본 대조로 v455 정상 · v513부터 오염 확인). head 안의 **텍스트는 head 를 조기 종료**시키므로
  본문 `<style>`(230KB)이 body 로 밀려났고, 그 결과 위젯의 동적 유리 스타일(`#wgDyn` —
  칸 rgba(24,28,38,.85) · 주말 rgba(57,52,61,.78) '붉은 끼')이 **뒤에 온 정적 규칙(.55/.5)에 역전**돼
  씻긴 톤으로 보였다. 위젯 설정의 대비(투명도) 슬라이더도 같은 이유로 그동안 무력했다.
- **수정**: 잉여 `">` 제거 — `<style>` 이 head 로 복귀, wgDyn 이 다시 마지막이 되어 이긴다.
  주말 색 값 자체는 예전과 동일(바뀐 적 없음) — 이긴 쪽이 달랐을 뿐이다.
- ⚠ **head 안에 글자 하나라도 흘리면 그 지점에서 body 가 시작된다.** 파비콘 data URL 처럼
  따옴표가 뒤엉키기 쉬운 속성을 고칠 때는 `document.head.contains(본문 style)` 로 확인할 것.
- 검증: sat 계산값 rgba(57,52,61,.78)·weekday rgba(24,28,38,.85) 실측(v455 와 동일),
  <style> 부모 HEAD 복귀, 전 스위트(static-audit·smoke·자체 3종) FAIL 0.
- app.js?v=523 · APP_VER 523.

### 524차 — 하자 인쇄 전면 회귀(519차 셸이 인쇄에 미친 것 3건) 수정
사용자 지적 "하자처리 현황·현장 페이지 인쇄가 다 틀어짐". 스텁 게시본으로 4개 인쇄 경로
(화면식 현장 4쪽 · 화면식 대시보드 2쪽 · 보고서 현장 3쪽 · 보고서 대시보드 2쪽)를 PDF 로 떠서 실측.
- **모든 인쇄가 1쪽에서 잘리던 주범** — 519차 셸의 `html,body{overflow:hidden}`(화면 스크롤바 방지)이
  인쇄 미디어에서 리셋되지 않아 **2쪽 이후가 통째로 사라졌다**(v520 배포부터). @media print 에서
  전 경로 무조건 `html,body{overflow:visible;height:auto}` 로 되돌린다.
  ⚠ html/body 에 overflow·height 를 못박으면 **인쇄 리셋도 반드시 함께** 넣을 것.
- **#appHdr(519차 새 헤더)가 지면 위에 찍혔다** — 인쇄 숨김 목록에 없었다(위젯 521차와 같은 유형).
  @media print 에서 무조건 숨김. #app 셸 패딩(위 44px)도 Ctrl+P 경로에서 리셋(df-printing 은 기존 990행대가 처리).
- **보고서 양식 @page** — 523차 head 복구로 rptPageCSS(margin:0)가 비로소 이긴다. 실측 비교:
  margin:0 = 지면 꼭 맞음(설계 · .page 794×1123px) / 8mm9mm(513~522 상태) = 기준일·전월대비 열이
  오른쪽에서 잘림. **현재가 설계 원복**이다 — 되돌리지 말 것.
- 검증 함정(재현 스텁): S.live 만 켜고 FB.db 없이 DF.plans/ana 를 안 채우면 rDefectSite 가
  로드 재시도 microtask 를 무한 반복해 **탭이 굳는다** — 스텁에는 plans/ana/FB.db 까지 채울 것.
- app.js?v=524 · APP_VER 524.

### 525차 — 현장 인쇄 빈 페이지의 진범(분석 카드 밀림) · 공가 페이지 소실 · 헤더 3건
사용자 스샷: 현장 화면식 인쇄에서 **머리만 있고 본문이 빈 페이지**. 실데이터 규모(긴 AI 분석)로 재현.
- **원인**: `#dfPrintPages .card{break-inside:avoid}` 가 페이지보다 **큰** 분석 카드에도 걸려,
  카드가 통째로 다음 장으로 밀리고 원래 자리에는 머리만 남았다. 원본 앱에는 이미 같은 처방이 있었다
  ("분석 카드는 분할 허용 → 빈 페이지 방지", #_sp_p4) — 이식 때 빠진 것. `.card:has(.aib)`·`.aib`·`.ait`
  에 break-inside:auto 로 이식. 분석이 길면 다음 장으로 **이어서** 흐른다(원본과 동일).
- **공가세대·공가상가 페이지가 조용히 사라지던 버그** — 게시본에 공가 필드가 결손(undefined)이면
  dfVacPane 의 `.toLocaleString()` 이 던지고, 231차의 페이지별 try 가 **페이지를 통째로 삼켰다**.
  숫자 강제(Number(v)||0)로 페이지는 항상 남긴다. ⚠ 페이지별 try 는 유실을 콘솔에만 남기므로
  "페이지가 안 나온다" 제보가 오면 콘솔의 `[print] … 조립 실패` 부터 볼 것.
- **헤더 3건**(사용자 지시): 검색창 150→170px · 사용 안내와 간격 4px 축소(-3→-7px) ·
  기준월 호버 글자색 --lbl(검정)→#fff(어두운 헤더 위에서 꺼져 보였다) · 로고 클릭 = 새로고침(app.reload).
- 검증: 긴 분석+공가 결손 실규모 스텁으로 현장 화면식 7쪽(빈 페이지 0 · 공가 2쪽 복원 · 분석 자연 분할),
  헤더 실측(폭 170 · 간격 9px · 호버 #fff · 클릭 리로드). 전 스위트 FAIL 0.
- app.js?v=525 · APP_VER 525.

### 603차 — 보고서 양식 인쇄 행간 붕괴(513차 `body{line-height:0}` 상속)
사용자 스샷: 보고서 양식 인쇄에서 4·6·7 표의 행이 서로 겹치고, 3쪽 `8 종합 분석 의견` 제목이
바로 아래 소제목과 포개짐. "페이지에 헤더 넣기 전까진 멀쩡했다".
- **원인**: 513차가 `body{line-height:0}`(공백 텍스트 노드가 줄 상자를 만들어 #app 을 밀어내던 문제)를
  넣으면서 150행에 `#app,#appHdr,#mo,#tko,#scrim,#toast,#nqPanel,#htip{line-height:1.5}` 로 되돌렸는데
  **`#rptRoot` 가 그 목록에서 빠졌다**. 보고서는 `rptPrint()` 가 `document.body.appendChild` 로
  직접 붙이므로(app.js) 복원 대상 밖 — 0 이 그대로 상속됐다.
  `#dfPrintPages` 는 `#view-defect` 안에 붙어서(app.js) 영향이 없다. 화면에서는 안 보이던 이유.
- **증상이 요소마다 갈린 이유**: `.rpt` 안에서 `line-height` 를 **직접 준** 요소만 멀쩡했다
  (`h1` 1.25 · `.asof` 1.5 · `.iss .m` 1.5 · `.ai li` 1.55 · `.plan .tx` 1.5).
  지정이 없는 `td`·`th`·`.sec-n`/`.sec-t`·`.kpi .k`/`.d`·`.cap` 만 줄 상자가 0 이 됐다.
  실측(v602 CSS 그대로, 12행 현장 표): 행 높이 8.3px · 행 간격 8.3px · `.sec-h` 높이 6px.
- **처방**: 150행 목록에 `#rptRoot` 를 넣지 **않는다** — 그러면 1.5 가 되어 원본보다 커진다.
  `.rpt` 는 독립 문서용으로 만든 규격이라 브라우저 기본값을 전제로 잡혀 있으므로
  `.rpt{line-height:normal}` 로 되돌린다. 복원 후 행 높이 16.7px(패딩 5.5 + 행간 10.2 + 테두리 1) ·
  `.sec-h` 18px = 513차 이전 상태.
- **딸린 결함**: 행간이 0 이던 동안 **접힌 둘째 줄이 높이 0 이라 한 줄처럼 보였다**.
  행간을 되돌리자 합계 행(굵은 글씨)에서 `▼` 와 숫자가 두 줄로 갈라졌다 →
  `.rpt .up,.rpt .dn2{white-space:nowrap}`.
  ⚠ 이 유형(줄바꿈이 line-height:0 에 가려져 있던 것)은 다른 좁은 열에서도 나올 수 있다.
- **검증**: 실운영 규모 하네스(팀 2쪽 = 12현장 + 업체 10곳 + 그 외 1행 · 현장 3쪽 = 처리계획 5+5행 +
  AI 6개 소제목 17항목)로 조립해 `rptFit` 을 태우고 쪽 넘침 실측.
  팀 1쪽 여유 211px · 2쪽 `dense` 적용 후 173px · 현장 1쪽 211px · 2쪽 107px · 3쪽 94px(`--aifs` 축소 없이 8.5px).
  `p2Cls` 임계값(23/27행)과 1쪽 `.sec` margin-top(7/18/13/12px)은 그대로 두고 통과 — 손댈 필요 없었다.
  ⚠ 하네스는 CSP(`script-src 'self'`) 때문에 인라인 `rptFit` 이 막힌다 — 하네스 생성 시에만 CSP meta 를 걷어낼 것.
- app.js?v=603 · APP_VER 603.

### 604차 — 보고서 시스템 글꼴을 Arial 계열로 · 목록↔피벗 모달 폭은 미재현
- **글꼴**: 사용자 지시 "더 베이직한 느낌 · 윈도우 기본폰트로". `f-sys` 가 `'Malgun Gothic'` 을 맨 앞에 두고
  있었는데, **맑은 고딕의 라틴·숫자는 Segoe UI 계열**(같은 설계)이라 화면 기본(Pretendard)과 인상 차이가
  거의 없었다 — "시스템 글꼴로 바꿔도 그대로"였던 이유. 라틴·숫자를 `Arial` 로 잡고 한글만 맑은 고딕에
  넘긴다(Arial 에 한글 글자가 없어 자동으로 넘어간다). 선택 라벨도 '맑은 고딕' → '윈도우 기본'.
  ⚠ **엑셀 기본은 Arial 이 아니다** — 2007~2021 Calibri, 365 Aptos, Arial 은 엑셀 2003 기본.
  Calibri/Aptos 로 바꾸려면 `.rpt.f-sys` 한 줄만 고치면 된다.
  검증: Arial·맑은 고딕은 리눅스 헤드리스에 없어 metric 호환(Liberation Sans)+NanumBarunGothic 으로
  대체 미리보기. 글꼴 확정 후 rptFit 재실행 실측 — 팀 1쪽 여유 220px · 2쪽 123px(dense 없이도 맞음) ·
  현장 1쪽 220px · 2쪽 108px · 3쪽 95px. **실제 윈도우 인상은 이 환경에서 확인 불가**.
- **목록보기 → 피벗 시 모달 폭 변화: 재현 실패(미수정)**. 재현 시도 — `?local=1` 실앱에 1,851행 스텁을
  넣고 `PIV.on` 토글, 1440/1600/1280/1024 · 위젯(`?w=1`) 430·1440 · 팀전체(행 현장 × 열 공종 22열) ·
  단일현장(행 시공업체›공종 · 열 없음) 조합 전부에서 `#mb` 폭·좌표가 소수점까지 동일했다.
  바뀌는 것은 `.rec-wrap`/`.pv-scroll` **높이**와 `.rl-band-bar` 0.5px 뿐(피벗은 `.rl-lim` 이 빠진다).
  `#mb.dfwide{width:88vw;max-width:88vw}` 는 `#mb{width:92%;max-width:560px}` 를 명시도로 이기고
  `rec.pivot` 은 클래스를 건드리지 않는다. 다음에 볼 곳: ① 피벗 셀 우클릭 드릴다운(`recOpen` 재호출 →
  `openModal` 이 dfwide 를 지웠다 다시 붙이므로 560px 로 한 번 좁아졌다 넓어진다) ② `#mb.kmw` 가
  `openModal` 의 클래스 제거 목록(app.js)에 **빠져 있다** — 지도 모달 뒤에 다른 모달을 열면 폭이 어긋난다.
- app.js?v=604 · APP_VER 604.

### 605차 — 목록↔피벗 모달 폭(진범: `kmw` 잔존) · 배포 전 전수 점검
- **604차에 재현 못 했던 그 버그.** `openModal` 의 폭 클래스 제거 목록에 **`kmw` 가 빠져 있었다**.
  `kmw` 는 `kmBigOpen`(app.js)에서 붙고 어디서도 떼지 않아, **조직 관리 지도 모달을 한 번 열면
  그 세션의 모든 모달에 남는다**. 604차 재현이 실패한 이유가 이것 — 지도를 안 거치면 안 난다.
  `#mb.kmw{width:auto;max-width:94vw}` 는 `#mb.dfwide{width:88vw;max-width:88vw}` 와 **명시도가 같은데
  CSS 에서 더 뒤(index.html 2281 vs 1343)** 라 이긴다 → 모달 폭이 `width:auto`(내용 폭)로 바뀌고,
  목록(넓은 표)과 피벗(열 수에 따라 폭이 다름) 사이에서 폭이 튄다.
  실측(1440×900, 단일현장 = 행 시공업체›공종 · 열 없음): 목록 1353.6px → 피벗 **452.5px**,
  1600×1000 팀전체도 1504 → 1402px. 처방은 제거 목록에 `'kmw'` 한 낱말.
  ⚠ **폭 클래스를 새로 만들면 반드시 그 줄에 함께 넣을 것** — 같은 사고가 세 번째다(`rdw`·`dfwide`·`kmw`).
- **죽은 자산 정리(사용자 지시)** — 지운 것: 미사용 SVG 심볼 4개(`i-bell`·`i-dash`·`i-sun`·`i-moon`,
  519차에 없앤 알림·다크모드의 잔재 · 위젯 소스까지 grep 해 확인), 미사용 CSS 변수 21개
  (`--fs-xs~2xl`·`--sp-1~6`·`--s5`·`--s6`·`--r-full`·`--b900`·`--b950`·`--shell-field`·`--glass2`·
  `--tint-am`·`--sbw`), 보고서 팔레트 사본 `--rp-n2~n6`(실제로 쓰는 것은 app.js 의 `RP_COL`),
  미참조 id `widSrch`. CSS 변수는 122 → 96개.
  ⚠ 남은 `--fc-*` 4개는 **FullCalendar 가 읽으므로 죽은 게 아니다** — 지우지 말 것.
  ⚠ `--fs-*`·`--sp-*` 는 "새 코드는 이 값만 쓴다(347차)" 던 토큰인데 실제로는 한 번도 안 쓰였다.
  다시 도입하려면 쓰는 곳을 함께 바꿔야 한다 — 정의만 되살리면 또 죽는다.
- **`xlsx.full.min.js`(930KB) 지연 로드**. 첫 화면에서 늘 받던 것을 **엑셀 버튼을 누를 때** 받도록 바꿨다
  (`loadXlsx()` — `loadMarked()` 와 같은 방식). 쓰는 곳이 `recWriteXlsx` 하나뿐이라 통로가 단순했다.
  실측: 첫 로드 자원 3,470 → 2,539KB · load 1,302 → 518ms. 헤드리스로 부팅 시 미요청 확인 후
  엑셀 버튼 → `xlsx.full.min.js?v=605` 요청 발생 + 파일 내려받기까지 확인.
  ⚠ `korea-geo.js`(561KB)도 조직 관리 지도에서만 쓰지만 **이번엔 두었다** — `krGeo()` 를 부르는 곳이
  동기 렌더 함수 7군데라 로더를 끼우면 재렌더 훅이 필요하고, 과제 1(`apt-geo.js` 채우기)이 바로 옆
  로딩 지점을 건드린다. 둘을 한꺼번에 지연 로드로 옮기는 편이 낫다.
  static-audit 의 버전 검사는 `app.js?v=` 만 본다 — vendor 쪽은 사람이 챙겨야 한다.
- **전수 점검 결과(3대 과제 착수 전 기준선)**: 중복 선언 0 · 미사용 함수 0 · 죽은 CSS 클래스 0 ·
  빈 stub 0 · 미참조 vendor 0 · 콘솔 오류 0(전 화면 순회). 남은 것은 아래 「보류」 절에 적었다.
- app.js?v=605 · APP_VER 605.

### 606차 — 현장 지도 카운트 배지 제거 · 헤더 로고 호버 발광
- **현장 지도 카드 머리의 `tm-cnt` 배지 삭제**(사용자 지시). 마크업(`#orgMapCnt`)과 `rOrgMap` 의
  갱신 두 줄을 함께 지웠다 — 마크업만 지우면 `$('#orgMapCnt')` 가 매번 헛돈다.
  ⚠ `.tm-cnt` 클래스 자체는 팀·권역 줄에서 계속 쓴다(app.js) — 지우지 말 것.
- **헤더 로고 호버 발광**. 로고는 클릭 대상(`app.reload`)인데 호버 반응이 0 이었다.
  `#appHdr .ah-brand:hover .ah-logo{filter:drop-shadow(0 0 3px rgba(255,255,255,.35))}` +
  부제(`.ah-name`)를 `--shell-dim` → `#fff`. `:focus-visible` 도 같은 처리(발광이 곧 포커스 표시).
  ⚠ **번짐 반경 3px 이 상한이다.** 4px 부터 워드마크 아래 'ENGINEERING & CONSTRUCTION'(높이 3px 남짓)이
  뭉개진다. 5안(7px 흰+16px 파랑 / 4px / 3px / 푸른기 5px / 발광 없이 밝기만)을 3배 배율로 떠 비교해 정했다.
  ⚠ 사용자가 "로그인 페이지 로고처럼 빛나는" 이라고 했지만 **로그인 로고에는 발광 효과가 없다** —
  순백 SVG 가 `#191919` 위에 놓였을 뿐이고, 빛나 보이는 건 감싼 카드의 유리 효과(`backdrop-filter:blur(20px)`
  + `box-shadow:0 24px 60px`)다. 옮길 원본이 없어 헤더에 맞는 값을 새로 잡았다.
  ⚠ 좁은 화면(≤900px)에서는 `.ah-brand{display:none}` 이라 이 규칙이 뜨지 않는다 — 정상.
- app.js?v=606 · APP_VER 606.

### 607~608차 — 화면 안 새로고침 원복 · 스냅샷 3건 · 변동 없음 배지
사용자 물음 "로그인 페이지부터 다시 들어가지 말고 안에서 새로고침할 수 없나" 로 `appRefresh()` 를
만들어 붙였다가, **사용자 지시로 `location.reload()` 로 되돌렸다.** 시도한 코드는 지웠다.
- 되돌린 이유를 남긴다: 화면 안 새로고침은 **새 배포본을 못 받는다**. 팀에 버전을 올렸다고 알려도
  로고만 누른 사람은 옛 app.js 를 계속 쓴다. 자료 갱신보다 이 위험이 컸다.
- 다시 만들 일이 있으면 알아 둘 것(그때 실측한 내용):
  · 업무·조직·게시월은 Firebase `.on()` 구독이라 **이미 최신이다 — 다시 받을 것이 없다**.
    한 번만 읽고 캐시에 두는 것은 `.once()` 로 받는 하자 게시본뿐
    (`DF.cache/kpi/sw/sam/vac/plans/ana/list` + `DF.rmIdx`·`DF.lastDash`)이다. 이것만 비우고
    `rAll()` + (하자 화면이면) `rDefect()` 를 부르면 화면이 새로 채워진다.
  · ⚠ **`DF.ch`(Chart.js 인스턴스)는 절대 함께 비우지 말 것** — 참조를 잃으면 `dfC()` 가 destroy 를
    못 해 같은 캔버스에 두 인스턴스가 붙는다.
  · 헤드리스 검증 결과: 페이지 재적재 0회 · 덮개 `display:none` 유지 · 상태·보던 화면 보존 · 오류 0.
    동작 자체에는 문제가 없었다 — 버린 것은 구현이 아니라 판단이다.
  · 제대로 하려면 `APP_VER` 를 서버 값과 대조해 **새 버전이 있을 때만** 통째 재적재로 넘겨야 한다.

**608차 — 위 원복 뒤 전수 재점검에서 나온 3건**
- **스냅샷에서 '엑셀' 버튼이 죽어 있었다(605차 회귀).** `dfSnapshot` 의 vendor 인라인 반복은
  index.html 의 `<script src>` **태그를 훑는데**, 605차에 xlsx 를 지연 로드로 바꾸며 그 태그를 지웠다.
  스냅샷은 파일 하나로 도는 문서라 `./vendor/…` 를 받아올 곳이 없다 → XLSX 미정의.
  이름으로 직접 챙겨 인라인한다. ⚠ **앞으로 다른 vendor 를 지연 로드로 바꿀 때마다 여기를 함께 볼 것.**
  (`build-single.mjs` 는 vendor 폴더째 복사하므로 영향 없다 — 확인함)
- **스냅샷을 열 때마다 `FB.db.ref(...).get is not a function` 이 터졌다(기존 버그).**
  `dfSnapBoot` 의 가짜 FB 가 `once/on/off/set/update` 만 갖고 **`get` 이 없었다** — `trashAll`(휴지통 정리)와
  `archLoad`(보관함)가 `.get()` 을 쓴다. LocalStore 부팅이 1.5s·1.8s 뒤로 예약해 둔 정리 작업이
  `S.live=true` 가 된 뒤 터진 것. `get` 을 스텁에 넣고, 읽기 전용 문서에서는 정리 자체를 돌리지 않도록
  `S.snap` 표식을 두어 `trashPurge`·`archMigrate` 를 막았다.
  ⚠ 새 Firebase 메서드를 쓰기 시작하면 이 스텁에 함께 넣을 것.
- **'— 변동 없음' 배지가 두 줄로 접혔다**(전월대비 실적 현황, 변동이 0 인 지표). nowrap 필요 폭 73.3px 에
  열이 78px 인데 `white-space` 지정이 없어 접히며 배지 높이가 19 → 37px 로 뛰었다. `nowrap` 추가.
  ⚠ 602차 보고서 배지와 **같은 유형**이다 — 좁은 열에 들어가는 짧은 문구는 nowrap 을 기본으로 볼 것.
  ⚠ 겸사 확인: `@media(max-width:768px)` 의 mom-row 4개 규칙(1129~1132행)은 **죽어 있다** —
  바로 아래 기본 규칙이 같은 명시도로 뒤에 와서 덮는다(600px 실측: 열이 그대로 78px). 되살릴 거면 순서부터.
- 남은 것(고치지 않음): **스냅샷 문서는 Pretendard 를 못 쓴다** — `vendor/pretendard.css` 링크가
  인라인되지 않아 파일 단독 실행에서 404 나고 OS 기본 글꼴로 떨어진다. 글꼴을 넣으려면 base64 로
  +2.4MB(스냅샷 2.5 → 5MB)라 판단이 필요하다.
- **배포본에 작업 잔재가 실려 나갔다** — 검증용으로 만든 `_modal.html`(529KB)이 저장소에 남아
  v605~608 zip 에 그대로 들어갔다. 앱은 멀쩡히 돌아서 기존 검사에 하나도 안 걸렸다.
  `static-audit` 에 **12. 작업 잔재 검사**를 넣었다(`_` 로 시작하거나 `.tmp/.bak/.orig/.rej`,
  `snap*.html`·`test*.html`, `~` 로 끝나는 이름). 일부러 파일을 두고 FAIL 이 나는 것까지 확인했다.
  ⚠ 검증 하네스는 앞으로도 `_` 로 시작하는 이름을 쓸 것 — 그래야 잊어도 이 검사가 잡는다.
- 검증: 스냅샷을 실제로 조립해 `file://` 로 단독 실행 — 외부 요청 0(글꼴 제외) · 콘솔 오류 0 ·
  `typeof XLSX === 'object'` · `S.snap === true`. 배지는 1440/1024/768/600px 에서 모두 한 줄(높이 20.5px).
- app.js?v=608 · APP_VER 608.

### 609차 — 위젯 부팅 누락 원인 3곳 · 위젯 글꼴에서 굴림 제거
**① 부팅 때 가끔 안 뜨는 이유 — 조용히 삼키던 실패 두 곳 + 재등록 구멍 하나.** 셋 다 "가끔"의 모양을 한다.
- `swap_and_restart` 가 교체 뒤 `let _ = Command::new(&target).spawn()` 으로 **결과를 버렸다**.
  부팅 직후 백신·정책이 갓 교체된 exe 실행을 막으면 새 판은 안 뜨고 이 판은 `return` 으로 끝나
  **위젯이 통째로 사라진다**. `pending_ver` 는 이미 지웠으니 다음 부팅에는 정상 = "가끔".
  → spawn 실패 시 `false` 를 돌려 **이 판을 그대로 띄운다**(디스크 exe 는 이미 새 판이라 다음 부팅에 반영).
- `hand_over_to_home` 도 같은 함정 — 넘기기가 실패해도 `true` 를 돌려 이 판까지 끝냈다.
- **자동 실행 재등록이 레지스트리를 안 봤다.** 예전 조건은 `want_auto && cur_path != home_exe()` 로
  **저장해 둔 경로끼리만** 견줬다. 사내 정책·정리 도구가 Run 키 값을 지워도 저장값은 그대로라
  **다시 등록하지 않는다** — 지워진 뒤부터 계속 안 뜬다. `run_key_value()` 로 레지스트리 실물을 읽어
  `"<문서폴더>\HPlanWidgetLite.exe"` 와 다르면 재등록한다.
- **진단 보강 2건**: ㉠ 창을 실제로 띄운 순간 `창 표시 · 모드 · 자리` 를 로그에 남긴다 —
  로그만으로 "프로세스가 안 떴다" 와 "떴는데 화면 밖·뒤에 있다" 를 가를 수 있다.
  ㉡ 트레이 '자동 실행 상태 확인' 이 **지금 돌고 있는 파일**(`exe_path`)을 '등록된 실행 파일' 로 보여 주고 있었다 —
  정작 알아야 할 Run 키 값이 아니라 진단에 쓸모가 없었다. 레지스트리 실물을 함께 보여 준다.
- ⚠ **아직 못 지운 후보 2개**(로그를 받아 봐야 가른다):
  · 문서 폴더가 OneDrive 로 리디렉션돼 로그온 직후 exe 가 '클라우드 전용' 이면 Run 키 실행이 실패한다.
  · 로그온 시점에 보조 모니터가 아직 안 잡히면 `clamp_to_screens` 가 주 모니터로 끌어오는데,
    나중에 모니터가 붙어도 재보정이 없다. `skip_taskbar(true)` 라 작업표시줄에도 안 떠 "안 켜졌다" 로 보인다.
    → 증상이 나온 PC 의 `문서\H 주요업무현황\widget-lite-log.txt` 를 받아 볼 것.
- ⚠ **이 Rust 변경은 컴파일 검증을 못 했다** — 이 작업 환경에 Rust 툴체인이 없고 `static.rust-lang.org` 가
  막혀 설치도 안 된다. 괄호 균형만 기계로 확인했다. **`.github/workflows/widget-lite.yml`(윈도우 러너)가
  실제 게이트**다 — 빌드가 통과하는지 반드시 확인하고 배포할 것. Cargo.toml 1.2.0 → **1.3.0**(버전이 올라야 exe 가 만들어진다).

**② 위젯 글꼴 — 굴림 제거, '맑은 고딕' → '윈도우 기본'.**
- 선택지가 `기본(Pretendard) · 맑은 고딕 · 굴림 · 돋움` 4개였다. 굴림을 뺐다(사용자 지시).
- ⚠ **맑은 고딕의 라틴·숫자 글자는 Segoe UI 계열이다**(606차 보고서 글꼴과 같은 사실).
  그래서 `wf-malgun` 은 `'Malgun Gothic'` 을 앞에 두고도 실제로는 윈도우 기본 조합과 같았다 —
  이름만 달랐던 셈이다. `win` 으로 바꾸고 스택도 `'Segoe UI','Malgun Gothic'` 순으로 뜻을 맞췄다.
- ⚠ **옛 저장값 매핑(`WID_FONT_OLD`)을 지우지 말 것.** 위젯 설정은 PC 마다 localStorage 에 남는다 —
  137차의 `'sys'`, 609차 이전의 `'malgun'`·`'gulim'` 을 쓰던 PC 가 그대로 있다. 매핑이 없으면
  그 사람들은 조용히 '기본' 으로 떨어진다. 셋 다 `'win'` 으로 흡수한다.
- 검증: 옛 값 7가지(`gulim`·`malgun`·`sys`·`dotum`·`app`·없음·쓰레기)로 위젯을 띄워
  클래스·표시 이름·계산된 글꼴을 실측했고, 순환 버튼이 3개만 도는 것까지 확인했다.
- app.js?v=609 · APP_VER 609 · widget-lite 1.3.0.

### 610차 — 담당자↔색 원 연동 · 편집 중 자리 고정 · body 직속 요소 행간(6개) · 지도 확대 버튼
- **담당자를 바꾸면 색 원이 곧바로 따라 바뀐다.** 폼을 다시 그리지 않고 그 원만 고친다(`peColorSync`).
  ⚠ 판정은 `planColor` 에 맡긴다 — 직접 고른 색이면 담당자를 바꿔도 그대로다('auto' 일 때만 담당자 색).
  ⚠ 공통(담당자 없음)은 색뿐 아니라 **꼴**이 바뀐다(속 빈 원) — 클래스도 함께 토글해야 한다.
  ⚠ 폼이 둘이고 규칙이 다르다: 인라인(`#dpEdit`·`peOwners`)은 속 빈 원을 쓰고,
  업무 현황 목록(`#tkNew`·`tnAsg`)은 원래부터 team 인자를 안 넘겨 늘 꽉 찬 원이다 — 그 차이를 지켰다.
  ⚠ 훅은 **`change` 리스너**에 둔다(select 는 change 가 확실히 온다). `input` 리스너는 8104행 한 줄짜리
  하나뿐이라 거기 두면 안 맞는다 — 처음에 `input` 에 넣었다가 안 불려서 옮겼다.
  ⚠ `kindOwnerSync` 가 '취합' 구분에서 담당자를 **코드로** 비운다 — 그때는 이벤트가 안 울리므로 직접 부른다.
- **수정 중 제목을 칠 때마다 업무가 자리를 옮기던 문제.** 정렬의 마지막 기준이 **제목**이었다.
  입력마다 `planAutosave` 가 다시 그리니 한 글자 칠 때마다 순서가 재계산됐다. 두 곳을 고쳤다:
  `sortPlans`(일자 패널)의 끝을 `createdAt → id` 로, FullCalendar `eventOrder` 를
  `-duration,ord,oky,start,allDay,title` → `…,cre,pid` 로. 둘 다 편집 중에 변하지 않는 값이다.
  ⚠ `createdAt` 이 없는 옛 업무는 id 로 가른다 — `uid()` 가 `Date.now().toString(36)` 로 시작해 대체로 만든 순.
  ⚠ 부수 효과: 같은 날·같은 등급 안의 순서가 **가나다순 → 만든 순**으로 바뀐다. 의도한 것이다.
- **`body{line-height:0}` 상속 6곳(602차와 같은 뿌리).** 71행의 그 규칙이 160행 복원 목록에 없는
  요소로 그대로 새고 있었다. 실측 높이: `#evePop` **14px**(글자 13px — 말풍선을 뚫고 나왔다) ·
  `#rlMenu` 0px · `#colPop` 28px · `#widMove` 16px · `.ctxmenu` 29px · `#pfPop`.
  복원 목록에 6개를 넣었다(`#evePop` 14 → **33.5px**, `#colPop` 201 → 224.5px).
  사용자가 말한 "오늘 남은 업무 토스트 높이가 딱 맞는다" 는 여백 취향 문제가 아니라 이 버그였다.
  ⚠ **body 직속으로 요소를 붙이면 그 요소를 160행 목록에 함께 넣을 것.** 이번이 세 번째다.
  (`#rptRoot` 는 보고서 규격이 달라 `.rpt{line-height:normal}` 로 따로 — 602차)
- **현장 지도 '크게 보기' 를 카드 머리 오른쪽으로.** 팀·권역 카드의 `+`(`.tm-add`)와 같은 자리·같은
  무몰딩 규격이라 셋이 한 줄로 맞는다(실측 24×24 · 오른끝 513px 동일). 지도 안에는 상태에 따라
  나타나는 '전체 보기' 만 남겼다 — 늘 있는 버튼과 가끔 있는 버튼이 한 묶음이면 자리가 흔들려 보였다.
- **자잘한 결함 자동 훑기**(4개 화면 순회): 중복 id 0 · 이름 없는 버튼 0 · 넘쳐 잘리는 글자 0 ·
  18px 미만 클릭 대상 0. '짧은 라벨이 두 줄' 8건은 **오탐**이었다(`.tm-h` 처럼 높이가 고정된
  flex 상자를 줄 수로 환산한 탓) — 스크린샷으로 한 줄임을 확인했다.
  ⚠ 이 훑기 스크립트를 다시 쓸 거면 줄 수 추정을 `Range.getClientRects().length` 로 바꿀 것.
- ⚠ CSS 주석 안에 `.식별자` 꼴을 쓰면 static-audit 의 '죽은 CSS 클래스' 검사가 오탐한다
  (`document.body.appendChild` 를 주석에 적었다가 WARN 이 났다). 주석에서는 점을 떼고 쓸 것.
- app.js?v=610 · APP_VER 610 · widget-lite 1.3.0(변경 없음).

### 611차 — 필터 버튼 아이콘을 꺾쇠에서 필터 아이콘으로
사용자 지시: "필터는 이 아이콘이 맞으니까". 현장 지도(`org.mapYrMenu`)가 쓰던 `#i-filter` 로 통일한다.
- 바꾼 곳 **둘**뿐이다 — 캘린더 툴바(`day.fmore` · index.html)는 **앱과 위젯이 같은 마크업을 쓴다**.
  그래서 "위젯·캘린더·업무 현황 셋" 이지만 손댈 자리는 캘린더 하나 + 업무 현황(`tkf-tg` · app.js) 하나다.
- ⚠ `.tkf-tg` 의 **회전 규칙(rotate 90/-90)을 함께 걷어냈다.** 필터 아이콘은 가리키는 방향이 없어
  돌리면 뜻이 깨진다(세 줄이 기울어 보인다). 여닫힘은 `.tkf-card.open .tkf-tg` 의 눌린 바탕과
  펼쳐진 본문이 알린다. `.tkf-tg.on::after` 파란 점(필터 걸림 표시)은 그대로다.
- `#i-filter` 는 이제 세 곳에서 쓴다 — 605차에 지운 심볼 4개와 달리 살아 있는 아이콘이다.
- 검증: 앱·위젯·업무 현황(접힘/펼침)에서 `use[href]` 가 `#i-filter` 인지, `transform` 이 `none` 인지,
  펼침 때 바탕이 채워지는지 실측 + 4장 스크린샷.
- app.js?v=611 · APP_VER 611.

### 612차 — 현장 위치 진단 스크립트 · 실패 원인 하나 확인
14개 현장 중 **7개가 안 찍히고 1개는 잘못 짚는다**는 사용자 보고. 앱은 이유를 알려 주지 않는다 —
이름이 색인에 없어서인지, 후보가 흩어져 버려진 것인지 구분이 안 된다.
- `scripts/site-geo-check.mjs` 추가. `kmFind`/`kmAuto` 를 **그대로 옮겨** 후보와 판정 사유를 함께 찍는다.
  ⚠ 현장명은 사내 자료다 — 이 스크립트는 밖으로 나가는 요청이 없고, 사내 PC 에서 node 로 돌린다.
  ⚠ app.js 의 `kmFind`/`kmAuto` 를 고치면 **이 파일도 함께 고쳐야 한다**(복사본이다).
- **가상 이름으로 돌려 본 것만으로 원인 하나가 드러났다: 권역 이름이 힌트 구실을 못 한다.**
  힌트는 `kmSiteHint` 가 권역 이름을 넘기는데 그 값이 '중부1'·'중부2'·'광주' 다.
  앞의 둘은 행정구역 이름이 아니라 아무것도 안 걸리고, '광주' 는 경기 광주시로도 걸려 되레 해롭다(546차).
  그 결과 '…두정역' 같은 이름은 **충남 두정동 ↔ 전남광주 두정동**이 둘 다 걸려 25km 넘게 흩어지고,
  538차 규칙(법정동은 좁히기에 안 쓴다 — 동명이의가 서로를 살린다)에 따라 **찍지 않고 버려진다**.
  즉 538차 주석이 예로 든 바로 그 상황이 실제 데이터에서 재현되고 있다.
- **주소를 넣으면 풀린다.** 같은 이름이라도 주소에 '광주광역시 광산구' 가 있으면 시도가 잡혀 좁혀진다
  (실측 확인). 552차의 `cfg.siteAddr` 가 이미 그 통로다 — 새로 만들 것이 없다.
- ⚠ 순수 브랜드명('…스카이시티')은 주소 없이는 **절대** 못 찾는다. 단지 색인(`apt-geo.js`)이 채워지거나
  주소가 있어야 한다. 이름만으로 되는 것은 이름에 지명이 든 경우뿐이다.
- 다음: 사용자가 14개로 이 스크립트를 돌린 결과를 보고 ① 주소 채우기로 끝나는지
  ② 단지 색인(건물DB)이 필요한지 ③ 수동 핀이 필요한지를 정한다. **재기 전에는 만들지 않는다.**
- **`kmSiteXY` 를 2단계로 고쳤다(사용자 지시: "도시랑 이름이랑 조합해서 못 찾나").**
  예전에는 `const txt=addr||st.name` — **주소가 있으면 이름을 통째로 버렸다.**
  그래서 주소 칸에 '천안' 만 적으면 시군구 중심에 그쳤다. 이제 ① 주소만으로 동 수준이 나오면 그것으로 끝,
  ② 안 나오면 이름을 보태 다시 찾는다.
  ⚠ **①을 먼저 보는 순서가 핵심이다.** 무조건 합치면, 도로명주소를 제대로 적었는데 이름의 지명이
  실제와 다른 현장에서 이름이 이겨 엉뚱한 동으로 간다 — 실측: 이름 '…두정역' · 주소 '…성성동 1' 일 때
  조합안은 **두정동**(틀림), 2단계는 **성성동**(맞음).
  ⚠ `KM_XY` 캐시 키도 `addr+name+hint` 로 바꿨다 — 셋 중 하나만 바뀌어도 다시 찾는다.
  실측(현행/조합/2단계 3자 비교): '천안'+두정역 → 시군구 → **두정동** · '대전'+도안 → 시도 → **도안동** ·
  '광주'+첨단 → 못찍음 → **첨단동** · '천안'+불당 → 시군구 → **불당동** · '천안'+신방 → 시군구 → **신방동**.
  나빠진 사례 0.
  ⚠ 이름만 있고 주소가 없으면 동명이의는 **여전히 못 가른다**(두정동 충남 ↔ 광주). 주소 한 단어가 답이다.
- ⚠ `scripts/site-geo-check.mjs` 도 같은 2단계로 맞췄다 — **한쪽을 고치면 다른 쪽도 고칠 것**(복사본이다).
- app.js?v=612 · APP_VER 612.

### 613차 — '전북 익산' 이 도 중심으로 끌려가던 문제(시도가 시군구를 안 양보했다)
사용자 보고: "전북 익산이라 해도 자꾸 완주를 잡아 준다".
- **완주를 잡은 게 아니라 익산시 중심과 전북도 중심의 평균이었다.** `kmAuto` 의 마지막 단계에서
  `base=gu.concat(sido.filter(x=>!gu.some(y=>near(x,y))))` — **거리(25km)로만** 시도를 걸렀다.
  시도는 시군구의 **상위**이지 별개 지역이 아닌데 둘을 나란히 놓고 평균을 냈다.
  전북도 중심이 완주·임실 부근이라 익산에서 남동쪽으로 크게 밀렸다.
- 처방: `y.pc===x.pc` 를 조건에 더해 **같은 시도의 시군구가 이미 잡혔으면 그 시도는 버린다.**
  ⚠ pc 가 다른 것끼리는 그대로 남긴다 — 권역 '대전·세종' 처럼 두 시도에 걸치면 평균이 맞다(531차 의도 보존).
- 실측(현행 → 수정): '전북 익산' 평균 → **익산시** · '충남 천안' 평균 → **천안시** · '경기 수원' 평균 → **수원시** ·
  **'전남 여수' 는 못 찍음 → 여수시**(도 중심과 100km 넘게 떨어져 546차 규칙으로 버려지고 있었다).
  유지된 것: '천안 아산' 2곳 평균(다른 시군구라 정상) · '대전·세종' 권역 평균 · '광주' 단독은 여전히 안 찍음(546차).
  나빠진 사례 0.
- ⚠ **지역을 정성껏 적을수록 틀리던 버그다.** '익산' 만 적으면 맞았고 '전북 익산' 이라 적으면 밀렸다.
  612차에 이름을 보태기 시작하면서 시도 단어가 섞일 일이 늘어 더 자주 드러날 수 있었다.
- `scripts/site-geo-check.mjs` 도 같은 규칙으로 맞췄다.
- app.js?v=613 · APP_VER 613.

### 614차 — 하자 생산자 이식 ①②③④ (report 앱 폐기 준비 · 업로드→집계→게시가 이 앱으로 들어옴)
report 앱을 폐기하고 calapp 만 운영하기로 결정. 소비자(대시보드·현장 5탭 등)는 이미 있었으므로
**생산자(HCS 엑셀 업로드 → 로컬 저장 → 집계 → 사내 게시)** 를 이식했다. app.js 안에 두 블록:
`하자 생산자 ① — 집계 엔진`, `하자 생산자 ② — 저장·업로드·주요이슈·게시`.

- **① 집계 엔진**: 원본 app-core/app-view 에서 calc·calcW·calcMo·capAm·capWks·capAll·critReason·
  RULE_DEF/CRIT_DEF·maskPII·redactUL·slimUL 등을 **바이트 그대로** 추출해 이식(mkport 스크립트로
  기계 추출 — 재타이핑 금지). 원본과 달라진 곳은 4가지뿐: dashSites/teamSites→dfDashSites/dfSites,
  S.rm→dfPubRm()(게시 기준월). ⚠ **이 블록의 함수는 임의 수정 금지** — 게시본 kpi 와의 일치가 생명.
  수정하면 반드시 `node scripts/test/calc-equiv.mjs --report <report-main>` 을 함께 돌릴 것(원본 대비
  12개 현장 전 필드 JSON 일치 + 불변식).
- **② 저장·업로드·게시**: IndexedDB 계층(dbOpen~defDelete)·업로드 파이프라인(norm/nd/csvToAoA/
  findHeaderRow/rowsToObjs/auditUpload — 전부 verbatim)·신규 현장 위저드(모달만 openModal 로 적응)·
  dfInsightsBuild(원본 rInsights 본문 그대로, DOM 캡처 대신 문자열 반환)·dfPublish(원본 fb2Publish 구조).
  설정 화면에 관리자 전용 게시 카드(#dfPubCard: 업로드존·제외 키워드·기준월·[등록])와 진행 오버레이
  (#uprog)를 넣었다. rCfg 가 카드 상태를 채운다.
  - ⚠ **IndexedDB 는 report 앱과 같은 오리진·같은 DB(hdec_db_v1)** — 기존 마스터 PC 의 원본 하자 행을
    그대로 읽는다(마이그레이션 없음). meta 스토어의 'state' 레코드는 report 것이니 건드리지 말 것 —
    calapp 은 'calapp' 레코드(pubRm·lastUp)를 따로 쓴다.
  - ⚠ 위젯(WebView2)은 IndexedDB 가 별개다 — 업로드·게시는 **브라우저 전용**(dfProdBoot 가 위젯·스냅샷
    조기 반환, ?w=1 검사).
  - ⚠ 원본과 다른 3가지(각 자리 ⚠ 주석): vac 는 로컬 S.cmt 대신 **금월 리프→전월 리프 이월**로 싣는다 /
    fb2SeedPlansAnalysis 생략(calapp 은 처리계획·분석의견을 리프에 실시간으로 씀) / 업로드 후 화면 이동
    대신 "게시 전" 토스트(이 앱 화면은 게시본 기준이라 이동해도 숫자가 안 바뀜).
  - 제외 키워드는 `calapp.exTk`(레거시 'exTk' 폴백 1회) — 기본값 'dummy'(원본과 동일).
- **④ 조직 원본 역전**: 팀·권역·현장의 원본이 `calapp/org` 가 됐다(_dash.sites/teams 는 게시 때
  dfOrgToDashSites/Teams 로 파생되는 스냅샷 — report 앱 뷰어 호환용). bindReportOrg 는 reportIndex 로
  **최신 게시월(ORG_RM)만** 추적, calapp/org 구독의 ORG_LIVE 가드 제거, orgSave 허용(스냅샷만 차단).
  ⚠ hasCommercial/showVacant 토글은 규칙상 org 에 안 실린다 — siteConfig 채널이 진실이고, org 스냅샷이
  S.org 를 갈아끼운 직후 `dfApplySiteCfg(DF._cfgLast)` 로 다시 입힌다(이 재적용을 지우면 토글이 조용히
  사라진다). orgFromDash·arrOf·deriveLul 은 사용처가 사라져 삭제. 규칙 파일은 무수정.
- **검증**(신규 하네스 2본 — 원본 저장소를 `--report` 로 지정):
  `node scripts/test/calc-equiv.mjs --report ../../report/report-main` — 집계 동등성.
  `node scripts/test/upload-equiv.mjs --report ../../report/report-main` — 파서 동등성 + 주요이슈 HTML
  원본 일치 + **게시 payload(12개 현장+_dash+_meta) 원본 수식과 완전 일치** + 부팅·카드 배선 스모크.
  static-audit FAIL 0 · WARN 0. ⚠ 하네스가 실제로 잡은 잠복 버그: capAll 의 `S.rm` 잔존 2곳(치환 누락) —
  calapp 엔 S.rm 이 없어 실게시에서 즉시 터졌을 것.
- ⚠ **브라우저 스모크(smoke.mjs)는 이 환경에서 크로미움 다운로드가 막혀 미실행** — 배포 전 로컬에서
  `node scripts/test/smoke.mjs` 실행할 것.
- **배포 후 확인(사용자)**: 실제 HCS 엑셀 업로드 → [등록] → 기존 report 게시본 kpi 와 대조.
  RTDB 규칙 조건식(report/$rm 쓰기)이 실제로 통과하는지도 이때 확인된다(605차 미확인 잔존 항목).
- **잔여**: ⑤ 뷰어 시점·다월 스냅샷·NLQ·AI 분석(runAI/runDashAI) 이식 → 그 뒤 report 앱 폐기(아카이브·
  안내 페이지). 기존 현장의 hasCommercial/showVacant 토글 **편집 UI** 는 아직 없음(신규 현장 위저드에만
  있음) — 필요 시 하자 현장 관리에 추가.
- app.js?v=614 · APP_VER 614.

### 615차 — 하자 생산자 이식 ⑤ 완결 (AI 분석·다월 스냅샷·NLQ · report 앱 기능 이전 종료)
614차 잔여 4건을 마감했다. 이로써 report 앱의 **모든 기능이 calapp 으로 이전**됐고, 남은 일은
report 앱 폐기 절차(아카이브·안내 페이지)뿐이다.

- **뷰어 시점 — 이식하지 않음(구조적으로 동일)**: 원본의 '뷰어 시점으로 보기'는 편집자 화면이
  로컬 계산이라서 필요했던 기능이다. calapp 은 관리자 포함 전원이 게시본을 보므로 **기본 화면이
  곧 뷰어 시점**이다. 다른 PC 에서 게시한 내용도 그대로 보인다.
- **AI 분석 — `하자 생산자 ③` 블록**: buildRules·runAI(현장 종합 분석)·runDashAI(주요이슈 재작성)
  이식. 프롬프트·규칙 조립은 원본 그대로(upload-equiv 6절이 요청 URL·본문 완전 일치를 검사한다 —
  **프롬프트를 손대면 이 검사부터 갱신할 것**). 적응 지점(각 ⚠ 주석):
  분석 저장 → `analysis/{sid}/{rm}` 리프 + meta 터치(화면은 기존 실시간 구독이 갱신) /
  주요이슈 재작성 → `report/{rm}/_dash/insightsHTML` 에 직접 반영(원본은 편집자 DOM 만 바꿨고
  게시 시점 DOM 이 실렸다 — calapp 화면은 게시본이라 [등록] 후 실행하는 흐름) /
  ⚠ 둘 다 **원본 행이 있는 마스터 PC 전용**(calc 를 로컬 행으로 돌린다 — 없으면 안내 토스트).
  버튼: 현장 「종합 분석 의견」 카드 [AI 분석], 대시보드 주요이슈 카드 [AI 다듬기](관리자 전용·no-print).
  Gemini 키는 설정 게시 카드의 입력(#dfCk) → `calapp.ck`(레거시 'ck' 1회 승계).
  CSP connect-src 의 *.googleapis.com 이 generativelanguage 를 허용한다 — CSP 를 죄면 여기가 죽는다.
- **다월 스냅샷**: 「하자 관리 스냅샷」이 게시된 달 목록에서 담을 달을 고른다(원본 pickSnapMonths
  방식·전체 선택 버튼). 달마다 `_dash`·현장 노드를 리프에서 직접 받아(dfDec — 소비자 dfRef 와 동일)
  `snap.months` 로 담고, dfSnapBoot 가 달별 트리+reportIndex 를 만들면 **기존 기준월 선택기('df.rm')가
  스텁을 읽어 파일 안 달 전환이 그대로 동작**한다. 구형 단월 스냅샷도 열린다(레거시 분기).
  ⚠ 모달을 Esc·배경으로 닫으면 closeModal 이 __SNAPPICK__ 를 취소로 종결한다(원본 app-core 641) —
  이 훅을 지우면 dfSnapshot 의 await 가 영원히 걸린다.
- **NLQ — `하자 생산자 ④` 블록 + 찾기 승격**: 해석기(nlqParse/Chips/Apply·_nlqKeys·NLQ_SYN)는 원본
  그대로. 별도 패널 대신 **기존 찾기 패널의 '하자' 절을 승격**했다(패널 중복·#nqPanel id 충돌 회피).
  "두정역 도배 30일 이상 누수" → 조건 칩(.ba .bbl 재사용) + 전 현장 미처리 목록 필터 + 상위 12건 +
  [전체 목록으로 보기]. 행 원천(⚠): 마스터 PC 는 원본 nlqRows 수식(calc().ul), 뷰어는 게시 목록
  (DF.list — 첫 사용 때 dfNqWarm 이 전 현장을 한 번 받는다, 진행 안내 표시). 목록 창 매핑 한계(⚠):
  현장·공종(첫 번째)·공가·장기(30일+)는 창 필터로, 동·호·본문은 목록 검색어로, 60일+ 는 밴드(d60)
  근사 — 세밀한 지연 조건의 정확한 수는 패널 칩이 답이다. 원본의 검색 히스토리 UI 는 이식하지 않음.
  이전의 '이미 불러온 목록만 훑는' nqSearch 하자 절은 제거(전 현장으로 확대·상위 호환).
- **검증**: upload-equiv 에 5절(NLQ — 사전·해석·칩·적용 6질의, 적중 건수까지 원본 JSON 일치)·
  6절(AI — runAI/runDashAI 요청 URL·본문 완전 일치) 추가, calc-equiv·upload-equiv ALL PASS,
  static-audit FAIL 0 · WARN 0, **브라우저 스모크 13항목 통과(페이지 오류 0)** + 설정 카드 실렌더
  스크린샷 확인. ⚠ 이 환경의 크로미움: playwright CDN 이 막혀 있어 npm 의 `@sparticuz/chromium` 으로
  받았다(`CHROMIUM=/tmp/chromium node scripts/test/smoke.mjs`) — CI 는 기존 워크플로가 정상 경로.
- **배포 후 확인(사용자)**: ① 마스터 PC 업로드→[등록]→기존 report 게시본 kpi 대조(RTDB 규칙 통과
  확인 포함) ② Gemini 키 입력 후 [AI 분석]·[AI 다듬기] 1회 ③ 다월 스냅샷 내보내 파일 안 달 전환
  ④ 찾기에서 "현장명 공종 30일 이상" 질의.
- **공가세대/공가상가 토글 편집(같은 회차 추가)**: 조직 관리 현장 표의 두 스위치가 disabled
  ("하자처리 현황에서 관리" — 구 시절 잠금)였다 — 조직 역전으로 이 앱이 편집 주체가 됐으니 활성화.
  액션 org.siteVac/org.siteShop, 관리자 게이트는 org.siteShow 와 동일 결. ⚠ 저장은 org 가 아니라
  **siteConfig 리프**(org 규칙이 이 필드를 불허·cleanOrg 도 걷어냄) — 신규 현장 위저드와 같은
  dfSiteCfgWrite 를 쓰고, DF._cfgLast 보관본도 함께 맞춰 org 스냅샷 재적용과 어긋나지 않게 한다.
  게시본 _dash.sites 에는 [등록] 때 실린다. 잠금 스타일(현장 표 공가 열)은 사용처가 사라져 삭제.
  브라우저에서 토글 전환·상태 반영을 실제로 확인함(스위치 렌더 스크린샷 포함).
- **가상데이터 전 구간 검증(같은 회차 · `scripts/test/e2e-defect.mjs`)**: 실브라우저에서 현시점
  가상 HCS CSV(7개 현장 7,079행 — 오늘까지의 접수일·실제 경과 지연일·상가·공가·중대하자·PII 포함)를
  실제 업로드 경로(onFile→rCSV→confirmUL→doSaveUL)로 넣고, 페이지 안 가짜 RTDB 로 [등록]까지 돌린 뒤
  소비 화면 전부를 검사한다 — 21항목: 기준월 자동 결정(전월 상한) · 게시 트리 무결성 ·
  **대시보드 합계=게시 kpi 합(원장 대조)** · 인수 전 현장 규칙 · 현장 탭 5종(활성 탭 일치까지) ·
  NLQ 칩·PII 무노출 · 목록 모달 조건 인계 · 전월 추가 게시·월 전환 · runAI/runDashAI(가짜 Gemini) ·
  공가 토글 즉시 반영 · 페이지 오류 0. `CHROMIUM=/tmp/chromium node scripts/test/e2e-defect.mjs`.
  ⚠ 과거월 재게시 때 '미래 게시월 정리' 확인창이 뜬다 — 최신월을 지우면 안 되는 상황이니 '취소'가 답
  (E2E 도 그렇게 답한다). 이 E2E 가 실제로 잡은 결함 2건:
  1. **NLQ 미리보기 PII 노출(마스터 PC)** — 로컬 원본 행(calc().ul)은 마스킹 전이라 찾기 미리보기에
     전화번호가 그대로 보였다. dfNqRawRows 로컬 분기에 redactUL(all=true)을 입혀 게시 목록(뷰어 경로)과
     같은 모양으로 통일(원본 report 편집자 화면은 생 데이터였다 — 의도적 이탈, ⚠ 주석).
  2. **찾기 재질의 무반응(기존 calapp 결함)** — rNq 의 빈 질의·결과 없음 분기가 innerHTML 직접 대입이라
     paintHTML 서명(__sig)과 어긋나, "X → 없는말 → X" 순서로 치면 X 결과가 다시 안 그려졌다.
     두 분기를 paintHTML 로 교체. ⚠ **같은 패턴 주의**: paintHTML 을 쓰는 상자에 innerHTML 을 직접
     대입하면 다음 렌더가 조용히 건너뛰어진다.
- **운영 개선 4건(같은 회차 · E2E 로 함께 검증)**:
  ① **NLQ 워밍 진행률** — 뷰어 첫 질의 때 "(i/n 현장) — 받는 대로 결과가 늘어납니다"를 표시하고
  현장 하나 받을 때마다 결과를 다시 그린다(_dfNqWarmProg).
  ② **게시 전 검토 확인창** — [등록]이 곧바로 쓰지 않고 요약(기준월·현장·전체 접수·전월 대비·미처리·
  처리율) 모달을 먼저 띄운다. 취소·Esc·배경 닫기는 게시 중단(closeModal 의 __PUBOK__ 훅 — __SNAPPICK__
  과 같은 결). ⚠ E2E 는 [게시] 버튼을 눌러 승인한다(pubWithOk) — dfPublish 를 그냥 await 하면 영원히 대기.
  ③ **업로드 이력** — meta 'calapp' 레코드에 최근 10회(시각·행수·현장수·기준월)를 남기고 게시 카드에
  최근 3회를 보여 준다(#dfUpHist).
  ④ **E2E 의 CI 편입** — smoke.yml 에 '하자 전 구간 E2E' 단계 추가. e2e-defect.mjs 는 CHROMIUM 환경변수가
  없으면 playwright 설치본을 쓴다(스모크와 같은 규약). ⚠ E2E 의 가상 데이터는 실행 시각에 따라 처리/미처리
  경계 몇 건이 흔들린다(오늘 날짜 캡) — 검사는 전부 같은 실행 안의 상호 대조라 영향 없다.
- **준공일 입력 초점 소실 수정(같은 회차)** — 현장 표 준공일 연도 첫 자리 '2' 입력이 0002-…-… 완전값이
  되면서 change→orgSave→**에코(calapp/org 구독)→rOrg 재렌더**로 입력 칸이 갈아끼워져 초점이 죽고 0002년이
  박히던 버그. 조직 구독 가드가 tkHold(업무 편집)만 알고 조직 표 입력을 몰랐다. 처방 3겹:
  ① orgHold()(#view-org 안 input/select/textarea 초점) — 구독 콜백이 이때는 PEND.org 로 미룬다,
  ② #view-org focusout 에서 미룬 그리기 몰아 처리(60ms · tk 쪽 flush 와 같은 결),
  ③ 준공일 연도<1900 과도기 값은 저장 자체를 건너뛴다(DB 에 0002-… 가 스치는 것도 방지).
  ⚠ 같은 패턴 주의: **구독 콜백이 다시 그리는 표의 입력 칸**은 반드시 hold 가드가 필요하다.
- **게시 카드 간소화(같은 회차)** — 7블록→4블록: 슬림 업로드존(.uz.slim — 원본 .uz 규칙은 보존, 변형만
  추가) / 상태 한 줄(현장·건수·최근 업로드 시각 병합) / 게시 한 행(기준월+[등록]+최신 등록) /
  접이 「자세히」(.dfadv <details> — 제외 키워드·Gemini 키·업로드 이력). id 는 전부 유지라 JS 무변경
  (dfProdCardFill 의 상태 문구만 병합).
- **위젯 새 게시월 말풍선(같은 회차 · 사용자 결정: 저녁 알림꼴·차콜·설정 꼬리)** — evePop 과 같은 골격의
  #dfNewPop: 새 게시월이 오면(bindReportOrg 의 ORG_RM 갱신) 위젯 설정(톱니)에 꼬리를 단 차콜(--lbl)
  말풍선이 한 번 뜨고 10초 뒤 접힌다. 문구 "하자 N월 게시 · 브라우저에서 보기", 클릭하면
  **?df=dash 딥링크**(615차 신설 — dfLinkOpen 의 특례, 대시보드로 직행)로 브라우저를 연다.
  본 것은 localStorage 'calapp.dfSeenRm' 에 월 단위로 기억(뜬 시점 기록 — evePop 과 같은 규칙).
  ⚠ 위젯 첫 렌더 전이면 톱니가 없어 1.5초 뒤 재시도한다.
- **업로드존 — 상용 정석 꼴로(같은 회차 · 사용자 지시로 슬림 한 줄안 폐기)**: 드래거형 업로드 관례
  (가운데 정렬 점선 영역 · 상단 아웃라인 구름 아이콘 · "파일을 끌어다 놓거나 **클릭해서 선택**(링크색)" ·
  "CSV · XLSX — HCS 전체 하자리스트" 힌트 · 호버/드래그 시 배경·아이콘 강조)를 .uz.std 변형으로 얹었다.
  ⚠ 전역 svg 규칙이 아이콘을 블록·좌측으로 밀어서 .uz-ic 에 display:block;margin:0 auto 고정.
  후속 다듬기(사용자 피드백): 존을 카드 인셋(var(--s4)) 안으로 넣어 제목 구분선·모서리와 점선이 닿지
  않게 했고, 아이콘은 10개 시안 비교 후 **문서+화살표(Lucide file-up)** 로 확정(사용자 선택 — 엑셀 '파일'이라는
  대상성이 분명). 「자세히」는 카드 인셋 + 살짝 왼쪽(-6px, 꺾쇠가 본문 시작선에 걸리게) + 게시 행과 같은
  머리선(inset 헤어라인)으로 간격 리듬을 맞췄다. 설정 화면 배치도 재편: 왼쪽 열=하자 데이터 등록 단독,
  오른쪽 열=백업·기록(이동)→도움말→바탕화면 위젯.
  원본 .uz 골격은 보존, 사용처가 사라진 그라디언트 타일(.uzi) 규칙만 삭제.
- **말풍선 문구·체급 확정(같은 회차)** — "하자처리현황 N월 게시" 굵은 한 줄, evePop 과 동일 체급
  (패딩·글자·꼬리·전환 동일), 색만 차콜(--lbl).
- **카드 통합(사용자 요청)** — '이 PC 데이터'·'게시' 두 행을 한 행으로: 굵은 줄=상태(현장·건수),
  아랫줄=업로드·최신 등록 시각, 우측=기준월+[등록](기준월 설명은 data-tip 으로). 카드는 존/행/자세히 3요소.
  ⚠ dfProdCardFill 이 #dfPubSub 를 innerHTML 로 다시 만들며 #dfPubAt 도 재생성한다 — at 조회는 반드시
  그 뒤여야 한다(현재 순서 유지).
- **사용 안내 ? 버튼 확대(사용자 요청)** — 헤더 .ah-help 28→32px(아이콘 15→18), 설정 도움말 카드
  [열기] 아이콘 14→17px.
- **컴포넌트 전수 리뷰(같은 회차)** — 생산자 이식으로 생긴 화면 요소 10종을 실렌더로 세워 놓고 점검.
  발견·수정 1건: **NLQ 조건 칩이 맨 텍스트로 렌더** — .ba/.bbl 배지가 #view-defect 스코프라 찾기 패널
  밖에서는 무효였다. 패널 전용 .nq-chip(같은 생김새: 11px·600·--rp·--b100/--bt1)으로 분리.
  ⚠ 같은 함정: **#view-defect 스코프의 클래스(.ba·.bbl 등)를 다른 화면에서 재사용하면 조용히 맨 텍스트가
  된다** — 새 자리에는 전용 클래스를 파거나 스코프를 확인할 것.
- **리뷰 후속 다듬기 7건(사용자 지시)**:
  ① 진행 오버레이 무몰딩 정합(라운드 16→10 · 그림자 --sh-2) ② 위저드 배치 개편 — 1행 팀(신설 셀렉트,
  기본 현재 팀)·권역·준공일 / 2행 동수·세대수·상가수, placeholder 는 예시값. confirmNewSite 가 #mtm 값을
  읽는다(없으면 tkSel 폴백) ③ 스냅샷 월 선택의 '현재'를 날짜 오른쪽 인라인으로 ④ 게시 확인을 요약 대신
  **현장 열거 표**(현장명·전체 접수·미처리·처리율, 인수 전 표기, 합계 행 굵게+전월 대비)로 ⑤ 업로드·등록
  시각 서식 통일(dfFmtDT: M/D HH:mm) + '최신 등록'→'등록' ⑥ 업로드 이력에서 행수 제거(meta 에는 남음)
  ⑦ **제외 키워드 칩 입력** — 엔터·쉼표로 칩 추가, ×·빈 칸 백스페이스로 제거, blur 잔여 글도 칩으로.
  값은 여전히 쉼표 문자열(S.exTk — handleParsed 무변경). 2열 그리드(.g2)는 위저드 3열 개편으로 삭제.
- **잔여**: report 앱 폐기 절차(아카이브·리다이렉트 안내) 하나.
- app.js?v=615 · APP_VER 615.

### 616차 — 업무 현황 목록 개편
- **업무 현황 목록 개편(사용자 지시 5건)**: ① 행의 인라인 수정(연필) 버튼 제거 — 수정은 펼침의
  체크 버튼 바로 아래(.tk-det-ed, 조작 열과 같은 세로선: 오른여백 61 − 행 안쪽 22 = 39px 바깥) ②③ 열 폭 —
  권역 62→50 · 이름 62→56(3글자 기준), 남는 폭은 업무(1fr)로. 그리드 5변형+반응형 3곳, 구분선 들여쓰기
  재계산(84→72 · 158→140), 펼침 왼여백 84→72 ④ **완료 업무 날짜 붉은색 버그** — di(dueInfo)가 상태와
  무관하게 over 를 붙였다 → st!==2 조건 추가 ⑤ 권역 글자색 --lbl2→--lbl.
  ⚠⚠ **템플릿 리터럴 안에는 주석 금지** — \${...} 뒤에 단 /* */ 가 화면에 그대로 렌더됐다(스크린샷
  검증에서 즉시 발각). 주석은 반드시 함수 밖·문자열 밖에.
- **수정 버튼 정렬 보정** — 좌변이 아니라 **중심** 기준: 상태 아이콘은 .stx 20px(+우측 1px 여백),
  연필은 .tk-ico 26px 라 좌변을 맞추면 6px 쏠려 보인다. 중심 일치 값 right:-41px(계산식은 CSS 주석),
  실측 중심차 −1px. ⚠ 두 아이콘 체계(.stx 20 vs .tk-ico 26)를 나란히 둘 때는 항상 중심으로 맞출 것.
- **수정 폼 배경 연속성** — 펼침(open, --fill)에서 수정으로 넘어가면 배경이 흰색으로 '툭' 빠졌다
  (388차가 대비 문제로 편집 배경을 뺐던 흔적). 편집 행도 --fill 로 잇고, 388차의 대비 문제는
  **입력칸을 흰색(--bg2)으로** 띄워 해소 — 회색 행 위 흰 칸이라 입력 자리도 더 또렷하다.
  editrest 왼여백도 권역 열 개편에 맞춰 84→72.
- 버전 정책: 이번 회차부터 세션 내 누적 수정도 배포 산출물이 갈리면 번호를 올린다(사용자 지적).
- app.js?v=617 · APP_VER 617.

### 618차 — 제목 띠 농도
- --surf2 #F5F7FA→#F0F3F7(반 단계). 3농도(A 현재/B 반 단계/C surf3급) 실렌더 비교 후 사용자 B 선택 —
  C는 표 머리·피벗 합계까지 진해져 대시보드가 무거워진다. surf2 는 카드 머리·주간 헤더·표 머리 등
  30곳의 단일 토큰이라 값 하나로 전 화면이 균일하게 따른다.
- app.js?v=618 · APP_VER 618.

### 619차 — 업무 날짜 열 축소
- 80→74px: 최장 문자열 "12/31–12/31" 실측 72~73px + 숨. 남는 폭은 업무(1fr)로. 그리드 9곳 일괄.
  520차의 80px 는 인라인 수정 버튼과의 겹침 때문이었는데 그 버튼이 사라져(616차) 근거도 사라졌다.
- app.js?v=619 · APP_VER 619.

### 620차 — 전수 점검(정적+핸즈온)
- **정적**: 정의 600개 중 고아 함수 defDelete 1개(아래에서 해소) · 중복 정의 0 · console.log 0 ·
  innerHTML 77곳 표본 점검 전부 esc/paintHTML 경유(사용자 문자열 무방비 0).
- **핸즈온(실브라우저 조작·페이지 오류 0)**: ① **다월 스냅샷 실파일** — blob 가로채기로 2.7MB 파일을
  실제로 만들어 부팅→기준월 선택기로 달 전환→현장 화면→목록 모달(500행)까지 전 경로 확인
  ② 대시보드 업체별 축(전 현장 kpi 지연 로딩) ③ 목록 모달: 검색·밴드·피벗·엑셀(xlsx 지연 로딩→파일
  생성) ④ 인쇄 두 양식(보고서 .page 2쪽 · 화면 .sp-print-page 2쪽) ⑤ 조직 지도 SVG·현장 추가/삭제 왕복
  ⑥ 업무 주간/월간/현장별 전환 ⑦ 위젯 날짜 팝업(같은 칸 두 번 규약 — FC dateClick 은 실마우스 필요).
- **수정 1건 — 현장 삭제 잔재**: org.delSite 가 조직·담당 배정만 지우고 ① 이 PC 원본 하자 행(IndexedDB)
  ② siteConfig 리프를 남겼다(defDelete 가 고아였던 이유). 둘 다 정리 추가 — 과거 게시월 노드는
  역사(스냅샷·과거 조회)라 남긴다. E2E 10절로 고정.
- app.js?v=620 · APP_VER 620.

### 621차 — 툴팁 인접 즉시
- 첫 진입 지연(180ms)은 유지하되, **툴팁이 떠 있거나 방금(400ms 안) 떠 있었다면 지연·페이드 없이
  즉시** 옮겨 뜬다(#htip.fast=transition:none · _tipWarm 시각 기록). 근거: 빈도 기반 애니메이션 원칙 —
  이미 뜬 상태의 연속이라 갑작스럽지 않고, 아이콘 줄을 훑을 때 굼뜬 느낌이 사라진다.
  실마우스 검증 5항목(첫 지연·인접 30ms 즉시·400ms 안 재진입 즉시·식으면 지연 복원).
- app.js?v=621 · APP_VER 621.

### 622차 — 보안 규칙 단일 앱 체제 정리(report 앱 폐기)
- database.rules.json 을 calapp 단일 앱 현실로 갱신. 앱 코드 무변경(규칙 파일만).
- **문구**: 머리 주석을 "두 앱 공존·기존 블록 불가침" → 단일 앱 선언으로 재작성, reportIndex 의
  fb2Publish(폐기 앱 함수) → dfPublish, analysis 마이그레이션 서술 → calapp 현실(레거시는 읽기 수용),
  calapp 블록의 폐기 앱 참조 제거.
- **불용 블록 삭제 2개**: aiRules(원본 앱 스스로 규칙 편집 기능을 폐지하고 RULE_DEF/CRIT_DEF 코드
  고정으로 바꿨던 것 — calapp 이식본도 동일하게 코드 고정이라 이식 누락 아님), calapp/mentions
  (멘션 기능 제거 — 위젯 Rust 는 window.newMentions JS 스텁만 폴링, RTDB 미사용). 잔존 데이터는
  규칙 제거로 콘솔 외 접근 차단(삭제는 안 함). 복원은 report-final 규칙 참조.
- **대조**: app.js 의 FB.db.ref 경로 전수 추출 — users·report·reportIndex·plans·analysis·meta·
  siteConfig·calapp/* 이 규칙 최상위와 1:1 일치. 게시 multi-path update 의 각 자식 경로도 확인.
- ⚠ 규칙은 저장소 사본 — **Firebase 콘솔에 수동 반영해야 실효**. 반영 후 마스터 PC 에서
  게시 1회·업무 저장 1회로 쓰기 경로 살아 있는지 확인할 것.
- app.js?v=622 · APP_VER 622.

### 623차 — 차트 톤 정합 1단계(사용자 선별: 툴팁·서체만)
- dfChartInit 에 전역 1회 설정(Chart.__calappTone): ① **서체** — Chart.defaults.font.family 를
  Pretendard 로(축·눈금·라벨·툴팁이 브라우저 기본체였다 — 도넛 중앙 텍스트만 명시돼 있던 결함)
  ② **툴팁** — 앱 툴팁(#htip)과 같은 차콜 rgba(28,32,42,.96)·라운드 3·12px 체급. 개별 차트의
  위치(aboveAll)·모드 옵션은 그대로.
- 보류(사용자 결정): 처리 완료 라인 주황(#F0B144) 유지, datalabels 그림자 유지, 막대 살몬 톤 유지
  (B안 실험 결과 스택 면적에는 현행 감쇄 톤이 옳았다 — 시안 기록 chart-variants.png).
- app.js?v=623 · APP_VER 623.

### 624차 — KPI 색·목록 머리(사용자 지시 3건)
- KPI: 미처리 숫자 앰버(#B45309)→**파랑(--bt1)**, 장기미처리 적(#B91C1C)→**차트 장기 막대와 동일색
  (--ch-d60)** — KPI 와 추이 차트가 같은 지표를 같은 색으로 말한다. 0건 미부착 로직은 그대로.
- 목록 모달: 결과 건수(#recN)를 rl-acts 에서 **제목 옆**으로 — 값이 변해도 검색창 자리가 안 흔들린다.
  '표시' 라벨 여백 2/8→6/10(캡슐 왼쪽에 딱 붙어 보이던 것).
- app.js?v=624 · APP_VER 624.

### 625차 — 자체 발굴 결함 2건(실사용 동선 사냥)
- **① 클릭 직후 툴팁 번쩍(621차 부작용)** — 펼침의 연필을 누르면 편집 폼으로 재렌더되며 커서 아래
  '취소' 버튼에 브라우저가 자동 mouseover 를 쏘고, warm(인접 즉시) 규칙이 이를 받아 **누르자마자
  '취소' 툴팁이 즉시** 떴다. 처방: 클릭·스크롤·창 이탈은 **콜드 숨김**(tipHideCold — warm 리셋),
  인접 즉시는 hover 이동에만. 이벤트 트레이스로 검증(재렌더 자동 OVER 확인). 180ms 뒤 정상 지연
  표시는 621차 이전부터의 동작이라 유지.
- **② 위젯 말풍선 포개짐** — 저녁 알림(내 업무 탭 앵커)과 새 게시월(톱니 앵커)의 앵커 중심이 68px
  차라 폭 420 위젯에서 거의 완전히 겹쳤다. 오후 게시가 흔해 실사용 충돌. 처방: 서로 감지해 아래로
  스택(양방향 — 어느 쪽이 먼저 떠도). 실렌더로 무겹침 확인.
- **③ 차콜 말풍선 납작(사용자 발견)** — 행간 복원 목록(#evePop,#rlMenu,… line-height:1.5, 513차)에
  #dfNewPop 누락. **body{line-height:0} 전역 함정** — body 에 붙이는 요소는 반드시 이 목록에 넣거나
  행간을 명시해야 한다(기존 함정을 새 요소가 또 밟았다). 목록에 추가로 해소, 두 말풍선 높이 일치.
- 조사 무혐의: 날짜 열 74px vs 링크 아이콘(목록 행엔 링크 아이콘이 없음 — 1852 낡은 주석만 갱신).
- **디자인 시안 기각 기록(재론 방지)** — 사용자 결정으로 현행 유지 5건: 처리 완료 라인 주황 /
  datalabels 그림자 / 미처리 막대 살몬 톤(chart-variants.png — B안이 무거움) / 현장 탭 캡슐
  (site-tab-mock.png — 대시보드 무게에 캡슐이 맞음) / 표시 캡슐 세그화(lim-seg-mock.png).
  검색창 포커스 글로우는 앱 .inp:focus 와 동일 문법이라 애초 무혐의.
- app.js?v=625 · APP_VER 625.

### 626차 — 외부 기술 리뷰(GPT) 반영 1단계
검증 결과 **맞는 지적 4건 즉시 반영**, 나머지는 판단 대기·보류(사유 아래).
- **① 규칙 과잉 권한(리뷰 1순위 — 사실 확인)**: calapp/org·people .write 가 viewer 까지 열려 있어
  UI(isEditor 게이트)와 불일치했다. org=EDITOR 단독, people=**$pid 층에서** EDITOR 또는 본인
  ($pid==auth.uid — 내 계정 프로필 저장 경로가 viewer 에게 필요, 리뷰가 못 본 결).
  ⚠ $pid 변수는 그 층에서만 유효 — people 레벨 .write 에 두면 규칙 컴파일이 깨진다.
  ⚠⚠ **콘솔 반영 후 viewer 계정으로 내 계정 프로필 저장 1회를 반드시 확인할 것.**
- **② 위젯 버전 메타 불일치(사실)**: Cargo.toml 1.3.0 vs tauri.conf.json 1.2.0 → 1.3.0 통일.
  다음 위젯 릴리스에 반영된다(웹 zip 과 무관).
- **③ 테스트 의존성 고정(사실)**: package.json+package-lock.json 도입(playwright 1.62.1 고정),
  CI 는 npm ci. ⚠ 패키징 루틴 변경 — **이제 node_modules 만 지우고 package*.json 은 zip 에 남긴다**
  (개발 의존성 명세 — 앱 런타임과 무관함을 파일 설명에 명기).
- **④ safeHTML 폴백 강등(타당)**: DOMPurify 부재 시 regex 살균 → **텍스트만(esc)**. 정규식 살균은
  보안 최종선이 못 된다. DOMPurify 는 자체 번들이라 폴백 발동 자체가 이상 상황.
  ⚠ upload-equiv 하네스에 DOMPurify 스텁 추가(실브라우저와 동일 조건 — 폴백 경로는 비교 대상 아님).
- **판단 대기(사용자)**: 백업 파일 암호화(DPAPI — 위젯 재배포 수반) / 업무 createdBy 감사 필드 /
  업무 권한 세분화(현행 전원 수정은 의도된 협업 모델 — 정책 문제).
- **보류(사유)**: app.js 모듈 분리(단일 파일 전제의 검증 게이트 재구축 비용 > 1인 개발 현 실익 —
  단 신규 대형 기능 착수 시 재론, 리뷰의 '20,000줄 전 지금이 적기' 경고에는 동의) / AI 서버화
  (사내 서버 부재·BYOK 1인 사용이라 현 모델 적정) / Tauri eval 브리지 명령형 전환(위젯 대공사,
  현재 고정 문자열만 전달) / CSP 축소(리캡차 등 로그인 경로 실검증 없이 좁히면 위험) / indexOn
  (팀 10~30명 규모 비해당 — 리뷰도 동의).
- app.js?v=626 · APP_VER 626.

### 627차 — 권한·감사 3종(사용자 확정: 프로필 축소·업무 소유·이력)
- **④ 프로필 축소**: 본인은 **이름·비밀번호·담당 현장**만 — 팀·직급·권역은 관리자 배정(온보딩:
  본인이 이름만 등록하면 관리자가 채움). 내 계정의 소속 3칸은 읽기 표시("관리자 배정"),
  담당 현장은 체크 목록(rankUses.sites 직급만, data-act="pf.site"). acctAutoSave 는 팀·권역·직급을
  기존값 그대로 싣고, **규칙이 본인 쓰기에서 세 필드 불변(최초는 빈 팀·빈 권역·member)을 강제**.
  canSetRank 제거 — 조직 관리 표의 직급 배정도 isEditor 로(규칙과 일치: 팀장 배정은 저장 거부됨).
- **③ 업무 소유(작성자만)**: 신규 업무에 createdBy(계정 uid, live 만). **완료 체크·수정·삭제·보류·
  복원 전부 작성자 또는 관리자만** — 게이트 7곳(tk.st/tk.edit/tk.del/컨텍스트 메뉴/보류 일괄 건너뜀/
  휴지통 복원/펼침 연필 미렌더), 차단 시 토스트 "작성자만 수정할 수 있습니다".
  **레거시(createdBy 없음)는 기존대로 전원**(사용자 확정 — 앞으로부터 적용).
  규칙: tasks $iid .write 에 소유 조건(관리자 ∥ 신규(작성자 표기 또는 레거시 무표기 — 복원 호환) ∥
  기존은 무표기·본인). trash/archive 는 MEMBER 유지(원본 삭제가 tasks 규칙으로 막힘).
  ⚠ 정책 함정 명시: **남이 담당자로 배정해 준 업무(작성자=배정자)는 담당자가 완료 처리 못 한다** —
  실사용에서 걸리면 '작성자+담당자'로 완화 검토(사용자 지시 문면대로 우선 적용).
- **② 업무 이력**: it.hist=[{t,u(이름≤30),k}] 최근 20, live 만 기록. k={new,edit,done,undone,hold,
  move,restore} — 기록 지점 11곳(histPush 래핑: 생성/폼 저장/인라인 저장 2/오늘로/보류 일괄/아침 확인/
  반복 회차/상태 토글 2/복원). 펼침에 "이력 N" 접이(최신순 "8/25 14:02 · 김동연 · 수정").
  cleanTask 에 createdBy·hist 보존, 규칙 validate 추가.
- 검증: 실브라우저 5항목(내 업무 연필·이력 / 남의 업무 연필 없음·토글 차단·토스트 / 레거시 전원 유지·
  이력 기록 / 계정 모달 소속 읽기 3칸·담당 현장 체크 / 오류 0) + 게이트 5종 전부 통과.
  static-audit 규칙 창을 블록 경계로 확장(고정 3500자가 모자랐다).
- ⚠⚠ **콘솔 반영 필수 + 반영 후 확인 4가지**: viewer 프로필 저장 / viewer 가 자기 업무 생성·완료 /
  viewer 가 남의 업무 완료 시도(거부돼야) / 관리자 조직·직급 배정 저장.
- 잔여: **① 위젯 백업 DPAPI 암호화** — 설계 확정(HPWENC1 매직+DPAPI blob, 복원은 위젯 안 되돌리기
  에서 브리지 복호·브라우저 단독이면 안내), Rust 착수 예정. 웹 '지금 내보내기'는 평문 유지(의도적 반출).
- app.js?v=627 · APP_VER 627.

### 628차 — 직급 위계 권한 모델(사용자 확정)
- **업무 수정 권한 확장** — canEditTask(it, ownerSid): 관리자 ∥ 레거시 ∥ 작성자 ∥ **지정 담당자
  (assignees — 남이 만들어 줘도 담당자로 지정되면 수정 가능, 627차 정책 함정 해소)** ∥
  **팀장(head)=모든 업무** ∥ **공구장(lead)=자기 권역 트리($sid 주인의 people.region)의 업무** ∥
  **담당자=업무의 현장(site)이 내 담당 현장이면 가능**. 팀 공통 트리(t1)는 people 에 없어 공구장
  권역 판정에서 자연 제외(공통 업무는 작성자·담당자·팀장·관리자만). 규칙 tasks .write 동일 확장.
- **명부 배정 위계** — 권역: 관리자·팀장(canAssignRegion) / 담당 현장: 관리자·팀장 전원, 공구장은
  자기 권역 사람, 담당자는 본인만(canAssignSites). 명부 권역 셀렉트·현장 [+]·칩 빼기가 이 판정을
  따르고(권한 없으면 읽기 칩 .site-ro), acct.set 위임에도 게이트(팀·직급은 관리자만).
  규칙 people: 팀장=전원 권역·현장(이름·이메일·팀·직급 불변) / 공구장=같은 권역 대상 현장만(권역 불변).
- **내 계정** — "관리자 배정" 문구 전부 제거(소속 3칸 담백한 읽기 표시). 담당 현장은 **조직 관리
  명부와 동일한 칩+선택창**(sitesChkHTML 전역 승격 — 627차 자체 체크리스트 폐기. 당시 체크 저장
  배선이 애초 누락돼 있었음도 이번에 발견·소멸). **담당자 본인 선택창은 자기 권역 그룹만** 노출,
  타권역 기존 배정은 숨은 checked 로 보존(저장 때 안 지워짐). 권역 미지정이면 안내 토스트.
  '자기 권역 내 현장만'은 org.sites 가 배열이라 서버 강제 불가 — 선택창 UI 가 제한(규칙 주석 명시).
- 검증: 실브라우저 14항목(담당자 지정/현장 매치/타권역 차단, 팀장 전부, 공구장 권역 경계,
  배정 위계 3종, 내 계정 칩·선택창 권역 제한, 팀장 명부 셀렉트 노출) + 게이트 5종 전부 통과.
- ⚠⚠ 콘솔 반영 후 확인(627차 4종에 더해): 팀장 계정으로 남의 업무 완료 1회 / 공구장 계정으로
  같은 권역·다른 권역 각 1회(후자는 거부돼야) / 담당자 계정으로 자기 담당 현장 업무 완료 1회.
- app.js?v=628 · APP_VER 628.

### 629차 — 위젯 1.4.0: 백업 DPAPI 암호화 + 자동 시작 미기동 진단
- **로그 분석(8-05~8-27)**: 자동 실행 재등록 로그가 한 번도 없다 = Run 키는 계속 일치(살아 있음).
  그런데 8-26·27 아침 부팅 시각에 로그가 0줄 → 프로세스가 시작조차 안 됐다. v1.3.0 설치(8-25) 다음
  콜드 부팅부터 증상. **유력 원인: 작업관리자·설정의 '시작 앱 사용 안 함'** — Run 키를 지우지 않고
  Explorer\StartupApproved\Run 값(첫 바이트 홀수)으로만 실행을 막아, 기존 재등록 검사(값 대조)가
  침묵한다. 새 exe 해시(1.3.0 교체)에 대한 보안 소프트 차단 가능성도 병존 — 아래 표식으로 판별 가능.
- **위젯 1.4.0(Cargo·tauri.conf)**:
  ① **자동 시작**: Run 값에 `--autostart` 인자 추가 → 시작 로그가 "시작(자동)/시작(수동)"으로 갈린다
     (구 등록값과 달라 첫 실행 때 재등록 1회 — 의도된 이행). StartupApproved 점검 —
     '사용 안 함'이면 값을 지워(기본=사용) 되살리고, 매 시작 "자동 실행 점검 · Run=… · 시작 앱=…"
     한 줄을 상시 기록. 다음 미기동 때는 로그만으로 (a)안 옴 (b)꺼져 있었음→복구를 확정할 수 있다.
  ② **백업 암호화**: run_backup 이 DPAPI(CryptProtectData, 사용자 계정 귀속)로 잠가
     `HPWENC1\n`+base64(blob) 꼴로 저장(실패 시 평문 폴백+로그 — 백업 부재보다 낫다).
     crypt32 직접 바인딩(win32 모듈, 의존성 무추가)·자체 base64. 로그 "백업 저장(암호화)".
  ③ **복원 브리지**: listen(hpw-restore-req{id,text}) → 복호 → emit(hpw-restore-res{id,json|err}).
- **웹 v629**: bk.restore 가 `HPWENC1` 감지 시 bkDecrypt() — 위젯 안(withGlobalTauri)이면 브리지
  왕복(8초 타임아웃), 브라우저 단독이면 "같은 PC 의 위젯 화면에서 되돌려 주세요" 안내.
  복호 실패는 "이 PC·이 계정에서 만든 백업만" 안내. 웹 '지금 내보내기'는 평문 유지(의도적 반출).
- 검증: 웹 3경로 실브라우저(비위젯 안내 / 브리지 성공→백업 판정 / 실패 안내) PASS + 게이트 5종.
  Rust 는 이 환경에 cargo 부재 — **GitHub Actions 빌드가 컴파일 검증**(푸시 후 릴리스 확인).
- ⚠ 1.4.0 배포 후: 새 자동 백업은 암호화 — 복원은 반드시 위젯 화면에서. 기존 평문 백업은 그대로 복원됨.
- app.js?v=629 · APP_VER 629 · widget 1.4.0.

### 630차 — GPT 2차 리뷰(v629 대상) 검증·반영
**판정**: 맞음 3(③④ + ①의 원리) · 전제 틀림 1(①의 방법) · 의도 확인·유지 3(②⑤⑥) · 문서화 1(⑦) · 보류 1(⑧).
- **④ people .write 인증 공통화(맞음)** — 인증(HDEC·메일 검증) 조건이 첫 분기에만 있었다.
  `인증 AND (관리자 ∥ 본인 ∥ 팀장 ∥ 공구장)` 구조로 전면 재작성. 실우회는 어려웠으나 방어적 재구성.
- **③ createdBy 불변(맞음 — 실결함)** — 작성자가 자기 업무의 createdBy 를 바꾸거나(양도·사칭)
  지워서(레거시화 → 전원 개방) 권한 구조를 흔들 수 있었고, **남이 레거시 항목에 자기 uid 를 심어
  소유권을 탈취**할 수도 있었다. .write AND 절 + createdBy .validate 이중으로 봉쇄(관리자만 예외,
  항목 삭제는 예외).
- **① sites 권역 서버 강제(원리 맞음·방법 틀림)** — 리뷰는 `org/sites.child($sid)` 참조를 제안했으나
  org.sites 는 **배열(인덱스 키)이라 현장 id 조회 불가**(내 기존 주석이 사실). 대신 **putOrg 가
  calapp/orgSiteRegion={sid:권역} 미러를 원자 기록**하고 people sites/$sid .validate 가 이를 참조 —
  관리자·팀장 외에는 현장 권역==내 권역일 때만. 미러 미존재는 이행기 허용(**관리자가 조직 저장을
  1회 하면 채워져 조여진다** — 콘솔 반영 절차에 포함). UI 도 일치: 공구장의 남 배정 선택창도
  자기 권역 그룹만(628차 selfLtd 조건 확장).
- **② 신규 무표기 허용(의도 유지)** — 레거시 항목의 휴지통 복원이 규칙상 '무표기 신규 set'과 구분
  불가. 우회 산물은 전원이 고칠 수 있는 공용 성격 업무 1건뿐(권한 상승 없음) — 위협 수용, 주석 명시.
- **⑤ trash/archive MEMBER(의도 유지)** — 삭제(작성자=viewer 가능)의 이동 대상이고 아카이브 이월은
  아무 세션이나 수행 — editor 로 조이면 기능이 깨진다. **⑥ assignees 변경(의도 유지)** — 수정
  권한자가 담당자를 지정하는 협업 모델. 수정권이 이미 있는 사람의 자기 추가는 순환이라 무의미.
- **⑦ 권한 매트릭스(문서화)**:
  | 데이터 | 담당자 | 공구장 | 팀장 | 관리자 |
  |---|---|---|---|---|
  | 본인 이름·담당현장 | O(자기 권역) | O(자기 권역) | O | O |
  | 본인 팀·권역·직급 | X(불변) | X | X | O |
  | 타인 담당현장 | X | 자기 권역 사람 | 전원 | 전원 |
  | 타인 권역 | X | X | 전원 | 전원 |
  | 팀·직급·조직(org) | X | X | X | O |
  | 업무: 작성·자기 것 | O | O | O | O |
  | 업무: 담당 지정받은 것 | O | O | O | O |
  | 업무: 내 담당현장 것 | O | (권역으로 포괄) | O | O |
  | 업무: 타인 것 | X | 자기 권역 | 전부 | 전부 |
  | createdBy 변경 | X | X | X | O |
  | users role | X | X | X | O |
- **⑧ 규칙 침투 테스트(보류)** — RTDB 에뮬레이터는 Java 의존이라 이 환경에서 불가. 대신 콘솔
  **규칙 시뮬레이터 수동 시나리오 8종**을 반영 절차에 포함: viewer→org 쓰기✗ / viewer→타인 people✗ /
  viewer→자기 people(소속 불변)○ / viewer→자기 sites 타권역✗·자기 권역○ / 남의 업무✗ /
  담당 지정 업무○ / lead 같은 권역○·타권역✗ / 레거시에 createdBy 심기✗.
- 부수: CI 주석 npm ci 로 현행화(리뷰 ⑳). eval 브리지·widget CSP·모듈 분리는 기존 보류 유지.
- 검증: v628 스위트 16항목(공구장 선택창 제한·putOrg 미러 추가) PASS + 게이트 5종.
- app.js?v=630 · APP_VER 630.

### 631차 — 규칙 침투 테스트 자동화(GPT 3차 리뷰 최우선 권고)
- 3차 리뷰(v630 대상) 판정: **신규 결함 0 · v630 반영 전부 승인(P0 CLOSED)** · ①의 미러 방식이
  자기 제안(직접 참조)보다 적합함을 인정 · ②⑤⑥ 유지 판단에 동의(명시적 risk acceptance 로 기록).
- **scripts/test/rules-auth.mjs — 여섯 번째 검증 게이트**: 에뮬레이터(Java 의존) 없이, 이 앱 규칙이
  실제로 쓰는 표현 부분집합을 JS 로 직접 평가하는 미니 평가기 + **AUTH-01~11 침투 시나리오 21항목**
  (org·people·sites 권역·createdBy 불변·레거시 탈취·업무 위계·미인증 방어). CI 에도 편입.
  ⚠ 근사 한계 명시: RTDB 의 상위 .write cascade 는 흉내내지 않는다 — 이 앱 규칙엔 상위 .write 가
  없어 발동하지 않으며, 전제(상위 .write 부재)가 깨지면 테스트가 FAIL 로 알린다.
  ⚠ 평가기 함정: 필드 .validate 안의 data 는 **그 필드의 기존값** — 재귀에서 data 를 함께 내려야
  한다(안 내리면 createdBy 불변 검사가 오판 — 초판에서 실제로 4항목 오판해 잡아냄).
- 권한 매트릭스 ↔ AUTH-ID 연결(리뷰 §15): AUTH-01 org 쓰기 관리자만 / AUTH-02·03 people 본인
  경계(+03b rank 승급 차단) / AUTH-04·05 공구장 권역 경계 / AUTH-06 createdBy 불변(+06b 레거시화
  차단) / AUTH-07 레거시 소유 탈취 차단 / AUTH-08 관리자 org / AUTH-09 sites 권역 강제 /
  AUTH-10a~g 업무 위계 전 분기 / AUTH-11 미인증 방어. 콘솔 수동 시나리오 8종(630차)은 이 자동
  스위트의 부분집합이 됐다 — 반영 후 확인은 여전히 실서버에서 몇 개 수행할 것.
- sites 값 검증 isNumber → **== 1** 로 조임(3차 리뷰 §8 — 배정 값은 늘 1).
- 리뷰 제시 로드맵 수용: 다음 대형 작업은 app.js 모듈 분리(대형 기능 착수 시), Gemini 서버화는
  사내 서버 확보 시 재론. 기능 동결 기조 — 이후 수정은 실사용 발견분 위주.
- app.js?v=631 · APP_VER 631.

### 633차 — 무지개 색 원/공통 업무 렌더링 보강
- `rainbow` 토큰이 업무 색 원에 그대로 `background: rainbow` 로 들어가던 누락 경로를 제거하고, 모든 색 원의 배경은 `colBg()`를 거치도록 정리.
- 공통 업무의 속 빈 색 원도 `p-col-rainbow` 클래스로 그라디언트 테두리를 렌더링하고 흰 속을 유지.
- 업무 현황 편집/읽기 색 점도 `colBg(planColor(...))`를 사용해 무지개를 정상 표시.
- FullCalendar는 담당자 유무와 관계없이 `ev-rb`를 붙이며, 공통 업무는 흰 속 + 무지개 테두리로 렌더링.
- `scripts/test/rainbow-render.mjs` 추가: 실제 브라우저에서 색 원 computedStyle 및 FullCalendar DOM의 `ev-rb` 렌더링을 검증. CI `smoke.yml`에도 편입.
- static-audit에 무지개 inline background 누락 경로 및 클래스 경로 회귀 검사 추가.
- app.js?v=633 · APP_VER 633.

### 632차 — 프로필 무지개색(재미용 기본색, 전면 적용)
- 색 파이프에 특수 토큰 **'rainbow'** 하나(RAINBOW_BG 그라디언트, users.avColor 검증 ≤16 통과).
  팔레트 스와치는 단색 뒤·[+] 앞(custom 판정에서 rainbow 제외). background 소비처 9곳은 colBg()
  경유(아바타 3형·--avc 2곳·own-chip·주간 표 점·일정 색 점·폼 점).
- **캘린더**: FC 는 backgroundColor 로 그라디언트를 못 받는다 — classNames 에 **ev-rb** 를 붙이고
  CSS 가 칠한다(!important — FC 인라인 'rainbow' 무효값을 덮는다). isLightColor('rainbow')=false 라
  흰 글자로 자연 낙착. ⚠ **RAINBOW_BG 는 app.js 와 index.html(.ev-rb) 이중 정의** — static-audit 에
  동기 검사 추가(갈리면 FAIL).
- ⚠ 검증에서 재확인한 도메인 사실: **담당자 색의 주인은 트리(sid)가 아니라 assignees** —
  taskAsPlan owners=assignees 뿐이라, 담당 지정 없는 업무는 팀 공통(속 빈 막대)으로 그려진다.
  하네스 함정 2종도 기록: 계정 저장 경로는 FB.auth.currentUser 를 요구(스텁 필요),
  store 는 부팅 때 로컬 바인딩(FbStore 직접 호출).
- 검증: 실파이프 5항목(팔레트 스와치·클릭→users.avColor='rainbow' 저장·아바타/사이드바 그라디언트·
  캘린더 .ev-rb 그라디언트·오류 0) PASS + 게이트 6종.
- app.js?v=632 · APP_VER 632.
- app.js?v=615 · APP_VER 615.

## 8. 보류하기로 한 것

- 업무 이력(변경 기록) — 과도하다고 판단
- 첨부파일 — 무료 플랜에서 스토리지 사용 불가
- 통계 요약 — 기능 완성도가 더 높아진 뒤에

### 605차 전수 점검에서 남긴 것 (3대 과제 착수 전 기준선)
- **`vendor/apt-geo.js` 는 248바이트 빈 표다**(`window.KRAPT=null`). 만드는 스크립트
  `scripts/apt-index.mjs` 는 완성돼 있고 **입력 자료(공공데이터포털 도로명주소 건물DB)만 없다**.
  현장명 자동 좌표(과제 1)는 새 코드가 아니라 이 자료를 받아 스크립트를 돌리는 일에 가깝다.
  ⚠ 건물DB 는 배포 회차마다 열 순서가 바뀐 적이 있다 — `apt-index.mjs` 의 `COL` 을 첫 줄과 대조할 것.
- (죽은 심볼·CSS 변수·팔레트 사본·xlsx 지연 로드는 605차에 처리했다 — 위 605차 절 참조)
- **`korea-geo.js` 561KB 는 아직 첫 로드에서 받는다**. 조직 관리 지도에서만 쓰지만 `krGeo()` 를 부르는
  동기 렌더 함수가 7군데라 로더를 끼우면 재렌더 훅이 필요하다. 과제 1 과 함께 처리할 것.
- **완전히 빈 `catch{}` 34곳**. 대부분 localStorage 차단 대비라 의도된 것이지만,
  `dfLoadPlans`·`dfLoadAna` 처럼 통신 실패를 삼키는 자리는 사용자가 "왜 비어 있지"를 알 길이 없다.
- **하자 데이터는 사실상 읽기 전용이다.** calapp 이 쓰는 곳은 `plans/{sid}/…`(처리계획)과
  `report/{rm}/{sid}/vac/…`(공가 수치) **둘뿐**이고, `report/{rm}/…` 본문·`reportIndex`·`analysis/`·
  `siteConfig` 는 전부 report 앱이 만든다. 과제 2(입력 기능 이식)는 이 세 자리를 쓰는 쪽을 새로 짜는 일이다.
  규칙(`database.rules.json`)은 `report/$rm`·`reportIndex/$rm`·`siteConfig/$sid` 에 이미 쓰기 조건이 있으니
  권한 구조를 새로 만들 필요는 없어 보인다 — 다만 조건식을 실제로 통과하는지는 확인이 필요하다.
- **Firebase 결합 지점(과제 3 범위)**: 인증 6개(`signInWithEmailAndPassword`·`createUserWithEmailAndPassword`·
  `sendEmailVerification`×2·`sendPasswordResetEmail`·`EmailAuthProvider.credential`) + `onAuthStateChanged`,
  App Check(ReCaptcha Enterprise) 1곳, RTDB `ref()` 22종·실시간 구독 `.on()` 7곳.
  `database.rules.json` 은 **29곳에서 `@hdec.co.kr` 도메인을 강제**한다 — MS(Entra ID)로 옮기면
  이 검사는 테넌트 소속으로 대체되고, RTDB 의 규칙 기반 권한은 서버 쪽 권한 검사로 옮겨야 한다.
  ⚠ 실시간 구독 7곳(조직·게시월·업무 등)이 화면 즉시 반영의 근간이다. 옮길 대상에 실시간 채널이
  없으면(예: 순수 REST) 폴링으로 바뀌며 체감이 달라진다 — 이전 설계에서 가장 먼저 정할 것.

## 9. 최근 방향

사용자는 프레이머 CMS의 레이아웃 감각(넓은 행, 명확한 열 구분, 여백)을 참고
방향으로 언급했습니다. 배지 크기·모양을 통일하고 역할은 색으로만 구분하는 원칙,
조작 버튼은 평소 접어 두고 마우스를 올렸을 때 펼치는 방식이 이 방향에서 나왔습니다.

디자인은 하자처리 현황 앱의 언어를 그대로 가져오는 것이 원칙입니다. 값을 임의로
조정하지 말고 그쪽 앱 값을 확인해 쓰세요.


### 634차 — 실제 Firebase E2E 게이트 추가

- `APP_VER=634`, `index.html`의 vendor/app 캐시 버전 `634`로 통일.
- `scripts/test/firebase-live-e2e.mjs` 추가: 실제 Firebase Auth + RTDB에서 2계정 로그인, 신규 업무 생성/재조회/새로고침/수정/삭제, 타 사용자 무단 수정 거부, `createdBy` 불변, 동시 partial update 보존을 검증한다.
- CI는 `LIVE_E2E_EMAIL`, `LIVE_E2E_PASSWORD`, `LIVE_E2E_SECOND_EMAIL`, `LIVE_E2E_SECOND_PASSWORD` 4개 GitHub Secrets가 있을 때만 실제 Firebase E2E를 실행한다.
- 테스트 계정은 별도로 준비해야 하며, 실제 데이터가 오염되지 않도록 `calapp/tasks/__e2e634__` 아래 임시 데이터만 사용 후 삭제한다.
- 모듈화는 보류. 634차의 목적은 구조 변경이 아니라 실제 Firebase 운영 경로를 검증하는 것.


### 635차 — 업무 동시 수정 덮어쓰기 방지 + partial update 권한 검증
- `FbStore.putTask()`의 기존 업무 저장을 전체 `set()`에서 변경 필드 diff 기반 `update()`로 보강했다. 신규는 `set()`, 삭제는 `remove()`를 유지한다.
- 중첩 객체(`assignees`, `links`, `recur` 등)는 경로별 diff로 처리하여 서로 다른 사용자의 변경을 불필요하게 덮지 않도록 했다.
- 실제 Firebase E2E에 `createdBy` 부분 삭제 우회 차단, 담당자 partial update, 비변경 필드 보존 검증을 추가했다. 시험 경로는 `calapp/tasks/__e2e635__`.
- 모듈화는 계속 보류한다. 이번 차수의 목적은 운영 데이터 안정화다.



### 637차 — 테스트 하네스 정리 + 독립 실행 회귀 게이트
- `APP_VER=637`, `index.html`의 `app.js?v=637`로 버전을 통일했다.
- 브라우저 테스트는 `playwright` 하나로 통일하고 `playwright-core` 중복 의존성을 제거했다. CI의 Playwright Chromium 설치 경로와 로컬 `CHROMIUM` 지정 경로를 모두 지원한다.
- `scripts/test/run-all.mjs`를 추가해 정적 감사 → Rules → smoke → rainbow → 하자 E2E를 `npm test` 한 번으로 실행한다. 실제 Firebase E2E는 4개 `LIVE_E2E_*` 환경변수가 모두 있을 때만 실행한다.
- `calc-equiv.mjs`·`upload-equiv.mjs`는 개발자 PC의 옛 `/report/report-main` 절대/상대 경로에 묶이지 않는다. 원본이 없으면 `SKIP`, `REPORT_MAIN` 또는 `--report <경로>`를 주면 비교한다.
- `static-audit.mjs`의 partial update 검사가 `process.exit()` 뒤에 있어 실행되지 않던 위치 오류를 바로잡았다.
- 브라우저 smoke에서 실패를 `.catch(()=>{})`로 삼켜 `FAIL 0`처럼 보일 수 있던 대기 구간을 실제 실패로 전파하도록 정리했다.
- 637차에서는 앱 기능·디자인을 추가하지 않았다. 목적은 테스트 신뢰성, 독립 실행성, 회귀 검출력 강화다.
- 검증 목표: `npm test` 전체 게이트에서 실제 실패는 1건이라도 즉시 종료 코드 1로 전달한다.

### 636차 — 오류 UX 안정화 + 문서/검증 현행화
- `APP_VER=636`, `index.html`의 `app.js?v=636`으로 버전을 통일했다.
- Firebase 저장 오류를 권한 오류와 연결 오류로 구분해 사용자에게 안내한다. 동일 오류의 짧은 시간 반복 토스트는 억제한다.
- `toast(msg,duration)`이 호출자가 지정한 표시 시간을 실제로 사용하도록 정리했다.
- 실제 Firebase E2E 시험 경로는 차수별 `calapp/tasks/__e2eNNN__`로 격리한다.
- README와 HANDOFF의 현재 차수·테스트 파일·오류 대응 내용을 현행화했다.
- 기능 추가나 모듈화는 하지 않고 안정화에 집중한다.
- 검증 목표: static-audit FAIL 0, build-single PASS, 기존 AUTH/rainbow/defect 게이트 유지.

### 666차 — 배포본에서 e2e 산출물 제거
- `e2e/` 는 소스가 아니라 **e2e-defect.mjs 가 뱉은 스크린샷 9장(3.0MB)** 이었다. 게이트를 저장소 안에서 돌리면
  그대로 쌓이고 zip 에 실려 나간다. 지웠고 9.2MB → 6.2MB.
- 재발 방지 둘:
  ① `e2e-defect.mjs` 의 기본 저장 위치를 저장소 밖(`os.tmpdir()/calapp-e2e`)으로 옮겼다. 보려면 `E2E_OUT` 지정.
  ② static-audit 에 검사 추가 — 산출물 폴더 이름(`e2e/ shots/ out/ tmp/`) 직접 검사 + **배포본 크기 8MB 초과 시 FAIL**.
     608차의 이름꼴 검사(`_modal.html`)로는 폴더 통째를 못 잡았다. 실제로 `e2e/` 를 만들어 FAIL 이 뜨는 것까지 확인.
- 남은 용량은 대부분 vendor(4.2MB) — Pretendard 두 벌 1.8MB, xlsx 932KB, korea-geo 564KB. 전부 실제로 쓰는 것들이다.
- APP_VER 666, index.html `app.js?v=666`.

### 665차 — 위젯 작성 중 팝업이 닫히거나 움직이던 문제
- 재현됨. 두 가지가 겹쳐 있었다.
  ① **Enter 를 치면 팝업이 통째로 닫힌다.** `peTitle` 에서 Enter → `savePlanInline()` → 끝에서 `selDate(p.date)`.
     위젯의 `selDate` 는 **같은 칸을 다시 누른 것**으로 보고 `S.widPop` 을 토글한다 → 열려 있던 팝업이 닫혔다.
     저장 전 `S.widPop` 을 기억했다가 되살린다.
  ② **폼이 커질 때마다 팝업이 움직인다.** 내용이 바뀌면 ResizeObserver 가 `widPlace()` 를 다시 부르고,
     높이가 늘면 y 가 위로 밀려 글 쓰는 중에 상자가 움직였다.
     `S.planEdit` 중에는 마지막 좌표(`dataset.px/py`)를 그대로 쓴다. 화면 밖으로 나갈 때만 다시 잡는다.
- 실측(900×760 위젯, 아래쪽 칸에서 작성): 제목 6회·본문 5줄 입력 동안 좌표 275,313 고정 · 팝업 계속 열림.
- APP_VER 665, index.html `app.js?v=665`.

### 664차 — 위젯 선택 음영
- 위젯에서는 왼쪽 파란 선만 보이고 면 색이 안 먹었다. 원인은 구체성 —
  `body.wid .wid-pop #dpList .plan{background:rgba(255,255,255,.08)}`(2766행)이
  `.plan.pick{background:var(--b50)}` 보다 구체적이라 배경을 계속 되돌렸다.
  같은 자리에서 `body.wid .wid-pop #dpList .plan.pick` 로 덮고, 어두운 판에 맞춰 파란 면(34%)과
  1px 하늘색 테두리를 준다. 실측 배경 rgba(62,113,210,.34) 적용 확인.
- ⚠ `--b50` 같은 밝은 토큰은 위젯(어두운 유리)에서 사실상 보이지 않는다 — 위젯용 색을 따로 줘야 한다.
- APP_VER 664, index.html `app.js?v=664`.

### 663차 — 위젯에서도 여러 건 고르기 · 모바일 툴바 폭
- 모바일 툴바가 화면 폭을 다 쓰던 것을 내용 폭으로 되돌렸다(`max-width:calc(100vw - 16px)` 만 유지).
- 선택 대상을 `PICK_SEL='#tkRoot .tk-item, .day-panel .plan'` 두 가지로 넓혔다.
  위젯은 업무 목록이 없고 **일자 패널(.day-panel)을 팝업 안으로 옮겨** 쓰므로, 카드가 위젯의 선택 대상이 된다.
  ⚠ 카드는 `data-sid` 가 없다(`data-pid` 만) → `pickKey()` 가 `findPlan()` 으로 소속(sid)을 찾는다.
  ⚠ 카드의 아이콘 줄(`.plan-acts`)은 선택에서 제외 — 안 그러면 수정·완료 아이콘이 안 눌린다.
- `plan.stCycle` 도 '고른 것 전체 따라가기'에 연결. 실측(위젯): 3장 선택 → 한 장 완료 → [3,1,1]→[2,2,2],
  Ctrl+Z → [3,1,1] 복귀.
- APP_VER 663, index.html `app.js?v=663`.

### 662차 — 토스트 문구 축약
- 위젯 폭(≈500px)에서 줄이 넘어가 두 줄로 뜨던 문구를 줄였다.
  '연결이 끊겼습니다 — 변경은 저장 대기로 쌓입니다' → '연결 끊김 · 저장 대기',
  '다시 연결되었습니다 …' → '다시 연결됨', '5건 되돌렸습니다' → '5건 되돌림',
  '업무 날짜를 옮겼습니다 · Ctrl+Z 로 되돌리기' → '날짜 옮김 · Ctrl+Z',
  권한 안내 '…건은 권한이 없어 건너뜀' → '…건 권한 없음'.
- `#toast{white-space:nowrap;max-width:calc(100vw - 24px)}` — 한 줄 유지, 화면보다 넓어지지 않게.
- APP_VER 662, index.html `app.js?v=662`.

### 661차 — 연결 상태는 토스트로 · 다중 작업 점검
- 660차의 위젯 주황 점(`#widOff`)을 걷고 **상태가 바뀌는 순간 하단 토스트**로 바꿨다.
  끊김 5초 · 복구 기본. ⚠ `netPaint()` 는 렌더마다 불리므로 이전 상태(`_netWas`)와 다를 때만 띄운다 —
  안 그러면 끊긴 내내 토스트가 반복된다. 재연결 안내도 `bindConn` 에서 빼고 여기 한 곳으로 모았다.
- 다중 작업 점검 결과(모두 설계대로):
  · 위젯에는 업무 목록(`#tkRoot`)이 없어 다중 선택 대상 자체가 없다 — 위젯은 달력 팝업의 카드 단위 조작만.
  · 위젯 팝업에서 두 건을 연달아 바꾸면 Ctrl+Z 는 **마지막 한 건만** 되돌린다(되돌리기 칸은 하나).
  · 일괄을 두 번 하면 두 번째 묶음만 되돌아간다. 첫 묶음은 그대로 남는다.
  · 주 이동 시 선택 해제 유지.
- APP_VER 661, index.html `app.js?v=661`.

### 660차 — 위젯에서도 되돌리기·연결 표시
- 위젯 점검에서 두 구멍이 나왔다.
  ① 위젯은 `body.wid #appHdr{display:none}` 이라 연결 끊김 알약이 아예 안 보인다
     → 위젯 오른쪽 위에 주황 점(`#widOff`) 하나를 띄운다(`body.wid.netoff` 일 때만).
  ② 위젯·달력·일자 패널의 상태 토글은 `plan.stCycle` 이라 657차 되돌리기(=`tk.st`)가 안 걸렸다
     → `plan.stCycle` 에도 스냅샷·커밋을 붙였다. 저장소는 결국 같다(`planToTask`).
- ⚠ 일정 객체는 `iid` 가 아니라 `id` 를 쓴다(`taskAsPlan`) — 657차 드래그 이동 스냅샷이 `p.iid` 라 안 떴다. 고침.
- ⚠ `S.live` 가 참인데 `FB.db` 가 없을 수 있다(로그아웃 직후) → 저장 경로 6곳에 `S.live&&FB.db` 가드.
- 실측: 위젯 일정 카드 3→2→Ctrl+Z→3, 연결 끊김 점 표시. 페이지 오류 0.
- 다섯 게이트 통과.
- APP_VER 660, index.html `app.js?v=660`.

### 659차 — 토스트는 되돌린 뒤에만
- 변경 직후 '되돌리기' 토스트를 없앴다 — 화면이 이미 바뀌었고 Ctrl+Z 가 있으니 군더더기다.
  토스트는 **되돌린 뒤 4초** 한 번만 뜨고, 버튼은 '다시 실행'(Ctrl+Y 와 같은 동작)이다.
- 일괄 처리도 성공 알림을 없앴다. 다만 권한으로 일부가 빠진 경우는 조용히 지나가면 안 되므로
  '3건 완료 · 2건은 권한이 없어 건너뜀' 만 남긴다.
- 다시 실행 뒤에는 토스트를 띄우지 않는다 — 되돌리기로 돌아갈 수 있음은 이미 알고 있다.
- 실측: 변경 직후 토스트 없음 / Ctrl+Z 뒤 '3건 되돌렸습니다 · 다시 실행' 표시. 페이지 오류 0.
- 다섯 게이트 통과.
- APP_VER 659, index.html `app.js?v=659`.

### 658차 — Ctrl+Z / Ctrl+Y
- 되돌리기 칸을 prev 만 갖던 것에서 prev·next 두 값으로 바꿔 다시 실행까지 만든다.
  `undoSnap()`(바꾸기 전) → `undoCommit()`(바꾼 뒤 결과값 채움) → `undoRun()`/`redoRun()` 이 한 칸을 주고받는다.
  ⚠ 새 변경이 생기면 REDO 는 버린다 — 안 그러면 갈라진 이력을 다시 실행하게 된다.
  ⚠ 토스트 버튼을 곧바로 누르면 `next` 가 아직 비어 있을 수 있어 `undoRun()` 이 그때 채운다.
- 단축키: `Ctrl/⌘+Z` 되돌리기, `Ctrl/⌘+Y` 또는 `Ctrl/⌘+Shift+Z` 다시 실행.
  ⚠ 입력칸(input·textarea·contenteditable)에서는 가로채지 않는다 — 브라우저 기본 실행취소가 우선이다.
- 실측: 1→2 →Ctrl+Z→1 →Ctrl+Y→2, 입력 중 Ctrl+Z 는 값 변화 없음. 페이지 오류 0.
- 다섯 게이트 통과.
- APP_VER 658, index.html `app.js?v=658`.

### 657차 — 연결 끊김 표시 · 되돌리기
- **연결 끊김**: 상단바에 주황 알약(`#ahOff`). `navigator.onLine`(online/offline 이벤트)과
  RTDB `.info/connected` 를 함께 본다.
  ⚠ onLine 만으로는 사내망은 붙어 있고 인터넷만 끊긴 경우를 못 잡는다 → 라이브에서는 `.info/connected` 가 기준.
  다시 붙으면 '밀린 변경을 보냈습니다' 토스트. RTDB 가 오프라인 큐를 갖고 있으므로 쓰기 자체는 우리가 다시 보내지 않는다.
  좁은 화면(≤720px)에서는 글자를 지우고 점만 남긴다.
- **되돌리기**: 변경 **직전 값**을 한 칸(`UNDO`)만 들고, 토스트의 '되돌리기' 버튼으로 되돌린다.
  ⚠ 스냅샷은 값을 바꾸기 **전에** 떠야 한다. 일괄 처리는 묶음 전체가 한 칸 — 한 번 누르면 통째로 돌아간다.
  붙인 곳: 한 건 상태 토글(`tk.st`), 일괄 상태 동기화(`pickSyncSt`), 달력 드래그 이동(`eventDrop`).
  ⚠ 토스트는 평소 `pointer-events:none` 이라 버튼이 있을 때만(`.has-btn`) 켠다.
- 실측: 한 건 1→2→되돌리기→1, 일괄 3건 2,2,2→되돌리기→1,1,1. 페이지 오류 0.
- 다섯 게이트 통과.
- APP_VER 657, index.html `app.js?v=657`.

### 656차 — 상태는 행에서, 툴바는 삭제·해제만
- 고른 것 중 아무 행의 완료 표시를 누르면 나머지도 같은 상태로 따라간다(`pickSyncSt`).
  방향은 누른 행 기준 — 그 행이 완료가 아니면 전부 완료로, 완료면 전부 진행으로.
  덕분에 툴바에서 완료·진행·보류 버튼을 뺐다. 남은 것은 '選 N건 · 안내 · 삭제 · 해제'.
- ⚠ 반복 업무는 **이 회차만** 바꾼다 — `st` 를 건드리면 다음 회차까지 닫힌다.
  회차 키는 행의 `data-occ`(`occSrc`)로 잡고 `doneOn` 에 넣고 뺀다. 실측: 반복 1건은 doneOn 1 · st 1 유지,
  일반 2건은 st 2 로 바뀜.
- `pickApply` 는 호출처가 사라져 제거(static-audit '정의뿐인 함수').
- 다섯 게이트 통과: static-audit(FAIL 0·WARN 0) · smoke · rainbow · e2e-defect(ALL PASS).
- APP_VER 656, index.html `app.js?v=656`.

### 655차 — 업무 여러 건 골라 한 번에 처리
- 데스크톱은 `Ctrl/⌘+클릭`(토글)·`Shift+클릭`(범위), 모바일은 500ms 길게 누르기로 선택 모드.
  범위는 **화면에 그려진 순서**(DOM index)로 계산한다 — 묶음·정렬이 매번 달라 데이터 순서로는 맞지 않는다.
- 막대는 화면 하단 가운데 플로팅(피그마 툴바꼴). `body` 직속으로 한 번만 만들고 보이기만 토글한다.
  ⚠ 목록 안에 넣으면 rTasks 마다 사라졌다 생긴다.
- 저장은 경로를 모아 `update()` **한 번**. 건별 putTask 는 쓰기·에코·재렌더가 건수만큼 난다.
  권한 없는 건은 `canEditTask` 로 걸러 '3건 완료 · 2건은 권한이 없어 건너뜀' 으로 알린다.
- ⚠ 세 곳에서 걸렸다(모두 고침):
  ① 목록을 다시 그리면 `.pick` 이 날아간다 → `restoreScroll()` 끝에서 `pickPaint()` 재적용.
  ② 길게 누르기를 마우스에도 걸면 데스크톱에서 잠깐 멈추기만 해도 선택이 켜진다 → `pointerType==='touch'` 한정.
  ③ 클릭 리스너가 둘(91행·9765행)이라 하나만 막으면 선택과 동시에 행이 펼쳐진다 → 두 곳 모두 같은 조건으로 통과.
- 화면 이동(`go`)·탭 전환(`tk.pick`)·주 이동(`mine.mon`) 시 선택 해제 — 목록 구성이 바뀐 뒤 남은 선택에
  일괄 적용되는 사고를 막는다(실제로 재현됐다: 달력 갔다 오니 선택이 남아 2건 선택인데 3건이 보류됐다).
- 미구현: 담당자·날짜 일괄 변경(반복 업무의 '이 회차만/전체' 규칙을 정한 뒤).
- APP_VER 655, index.html `app.js?v=655`.

### 654차 — 권역 이름·주차 라벨 정합 · 중복 CSS 정리 · 전 게이트 통과
- 프로필 소속의 권역이 id(`r1`) 로 나왔다 — 팀은 `teams.find().name` 을 쓰는데 권역만 `me.region` 원본을 썼다.
  조직 관리 표(8448행)와 같은 방식으로 이름을 찾고, 못 찾으면 원본 → '미지정' 순으로 떨어진다.
- 미니달력 주차 라벨이 첫 진입만 어긋났다(9월 1주차인데 '8월 1주차').
  달은 `S.mineYm`, 주차는 `tkWeekNo(nxt.start)` 로 출처가 달랐고 첫 진입은 mineYm 이 비어 오늘의 달을 썼다.
  `mine.mon` 의 d=0 과 같은 규칙(예정 주가 있는 달)으로 기본값을 맞춘다. 초기·복귀 모두 '9월 1주차'.
- 완전 중복 CSS 4건 제거: `body.wid .cal-title{font-size:14px}`(2회 중 1), `body.wid.glass .cal-title` 색,
  `.tk-ico.on`, `@media` 안의 `.mg-grid{grid-template-columns:minmax(0,1fr)}`.
  ⚠ `body.wid .cal-title` 은 뒤에 `--wfz` 규칙이 덮으므로 남은 하나도 사실상 초기값 역할만 한다.
- 점검 결과: 중복 함수·const·id 0, 죽은 함수·죽은 CSS 클래스 0, console.log/debugger 0,
  미사용 CSS 변수 4개는 FullCalendar 내부 변수라 유지.
- 다섯 게이트 전부 통과: static-audit(FAIL 0·WARN 0) · smoke(FAIL 0) · e2e-defect(ALL PASS) ·
  rainbow-render(FAIL 0) · rules-auth(ALL PASS).
  ⚠ 이 컨테이너에는 `playwright` 패키지가 없다 — `node_modules/playwright` 에 playwright-core 재노출 shim 을 만들고
  `CHROMIUM=/tmp/chromium` 으로 실행해야 게이트가 돈다(검증 후 shim 은 지웠다).
- APP_VER 654, index.html `app.js?v=654`.

### 653차 — 도구띠 보류함 켜짐 표시
- 모바일 도구띠의 보류함 알약은 켜져도 `.tks-item.act` 의 옅은 파랑(#F1F7FD)뿐이라 꺼짐(#F0F3F7)과 거의 같아 보였다.
  실측 확인 결과 클래스·색은 정상 적용되고 있었고, 문제는 대비였다 → 켜짐은 파란 면(--bt1) + 흰 글자로 바꿨다.
  ⚠ 646차에 `.c`(카운트)를 숨겨 두어 `.n` 색만 남는다 — `.act .n{color:#fff}` 를 같이 준다.
- 조직 관리 모바일의 현장 지도 위치는 측정만 하고 손대지 않았다(645차 배치 그대로).
  실측: 화면 높이 775px, 지도 카드 상단 1102px — 계정 카드 목록(667px)을 다 지나야 나온다.
- APP_VER 653, index.html `app.js?v=653`.

### 652차 — 미니달력 머리 가운데 · 모바일 필터 정렬
- 업무 현황 미니달력 머리가 왼쪽으로 쏠려 있었다. 원인은 643차의 `.cal-move{margin-right:auto}` —
  달력 머리에서 우측 묶음을 밀기 위한 값인데 미니달력에도 상속돼 알약을 왼쪽으로 붙였다.
  `.mc-move{margin-right:0}` 로 풀고 `.mc-h{justify-content:center}`. 실측 머리·격자 중심 모두 398px.
- 모바일 달력 필터 아이콘을 토요일 칸 중심에 맞췄다 — `right:calc((100% - 16px)/14 - 4px)`.
  칸 폭의 절반에서 아이콘 반폭을 뺀 값. 실측 필터 중심 349px = 토요일 중심 349px.
- 643차에 사라진 `.mc-now` 잔여 규칙 제거(static-audit 미사용 클래스).
- APP_VER 652, index.html `app.js?v=652`.

### 651차 — 아바타 색 최근 목록
- 직접 고른 색을 칩 **한 칸**에 덮어써서 새 색을 고르면 이전 색이 사라졌다 → 업무 색 팝오버와 같은
  저장소(`palCustom`/`palAdd`, 계정별 localStorage, 최근 7개)를 프로필 팔레트도 함께 쓰게 했다.
  칩 클릭으로 고른 색도 `palAdd` 로 남긴다(기본 12색·무지개 제외).
- ⚠ `input` 은 네이티브 색 선택기를 드래그하는 내내 울린다 — 여기서 저장하면 최근색이 중간 색으로 찬다.
  드래그 중에는 미리보기 칩(`.pal-live`) 하나만 움직이고, `change`(손을 뗄 때)에 `palAdd` + 칩 확정.
  이미 같은 색 칩이 있으면 미리보기를 지우고 그 칩을 선택 상태로 만든다(중복 방지).
- APP_VER 651, index.html `app.js?v=651`.

### 650차 — 프로필 소속 순서 팀 · 권역 · 직급
- 조직의 포함 관계(팀 ⊃ 권역)를 먼저 두고 그 안에서의 역할(직급)을 뒤에 둔다. 표시 순서만 바뀌고 값·권한은 그대로.
- APP_VER 650, index.html `app.js?v=650`.

### 649차 — 비밀번호 탭 이름 아래 구분선
- 프로필 탭은 `.myorg` 의 `border-top` 이 이름과 소속을 갈랐는데 비밀번호 탭에는 그 선이 없었다.
  `.acct-sec`(margin-top:16 / padding-top:14 / border-top)로 같은 값을 써서 두 탭의 선 위치를 맞춘다.
  ⚠ 섹션 첫 라벨은 `.il-first{margin-top:0}` 로 눌러야 선과의 간격이 프로필 탭과 같아진다.
- 두 탭 높이 실측 319px 로 여전히 동일.
- APP_VER 649, index.html `app.js?v=649`.

### 648차 — 계정 두 탭 높이 정렬 · 담당 현장 테두리 제거
- 비밀번호 탭에도 이름 칸을 넣어 두 탭의 세로를 맞췄다. 실측 둘 다 306px.
  ⚠ 프로필 탭의 이름 입력은 `#acctName`, 비밀번호 탭은 `#acctNamePw` — 같은 id 를 두 번 쓰면 저장 로직이
  어느 칸을 읽을지 모호해진다.
- 이름 칸이 들어가며 세로가 늘어 '비밀번호 변경'을 전폭에서 반폭으로 줄이고 나머지 반에 '비밀번호 찾기'.
  `.acct-btn-full` 은 사용처가 사라져 제거(static-audit 미사용 클래스 경고).
- 담당 현장 테두리 제거 — 칩 자체가 상자라 한 겹 더 두르면 이중 상자가 된다.
  대신 줄 높이 34px·칩 26px 로 위 상자(팀·직급·권역)와 시각적 줄을 맞춘다.
- APP_VER 648, index.html `app.js?v=648`.

### 647차 — 프로필 담당 현장 상자화 · 비밀번호 찾기
- 담당 현장 칩만 맨몸으로 떠 있어 팀·직급·권역 상자와 줄 높이가 어긋나 보였다 → `.myorg-f-wide .site-chk` 를
  `--surf2` 면 + `--sep` 테두리로 감싸 같은 상자로 만들고(높이 36px), 행 간격 10→12.
- 비밀번호 탭 아래 여백에 '현재 비밀번호가 기억나지 않나요?' — 구분선 위로 띄운 글자 버튼.
  ⚠ 로그인 화면의 `fbDoReset` 은 로그인 폼(`fbCreds`)을 읽으므로 프로필에서 재사용 불가 →
  `acctResetPw()` 를 따로 두고 대상은 항상 `S.user.email`. 이미 로그인한 상태라 계정 존재 여부 은폐가 필요 없어
  안내 문구가 단순하다(성공 시 보낸 주소를 그대로 알려 준다).
- 담당 현장 기능 자체는 유지 — `canEditTask` 의 `it.site && mySites()[it.site]` 조건이 권한 판정에 쓰인다.
  지우면 담당자 직급이 자기가 만들거나 지정된 업무만 고칠 수 있게 된다.
- APP_VER 647, index.html `app.js?v=647`.

### 646차 — 모바일 달력 머리 무몰딩 · 도구띠 배지 제거 · 도움말 농도
- 모바일 달력 머리: 년월을 알약 채움 없이 가운데로(`cal-head{justify-content:center}` + `cal-move` 배경 제거),
  필터는 `cal-ctl` 를 absolute 로 오른쪽 끝에 붙이고 채움 제거. 데스크톱·위젯은 그대로.
- 업무 현황 도구띠의 보류함 카운트 배지(.c) 숨김 — 한 줄에서는 이름만.
- `.ah-help` 의 `opacity:.72` 제거 — 다른 상단바 아이콘과 농도가 달라 혼자 흐렸다.
- APP_VER 646, index.html `app.js?v=646`.

### 645차 — 모바일 3종(달력 미니달력·업무 도구띠·조직 한 열)
- **모바일 달력**: FullCalendar 를 숨기고 `#calMini` 에 미니달력을, 아래에 업무 패널을 상시로 둔다.
  ⚠ 업무 현황의 `miniCalHTML()` 은 S.mineYm·mine.day(업무 현황 상태)에 묶여 있어 재사용 불가 →
  `calMiniHTML()` 을 따로 두되 마크업 클래스(.mini-cal/.mc-*)는 같은 것을 쓴다.
  ⚠ 점은 `CAL.getEvents()` 에서 뽑는다 — 달력 필터가 이미 반영된 목록이라 필터를 두 번 구현하지 않는다.
    FullCalendar 의 end 는 배타적이라 하루 당겨야 마지막 날이 맞는다.
  ⚠ 위젯도 폭이 좁아 `isMob()` 이 참이다 — `!WIDGET` 로 제외하지 않으면 위젯에 미니달력이 겹쳐 뜬다.
  날짜 탭은 `cal.day` → `selDate`. 시트를 안 쓰므로 `dpSheet` 의 스크림은 사이드바 드로어 전용으로 바꿨다.
  `rCalMini()` 는 `rMonTitle()`(달 이동)과 `rDay()`(목록 변경) 두 곳에서 부른다.
- **업무 현황 도구띠**: 미니달력 머리·보류함·찾기/필터를 `.tkbar-wrap` 하나로 묶고, 모바일에서만 한 줄로 편다
  (데스크톱은 세로로 쌓여 종전과 같다). 격자·요일은 숨기고 머리줄만 남긴다. 실측 364px = 상자 폭, 넘침 없음.
- **조직 관리**: 두 열의 자식을 한 흐름에 세우려고 `.set-col{display:contents}` 를 쓰고 order 로
  세그 → 권역 탭 → 목록 → 팀·권역·지도 순으로 재배치. 목록이 전폭을 쓴다.
- APP_VER 645, index.html `app.js?v=645`.

### 644차 — '오늘' 폐기 · 12월 공통 막대 · 흐르는 무지개
- **오늘 버튼 폐기**: 이동은 `‹ 년월 ›` 알약 하나로 끝낸다. 이번 달 복귀는 년월을 눌러 뜨는 연·월 팝업이
  이번 달을 `now` 로 강조하므로 두 번이면 된다. `#calNow`·`.cal-now`·`'cal.today'` 핸들러까지 짝지어 제거
  (하나라도 남기면 static-audit 의 '발신처 없는 핸들러/미사용 클래스'에 걸린다).
  업무 현황 미니달력은 년월·주차 라벨 자체가 복귀 버튼이다(643차).
- **12월 공통 업무 막대**: 보고 있는 달이 12월이면 `body.dec` → `.fc-event.team` 만 크리스마스 줄무늬.
  기간 한정 스킨은 공휴일에 앱을 안 보므로 무의미하지만, 12월 한 달은 실제로 본다.
  ⚠ 담당자 색은 손대지 않는다 — 공통 업무만 색이 없으므로 여기만 바꿔야 색=담당자 규칙이 유지된다.
  무지개를 고른 공통 업무는 제외(`:not(.ev-rb)`).
- **흐르는 무지개**: `ev-rb` 에 8초 주기 애니메이션. 그라디언트 끝을 첫 색(#F43F5E)으로 닫고
  타일 폭(400px)만큼만 밀어 이음매를 없앤다. `prefers-reduced-motion` 존중, `body.wid-await`(위젯 비활성)에서 정지.
  ⚠ RAINBOW_BG(app.js)와 .ev-rb(index.html)는 같은 값이어야 한다 — static-audit 이 이중 정의를 검사한다.
- APP_VER 644, index.html `app.js?v=644`.

### 643차 — 이동/목적지 분리(‹ 년월 ›, 오늘) · 모바일 검색 무몰딩
- **달력 머리**: `‹ ⌂ ›` 한 묶음을 `‹ 년월 ›`(이동)과 `오늘`(목적지)로 갈랐다. ⌂ 아이콘은 홈으로 읽혀 글자로 바꿨다.
  이번 달이면 `#calNow`를 `disabled`(흐림) — 없애면 우측 묶음 자리가 흔들린다(609차와 같은 이유).
  `.cal-move` 신설, `rMonTitle()`에서 흐림 토글. 데스크톱·모바일·위젯 공통(같은 DOM).
- **미니달력 머리**: 같은 배치. `이번 주`(주간)·`이번 달`(월간) 글자 버튼, `miniIsNow()`로 흐림 판정.
  `.mc-h`의 `.cal-nav{margin-left:auto}` 제거 — 화살표가 년월을 감싸므로 더 이상 우측 밀기가 아니다.
- **모바일 검색**(≤620px): 525차 `.ah-srch .ah-srch-i{left:4px}`(0,2,0)가 `left:50%`(0,1,0)를 이겨 아이콘이 상자 밖으로
  반쯤 나가 있었다 — 같은 구체성으로 맞춤. 461차 회색 면·밑줄 잔재를 걷어 데스크톱과 같은 무몰딩으로,
  펼쳤을 때만 밑줄. `.ah-srch-x`는 흰 셸용 색이라 밝은 상단바에서 안 보여 `--fill`/`--lbl2`로.
- 시안만(미구현): 모바일 달력 미니달력 이식+상시 패널, 업무 현황 도구띠 통합, 조직 관리 한 열, 금·은·오로라·크리스탈·펄 마감색.
- APP_VER 643, index.html `app.js?v=643`.

### 642차 — 모바일 헤더 겹침 · 모바일 달력 보류함 제거
- **헤더 겹침**(≤900px): 461차 규칙 `#appHdr{position:absolute;right:12px}` 이 기본 규칙(94행, 456차 fixed 헤더)의
  `left:0` 을 풀지 않아 헤더가 화면 왼쪽부터 펼쳐지며 상단바의 메뉴 버튼(.mc)을 덮었다 — 탭이 헤더에 먹혀 드로어가
  열리지 않았다. `left:auto` 추가. 실측(390px): 헤더 x 0→307, 메뉴 탭 후 `#sidebar.mob-open` 참.
- **모바일 달력 보류함**: 하단 시트(.dp-col)에 #holdCard 가 함께 올라와 시트가 길어졌다. 모바일 달력에서는 감춘다
  (`body:not(.wid) #view-calendar .hold-card{display:none}`, ≤960px 블록) — 보류는 업무 현황에서 본다. holdFit 은
  이미 ≤960 에서 손을 떼므로 JS 변경 없음. 위젯은 예외(body.wid).
- 검토 중(시안만, 미구현): 모바일 달력을 미니 달력(점/건수/주간 줄)으로 바꾸고 아래에 상시 업무 패널을 두는 안.
- APP_VER 642, index.html `app.js?v=642`.

### 641차 — 무지개 공통 업무 막대 실DOM 구체성 고정
- 640차 검수에서 확인된 FullCalendar 공통 업무의 흰 배경 우선 문제를 기준으로 CSS를 `#fcal .fc-event.ev-rb` / `#fcal .fc-event.ev-rb.team`으로 고정한다.
- 기존 `#fcal .fc-event.team` 규칙보다 높은 동일 ID 계층에서 `!important`로 무지개 배경/테두리를 확정한다.
- `rainbow-render.mjs`의 실제 DOM `computedStyle` 검증을 최종 게이트로 유지한다.
- APP_VER 641, index.html `app.js?v=641`, README 기준선 641.

### 640차 — 검수: 공통 무지개 막대 CSS 구체성 결함 수정
- 검수(Claude)에서 npm test 실패 1건 발견: rainbow-render 의 "실제 FullCalendar DOM — 공통 무지개
  막대". 원인은 **CSS 구체성** — `#fcal .fc-event.team{background:#fff!important}`(231차, ID 셀렉터)가
  633차의 `.fc .ev-rb.team`(클래스 3개)을 압도해 공통 업무의 무지개 테두리가 흰 배경에 졌다.
  프로브 div(#fcal 밖)에서는 규칙이 정상 적용돼 보여 스타일시트만 봐서는 안 잡히는 결함 —
  실 DOM computedStyle 검증이 잡아냈다.
- 수정: 무지개 두 규칙을 `#fcal .fc-event.ev-rb`/`#fcal .fc-event.ev-rb.team` 으로 승격(ID 급).
  static-audit 의 이중 정의 동기 검사 정규식도 새 셀렉터로 갱신.
- 검수 소견(633~639 전반): putTask diff-update(635)는 규칙 createdBy 불변과 정합(자식 경로 쓰기도
  $iid .write 스코프에서 평가) · IndexedDB 이전(639)은 dfProdBoot 초입에서 1회 호출·마크 정상 ·
  equiv 게이트 제거는 report 폐기와 일관(⚠ 실데이터 대조(마스터 PC 게시본 수치 비교)는 아직
  안 했다면 그것이 마지막 등가 확인 기회) · 638차는 결번(기록 없음).
- APP_VER 640.

### 639차 — 하자처리 운영 경로 독립화
- 하자 업로드·집계·게시·조회가 현재 앱 하나에서 완결되는 구조로 문서와 런타임 주석을 정리했다.
- 하자 원본 IndexedDB를 `calapp_defects_v1`로 분리하고, 기존 설치의 로컬 하자 데이터가 있으면 최초 1회만 자동 이전한다.
- 제외 키워드 설정은 `calapp.exTk`만 사용하도록 정리했다.
- 외부 비교 전용 `calc-equiv.mjs`·`upload-equiv.mjs`를 제거하고 `npm test`를 저장소 단독 회귀 게이트로 단순화했다.
- README와 `database.rules.json`은 현재 앱의 운영 구조만 설명하도록 현행화했다.
- APP_VER 639.
