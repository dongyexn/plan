# 인수인계 — H · 주요업무현황 (calapp)

**현재 기준: v821.**

- **업무 도구 › 도면 인쇄(815차)**: DWG 를 그대로 읽어 **도면틀마다 한 장**으로 나눠 인쇄한다(ZWCAD 를 열지 않으려는 목적). `#view-dwg` / `rDwg()` / 상태 `DW`. 이 창 메모리에서만.
  ⚠ 엔진(vendor/libredwg, GPL-3.0, wasm 9.7MB)은 **워커에서만** 돌린다 — 엠스크립튼 글루가 `new Function` 을 써서 문서 CSP(script-src 'self')에 막히지만, 워커 전역에는 그 CSP 가 상속되지 않아 그대로 돈다(실측). 그래서 **CSP 는 손대지 않았다**.
  워커(`vendor/libredwg/dwg-worker.js`)가 파싱 + 도형 펴기(블록·치수 블록 전개, MTEXT 서식코드 제거)까지 하고 선 묶음·글자 목록만 돌려준다. 본체는 도면틀 찾기(`dwRects` — 닫힌 네모 + **선 4개로 된 틀**)와 덩어리 나누기(`dwClusters`), 미리보기·인쇄를 맡는다.
  ⚠ 글자는 도형과 **같은 좌표계(y 위로)** 로 적는다 — `-y` 로 적으면 바깥 그룹의 뒤집기와 겹쳐 위아래가 뒤집힌다(815차에 그 버그를 겪었다).
  816차: 카드는 「도면 파일 / 기준(분리 자동·도면틀·오브젝트 + 용지 A4·A3) / 목록」. 용지는 A4·A3 만. 미리보기 세로 막대는 사진대지와 같은 방식(`dwSbSync`/`dwSbFlash`, 구를 때만). 817차: 바는 「미리보기 ◀ n / 전체 ▶」(쪽 이동을 제목 옆에), 왼쪽은 파일 이름·도형/글자 수·못 그린 것만 적는다(읽기 시간은 빼고 토스트에도 안 적는다), 초기화 단추.
  818차(외부 검토 반영): ⚠ **용지·방향은 인쇄 한 번에 하나**로 고정한다(`dwPaperMM()` 은 인자를 받지 않는다) — 장마다 다르면 브라우저가 첫 장 기준으로 찍어 나머지가 어긋난다. 자동이면 장들의 가로/세로를 세어 많은 쪽. 도면은 그 용지 안에 비율 그대로(contain).
  선 굵기·선종류는 워커가 «엔티티 → 레이어 → 기본 0.25mm» 로 물려받아 `styles[]` 로 넘기고, 쪽 SVG 안에 `polyline.sN{stroke-width…;stroke-dasharray…}` 로 붙는다(⚠ 바깥 `.dw-svg polyline` 과 명시도를 맞춰야 이긴다). 「선 / 반영·일정」 으로 끌 수 있다. 색상은 아직 흑백 고정.
  도면틀 오검출 줄이기: 같은 크기(3% 안)가 되풀이되면 그 무리만, 아니면 가장 큰 것의 40% 미만 후보는 버린다.
  실측: 480KB 0.6초 · 1.3MB 2.0초 · 3.4MB 2.2초. 못 그리는 것: POINT · SPLINE · VIEWPORT · 선굵기/선종류/해치 무늬.
- **업무 도구 › 견적 검토(811차)**: 업체 산출서의 「산출 근거」 열을 붙여넣어 계산식을 확인한다. `#view-qc` / `rQc()` / 상태 `QC`. 사진대지와 같이 **이 창 메모리에서만** — 저장소·서버에 쓰지 않는다.
  읽는 규칙: 「1. 공용욕실」의 번호가 **1 로 다시 시작하면 새 세대**(그 안의 2·3 은 같은 세대의 다른 공간) · 「- 항목」 + 「 : 식=수량단위」 · 식이 길어 「 = 11.1m」 이 다음 줄이면 이어 읽음 · 수량 없는 항목과 「조공 0.25인」 같이 식 없는 수량은 제외.
  ⚠ 식 계산은 **직접 만든 파서**다 — `eval`·`new Function` 은 이 앱 CSP(script-src 'self')가 막는다(처음엔 그걸로 짜서 전 줄이 「읽을 수 없는 줄」이 됐다). ⚠ 수량은 `=` **바로 뒤**에서 읽는다(끝에서 찾으면 「1.8m2」의 2 를 수량으로 읽는다). 단위 m2 → ㎡.
  기준: 반올림 정수/0.0(기본)/0.00/안 함 · 허용 오차 없음/±0.1/±0.5. 표는 머리 붙박이 + 위·아래 페이드(`--qh` 아래에서 시작) + 구를 때만 보이는 세로 막대, 좁은 화면은 가로로 흐르고 `ovsAttach` 막대. 세대·줄 수는 「검토 결과」 옆에만 적는다(812차). 사이드바 아이콘 `i-calc` 는 Lucide plus·minus·x·equal 을 2×2 로 얹은 것(813차 — 기호가 작아 `stroke-width:3.4`). 결과 복사는 **우클릭 메뉴**(이 줄·오답만·전체·판정/사유 2열, 탭 구분).
- **업무 도구 › 사진대지 작성(798차, 810차 메뉴 이름 「사진대지」→「사진대지 작성」 · 799차 섹션명 「업무 보조」→「업무 도구」)**: 사이드바 「업무 관리」와 「하자 관리」 사이. ⚠ 화면(flex)의 자식은 `#pdRoot` — 여기에 `flex:1;min-height:0` 이 없으면 쪽이 많을 때 미리보기가 스크롤 대신 늘어나 잘린다(799차). `#pdView` 는 `[data-sb]`(scroll-behavior:smooth)라 코드로 스크롤할 땐 `behavior:'instant'`. `#view-photo` / `rPhoto()`(전체) · `pdPages()`(미리보기만 — 왼쪽 입력 포커스·스크롤 유지) · 상태 `PD`.
  ⚠ **이 창 메모리에서만** — 서버·localStorage·IndexedDB 에 쓰지 않는다(사용자 지시: 창을 닫으면 사라지는 게 정상, 초기화 버튼 제공). 네트워크 요청 0 을 E2E 로 확인했다.
  사진은 넣을 때 긴 변 2000px JPEG blob 으로 줄여 든다(`src`), 자르기는 `src` 에서 잘라 새 blob(`url`) + 영역 `crop`(비율 좌표). 폴더 넣기는 `webkitdirectory`(하위 폴더 전부), 경로 자연 정렬.
  선택: 클릭 / Ctrl 토글 / Shift = 기준점(마지막 클릭)부터 범위. 끌기는 포인터 이벤트로 묶음째 끼워 넣기(가로 반쪽 · 1열이면 세로 반쪽), 빈 칸에 놓으면 맨 끝. 더블클릭 = 자르기, Delete = 삭제, Esc = 선택 해제.
  용지는 mm 로 짓고 미리보기만 `zoom:.54`. 인쇄는 상단바 인쇄 버튼(하자처리 현황과 같은 `#tbPrintWrap`, `dfTopbar`) · Ctrl+P · 브라우저 메뉴(beforeprint) 모두 `#pdPrint` 를 body 에 붙이고 `body.phs-printing` + `@page{size:A4 세로|가로;margin:0}` 를 뒤에 끼운다 — PDF 는 인쇄 창의 「PDF로 저장」(별도 버튼 없음, 사용자 지시).
  배치: 세로 2=1×2 · 4=2×2 · 6=2×3 / 가로 2=2×1 · 4=2×2 · 6=3×2(열×줄). **기본 세로 6장 · 제목 「사진대지」 · 현장명 공백**(800차).
  800차: 제목·현장명 쪽은 테두리 없음(테두리는 사진 표 `.phs-cells` 에만) · 쪽마다 꼬리말 `n / 전체`(803차: 6.5mm 띠의 **아래쪽**에 붙여 글자 밑선이 여백선과 맞는다. 미리보기 용지 밑 쪽 번호는 바와 중복이라 없앴다) · 사진 맞춤 기본 「늘림」(object-fit:fill, 자르지 않음) + 선택 카드에서 맞춤(contain)·채움(cover) · 미리보기 배율 한 쪽(폭 맞춤, 최대 1배)·두 쪽·여러 쪽(`pdZoom` 이 `--pz` 계산, resize 는 260ms 뒤 — 사이드바 자동 접기가 먼저 끝나야 폭이 맞다) + 배율(802차: Ctrl+휠 확대·축소 10~400%·커서 지점 유지, ＋－ 15%씩, 맞춤 4단 쪽/폭/두 쪽/여러 쪽 — `PD.zoom==="free"` 가 자유 배율) · 용지는 **A4 고정** · 안쪽 여백 **10mm 고정**(808차: 용지 크기·여백·사진 번호는 인쇄 창에서 고르면 되는 것이라 뺐다. 방향 세로/가로는 남아 `@page{size:210mm 297mm|297mm 210mm;margin:0}` 로 넘어간다) + 쪽 이동 ◀ n/전체 ▶(801차: 「미리보기」 바로 옆, 배율 seg 는 오른쪽 끝. 쪽 수를 두 군데 적지 않는다 — `#pdPgs` 없음. `pdPgIndi` 는 위에서 처음 보이는 쪽) · 미리보기 안쪽 여백 12px·쪽 사이 14px, 두 쪽 보기라도 **쪽이 하나면** 한 쪽 기준으로 크게(805차 — 쪽 수가 배율을 바꾸므로 `pdPages` 도 `pdZoom` 을 부른다) · 세로 스크롤 막대(`pdSbSync`/`pdSbFlash`, 809차 — 앱은 네이티브 스크롤바를 전역으로 숨기므로 얇은 막대를 띄운다. 구를 때만 보이고 0.9초 뒤 사라진다. ⚠ 미리보기 안은 사진을 끌어 옮기는 곳이라 `pointer-events:none` — 막대를 잡아끌지는 못한다) · 쪽이 칸을 못 채우면 위아래 가운데(`pdVC`, 804차 — ⚠ 넘칠 때 켜 두면 flex 가운데 정렬이 위쪽을 잘라 먹으므로 쪽 수·배율이 바뀔 때마다 다시 판단) · 쪽 나눔 이어서/폴더별(`PD.split`, 807차 — 폴더가 바뀌면 새 쪽. 쪽 구성은 `pdPageList` 가 만들고 빈 칸은 `data-fill` + 다음 사진의 자리번호를 달아 거기에 놓으면 그 사진 **앞**으로 들어간다) · 선택 사진 회전 90°(`pdRotOne` — 그림을 다시 만들고 잘라 둔 영역도 함께 돌린다) · 사진 교체(`pdSwap` — 위치·내용·맞춤·순서 유지) · 첫 값 복사 / 빈칸 채우기(`pdFill`) · 파일·폴더 이름에서 위치·내용 자동(`pdParseName`, 806차: 「101동 1203호」 띄어쓰기. 넣는 순서는 이름 자연순이되 **단계만 다르면 전→중→후(완료)**) · 이름 속 현장명이 조직 현장과 맞으면 머리글 현장명을 채운다(비어 있을 때만).
  ⚠ 클래스 접두는 `phs-` — `pd-b/pd-h/pd-sec` 는 업무 상세가 이미 쓴다(감사기 재선언으로 잡혔다). ⚠ 인쇄 숨김 규칙은 전역 `body:not(.df-printing):not(.rpt-on) #app{display:block!important}`(1-2-1)를 이겨야 해서 `body.phs-printing.phs-printing>*:not(#pdPrint):not(#pdPrint)`.

- **조직 관리 스크롤(796차, 데스크톱 ≥981px)**: `#view-org` 는 굴리지 않고 계정(`#acctRoot`)·미배정(`#freeRoot`)·현장(`#siteRoot>[data-sbx]`) 표 칸만 스크롤, 열 이름 줄 `position:sticky`(붙박이 칸은 collapse 테두리가 안 따라와 선을 inset 그림자로). 카드는 내용만큼이되 화면보다 길면 줄어든다 — 층마다 `flex:0 1 auto; min-height:0`(카드 → pane → 표 칸)를 빠뜨리면 다시 페이지가 늘어난다. 초안 저장 줄은 표 칸 밖(위)에 선다. 표 칸은 `data-sbh` — 위 페이드가 **머리 줄 아래에서** 시작한다(fadeOne 이 `--sbh`=thead 높이를 넣고 `sbh-t/b/l/r` 를 붙이며, CSS 가 세로·가로 마스크 두 판을 `mask-composite:intersect` 로 교차). ⚠ 스크롤 이벤트의 fadeOne 은 SB_SEL 밖 요소에도 불리므로, 머리 붙박이 스크롤 칸을 새로 만들면 `data-sbh` 를 달지 않으면 머리가 흐려진다(797차). 왼쪽 열(팀·권역·지도)은 창이 낮을 때만 자체 스크롤. 폰(≤980)은 685차 한 줄 쌓기·화면 스크롤 그대로. 지도 모달의 오른쪽 페이드(571차)는 제거, 현장 목록 줄은 빈 현장수 칸(`.kml-s`) 없이 이름이 그 폭을 쓴다(권역·합계·담당자 줄은 그대로).

- **현장 토글 · 현장 추가(795차)**: siteConfig 구독(`dfSubSiteCfg`)은 **부팅 때(bindShared) 건다** — 하자 화면에서만 걸면 조직 화면의 공가세대·공가상가·소송 토글이 저장값과 다르게 보이고, 그 상태에서 다른 토글을 누르면 전체 set 이 화면값으로 서버를 덮었다. 조직 표 토글은 바뀐 필드만 `update`. 「현장 추가」는 권역 ''(→ 공구장·담당자 빈 칸) · 하자현황(dfHide)·공가세대·공가상가·소송 전부 끔 · 표 맨 아래(`siteCmp` 가 이름 없는 행을 맨 끝으로) · 권역 탭이면 전체 탭으로 바꾼다. HCS 업로드 위저드의 새 현장(confirmNewSite)은 손대지 않았다.

- **결정 기록(780차)**: ① 처리계획(plans) 편집 권한은 **전원 편집 유지**(담당자 제한 안 함) ② **배포용 압축(minify) 빌드는 도입하지 않는다** — 첫 로드 JS 1.48MB 중 app.js 846KB(주석 103KB), 전송은 GitHub Pages gzip 으로 이미 296KB 라 압축 빌드로 줄어드는 건 주로 파싱 시간(수십 ms 대)뿐이고, 대신 배포마다 빌드 산출물·캐시 버전·디버깅(줄 번호 불일치)이 하나씩 늘어난다. 초기 로드가 더 문제되면 지연 로드(762차)를 넓히는 쪽이 먼저.
- **저장 신뢰성(763·765차)**: `defSave` 는 IndexedDB 실패 시 false — 업로드 루프(`doSaveUL`)는 저장 성공 때만 교체를 확정하고, 실패하면 이 세션의 옛 원본·`site.lastUploadedAt`·`DFMETA.lastUp` 을 되돌린 뒤 오류로 중단(765차: 763차의 delete 는 옛 원본까지 지웠다). e2e 검사 2건(defSave false · 835건 현장에 새 파일 저장 실패 → 옛 원본·시각 유지). 구버전 저장소 이전(`migrateDefectStore`)은 `indexedDB.databases()` 가 없는 브라우저에서 옛 DB 를 직접 열어 확인(없으면 onupgradeneeded 에서 취소·삭제).
- **추이 차트 툴팁(782차)**: 캔버스 툴팁으로는 「이름 왼쪽 / 값 오른쪽」 두 열을 못 만든다(한 줄 문자열뿐) → `tooltip:{enabled:false,external:chTipDraw}` 로 HTML 툴팁 `#chTip`. 커서 아래 15px, 화면 밖이면 위로 뒤집는다. 위치는 **transform** 으로 옮기고(left/top 은 레이아웃을 다시 잡아 끊겼다) 세로는 **커서 y**(canvas.__my) 기준이다 — `tooltip.caretY` 는 데이터 점의 y 라 계열마다 튄다. 같은 주차 안에서 위아래로만 움직이면 Chart 가 external 을 다시 안 부르므로 canvas mousemove 에서 `el.__pos()` 로 자리만 다시 잡는다(784차). ⚠ 캔버스를 벗어나면 Chart 가 external 을 더 부르지 않는다 → `mouseleave` + document mousemove 로 닫는다(`chTipHide`).
- **도넛 가운데 수치(768·770차)**: 조각·범례에 마우스를 올리면 그 항목의 건수·이름, 떼면 전체(`centerText` 옵션을 바꾸고 `update('none')`). 툴팁은 끔(770차). 모든 도넛(dfDonutDraw) 공통.
- **오늘 남은 업무 말풍선(779차)**: 사이드바가 접혀 있으면 아이콘 아래가 아니라 오른쪽 옆(`#evePop.side`, 화살표 왼쪽)에 붙는다 — 좁은 아이콘 기준으로 아래에 깔면 달력 위로 떠 가리키는 곳이 없어 보였다.
- **사이드바 목록 페이드(778차)**: `.sbsc` 에 `data-sb` — 현장이 많아 넘치면 위·아래 26px 페이드. 아래 끝은 「관리」 위 구분선(sb-bottom top)에 딱 맞춘다(margin-bottom −8 → 0, padding-bottom 0).
- **사이드바 접힘 중 차트 동결(777차)**: `chartsFreeze` — 움직이는 동안 Chart.js 인스턴스의 `resize` 를 빈 함수로 덮고(responsive 옵션은 생성 뒤 안 먹는다) 캔버스는 CSS 폭 100% 로 같이 늘렸다가 settle 때 원복+`resize()` 1회. 하자 화면 700ms 긴 프레임 합 433→200ms. FC 중간 updateSize 는 220ms 하나만.
- **위젯 막대 단계(776차)**: `calFitApply` 가 위젯에서도 돌며(datesSet·resize) 칸 높이로 ① 보통 → ② `.wid-tight`(막대 14px·숫자 여백 축소) → ③ `.wid-nomore`(「외 N건」 없이 1줄) 순으로 내려간다. 620×520·520×420·460×360 넘침 0.
- **「오늘」 알약(773·774차)**: 캘린더 필터 알약 왼쪽 `#calTodayWrap .cal-today`(폰 헤더는 년월과 필터 사이, 위젯도 표시) — `cal.today` = CAL.today()+오늘 선택(+폰 미니 격자 재렌더).
- **사이드바 mini(775차)**: 펼침과 **같은 줄·같은 좌표**(실측 아이콘 16,133 · 권역 머리 y 278 h30 · 현장 점 21,320 4px — 두 상태 동일). mini 는 폭·글자 투명도만 바뀐다: 권역 머리는 글자 투명 + 짧은 선, 현장은 같은 4px 점(현재 현장 흰 점), 접힌 권역의 현장은 펼침에서만 숨김 — **접힘에서는 모든 현장이 점으로 보인다**(779차: 안 그러면 권역 선만 줄줄이 남는다). 776·777차: 아이콘·점을 64px 가운데(x 32)에 두되 **padding-left 만** 바꾼다(nvi 11px, df-site 11px + 점 margin 11 — 펼침에서도 점 중심이 아이콘 중심선 x 29 에 맞아 이동량이 3px 로 같다) — 높이는 그대로라 트랜지션으로 미끄러진다. ⚠ center 정렬·높이·패딩 0 같은 별도 값을 주지 말 것.
- ⚠ 지도 자료 지연 로드 함정: `kmSiteXY` 는 KRGEO 없으면 계산·캐시하지 않는다(null 이 `KM_XY` 에 박혀 자료가 와도 점이 안 찍히던 것 — 774차). loadGeo 뒤 KM_XY 비움.
- **UI 잔손질(771차)**: 일자 패널 머리에 날짜(`#dpDate`, 공휴일명 포함) · 검색 × 전 검색창(tkQ `.dp-srch.has`, recQ·rkQ·rkQs `.rl-q-wrap.has`) · 월별/주차별 표 기준월 행 `.cur-row` · 조직 계정 현장 없음 → 「+ 현장 추가」 글자 버튼 · 설정 「하자 데이터」 카드 아래 스냅샷, 버전·오류는 도움말 카드 · 「자세히」 링크색 · 등록 토스트 3.5초+호버 유지. **조직 현장 표**: 담당자 [+] 제거(계정 탭에서만), 공구장 열(권역의 rank=lead), 공가상가 뒤 **소송현장 토글** `lawsuit`(siteConfig 필드·규칙 추가 → **규칙 먼저 게시**, 게시본 _dash.sites 에도 실림). 표시 용도는 아직 없음(현장 페이지 배지 등은 다음).
- **지연 로드(762차)**: chart.umd·datalabels 는 `loadChart()`(하자 화면 첫 차트 그릴 때, dfChartInit 이 부른다), korea-geo·apt-geo 는 `loadGeo()`(조직 관리 지도 첫 렌더, krGeo 가 부른다) — 첫 로드 JS 2,270→1,476KB. 읽히면 그 화면을 한 번 다시 그린다. 인쇄·보고서는 시작에서 `await loadChart()`. 검사 스크립트가 지도를 바로 쓰면 `loadGeo()` 를 먼저(rainbow-render). 이 문서는 지금 코드가 어떻게 되어 있는지만 적는다. 회차별 변경 이력은 `HANDOFF-log.md`(읽기 전용 참고).

## 1. 무엇인가

H건설 서비스중부팀(중부1·중부2·광주, 현장 약 14개)의 내부 웹앱. 한 화면에서 **업무 일정(달력) · 업무 현황 · 하자처리 현황(민원 현황 포함) · 조직 관리 · 설정**을 운영한다.

- 서버 없음. `index.html` + `app.js` 정적 파일을 GitHub Pages(`https://dongyexn.github.io/plan/`)가 배포하고 브라우저가 Firebase(RTDB·Auth)와 직접 주고받는다.
- 데스크톱 위젯은 `widget-lite/`(Tauri, WebView2)가 같은 URL을 띄운다. 팀원 대부분은 위젯, 관리자는 브라우저.
- AI 기능 없음. Azure·MS 저장소 없음. **결정 기록**: 회사가 Entra 앱 등록·Azure 정식 구독을 승인하지 않아 데이터는 Firebase 에 둔다. 다시 시도할 조건은 「Entra 앱 등록 1건 승인」.

## 2. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 마크업 + CSS 전체(단일 `<style>`) |
| `app.js` | 앱 전체(단일 파일). 상단에 `APP_VER` |
| `database.rules.json` | Firebase RTDB 규칙. 권한의 진실 |
| `scripts/test/static-audit.mjs` | 정적 감사(버전 동기·죽은 코드·CSS 재선언·주석 삼킴·data-act 짝) — FAIL 0·WARN 0 이 기준 |
| `scripts/test/smoke.mjs` | `?local=1` 로 부팅해 CRUD·달력·표·페이드 등 브라우저 검사 |
| `scripts/test/rules-auth.mjs` | 규칙 파일을 흉내내 역할별 read/write 허용·거부 |
| `scripts/test/rainbow-render.mjs` | 색(무지개·그라디언트) 렌더·지도 점·미니달력 점 |
| `scripts/test/e2e-defect.mjs` | 하자 전 구간: 가짜 HCS 업로드 → 게시 → 대시보드·현장 탭 → 부분 등록 유지 → 미게시 판정 |
| `scripts/test/risk-gold.mjs` | 민원 세대 레벨화 — `fixtures/risk-gold.md` 60건 정답셋 |
| `scripts/test/mobile-fit.mjs` | 폰 폭(375·390·360) 겹침·잘림 + 데스크톱 폭(1024~1920) 가로 스크롤 |
| `scripts/test/firebase-live-e2e.mjs` | 실제 Firebase 2계정 E2E(환경변수 있을 때만) |
| `scripts/test/run-all.mjs` | 위 게이트 순서대로 |
| `scripts/test/shot-prod.mjs` | 배포본 스크린샷(수동) |
| `scripts/font-subset.py` · `geo2026.mjs` · `coast-land.mjs` · `apt-index.mjs` · `site-geo-check.mjs` | 폰트 서브셋 · 지도 데이터 생성/검사(거의 안 만짐) |
| `vendor/` | FullCalendar · Firebase SDK · Pretendard · `korea-geo.js`(행정경계) |
| `widget-lite/` | Tauri 위젯(Rust). `min_inner_size(360,380)` |
| `build-single.mjs` | 단일 HTML 묶기 |
| `.github/workflows/` | push 시 smoke · 위젯 exe 빌드 |

## 3. 검증 — 순서 엄수

```
node --check app.js
node scripts/test/static-audit.mjs      # FAIL 0 · WARN 0
CHROMIUM=/path/to/chrome node scripts/test/run-all.mjs   # rules-auth · smoke · rainbow · e2e-defect · risk-gold
```
- 헤드리스 크로미움이 필요하다. `playwright` 패키지 대신 `playwright-core` + `node_modules/playwright/index.js` 재수출 심(shim)으로 돌아간다.
- 배포 zip 은 `calapp-vNNN/` 폴더 통째. **세 곳 버전 동기**: zip 이름 · `index.html` 의 `app.js?v=NNN` · `app.js` 의 `APP_VER`. 감사기가 어긋나면 FAIL.
- 코드 수정은 유일한 문맥 문자열 + `assert count==1` 로. **`s.find()` 가 -1 을 돌려준 채 슬라이스하면 파일 꼬리가 복제된다** — 718차에 실제로 났고(node --check 통과) 감사기 10c(함수 중복 정의)가 잡는다. 문자열 편집 뒤엔 반드시 감사기. `find(';')` 같은 넓은 앵커로 고치다 `<style>` 안에 URL 을 넣어 CSS 주석이 뒤를 삼킨 사고가 있었다(감사기 10b 가 잡는다).

## 4. 데이터 (RTDB)

| 경로 | 내용 |
|---|---|
| `calapp/tasks/{sid}/{iid}` | 업무. `sid` = 담당자 uid 또는 팀 id(공통). `assignees{uid:1}` · `date`/`end` · `color`(`auto`·hex·`grad-*`·`rainbow`) · `st`(1 진행 2 완료 3 보류) · `site` · `recur` |
| `calapp/plans/{uid}/{id}` | 달력 일정(휴무일 등) |
| `calapp/org` | `teams[]` · `regions[]` · `sites[]`(`normOrg` 가 권역→준공일 내림차순→이름으로 **정렬해 저장**) |
| `calapp/people/{uid}` | 명단: team · region · rank · sites{} |
| `calapp/cfg` · `prefs/{uid}` · `trash` · `archive` | 설정 · 개인 설정 · 휴지통(30일) · 보관함 |
| `users/{uid}` | 계정: role(editor/viewer) · avColor · avIcon |
| `report/{YYYY-MM}/_dash` · `/_meta` · `/{sid}/kpi` | 하자 게시본. kpi 안에 `hh`(세대 레벨) |
| `reportIndex/{YYYY-MM}` | 게시된 달 목록 |
| `siteConfig/{sid}` | 현장별 하자현황·공가 토글 |

원본 하자 데이터(HCS 엑셀)는 **이 PC 의 IndexedDB(`calapp_defects_v1`)** 에만 있다. 서버엔 게시본만 올라간다.

## 5. 권한

- `users/{uid}.role`: editor = 관리자(조직·현장·게시·삭제), viewer = 나머지. 규칙이 강제한다(`rules-auth`).
- 업무: 작성자·담당자만 수정. 팀장(head)·공구장(lead)은 자기 팀/권역 범위. 휴지통·보관함도 같은 소유 검사.
- 조직 삭제(담당자·현장)는 **우클릭(모바일은 길게 누르기) → 확인 모달**로만. 담당자 「삭제」는 레코드를 지우지 않고 팀·권역·현장을 비워 「미배정」으로 보낸다(업무에 이름이 남는다).

## 6. 화면별 핵심 로직

### 달력
- 데스크톱: 사이드바 · FullCalendar 월 뷰 · 오른쪽 업무 패널. 901~1280px 는 사이드바가 아이콘 레일로 자동(`sbAuto`, 사용자가 직접 토글하면 중단).
- **폰(세로)**: FullCalendar 를 안 쓰고 **미니 달력**만 그린다(`calMiniHTML` → `mcalPane` 3장: 이전·이번·다음 달을 `.mc-track` 에 가로로 이어 붙인다, 사건은 `buildEvents(from,to)` 로 세 달 범위 한 번). `S.mcal` = `full`(셀 큼 + 막대, 줄 수는 `mcalCap()` 이 셀 높이로 계산, 넘치면 「+N」) ↔ `mini`(셀 46px + 점 + 아래 업무 패널).
  - **제스처(733차, 손가락 추종)**: 가로 = 트랙을 transform 으로 끌고 놓으면 폭 25%·속도 0.35px/ms 로 이웃 달에 스냅 → `CAL.next()/prev()` 가 다시 그리면 새 트랙이 -33.33% 로 서서 이음매가 없다. 세로 = `#view-calendar` 의 `--mch`(셀 높이)·`--mcp`(0=점 … 1=막대)를 보간(달력 전체 높이가 손가락과 1:1, `dy/6`), 점↔막대·패널은 opacity 로 섞고, 놓으면 목표 높이까지 트랜지션(`mcal-settle`) 후 `mcalSet()` 확정. 드래그 중 `S.mcalDrag` 가 참이면 resize 의 `mcalSet` 재적용을 건너뛴다(iOS 주소창 접힘). 큰 달력에서 날짜 탭 → 점 달력+패널. ⚠ 두 높이(46 · (100dvh − th − 78)/6)는 CSS 와 `mcalFullH()/MCAL_MINI_H` 에 같이 있다.
  - **막대는 FullCalendar 모바일 규격 그대로**(15px·10.5px·500·모서리 3·완료 .72·공통=속 빈 막대+삼각·밝은 색 어두운 글자·고위험 표식). 주 단위 lane 배정 + 조각 이어붙이기, 머리 조각의 제목은 `z-index:1` 로 이어진 조각 위에 흐른다(폭은 주 안 길이만큼 인라인). 숫자 줄은 16px 고정, 숫자는 **두 모드 모두 오른쪽 위**(점도 오른쪽 아래, 742차 — 전환 때 가운데↔오른쪽으로 튀던 것 고정), 휴일명은 같은 줄 왼쪽 절대배치 — 칸마다 막대 시작선이 같다. 격자 열은 `minmax(0,1fr)`.
- **폰(가로)**: `mcal-land` — 데스크톱 레이아웃(FullCalendar + 오른쪽 300px 업무 패널, ‹ › 복귀). 아래 시트 안 쓴다. 칸이 41px 라 막대 1줄뿐이고 「외 N건」은 숨긴다(나머지는 날짜 탭 → 패널). `#fcal` 은 여기서도 flex.
- 상단바: 가운데 「2026. 9. ▾」(800·19px — ⚠ `font:` 단축에 `inherit` 을 섞으면 선언째 죽는다, 733차에 개별 속성으로), 다시 누르면 닫힘(`ymOutside` 예외에 `.tbt-ym` 포함), 도움말 숨김, 필터는 검색 바로 왼쪽(`right:54px`, 캡슐 채움 없음), 검색은 맨 오른쪽 아이콘 → **전체화면 검색 패널**(`nq.mob`).
- FC 루트 `#fcal` 은 **display:flex** 여야 한다(block 이면 높이 0). 헤드리스 터치 에뮬레이션에서 오른쪽 스와이프는 브라우저 뒤로가기다(앱 버그 아님).
- 색: `colBg()` 를 거쳐야 그라디언트·무지개가 칠해진다. SVG(지도 점)는 `colSvgDefs/colSvgFill`. 공통 업무 원은 `--gb` 로 링 그라디언트.

### 업무 현황
- 주간 표(`.tkl-*` grid, 날짜 열 `auto`) · 현장별 카드(폰은 grid 대신 flex — grid 행 높이가 늦게 갱신돼 카드가 겹쳤다). 기간 업무는 `it.date–it.end`.
- 놓친 업무 확인 모달(`morningReview`)은 날짜가 지난 미완료 업무를 아침에 한 번.

### 하자처리 현황
- **업로드**(설정): HCS 엑셀 → `norm()` → IndexedDB. 업로드 시 `site.lastUploadedAt` 을 조직에 저장(모든 PC 에서 「업데이트일」 표시).
- **계산** `calc(items,site,rm)`: KPI·주차 추이·공종·공가·**세대 레벨(`hh`)**. `capAll()` 은 지문 캐시(`dfCapCached`).
- **게시** `dfPublish`: 원본 있는 현장만 `report/{rm}/{sid}/kpi` 갱신, 없는 현장은 직전 게시본 유지. `_dash.wks/am` 은 이 PC 원본만으로 계산되므로 부분 등록이면 확인창에 경고(운영은 월초 전 현장 일괄 등록).
- **읽기 정책** `dfLocalDirty(sid)`: 이 PC 원본이 「마지막 게시」보다 새것일 때만 로컬로 그린다. 마지막 게시 = max(이 PC 도장 `DFMETA.pubAt`, 서버 `_meta/publishedAt`). 게시하면 마스터 PC 도 게시본을 읽는다. 상단바 「이 PC 원본 · 미게시 변경」 배지, 설정 카드에 미게시 현장 목록.
- 모바일: 인쇄 없음, 상세표는 첫 열 sticky + 가로 페이드(`data-sbx`), 추이 차트 점 작게. **표·막대 카드는 폰에서도 데스크톱 폭(1118px)을 유지하고 가로로 민다** — 좁혀서 맞추면 숫자·배지가 겹치고 앞자리가 잘린다(730차, `mobile-fit` 게이트가 감시).

### 민원 현황 (세대 레벨화)
- `RISK_RULES` 한 곳. 접수내용을 `/`·`//`·`-`·`■` 조각으로 나눠 고객 발화만 보고, 감지요소 10종 + 장기방치. 소송 미참여 세대 태그·안내방송·화장대·"자재발주"의 "재발" 등 오탐 제외.
- **감지(743차, `riskDetect`)**: 조각(`//` `/` `|` ` - ` `■` `▶`)마다 ① 직원 메모 조각이면 memoOk 요소(보수품질·반복하자, 반복민원)만 ② 요소별 `rx`(직접어)면 적중 ③ `cond`(조건부어: 사고·위험·불안·청구·비용·손실·책임·스트레스·경찰·주거·손상…)는 그 낱말을 뺀 나머지에 `ctx`(문맥어)가 있어야 적중 ④ `no`(제외·직원 방향 문장)면 무효. 요소 12종(반복민원 신설: 재문의·재요청·재접수·여러 차례·지난번에도…). 판정 로그: `riskDetect(text,log)` → 콘솔 `riskAuditCSV(sid)` 로 접수번호·요소·검출어·판정·사유 CSV.
- **자동 요소(접수 데이터)**: 지연 60일+ → 장기방치 / 세대 접수 ≥ `autoRepeatN`(8) 또는 ≥ `autoRepeatN2`(5)+미처리 ≥ `autoRepeatOpen`(2) → 반복민원 / 접수 3건+ 이면서 같은 공간·하자유형 ≥ `autoSameDefectN`(2) → 보수품질·반복하자 / 접수내용에 날짜(M/D) 2개 이상 나열 또는 「독촉」 2회 이상 → 반복민원(749차, 「8/21 // 8/30 / 9/2 독촉」). 직원 메모 조각은 쉼표와 「~으나/지만/하고/며」 뒤에서 절 단위로 더 쪼개 섞인 고객 발화를 살린다(756차). 날짜 나열은 독촉·재촉 같은 행동어가 함께 있어야 반복민원. **등급·점수·장기방치 자동 태그는 현재 미처리 건의 지연일(`openDelay`)만 본다** — 끝난 옛 건의 지연일은 `maxDelay` 이력만. 자동 요소(반복민원·보수품질·장기방치)는 근거 표에 `auto:true` 줄로 산출 근거(「세대 접수 8건」「주방 / 파손 3건 반복」「현재 미처리 최장 73일」)가 붙는다. 근거 원문·`riskAuditCSV` 원문은 `maskPII` 처리. (`why` 는 데이터에만 — 750차 화면 표시 철회).
- **점수(`riskScore`, 상한 12)**: ★중대 +3 · 일반 +1 · 장기방치 +1 · 180일+ +1 · 365일+ +2 · 물리4 +2 · 물리3 +1. 표 기본 정렬은 **레벨 → 점수 → 미처리**(750차), 「점수」 열. 365일+ 방치는 등급을 한 단계 올린다(심각까지). 감지요소 칩은 ★중대 → 일반 → 장기방치 순(hh 생성·렌더 둘 다 정렬).
- 레벨 5단계(`riskLevel`): 긴급 = ★중대 **3개** 이상(738차, 종전 2) 또는 법적·외부확산 + 미처리 잔여 **+ 점수 ≥ `urgentMinScore`(7, 745차)** / 심각 = 중대 1개 또는 비중대 3개 / 경계 = 비중대 1~2개 또는 물리 심각 / 주의 = 장기방치만 또는 물리 경계 / 양호. ⚠ 734차까지 코드는 「심각 + 미처리 잔여 → 긴급」이라 심각이 0 이었고, 처리 판정이 `'완료'` 를 찾아(norm 은 `'처리'`) 전 건이 미처리였다 — 둘 다 고쳤다. **레벨은 게시본에 실리므로 재등록해야 화면이 바뀐다.**
- 게시본 `kpi.hh = {cnt, list, fac}`. `list` 는 전 세대(양호는 근거 없이 얇게), 주의 이상에 `evid[]`(접수건 단위: 감지요소·접수일·공간·공종·하자유형·지연일·내용 200자·마스킹).
- 요소 10종(760차 합침): ★중대 4(안전위험·보상요구·**법적·외부확산**(=법적·외부기관+외부확산)·감정격화·위협표현) · 일반 4(**반복하자·반복민원**(=보수품질·반복하자+반복민원)·응대·소통불만·생활불편·책임·형평성불만) · soft 2(**처리지연·장기방치**(=처리지연·일정피해+장기방치, 단독이면 주의)·사설시공·인테리어(763차: 인테리어·줄눈·중문·사설은 시공/공사/업체/세대 문맥이 붙을 때만)). **정답셋(risk-gold.md)은 763차에 현재 규칙으로 재확정 — 60/60·정밀도·재현율 100%. 규칙을 바꾸면 정답셋도 같이(코드를 정답셋에 맞추지 않는다).** **riskHH 는 하자구분=세대 접수만**(공용부·상가 제외, 열이 없는 자료면 전부). 정답셋 이름도 갱신.
- 현장 민원 탭 세대 현황도(`dfRiskMapHTML`, 746차 팀 현황도 꼴 · 748차 「민원 세대 현황」 카드 안 인셋 구분선 아래, 제목·범례 없음, 가로 스크롤은 현황도 구간만): 동마다 층 「27F」(위→아래) × 라인 「1호」 격자, 칸엔 호수, 칸 색 = 레벨, 1F 부터 라인별로 자료에 보인 최고층까지 칸을 그린다(758차 최저층 규칙은 760차에 취소 — 세대만 다루므로 불필요). 접수 없는 세대는 흐린 칸(세대 목록이 없어 그 이상은 모른다). 칸 클릭 → 표를 그 세대로 검색(「101동 2001호」 형식). 세대만(760차). 실데이터에 「49014」 같은 오기입 호가 있어(764차) 층이 `RISK_MAP_MAXFL`(70) 넘거나 아래 무리와 `RISK_MAP_GAP`(8)층 이상 떨어진 3세대 이하 위 무리는 격자에서 조용히 뺀다(표에는 남는다, 별도 칩 없음). 호가 3자리 이상 숫자인 세대만 격자. HTML 은 같은 목록·같은 판이면 재사용(memo). 가로 이동은 휠(.rkm-wrap 이 pv-scroll 예외에 포함)과 카드 밑 ovs 막대 — 스크롤바는 앱 정책대로 숨김.
- 화면: 대시보드 「현장별 민원 현황」 히트맵(현장 × 레벨, 현장명 풀네임, 긴급·심각·경계 = 장기미처리 차트 3단 색, 최소 농도 .5~.55(글자 흰색 600 + 진한 그림자, 769차), 셀 그림자·호버는 살짝 뜸(translateY −1 · scale 1.04 · .22s, position 은 늘 relative), 제목과 표 사이 여백 0(770차), 앞에 권역 열 52px + 현장명 폭은 가장 긴 이름 기준 `--rkhn`(12.5px/글자, 120~240) — 셀 클릭 → 그 현장·레벨 세대 모달, 현장명 클릭 → 현장 페이지). 761차 분포 막대 시안은 762차에 롤백. · 현장 탭에 「민원 세대 현황」(장기미처리 비율 현황과 같은 카드 — 제목+범례 머리, 전월/금월 두 줄, 레벨별 색 누적(색은 장기미처리 차트 계열 `--ch-d60/d30/d0`) 막대, 오른쪽 전월대비 배지는 **긴급 비율 %p 증감**(왼쪽 값도 「N세대 · N%」)) + 세대 표. 펼침 하자건 표: NO · 접수일 · 공간 · 공종 · 하자유형 · 접수내용 · 감지요소 · 처리상태. 열폭은 바깥 표에 맞춘다: 바깥 하자 유형 251px = 안쪽 감지요소 177 + 처리상태 74, 안쪽 NO~하자유형 합 384px = 바깥 레벨~미처리 합. 전부 픽셀이라 현장·모달(88vw) 어느 폭에서도 세로선이 맞는다 — 한쪽을 고치면 같이 고칠 것. 펼친 세대 행은 호버색 바탕, 펼침 블록은 통째로 호버하지 않고 안쪽 표 행만 호버(741차), 감지요소 칸은 잘리면 data-tip 으로 전체, 부모 행·펼침 블록 왼쪽에 파란 선(둘 다 inset box-shadow — border 를 섞으면 단차가 난다). 표는 `dfRiskTableHTML(list, ns)` 하나를 현장(`ns='site'`)·모달(`ns='modal'`)이 같이 쓴다(열: 레벨·동·호·접수·처리완료·미처리·감지요소·하자 유형, 펼치면 문제 접수건을 하자 목록 규격의 표 `.rk-etbl` 로) — 머리 클릭 정렬(전 열), 필터 줄은 **처음엔 없고 머리 우클릭 → 「필터 줄 켜기」**(목록보기와 같은 방식, `;` OR), 상태는 `S.rk[ns]`(q·sort·limit·search·filt). 모달·현장 표 공통으로 **감지요소·하자유형 칩(누르면 `;` OR 필터) + 검색(세대·감지요소·유형·근거 접수내용, 값이 있으면 × 지우기 760차) + 표시 건수(100·300·1,000·전체)** — 모달은 목록보기와 같은 머리(제목 · 결과 N/전체 N · 검색창 `#rkQ` · 닫기)에 검색이 있고 `.rk-wrap` 세로·가로 스크롤바·머리 sticky, 칩 줄은 두 줄 — 모달: 1행 하자유형 칩 + 표시 건수, 2행 감지요소 칩(검색은 머리) / 현장: 1행 하자유형 + 검색 `#rkQs`, 2행 감지요소 + 표시 건수(741차 확정). 칩 묶음(`.rk-chips[data-sbx]`)은 줄바꿈 없이 가로 스크롤+페이드라 감지요소 11종이 다 나와도 한 줄(모달은 `fadeSoon()` 을 직접 부른다 — 페이드 관찰자가 #app 만 본다). 모달의 검색 `#rkQ` 는 머리(닫기 옆). ⚠ 돋보기가 안 보이던 원인은 78행 `:focus-visible{position:relative;z-index:1}` — 초점 잡힌 입력이 아이콘 위로 올라온다 → `.rl-q-wrap .icn-sm{z-index:2}`. 현장은 레벨 탭이 카드 밖 `.tkbar`(업무 현황과 같은 탭 줄)이고 카드엔 칩·검색·표만. 두 표 모두 `table-layout:fixed`(auto 면 긴 내용에 열이 밀려 안팎 선이 어긋난다). 열폭(757차): 바깥 레벨 60·점수 38·동 62·호 58·접수 52·처리완료 62·미처리 54(합 386) · 감지요소 auto · 하자 유형 300(칩 4개). 안쪽 NO 58(=레벨−선 2) · 접수일 100(=점수+동) · 공간·공종·하자유형 74+76+76(=호~미처리 226) · 접수내용 auto(=바깥 감지요소) · **처리상태 74 는 바깥 하자 유형 시작선** · 감지요소 226. 실측 전 선 일치. 검색·칩·표시 건수는 `rkRerender('site')` 가 표 카드 안쪽만 다시 그린다(목록은 `S._rkSiteList`) — 전체 rDefect 는 탭·현황도 전환 때만. ⚠ 게시본은 빈 배열을 떨어뜨리므로 `factors/types/evid` 는 렌더 진입에서 `||[]` 로 보정한다 — 없으면 모달이 「불러오는 중…」에 멈추고 현장 탭이 안 열린다.
- 보고서 양식(`rptDashboard`/`rptSite`): 3절 「주요 이슈」는 규칙 자동 선별(`rpIssDash`/`rpIssSite`, 중요도순 — 대시보드는 최대 4건이고 「중대하자 미처리」 카드 없음(764차), 현장은 최대 6건: 특정 현장 집중·미처리 급증·장기미처리 증감·장기화 현장·처리 속도·공종/업체 집중·중대하자(현장만)·세대당·공가). 민원(레벨) 절은 보고서에 넣지 않는다(736차 사용자 결정). 현장 보고서는 2쪽(708차 AI 제거 뒤 빈 3쪽이 남아 있던 것을 736차에 제거).
- 인쇄(`dfPrint`, 대시보드): 1면 KPI 5칸 · 추이 · 전월대비+현장별 미처리 · 현장별 민원 현황+공종별 미처리 / 2면 월별·현장별·공종별 처리 현황(행 19px, `.sp-p2`). A4 폭이 폰 규칙(≤960)에 걸리므로 인쇄 CSS 에서 첫 KPI 카드 전폭·버튼 숨김을 되돌린다. 인쇄 페이지 `min-height` 는 1000px(1050 은 여백 설정에 따라 빈 마지막 장을 만들었다).
- 정답셋과 어긋나는 3건은 정답셋 쪽 판단 차이(사용자 결정) — 완전일치 95% 기준선.

### 조직 관리
- 현장 표 편집은 초안(`ORG_DRAFT`) → 「저장」 한 번. 나가면 저장/버림 묻는다. 현장 탭에서도 담당자 지정(`org.siteOwnPick`).
- 지도(`kmSVG`)는 통계청 행정경계 SVG, 외부 API 없음. 현장 점은 실제 좌표 그대로(787차)이되, **좌표가 똑같은 현장(같은 동으로 찍힌 것)만** `kmSpots` 가 가로로 나란히 붙인다(간격 = 지름 + 2px, 묶음 가운데 = 원래 자리). 라벨 예약 상자도 같은 자리를 쓴다(791차). 현장명(z>2)은 점 위 → 아래 → 오른쪽 → 왼쪽(점 가운데 높이, text-anchor start/end) 순으로, 먼저 놓인 현장명과 **다른 현장의 점**을 피하는 첫 자리에 놓고 다 막히면 생략(툴팁으로 본다), 고른 자리를 예약한다(`snm[sid]={x,yb,a}`, 794차). 충돌 상자는 글자 잉크 기준(기준선 −10.5~+2.5px, 폭 `kmTextW`+2px) — 흰 테끼리 닿는 것은 허용해야 위·아래 이름 사이에 옆 이름이 들어간다. 이름 폭은 글자 수로 어림하지 말 것(793차). 현장명 그림자는 **인라인 drop-shadow 에 u 를 곱한다** — SVG 요소의 CSS filter 길이는 viewBox 단위라 CSS 로 주면 확대할수록 번진다. 시도 이름 = 시·군 이름 크기(`ctFs`) + 1px(최소 11). 동·읍·면 이름(subl·dong)은 z>10(`dongZ`), 읍면동 경계선은 deep(z>5) 그대로(792차).

### 상단바·연결
- 「연결 끊김」은 6초 이상 이어질 때만, 탭이 숨겨진 동안은 판정 안 함. 잠시 뜨는 상태는 줄 맨 왼쪽(늘 있는 것이 안 밀림).

## 7. 반복해서 걸린 함정
- 모바일 미디어 쿼리는 `body:not(.wid)` 접두(위젯 영향 방지). 스크롤바는 숨기고 페이드로만(`[data-sb]`·`[data-sbx]`).
- 감사기는 `@media` 안 재선언은 봐주지만 `@container` 안은 못 가른다 → 베이스라인에 등록.
- 요소 하나만 보지 말고 관계를 본다(카드 겹침·grid 행 높이·flex 자식 축소). 변경 보고 전 스크린샷 확인.
- 현실적인 데이터 규모(28개 업무 · 14개 현장 · 650세대)로 검증하지 않으면 릴리즈 뒤 붕괴한다.

## 8. 배포
1. 게이트 통과 → `calapp-vNNN.zip`.
2. GitHub 저장소에 폴더째 업로드(Pages). 규칙이 바뀌었으면 Firebase 콘솔에서 `database.rules.json` 게시.
3. 강력 새로고침(Ctrl+Shift+R). 위젯은 exe 재빌드가 필요할 때만(`widget-lite/` 변경).

## 9. 남은 과제
- 실데이터 첫 달 결과로 민원 레벨 경계값·사전 조정.
- 사이드바 회전 증상(재현 안 됨 — 실기기 캡처 필요).
- 세대 표 필터 입력 시 전체 재렌더(포커스 복원으로 버팀).
